## Test environments

* local macOS, R 4.5.2

## R CMD check results

0 errors | 0 warnings | 3 notes

* This is a new submission. The incoming check reports the maintainer, new
  submission status, and tarball size of 5,357,670 bytes.
* The local macOS check reported that it was unable to verify the current time
  while checking for future file timestamps.
* On the local macOS check machine, HTML manual validation was skipped because
  the installed `tidy` binary is Apple HTML Tidy from 2006. This is a local
  validation-tool limitation, not a package HTML-generation failure.

## Submission notes

This is the first CRAN submission of drmTMB.

drmTMB fits one-response and two-response distributional regression models
using Template Model Builder. The README, vignettes, and reference topics mark
currently fitted routes separately from planned or unsupported syntax.

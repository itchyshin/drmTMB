## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new submission: the first CRAN release of drmTMB.

## Notes for maintainer

* Refresh the check summary above from `devtools::check(remote = TRUE,
  manual = TRUE)` and `devtools::check_win_devel()` immediately before
  submission, and record any remaining NOTEs (e.g. "New submission") here.
* The package compiles a TMB model template; reverse-dependency checks are
  not applicable for a first release.

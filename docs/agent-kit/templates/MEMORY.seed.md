# <PROJECT> Memory Seed

Use this file as the starting note for any external or private memory system.
It is intentionally short because repository files remain authoritative.

- Repository: `<absolute path or GitHub URL>`
- Project type: `<R statistical package / ML package / ABM package /
  data-wrangling package / other>`
- Read first: `AGENTS.md`, `docs/design/00-vision.md`,
  `docs/design/10-after-task-protocol.md`, `docs/dev-log/check-log.md`.
- Standing roles: Ada, Boole, Gauss, Noether, Darwin, Fisher, Pat, Jason,
  Curie, Emmy, Grace, Rose.
- Operating rule: verify drift-prone facts such as git branch, PR state, CI,
  test output, and current implementation status before answering.
- Recovery rule: after crashes or context compaction, rehydrate from git status,
  git diff, latest check-log, and latest after-task reports.
- Boundary rule: do not present planned features as implemented unless code,
  tests, docs, examples, and validation evidence exist.

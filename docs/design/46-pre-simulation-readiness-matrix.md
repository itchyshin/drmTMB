# Pre-Simulation Readiness Matrix

This note answers one practical question before broad Phase 18 simulation:
which model surfaces are ready for operating-characteristic grids, and which
surfaces still belong in the failure ledger?

The rule is stricter than "the model fits once". A surface is ready only when
the fitted likelihood, parser boundary, extractors, diagnostics, interval
status, focused recovery tests, and reader-facing boundary are all visible.
The matrix below keeps location, scale, shape, inflation, structured
dependence, and bivariate responses separate so simulation reports do not mix
evidence from one layer into another.

Slice 283 adds the family-level crosswalk in
`docs/design/02-family-registry.md`, listing each public family route,
distributional-parameter link, shape or coscale slot, fitted random-effect
allowance, and test-evidence state. Treat that map as the source check before
adding a family to a Phase 18 grid.

## Slice 291 Evidence-Ledger Gate

This gate maps the public stable-core rows to the evidence a Phase 18 report
writer must find before a surface enters a simulation grid. "Advertised" here
means a surface appears in README, the model-map article, a package reference
topic, or a Phase 18 DGP row as fitted, stable, first slice, or opt-in
control. Planned or blocked rows can appear only as failure-ledger rows.

| Public row | Implementation evidence | Test, diagnostic, or interval evidence | User-facing boundary | Simulation status after Rose/Fisher signoff |
| --- | --- | --- | --- | --- |
| Fixed-effect one-response families | Family registry entries and likelihood rows in `docs/design/02-family-registry.md` and `docs/design/03-likelihoods.md` | Family-specific tests listed in `docs/design/34-validation-debt-register.md`; fixed-effect Wald rows for count, proportion, ordinal, and continuous-shape paths where recently hardened | Distribution-family tutorial and stable-core matrix separate fitted fixed-effect families from random-effect extensions | Admit named fixed-effect families; do not borrow this evidence for non-Gaussian random effects |
| Gaussian ordinary random effects | Gaussian `mu` intercepts, independent slopes, one-slope correlated blocks, q > 2 numeric `mu` blocks, `sigma` intercepts, and independent `sigma` slopes are fitted | Random-effect, profile-target, comparator, weak-boundary, and `check_drm()` evidence in the validation-debt register and recent random-slope slices | README, model-map, and which-scale docs mark larger q blocks as fitted but sample-size hungry | Admit small Gaussian ordinary grids; label q > 2 grids as advanced |
| Random-effect scale `sd(group)` models | Unlabelled Gaussian `mu` random-intercept SD surfaces are fitted | Recovery and comparator tests exist; row-specific SD summaries remain derived | `sd(group) ~ x_group` is the public fitted syntax; coefficient-specific slope-SD formulas stay reserved | Admit only the unlabelled intercept-SD surface |
| Known sampling covariance | Gaussian `meta_V(V = V)` fits diagonal, dense, and row-paired bivariate known sampling covariance | Vector, dense, bivariate, interval-safety, and `check_drm()` evidence exist | Meta-analysis docs keep known sampling covariance `V` separate from latent relatedness and fitted residual `sigma` | Admit vector and dense known-`V` Gaussian grids with `V` treated as input data |
| Bivariate residual `rho12` | Two-response Gaussian models fit fixed and predictor-dependent residual `rho12` | `rho12()`, profile-target, row-specific profile, and tutorial evidence exist | Docs keep residual coscale separate from group, phylogenetic, spatial, and known-sampling covariance | Admit residual-correlation grids; do not treat `rho12` as a random-effect or structured-correlation layer |
| Ordinary bivariate covariance and `corpairs()` | Selected labelled intercept blocks, matching slope-only `mu1`/`mu2` blocks, and q=2 `corpair(..., level = "group") ~ x` surfaces are fitted | `corpairs()`, summary rows, direct profile targets for supported q=2 rows, slope-slope covariance diagnostics, and interval-source provenance exist | q=4 intercept rows expose derived interval unavailability; intercept-plus-slope q=4, residual-scale slope, and p8/q8 slope covariance remain planned | Admit selected intercept-block, slope-only `mu1`/`mu2`, and q=2 rows; keep broader slope-level covariance out |
| Phylogenetic structured effects | Univariate `mu` intercepts and one numeric `mu` slope, bivariate `mu1`/`mu2`, selected full and block-diagonal q=4 location-scale, direct-SD, and q=2 phylogenetic `corpair()` slices are fitted | Profile, direct-SD, covariance-row, diagnostic, one-slope recovery, and example evidence exist; Ayumi Mass + Beak keeps the q=4 fallback in the weak-fit ledger | Structural-dependence docs keep multiple phylogenetic slopes, slope correlations, bivariate slopes, non-Gaussian phylogenetic effects, and structured `rho12` planned | Admit intercept/direct-SD subsets and focused one-slope smoke cells |
| Coordinate spatial structured effects | Univariate Gaussian `mu` and `sigma` intercepts, matching univariate `mu`/`sigma` correlation, one numeric coordinate-spatial `mu` slope, constant bivariate Gaussian `mu1`/`mu2` q=2 covariance, constant q=4 location-scale covariance, and ordinary Poisson/NB2 q=1 `mu` intercepts are fitted | Direct SD/correlation targets, `ranef("spatial_mu")`, `ranef("spatial_sigma")`, `corpairs(level = "spatial")`, `summary()$covariance`, one-slope smoke evidence, q=2 dense-comparator evidence, q=4 extractor/diagnostic tests, diagnostics, the spatial q=2 ADEMP sheet, q=2 DGP/smoke-runner evidence, fixed-effect Wald CSVs, profile-status/evidence CSVs, and count structured source tests exist | Mesh/SPDE, multiple slopes, residual-scale structured slopes, direct-SD, spatial `corpair()` regression, count spatial slopes or labels, and zero-inflated spatial effects remain planned | Admit univariate coordinate-spatial `mu`/`sigma` intercept, one-slope, constant q=2 bivariate location-covariance, and count q=1 source-test grids; treat constant q=4 location-scale as fitted smoke/artifact evidence until q=4 recovery and convergence evidence exist |
| Profile intervals and diagnostics | `summary()`, `confint()`, `profile_targets()`, `check_drm()`, `predict_parameters()`, and `corpairs()` expose target-specific status | Slice 278 records interval routes; Slice 289 records extractor and plot interval provenance | Docs state that bootstrap and many derived-summary intervals remain unavailable | Use as supporting infrastructure after the model surface itself is admitted |
| Large-data controls | Memory-light objects, `se = FALSE`, sparse Gaussian fixed-effect `mu`, and Gaussian sufficient-statistic aggregation are opt-in controls | `check_drm()` exposes sparse-design, aggregation, and `sdreport` diagnostics where fitted | Control docs state these are hardening or memory routes, not general scalability claims | Admit only as opt-in stress cells, not as broad performance evidence |
| Reserved, planned, or blocked neighbours | Parser, reference, or roadmap syntax may exist for future features | Boundary tests and error messages are evidence only that unsupported paths stay closed | README, model-map, family registry, and structural docs tell users what to fit instead | Keep in the failure ledger until likelihood, tests, diagnostics, docs, simulation status, and after-task evidence exist |

The signoff result is conditional rather than global. The current evidence can
start Phase 18 on the admitted named surfaces, but it does not yet support a
comprehensive all-feature grid. Every new DGP row should cite the public row
above, the register row in
`docs/design/34-validation-debt-register.md`, and the specific tests or
after-task report that make the row admissible.

### Slice 390 Ayumi Q2 Hard-Case Addendum

Slices 363-390 add an Ayumi full-species stress case for bivariate Gaussian q2
phylogenetic species effects. The aggregate all-species phylogenetic q2 source
fit converged, but the row-capped all-species target false-converged under
default starts, fixed/residual source starts, ordinary species source starts,
aggregate phylogenetic q2 source starts, and modest covariance jitter. In every
full-species target attempt, residual `rho12` landed essentially at 1.

This does not remove the fitted q2 phylogenetic intercept surface from the
readiness matrix. It does narrow the claim. The current q2 phylogenetic
mean-mean path is ready for small controlled recovery grids and extractor or
diagnostic checks; it is not ready as a general full-species row-level
convergence showcase. Larger data should be described as helpful only when it
adds information that separates residual covariance from structured species
covariance.

The same addendum keeps structured covariance priorities clear. Ordinary
Gaussian `sigma1`/`sigma2` intercept covariance is already fitted where the
ordinary bivariate covariance rows say so. Standalone phylogenetic, spatial, or
future animal `sigma`-`sigma` covariance is not a near-term admission target.
The structured priority remains `mu`-`mu` first for phylogenetic, spatial, and
future animal relatedness, with residual `rho12` kept separate.

### Slice 391-402 Mass-Beak PV2 Addendum

The corrected Ayumi Issue #1 target is Mass + Beak, not the earlier lightness
stress fixture. In this model body mass has two roles: `Mass_z` is response 1,
and `Mass_cov_z` is the fixed allometric covariate for Beak. The two columns
are identical on the observed data, but the split is required for simulation
and bootstrap so a simulated response does not silently overwrite the
conditioning covariate.

The stable anchor is the q2 location-only phylogenetic model. On all 6,196
species it converged with `pdHess = TRUE`, residual `rho12 = -0.789`, and
phylogenetic `mu1`-`mu2` correlation `-0.841`. Slice 403-412 implements the
prereg block-diagonal phylogenetic fallback with one q2 location block and one
q2 scale block for the same tree. On the same Mass + Beak data it now fits
rather than aborting, but it returns false convergence, `pdHess = FALSE`,
residual `rho12 = -0.720`, phylogenetic mean-mean correlation `-0.750`, and a
scale-scale phylogenetic correlation essentially at `-1`. A 10-core parametric
bootstrap smoke run (`B = 10`) refitted all replicates but all retained
convergence code 1; the mean bootstrap values were residual `rho12 = -0.746`,
phylogenetic mean-mean `-0.824`, scale-scale `-0.99999`, and Beak-on-Mass
coefficient `1.81`. The full q4 location-scale model can be optimized with
`se = FALSE`, but it still returns false convergence, large gradients,
residual `rho12 = -0.984`, and several q4 correlations near `+/-1`.

This addendum narrows Phase 18 admission further:

- admit Mass + Beak locphylo as a high-value real-data convergence and
  bootstrap diagnostic target;
- keep full q4 PV2-main in the failure/diagnostic ledger until the selected
  optimum is stable under restart, bootstrap, or fallback simplification;
- keep the block-diagonal phylogenetic fallback fitted but diagnostic-only for
  this dataset because the scale-scale row sits on a boundary;
- treat fallback `profile_targets()` rows as mechanically profile-ready, not
  profile-proven: a bounded full-species Ayumi mean-mean profile took about 512
  seconds and still failed to extract a 95% interval;
- do not treat intercept-only scale formulas as a rescue for this target yet:
  the first simplified scale-phylo fallback still false-converged with
  `pdHess = FALSE`, worse AIC, and scale-scale phylogenetic correlation near
  `-1`;
- use the clean `PV2_locphylo` model as the Mass + Beak demonstration: the same
  10-core bootstrap diagnostic refitted all ten replicates with convergence 0
  and small gradients;
- use parametric bootstrap as a diagnostic route for weak-Hessian fits, but do
  not report bootstrap intervals as scientific uncertainty until convergence
  and boundary diagnostics improve or the model is simplified.

## Slice 268 Capability Audit

This audit condenses the readiness matrix into the model classes that a Phase
18 report writer is most likely to ask about. "Implemented" means a fitted
likelihood or exported parser surface exists. "Tested" means there is focused
test, smoke-simulation, profile, diagnostic, or rendered-document evidence for
the implemented surface named in the row. "Planned" and "unsupported" rows stay
out of operating-characteristic tables except as failure-ledger entries.

| Capability area | Implemented status | Tested status | Planned or unsupported boundary | Phase 18 admission |
| --- | --- | --- | --- | --- |
| Gaussian models | Implemented for fixed-effect location-scale, ordinary `mu` random intercepts and q > 2 random slopes, independent `sigma` random slopes, selected location-scale covariance blocks, all-Gaussian bivariate `mu1`/`mu2` surfaces, and matching slope-only bivariate `mu1`/`mu2` covariance | Tested with focused recovery tests, smoke runners, direct profile targets, diagnostics, and reader examples for the fitted subsets | Intercept-plus-slope bivariate covariance, slope-level location-scale covariance, q=6/q=8 endpoint blocks, and richer structured Gaussian covariance remain planned | Admit the named fitted subsets as small grids, not as a blanket "all Gaussian" claim |
| Non-Gaussian models | Implemented for fixed-effect count, bounded, positive-continuous, shape, and ordinal families, ordinary non-zero-inflated Poisson and NB2 `mu` random intercepts and independent numeric slopes, ordinary Student-t/zero-truncated NB2/lognormal/Gamma/beta/beta-binomial `mu` random intercepts and independent numeric slopes, ordinary NB2 log-`sigma` random intercepts, and ordinary Poisson/NB2 q=1 `phylo()`, `spatial()`, `animal()`, and `relmat()` `mu` intercepts | Tested for the fixed-effect paths, Phase 18 fixed-effect proportion, zero-one beta, positive-continuous, and ordinal DGP/smoke/grid artifact lanes, bounded-response, positive-continuous, Student-t, and zero-truncated NB2 `mu` random-intercept artifact lanes, Poisson/NB2 `mu` random-effect smoke surfaces, selected non-Gaussian `mu` slope focused recovery, lognormal/Gamma/beta/beta-binomial `mu` random-effect focused recovery, NB2 log-`sigma` focused recovery, Wald rows, direct `log_sd_mu` and `log_sd_sigma` profile-target rows, a small NB2 log-`sigma` smoke-grid artifact writer, weak-SD diagnostics, Poisson phylogenetic q=1 smoke/formal infrastructure, NB2 phylogenetic q=1 smoke/boundary tests, a 288-cell NB2 q1 formal-condition sentinel, a 24-cell x 5-replicate representative NB2 q1 audit, the merged 16-shard NB2 q1 formal audit, and focused count structured source tests | Non-Gaussian `sigma` random effects outside the ordinary NB2 intercept gate, correlated Student-t/zero-truncated/positive-continuous/bounded-response random slopes, Student-t `nu` random effects, zero-one beta random effects, hurdle random effects, structured slopes, simultaneous structured count types, labelled count covariance, mixed-response bivariate families, and most non-Gaussian random-effect covariance remain planned or unsupported | Admit ordinary Poisson/NB2 `mu` random-effect smoke grids, q=1 count structured source-test grids, the zero-truncated NB2 `mu` random-intercept artifact lane, the bounded-response beta/beta-binomial, positive-continuous lognormal/Gamma, and Student-t `mu` random-intercept artifact lanes, the separate NB2 log-`sigma` random-intercept smoke lane, the selected non-Gaussian `mu` slope source-test slice, and the fixed-effect proportion, zero-one beta, positive-continuous, and ordinal lanes; keep Poisson/NB2 phylogenetic q=1 in separate smoke/formal-admission lanes until supportive formal recovery evidence exists |
| Ordinal location | Implemented for fixed-effect univariate `cumulative_logit()` models with ordered cutpoints and fixed latent logistic scale | Tested for likelihood recovery, category probabilities, prediction, expected-score summaries, simulation, fixed-effect Wald intervals, internal cutpoint profile targets, malformed inputs, unsupported random-effect boundaries, Phase 18 DGP/smoke/grid artifacts, first-wave runner inclusion, and manual `ordinal_fixed_effect` Actions dispatch | Ordinal random effects, ordinal scale/discrimination formulas, structured ordinal effects, bivariate ordinal, and mixed-response ordinal models remain planned or unsupported | Admit only the fixed-effect ordinal artifact lane as a small grid; exclude ordinal mixed-model grids |
| Shape and skewness | Implemented only where fixed-effect shape formulas already exist, such as Student-t `nu`; Slice 286 records skew-normal and skew-t as fixed-effect-first design lanes | Tested for existing fixed-effect shape boundaries, not for shape random effects or skew-family likelihood recovery | `nu` random effects, future `tau`, skew-normal/skew-t likelihoods, and ID-level skewness random effects remain planned or unsupported | Do not admit shape or skewness random-effect grids |
| Inflation and hurdle parameters | Implemented as fixed-effect `zi` or `hu` formula paths for selected count models; fixed-effect `zoi` and `coi` are implemented only for `zero_one_beta()` | Tested as fixed-effect response components and unsupported-boundary cases where applicable | `zi`, `hu`, `zoi`, and `coi` random effects, plus cross-parameter covariance with inflation parameters, remain planned or unsupported | Keep inflation-random-effect, hurdle-random-effect, and zero-one-inflation random-effect surfaces in the failure ledger |
| Bivariate models | Implemented for two-response Gaussian models with separate `mu1` and `mu2` formulas, residual `rho12`, selected labelled random-intercept covariance blocks, matching slope-only `mu1`/`mu2` covariance, and selected phylogenetic location correlations | Tested with bivariate Gaussian, `rho12`, `corpairs()`, profile-target, summary-covariance, diagnostic, and tutorial evidence for the fitted subsets | Mixed-response bivariate models, broader bivariate random slopes, `rho12` random effects, and broad q=4/q=8 slope covariance remain planned or unsupported | Admit constant or predictor-dependent residual `rho12`, selected intercept-block covariance grids, and the slope-only `mu1`/`mu2` grid |
| Post-fit extractor provenance | Implemented for interval-aware prediction tables, `summary()`, `confint()`, `corpairs()`, and the exported plotting consumers | Slice 289 verifies that `corpairs()` carries `conf.status` and `interval_source` beside profile-target metadata, while `plot_corpairs()` draws intervals only when those provenance columns mark a real interval source; existing `predict_parameters()` and `plot_parameter_surface()` tests cover the same rule for distributional-parameter surfaces | `vcov()` remains a covariance matrix with row and column-name provenance rather than a status table; `emmeans()` returns an external `emmGrid` for the narrow fixed-effect univariate `mu` path | Use these extractors as simulation and figure inputs only after the fitted surface itself is admitted |
| Random slopes | Implemented for ordinary Gaussian `mu` multi-slope blocks, independent Gaussian `sigma` one-slope terms, matching slope-only bivariate Gaussian `mu1`/`mu2` blocks, ordinary Poisson/NB2 `mu` independent numeric slopes, selected Student-t/lognormal/Gamma/beta/beta-binomial/zero-truncated NB2 `mu` independent numeric slopes, and coordinate spatial, phylogenetic, animal-model, and `relmat()` Gaussian `mu` one-slope fields | Tested with focused recovery, smoke, direct profile, weak-boundary, diagnostic checks, selected non-Gaussian source tests, and parser-boundary checks for unsupported correlated or multiple structured slopes | Correlated non-Gaussian slopes, broader bivariate random slopes, multiple structured slopes, structured slope correlations, and spatial slope correlations remain planned or unsupported | Admit fitted ordinary, count-`mu`, selected non-Gaussian source-tested, slope-only bivariate, and one-slope Gaussian structured cases |
| Meta-analysis | Implemented for Gaussian models with known sampling covariance through `meta_V(V = V)` and deprecated compatibility alias `meta_known_V(V = V)` | Tested for vector and dense `V`, summary-smoke output, Wald rows for estimated targets, and interval safety that keeps known `V` out of confidence-interval targets | Proportional sampling-variance models, multiple variance-component meta-analysis, and phylogenetic-plus-study extensions remain planned | Admit vector and dense known-`V` Gaussian grids, with `V` treated as input data |
| Phylogenetic models | Implemented for Gaussian `mu` and `sigma` intercept effects, matching univariate `mu`/`sigma` structured correlations, one numeric `mu` slope, selected bivariate `mu1`/`mu2` phylogenetic location correlations, constant q=4 location-scale blocks, direct `sd_phylo*()` paths, and ordinary Poisson/NB2 q=1 phylogenetic `mu` intercepts | Tested with profile targets, direct-SD surfaces, bivariate phylogenetic covariance rows, univariate `sigma` smoke tests, one-slope recovery, diagnostics, examples, multiple-slope rejection boundaries, the Ayumi q2 hard-case stress ledger, Poisson q=1 smoke/boundary/grid/formal tests, NB2 q=1 smoke/extractor/diagnostic/boundary tests, and the completed NB2 q=1 16-shard formal audit | Multiple phylogenetic `mu` slopes, residual-scale structured slopes, Poisson/NB2 phylogenetic slopes, zero-inflated phylogenetic effects, NB2 `sigma` phylogeny, phylogenetic slope correlations, richer q=4 predictor-dependent `corpair()` regressions, direct-SD formulas combined with structured `sigma`, and broad phylogenetic non-Gaussian effects remain planned or unsupported | Admit small controlled Gaussian intercept and one-slope smoke cells; keep structured `sigma` intercepts as smoke-tested first slices until recovery grids land; keep Poisson/NB2 q=1 in separate smoke/formal-admission lanes until supportive formal recovery evidence exists; keep Ayumi-style row-level q2 stress cases in the failure/diagnostic ledger until start, profile, and Hessian evidence improves |
| Spatial models | Implemented for coordinate spatial Gaussian `mu` and `sigma` intercepts, matching univariate `mu`/`sigma` structured correlations, one numeric `mu` slope with independent spatial fields, constant bivariate `mu1`/`mu2` q=2 spatial covariance, constant q=4 location-scale covariance, and ordinary Poisson/NB2 q=1 `mu` intercepts | Tested with a coordinate-spatial `sigma` smoke test, coordinate-spatial one-slope smoke surface, direct SD targets, `ranef("spatial_mu")`, `corpairs(level = "spatial")`, q=2 and q=4 profile-target labels, q=2 recovery and dense-comparator evidence, q=4 extractor/diagnostic tests, diagnostics, reader-facing boundary text, the spatial q=2 ADEMP sheet, q=2 DGP/smoke-runner evidence, fixed-effect Wald CSVs, profile-status/evidence CSVs, and focused count structured tests | Mesh/SPDE models, multiple spatial slopes, residual-scale structured slopes, spatial slope correlations, spatial direct-SD, spatial `corpair()` regressions, count spatial slopes or labels, and zero-inflated spatial effects remain planned | Admit univariate Gaussian coordinate-spatial `mu` and `sigma` intercept smoke cells, constant q=2 bivariate spatial location-covariance, and count q=1 source-test cells; keep q=4 as smoke/artifact evidence until q=4 recovery, convergence, and interval-status artifacts land |
| `animal()` models | Gaussian `mu` and `sigma` intercepts, matching univariate `mu`/`sigma` structured correlations, one numeric `mu` slope, matching bivariate `mu1`/`mu2` q=2 location covariance, constant all-four q=4 location-scale covariance, and ordinary Poisson/NB2 q=1 `mu` intercepts are fitted through `pedigree`, `A`, or `Ainv` | Tested with pedigree-to-additive-matrix equivalence, known-matrix recovery, one-slope recovery, univariate `sigma` smoke tests, dense-likelihood comparison, pedigree and matrix validation errors, `sdpars$mu`, `sdpars$sigma`, `corpars$animal`, `ranef("animal_mu")`, `corpairs()`, `summary()$covariance`, direct q=2 and derived q=4 profile-target status, `check_drm()` diagnostics, runnable examples, an explicit Phase 18 ADEMP sheet, q=2 DGP/smoke/grid-writer artifacts, an animal-only pedigree q=2 smoke cell, fixed-effect Wald CSVs, profile-status/evidence CSVs, opt-in profile smoke, q=4 DGP/smoke/grid-writer artifacts, derived-unavailable q=4 interval-status rows, focused q=4 smoke tests, and count structured tests | Sparse large-pedigree precision construction, multiple structured slopes, residual-scale structured slopes, slope correlations, predictor-dependent `corpair()` regressions, count animal slopes or labels, and direct-SD grammar remain planned | Ready for known-matrix and small dense-pedigree Gaussian `mu`/`sigma` intercept smoke cells, q=2 bivariate smoke-grid artifacts, count q=1 source-test cells, and focused q=4 smoke artifacts; fixed-effect Wald coverage is present for q=2, while q=4 defaults to point-estimate smoke with derived structured-correlation intervals unavailable |
| `relmat()` models | Known-matrix Gaussian `mu` and `sigma` intercepts, matching univariate `mu`/`sigma` structured correlations, one numeric `mu` slope, matching bivariate `mu1`/`mu2` q=2 location covariance, constant all-four q=4 location-scale covariance, and ordinary Poisson/NB2 q=1 `mu` intercepts are fitted through `K` or `Q` | Tested with covariance/precision equivalence, one-slope recovery, univariate `sigma` smoke tests, dense-likelihood comparison, matrix validation errors, `sdpars$mu`, `sdpars$sigma`, `corpars$relmat`, `ranef("relmat_mu")`, `corpairs()`, `summary()$covariance`, direct q=2 and derived q=4 profile-target status, `check_drm()` diagnostics, runnable examples, an explicit Phase 18 ADEMP sheet, q=2 DGP/smoke/grid-writer artifacts, fixed-effect Wald CSVs, profile-status/evidence CSVs, opt-in profile smoke, q=4 DGP/smoke/grid-writer artifacts, derived-unavailable q=4 interval-status rows, focused q=4 smoke tests, and count structured tests | Multiple structured slopes, residual-scale structured slopes, slope correlations, predictor-dependent `corpair()` regressions, count `relmat()` slopes or labels, and direct-SD grammar remain planned | Ready for known-matrix Gaussian `mu`/`sigma` intercept smoke cells, q=2 bivariate smoke-grid artifacts, count q=1 source-test cells, and focused q=4 smoke artifacts; fixed-effect Wald coverage is present for q=2, while q=4 defaults to point-estimate smoke with derived structured-correlation intervals unavailable |

## Current Readiness Matrix

| Surface | Fitted status | Evidence now available | Phase 18 status |
| --- | --- | --- | --- |
| Gaussian fixed-effect location-scale | Fitted | DGP, smoke runner, Wald intervals for fixed formula coefficients, `sigma` summaries, manifests, and failure ledgers | Ready for first small grids |
| Proportion fixed-effect | Fitted for `beta()`, `zero_one_beta()`, and `beta_binomial()` | Phase 18 DGP, smoke runner, grid writer, first-wave runner inclusion, manual Actions dispatch, fixed-effect Wald rows, Wald coverage, manifests, and failure ledgers now exist for the beta/beta-binomial artifact lane and the zero-one beta artifact lane; Slice D3 records the zero-one bounded-response design gate; `tests/testthat/test-zero-one-beta.R` keeps source-level fixed-effect likelihood, recovery, Wald-row, method, simulation, and malformed-neighbour evidence for zero-one beta | Ready for first small beta, beta-binomial, and zero-one beta fixed-effect grids; broader bounded-response random-effect grids beyond the beta/beta-binomial ordinary `mu` intercept/slope lane, correlated slopes, structured bounded responses, and mixed-response bounded models remain excluded |
| Bounded-response ordinary `mu` random effects | Fitted for `beta()` and `beta_binomial()` with `(1 | id)` and independent `(0 + x | id)` slopes in the location formula and fixed-effect `sigma` formulas | Focused deterministic recovery tests cover convergence, positive-definite Hessian status, fixed effects, random-effect SDs, conditional random effects, direct profile-target rows, `check_drm()` replication rows, strict or count-boundary filtering, independent numeric slopes, and malformed correlated/labelled/`sigma` neighbours. The Phase 18 artifact lane currently adds DGP, smoke runner, grid writer, first-wave runner inclusion, manual `bounded_response_mu_random_intercept` Actions dispatch, fixed-effect Wald rows, Wald coverage, direct-SD profile rows, manifests, and failure ledgers for the intercept slice | Ready for first small beta/beta-binomial ordinary `mu` random-intercept grids and focused source-tested slope checks; correlated bounded-response random slopes, labelled covariance, `sigma` random effects, exact 0/1 boundary mass, zero-one beta random effects, structured bounded responses, known covariance, and mixed bounded-response models remain later gates |
| Positive-continuous fixed-effect | Fitted for `lognormal()` and `Gamma(link = "log")`; first fixed-effect semicontinuous route fitted for `tweedie()` with intercept-only `nu ~ 1` | Phase 18 DGP, smoke runner, grid writer, first-wave runner inclusion, manual `positive_continuous_fixed_effect` Actions dispatch, fixed-effect Wald rows, Wald coverage, manifests, and failure ledgers for lognormal/Gamma; `tests/testthat/test-tweedie-location-scale.R` adds focused Tweedie recovery, support-boundary, fitted-response, simulation, and malformed-neighbour evidence; `docs/design/133-phase-18-tweedie-fixed-effect-artifact-preflight-slices-1644-1646.md` names the future Tweedie artifact schema before runner code | Ready for first small lognormal/Gamma fixed-effect grids and focused Tweedie fixed-effect checks; Tweedie has a preflight artifact schema but not a DGP, runner, writer, or coverage grid yet; generalized Gamma, structured positive responses, known covariance, mixed-response positive-continuous models, and Tweedie extensions beyond fixed-effect intercept-only power remain excluded |
| Positive-continuous ordinary `mu` random effects | Fitted for `lognormal()` and `Gamma(link = "log")` with `(1 | id)` and independent `(0 + x | id)` slopes in the location formula | Focused deterministic recovery tests cover convergence, positive-definite Hessian status, fixed effects, random-effect SDs, conditional random effects, direct profile-target rows, `check_drm()` replication rows, independent numeric slopes, and malformed correlated/labelled/`sigma` neighbours. The Phase 18 artifact lane currently adds DGP, smoke runner, grid writer, first-wave runner inclusion, manual `positive_continuous_mu_random_intercept` Actions dispatch, fixed-effect Wald rows, Wald coverage, direct-SD profile rows, manifests, and failure ledgers for the intercept slice | Ready for first small lognormal/Gamma ordinary `mu` random-intercept grids and focused source-tested slope checks; correlated random slopes, labelled covariance, `sigma` random effects, structured positive-continuous effects, known covariance, and bivariate or mixed positive-continuous models remain later gates |
| Student-t ordinary `mu` random effects | Fitted for `student()` with `(1 | id)` and independent `(0 + x | id)` slopes in the location formula and fixed-effect `sigma`/`nu` formulas | Focused deterministic recovery tests cover convergence, positive-definite Hessian status, fixed effects, random-effect SDs, conditional random effects, direct profile-target rows, `check_drm()` replication and `nu` rows, independent numeric slopes, and malformed correlated/labelled/`sigma`/`nu` neighbours. The Phase 18 artifact lane currently adds DGP, smoke runner, grid writer, first-wave runner inclusion, manual `student_mu_random_intercept` Actions dispatch, fixed-effect Wald rows for `mu`, `sigma`, and `nu`, direct-SD profile rows, manifests, and failure ledgers for the intercept slice | Ready for first small Student-t ordinary `mu` random-intercept grids and focused source-tested slope checks; correlated Student-t random slopes, labelled covariance, `sigma` random effects, `nu` random effects, structured effects, known covariance, and bivariate Student-t models remain later gates |
| Zero-truncated NB2 ordinary `mu` random effects | Fitted for `truncated_nbinom2()` with `(1 | id)` and independent `(0 + x | id)` slopes in the location formula and fixed-effect `sigma` formulas | Focused deterministic recovery tests cover convergence, positive-definite Hessian status, fixed effects, random-effect SDs, conditional random effects, direct profile-target rows, `check_drm()` replication rows, positive-count filtering, independent numeric slopes, and malformed correlated/labelled/`sigma` neighbours. The Phase 18 artifact lane currently adds DGP, smoke runner, grid writer, first-wave runner inclusion, manual `truncated_nbinom2_mu_random_intercept` Actions dispatch, fixed-effect Wald rows, Wald coverage, direct-SD profile rows, manifests, and failure ledgers for the intercept slice | Ready for first small zero-truncated NB2 ordinary `mu` random-intercept grids and focused source-tested slope checks; correlated zero-truncated NB2 random slopes, labelled covariance, `sigma` random effects, hurdle random effects, zero-inflated zero-truncated models, structured effects, and bivariate count models remain later gates |
| Gaussian ordinary `mu` random intercepts and q > 2 random slopes | Fitted | Focused recovery tests, q=3 smoke surface, q=4 output-name check, `sdpars$mu`, random effects, direct profile targets, and diagnostics | Ready for focused grids, with larger q treated as advanced |
| Gaussian `sigma` random slopes | Fitted for multiple independent one-slope terms, including separate grouping factors | Smoke surface for `(0 + w | id)` on `log(sigma)`, cross-group output-contract checks, summaries, manifests, and failure ledgers | Ready only for independent one-slope grids |
| Gaussian location-scale covariance | Fitted for supported intercept blocks | `corpairs()`, `summary()` covariance rows, profile targets for direct rows, and diagnostics | Ready for small intercept-block grids, not slope-level covariance |
| Bivariate Gaussian `rho12` | Fitted | Constant and predictor-dependent residual coscale, `rho12()`, row-specific profile support, and tutorial examples | Ready for focused residual-correlation grids |
| Bivariate Gaussian random-effect intercept blocks | Fitted for selected labelled intercept blocks | `sdpars`, `corpars`, `corpairs()`, `summary()` covariance rows, direct profile targets, and diagnostics | Ready for selected intercept-block grids |
| Bivariate Gaussian random slopes | Fitted for matching slope-only `mu1`/`mu2` blocks only | Focused extractor, profile-target, diagnostic, and registry coverage for `(0 + x | p | id)` in both location formulas | Ready for a small slope-only `mu1`/`mu2` smoke grid; intercept-plus-slope q=4, residual-scale slope, same-response location-scale slope, and all-four p8/q8-style endpoints remain planned |
| Mixed-response bivariate families | Planned; Slice 288 keeps Gaussian-count, Gaussian-proportion, count-proportion, ordinal mixed, and other two-response combinations out of the fitted surface | Clear errors for mixed composed families in both `c()` and `list()` spellings, plus the all-Gaussian composed-family positive path | Not ready until a joint likelihood or copula/latent-variable contract, prediction, simulation, extractors, intervals, examples, and comparator or independent-likelihood tests exist |
| Meta-analysis with known `V` | Fitted through `meta_V(V = V)` | Vector and dense `V` DGPs, smoke runner, Wald intervals for estimated targets, and safeguards that known `V` is not an interval target | Ready for first small grids |
| Coordinate spatial Gaussian `mu` | Fitted for intercept, one numeric slope, and constant bivariate `mu1`/`mu2` q=2 location covariance | Spatial one-slope smoke surface, direct SD targets, spatial `corpairs()` profile target, q=2 dense covariance comparator, diagnostics, `docs/design/56-phase-18-spatial-q2-ademp.md`, `phase18_run_spatial_q2_smoke()`, and `phase18_write_spatial_q2_grid_outputs()` | Ready for focused univariate Gaussian `mu` grids and constant q=2 bivariate spatial location-covariance smoke-grid artifacts; fixed-effect Wald coverage is present, while spatial SD/correlation and residual `rho12` coverage require explicit profile requests |
| Phylogenetic Gaussian `mu` | Fitted for intercept, one numeric slope, and selected bivariate intercept structures | Profile targets, direct-SD surfaces, bivariate phylogenetic covariance rows, one-slope recovery, diagnostics, examples, and Ayumi q2 hard-case stress artifacts | Ready for small controlled intercept and one-slope grids; row-level all-species q2 stress cases remain diagnostic/failure-ledger material |
| `animal()` and `relmat()` | Fitted for animal pedigree or known-matrix Gaussian `mu` intercepts and one numeric slope, matching bivariate `mu1`/`mu2` q=2 location covariance, and constant all-four q=4 location-scale covariance | Pedigree-to-additive-matrix equivalence, known-matrix recovery, one-slope recovery, bivariate dense-likelihood comparison, validation-error tests, extractor/profile/diagnostic rows, runnable examples, q=2 and q=4 design notes, q=2 and q=4 smoke-grid artifacts, fixed-effect Wald interval CSVs for q=2, profile-status/evidence CSVs, an opt-in q=2 profile smoke, derived-unavailable q=4 correlation rows, ASReml efficiency note, structured-slope parity gate, and reader-facing boundaries | Ready for known-matrix and small dense-pedigree Gaussian `mu` intercept and one-slope smoke cells, q=2 bivariate smoke-grid artifacts, and focused q=4 point-estimate smoke artifacts; q=4 correlation intervals remain derived-unavailable |
| Poisson `mu` random effects | Fitted for ordinary non-zero-inflated intercepts and independent numeric slopes | Smoke runner, fixed-effect Wald intervals, direct SD profile intervals, weak-SD boundary test, and diagnostics | Ready for first small non-Gaussian `mu` grids |
| Poisson phylogenetic q=1 `mu` | Fitted for ordinary non-zero-inflated `phylo(1 | species, tree = tree)` on the log-mean scale | Focused likelihood, extractor, direct `log_sd_phylo` profile-target, `ranef("phylo_mu")`, `check_drm()`, malformed-neighbour boundary tests, ADEMP sheet, DGP, fit summariser, smoke runner, aggregate summaries, manifests, failure ledgers, Wald fixed-effect intervals, Wald coverage, profile-target status rows, optional direct profile-interval artifacts, formal-grid spec/read-back QA, promotion decision, and manual Actions task | Ready for opt-in smoke and formal-admission runs only; not yet broad Phase 18 operating-characteristic evidence |
| NB2 `mu` random effects | Fitted for ordinary non-zero-inflated intercepts and independent numeric slopes | Smoke runner, fixed-effect Wald intervals, direct SD profile intervals, weak-SD boundary test, and diagnostics | Ready for first small non-Gaussian `mu` grids |
| NB2 `sigma` random intercepts | Fitted for ordinary non-zero-inflated independent grouped intercepts on log-`sigma` | Focused recovery, extractor, direct `log_sd_sigma` profile-target, prediction, `sigma()`, `check_drm()` replication tests, seeded DGP, smoke runner, repeatable CSV artifact writer, manifest, failure-ledger, Wald interval, Wald coverage, profile-target, and interval-status tables | Ready for a separate small overdispersion-random-intercept smoke lane; not ready for slopes, joint `mu`/`sigma`, zero-inflated, truncated, hurdle, or structured scale grids |
| NB2 phylogenetic q=1 `mu` | Fitted for ordinary non-zero-inflated `phylo(1 | species, tree = tree)` on the log-mean scale with fixed-effect `sigma` overdispersion | Focused likelihood, extractor, direct `log_sd_phylo` profile-target, `ranef("phylo_mu")`, `check_drm()`, deterministic recovery smoke, malformed-neighbour boundary tests, overdispersion-aware DGP, smoke runner, CSV grid writer, optional direct profile artifacts, formal-grid spec/read-back QA, grouped-comparator rows, manual Actions dispatch, a 288-cell one-replicate formal-condition sentinel, a 24-cell x 5-replicate representative audit, cancelled singleton runtime evidence, sharded formal-grid dispatch inputs, and a completed 16-shard x 500-replicate artifact audit | Ready for smoke/formal-admission runs only; the full shard audit has 288 condition cells and 144,000 `ok` manifest rows but still returns a hold decision because profile intervals fail frequently at the true-zero phylogenetic-SD boundary and low-count fixed-`sigma` recovery remains unstable |
| Supported non-Gaussian evidence goal | Evidence closeout, not new model syntax | `docs/design/79-supported-nongaussian-evidence-goal.md` audits fixed-effect family evidence, ordinary Poisson/NB2 `mu` random effects, the NB2 log-`sigma` random-intercept gate, and Poisson/NB2 q=1 phylogenetic `mu` gates in one table | Use this as the goal-level ledger; do not infer support for blocked mixed-response, inflation/hurdle random-effect, shape/skew random-effect, cross-parameter covariance, or broad structured non-Gaussian routes |
| Ordinal fixed-effect | Fitted for univariate `cumulative_logit()` location | Phase 18 DGP, smoke runner, grid writer, first-wave runner inclusion, manual `ordinal_fixed_effect` Actions dispatch, fixed-effect Wald rows, Wald coverage, cutpoint summaries, manifests, and failure ledgers | Ready for first small fixed-effect grids; ordinal random effects, scale/discrimination formulas, cutpoint-specific predictors, known-covariance ordinal models, structured ordinal effects, bivariate ordinal, and mixed-response ordinal models remain excluded |
| Non-Gaussian `sigma` random effects outside ordinary NB2 intercepts | Blocked | Fixed-effect `sigma` formulas exist for supported families; random-effect bars error clearly outside the ordinary NB2 intercept gate | Not ready |
| Shape or skewness random effects | Planned | Shape formulas exist only where already fitted as fixed effects; Student-t `nu` random-intercept and random-slope requests are blocked with a shape-specific test gate; skewness remains design/research | Not ready |
| Zero-inflation, hurdle, zero-one inflation, and one-inflation random effects | Planned or blocked | Fixed-effect `zi` and `hu` exist for selected count families; fixed-effect `zoi` and `coi` exist for `zero_one_beta()`; random-intercept and random-slope requests in `zi`, `hu`, `zoi`, and `coi` are blocked | Not ready |
| Ordinal random effects | Blocked | Cumulative-logit fixed-effect path exists; random effects error clearly | Not ready |
| Structured non-Gaussian dependence | First Poisson/NB2 q=1 slices only | Ordinary Poisson and ordinary NB2 `phylo(1 | species, tree = tree)` in `mu` have focused extractor, profile-target, diagnostic, and malformed-neighbour evidence. The Poisson route has optional profile-interval artifacts, the ADEMP gate in `docs/design/70-phase-18-poisson-structured-q1-ademp.md`, the runner contract in `docs/design/72-poisson-phylo-q1-runner-contract.md`, an opt-in smoke runner, a formal-grid spec/QA wrapper, and a manual Actions task. The NB2 route has the overdispersion-aware ADEMP gate in `docs/design/74-phase-18-nbinom2-phylo-q1-ademp.md`, an opt-in smoke runner, grouped-comparator rows, a formal-grid spec/QA wrapper, a manual Actions task, the Slices 541-555 formal-audit note in `docs/design/75-phase-18-nbinom2-phylo-q1-formal-audit.md`, the Slices 561-575 sharded-grid dispatch note in `docs/design/76-phase-18-nbinom2-phylo-q1-sharded-formal-grid.md`, and the completed D1 merged-shard audit; neighbouring structured count effects are still planned | Poisson/NB2 q=1 are ready only for focused smoke and formal-admission lanes; broad non-Gaussian structured grids are not ready until formal recovery artifacts pass review |
| Cross-parameter non-Gaussian covariance | Blocked | Cross-distributional-parameter correlation gate records the boundary | Not ready |
| Post-fit extractor and plotting inputs | Supporting infrastructure | `prediction_grid()`, `predict_parameters()`, `marginal_parameters()`, `corpairs()`, `summary()`, `confint()`, `profile_targets()`, `vcov()`, the narrow fixed-effect univariate `mu` `emmeans()` bridge, `plot_parameter_surface()`, and `plot_corpairs()` have explicit status, provenance, or naming contracts; Slice 289 adds default `corpairs()` interval provenance and plot-side source checks | Ready as supporting infrastructure, but not evidence that any blocked model surface is simulation-ready |

## What This Means For Simulation

The first broad-looking simulation report should still be built as a set of
small named grids, not as one all-features grid. The ready and opt-in lanes now
include Gaussian location-scale, selected Gaussian random-effect/covariance
blocks, meta-analysis with known `V`, coordinate spatial Gaussian `mu`, ordinary
Poisson/NB2 `mu` random effects, selected source-tested non-Gaussian `mu`
slopes, first structured Gaussian one-slope `mu` routes, q=1 structured count
source-test lanes, and selected animal/`relmat()` smoke-artifact lanes. Each
report should keep its own failure ledger and should not borrow interval or
diagnostic evidence from a neighbouring surface.

The main blocked surfaces are not small omissions. Non-Gaussian scale random
effects beyond the ordinary NB2 intercept gate, shape/skew random effects,
inflation random effects, ordinal random effects, mixed-response bivariate
families, sparse large-pedigree or large-matrix animal/`relmat()` claims,
multiple structured slopes, residual-scale structured slopes, structured slope
correlations, and broad structured non-Gaussian dependence can all change
identifiability and runtime. They should stay out of comprehensive Phase 18
tables until their own focused gates close.

## Next Surface Decisions

The next implementation choices are therefore narrow:

1. Run tiny optional grids for Poisson and NB2 `mu` random effects, varying
   group count, repeats, true SD, and mean/overdispersion.
2. Return to a blocked model class such as ordinal random intercepts, the
   first non-Gaussian `sigma` random-effect likelihood beyond the ordinary NB2
   intercept gate, or a separate zero-truncated NB2 random-slope design.
3. Build reader-facing examples for the ready surfaces, especially NB2 counts,
   meta-analysis with known `V`, and coordinate spatial Gaussian `mu`.

The simulation programme can start now for ready surfaces, but the package
should not claim "comprehensive non-Gaussian simulation" until the blocked rows
above have either implementation evidence or a deliberate exclusion rationale.

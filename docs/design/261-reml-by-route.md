# 261: REML support by route -- native TMB vs native DRM.jl vs the bridge

GENERATED by `tools/write-reml-route-table.R`. Do not hand-edit; re-run the
script to regenerate. drmTMB #1142 / DRM.jl #624 ("Capability parity between
engines: ML everywhere, REML where possible, and no silent REML-to-ML
downgrade"). Table first measured 2026-09-05 (arc A9f) at DRM.jl pin
`430ef64cc`. Rows measured after a fix landed name their own DRM.jl sha in
their evidence cell.

## Scope and how to read this

30 routes, drawn from two sources: the 25 rows of the committed
`inst/extdata/julia-capabilities.tsv` (which already carries A3's nine
fixed-effect routes, PR #1168), plus A5's three ordinary-random-effect
shapes (PR #1170), which are bridge-admitted routes with no TSV row yet
(A2 in the ultra-plan backlog owns adding one). A route with more than one
REML-relevant family (`phylo_count_large_p`, `phylo_gamma_beta_binomial`,
`general_covariance_structured`) is split into one row per family. No
route below was hand-typed without a source: the generator asserts every
`TSV`-sourced `capability_id` is present in the committed TSV at
generation time.

Every cell is FITS, REFUSES, or N/A (the model or the estimator fork does
not exist on that engine at all), each with a receipt: either measured in
THIS run (script + log path under
`docs/dev-log/evidence/julia-r-parity/reml-by-route/`), or a citation to an existing receipt (A3/A5's own
PRs, a DRM.jl issue, or DRM.jl's own committed test suite) reproduced
verbatim rather than re-measured. The A9f census that built this table
held "Do NOT implement REML anywhere": it recorded every disagreement as a
finding rather than fixing it. Later leaves DO close individual rows -- a
row whose gap note says GAP CLOSED was fixed after that census, and its
evidence cell names the PRs and the build the new numbers came from.

## Summary

| Route | Native TMB | Native DRM.jl | Bridge | Agree? |
|---|---|---|---|---|
| base_gaussian_location_scale | FITS | FITS | FITS | YES |
| biv_gaussian_residual | FITS | FITS | FITS | YES |
| gaussian_phylo_mean | FITS | REFUSES | REFUSES | NO |
| gaussian_response_mask | REFUSES | REFUSES | REFUSES | YES |
| biv_q4_phylo_reml | FITS | FITS | FITS | YES |
| phylo_count_large_p (poisson) | REFUSES | FITS | FITS | NO |
| phylo_count_large_p (nbinom2) | REFUSES | REFUSES | REFUSES | YES |
| phylo_gamma_beta_binomial (gamma) | REFUSES | REFUSES | REFUSES | YES |
| phylo_gamma_beta_binomial (beta) | REFUSES | REFUSES | REFUSES | YES |
| phylo_gamma_beta_binomial (binomial) | REFUSES | REFUSES | REFUSES | YES |
| general_covariance_structured (gaussian) | FITS | REFUSES | REFUSES | NO |
| general_covariance_structured (poisson) | REFUSES | FITS | REFUSES | NO |
| general_covariance_structured (nbinom2) | REFUSES | REFUSES | REFUSES | YES |
| general_covariance_structured (gamma) | REFUSES | REFUSES | REFUSES | YES |
| cross_family_latent | N/A | N/A | REFUSES | YES |
| engine_control_surface | N/A | N/A | N/A | N/A |
| plain_binomial_nonphylo | REFUSES | REFUSES | REFUSES | YES |
| location_scale_scale | FITS | FITS | FITS | YES |
| fe_student | REFUSES | REFUSES | REFUSES | YES |
| fe_lognormal | REFUSES | REFUSES | REFUSES | YES |
| fe_gamma | REFUSES | REFUSES | REFUSES | YES |
| fe_poisson | REFUSES | REFUSES | REFUSES (via a RAW Julia stack trace, not the polished refusal) | YES |
| fe_nbinom2 | REFUSES | REFUSES | REFUSES | YES |
| fe_beta | REFUSES | REFUSES | REFUSES | YES |
| zi_poisson | REFUSES | REFUSES | REFUSES (via a RAW Julia stack trace) | YES |
| zi_nbinom2 | REFUSES | REFUSES | REFUSES | YES |
| hurdle_nbinom2 | REFUSES | REFUSES | REFUSES | YES |
| gaussian_random_intercept | FITS | FITS | FITS | YES |
| gaussian_random_slope | FITS | REFUSES | REFUSES (via a RAW Julia stack trace) | NO |
| gaussian_sigma_random_intercept | FITS | REFUSES | REFUSES | NO |

30 rows: 23 agree (all three engines/layers land the same way), 6 disagree, 1 not classified (orthogonal to REML/ML, or the model does not exist under any estimator).

## Detail, one row per route

### base_gaussian_location_scale

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ x), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-50.3636628254 (native_reml_probe.R, row 1)
- **Native DRM.jl**: FITS -- DRM.jl #624 census (14-fit list, item 1: "univariate fixed-effect Gaussian location-scale")
- **Bridge (`engine = "julia"`)**: FITS -- RE-MEASURED this run with the fixed estim_method oracle read (fit$bridge$estim_method, DRM.jl #625): bridge fit succeeded, estim_method=REML (fit$estimator=REML, fit$effective_REML=TRUE -- all three agree), 35.7s incl. Julia boot (bridge_reml_probe.R, row 1)
- **Agree?**: YES

### biv_gaussian_residual

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(mu1=y1~x, mu2=y2~x, sigma1=~1, sigma2=~1, rho12=~1), biv_gaussian()`
- **Native TMB**: FITS -- RE-MEASURED 2026-09-05 on the same committed fixture: logLik=-97.021205818372, fit$estimator=REML, nobs=60, df=7; no abort branch in drm_validate_reml_spec_biv() triggers for a fixed-effect-only bivariate model, and drm_apply_estimator_spec() hands TMB tmb_random_names=c("beta_mu1","beta_mu2") -- a Laplace step that is EXACT because the likelihood is quadratic in that block
- **Native DRM.jl**: FITS -- WAS REFUSES at pin 430ef64cc. Now measured at DRM.jl claude/parity-reml-biv-residual-drmjl 3a87fe385 (PR itchyshin/DRM.jl#652): native drm(...; method=:REML) fits, estimation_method=:REML, reml_loglik=-97.021205818372, ml_loglik=-90.236796399337. src/gaussian_bivariate.jl `_fit_bivariate_residual_reml` profiles beta_mu1/beta_mu2 by GLS and adds -0.5*logdet(sum_i Z_i S_i^-1 Z_i) plus the (p_beta/2)*log(2*pi) normalised Patterson-Thompson constant (DRM.jl #477) -- the SAME set TMB marginalises on the SAME constant, so there is no offset to remove before comparing
- **Bridge (`engine = "julia"`)**: FITS -- WAS REFUSES. Now measured this run through drmTMB(..., engine="julia", REML=TRUE): logLik=-97.021205818372, fit$estimator=REML, fit$bridge$estim_method=REML, fit$effective_REML=TRUE. Same-target vs engine="tmb" REML on the committed fixture: 7/7 coefficient NAMES identical, max coef diff 4.329e-07 (bar 1e-4), logLik diff 0.000e+00, max SE 6.540e-07 relative (bar 1e-3). ML route untouched: -90.202703298791 on both engines (diff 4.8e-13). tests/testthat/test-julia-reml-biv-residual.R (failed=0 skipped=0 passed=19); receipt docs/dev-log/evidence/julia-r-parity/reml/reml-biv-residual-receipt.md
- **Agree?**: YES -- GAP CLOSED 2026-09-05 (drmTMB #1142 / DRM.jl #624, "REML wherever possible" half; drmTMB PR for the gate + receipt, DRM.jl PR #652 for the objective). This row read FITS/REFUSES/REFUSES: native TMB fitted a plain fixed-effect bivariate Gaussian REML that neither DRM.jl native nor the bridge admitted. BOUNDARY, deliberately narrower than DRM.jl's code: drm_julia_biv_residual_reml_supported() admits ONLY this shape -- no structured marker, no meta_V, no random bar, and intercept-only sigma1/sigma2/rho12. DRM.jl's closed form extends to covariate-carrying sigma/rho designs but nothing has MEASURED those, so the bridge keeps refusing them. meta_V + REML remains a permanent DRM.jl refusal (that route marginalises nothing).

### gaussian_phylo_mean

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree), sigma ~ 1), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-14.6046181690 (native_reml_probe.R, row 6); corroborated by tests/testthat/test-reml-phylo-location.R (existing suite, not re-run here)
- **Native DRM.jl**: REFUSES -- DRM.jl #624 item (c) verbatim: "a Gaussian mean-only phylo(1|species) model refuses REML, while the same tree with an sd() submodel fits it... certainly possible; that asymmetry is a capability gap"
- **Bridge (`engine = "julia"`)**: REFUSES -- measured this run: `engine="julia" cannot fit mean-only phylogenetic Gaussian models by REML=TRUE` (bridge_reml_probe.R, row 3)
- **Agree?**: NO -- DRM.jl #624 item (c) -- explicitly OUT OF SCOPE for this leaf per the task brief; recorded verbatim, not re-litigated.

### gaussian_response_mask

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `same phylo-mean cell as above, missing=miss_control(response="include")`
- **Native TMB**: REFUSES -- measured this run: `REML is not implemented with explicit missing-data engines yet` (native_reml_probe.R, row 7) -- a DIFFERENT refusal reason than the phylo-mean gate above; response="include" itself trips a generic missing-data-engine gate for ANY family
- **Native DRM.jl**: REFUSES -- same underlying cell as gaussian_phylo_mean (DRM.jl #624 item (c)); no separate native call corresponds to "include" outside the bridge
- **Bridge (`engine = "julia"`)**: REFUSES -- measured this run: same mean-only-phylo refusal text as gaussian_phylo_mean (bridge_reml_probe.R, row 4) -- the missing-data mask does not change which REML gate fires first
- **Agree?**: YES -- Agree on REFUSE, but for two DIFFERENT reasons across engines (TMB: missing-data-engine gate; bridge: mean-only-phylo gate). Noted, not a defect.

### biv_q4_phylo_reml

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `biv_gaussian() q4 phylo on mu1,mu2,sigma1,sigma2, REML=TRUE`
- **Native TMB**: FITS -- existing evidence, not re-run (expensive fixture): R/julia-bridge.R capability-comparison comment block; max|d_coef|=0.002889, loglik constant residual 0.001938
- **Native DRM.jl**: FITS -- DRM.jl #624 census (14-fit list): "bivariate q=4 phylo native AND through drm_bridge"
- **Bridge (`engine = "julia"`)**: FITS -- existing evidence, not re-run: same comment block, PARITY_PASS 33/33
- **Agree?**: YES -- Agree on FITTING, but SE/vcov are NOT comparable: DRM.jl #624 item 3 (`_q4_fd_vcov` finite-differences the ML objective on a REML fit; 10.5% SE gap on the committed biv-q4-phylo-reml fixture). Explicitly named out of scope by this leaf's brief ("the q4_vcov-on-REML question" remains unresolved for A11).

### phylo_count_large_p -- poisson

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree)), poisson()`
- **Native TMB**: REFUSES -- measured this run: `REML is implemented for univariate/bivariate Gaussian and binomial models` (native_reml_probe.R, row 9)
- **Native DRM.jl**: FITS -- DRM.jl test suite test_cox_reid_poisson_phylo.jl (#450, "Cox-Reid REML on Poisson phylo/relmat Laplace"); DRM.jl #624 census (14-fit list): "Poisson phylo(1|species) Laplace"
- **Bridge (`engine = "julia"`)**: FITS -- RE-VERIFIED this run at pin 430ef64cc (prior citation in R/julia-bridge.R was at pin e0a65f96b) with the fixed estim_method oracle read: bridge fit succeeded, estim_method=REML (fit$estimator=REML, fit$effective_REML=TRUE -- all three agree), 6.8s (bridge_reml_probe.R, row 5)
- **Agree?**: NO -- Not a defect: this is "REML wherever possible" working -- DRM.jl/the bridge offer a genuine REML route (Cox-Reid Laplace) that native TMB's REML implementation (Gaussian/binomial only) does not have. Documents the asymmetric REML surface drmTMB #1142 item 4 asks about.

### phylo_count_large_p -- nbinom2

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree)), nbinom2()`
- **Native TMB**: REFUSES -- same shared code branch as the poisson sub-row (model_type not in {gaussian, binomial}); not re-run separately
- **Native DRM.jl**: REFUSES -- DRM.jl's Cox-Reid REML route (#443/#450) is wired for Poisson only, not NB2 (test_cox_reid_poisson_ranef.jl, test_cox_reid_poisson_phylo.jl)
- **Bridge (`engine = "julia"`)**: REFUSES -- drm_julia_reml_supported()'s poisson_reml branch checks family_type=="poisson" only; nbinom2 falls through to REFUSE
- **Agree?**: YES

### phylo_gamma_beta_binomial -- gamma

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree)), Gamma(link="log")`
- **Native TMB**: REFUSES -- shared code branch (model_type not in {gaussian,binomial}); measured this run for the fe_gamma row (identical branch)
- **Native DRM.jl**: REFUSES -- not among DRM.jl's two Cox-Reid REML families (Poisson only)
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "Gamma" not in drm_julia_reml_supported()'s gate
- **Agree?**: YES

### phylo_gamma_beta_binomial -- beta

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree)), beta()`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not among DRM.jl's two Cox-Reid REML families
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "beta" not in gate
- **Agree?**: YES

### phylo_gamma_beta_binomial -- binomial

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|species, tree=tree)), stats::binomial()`
- **Native TMB**: REFUSES -- measured this run (shared branch with plain_binomial_nonphylo): binomial REML requires exactly one ORDINARY unlabelled mu random-effect term; a phylo term does not count
- **Native DRM.jl**: REFUSES -- DRM.jl test_cox_reid_poisson_phylo.jl testset "Binomial still rejects :REML"
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "binomial" not in drm_julia_reml_supported()'s gate
- **Agree?**: YES

### general_covariance_structured -- gaussian

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + relmat(1|g, K=K), sigma ~ 1), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-51.6093435424 (native_reml_probe.R, row 8) -- agrees with the ordinary-random-intercept logLik to about 12 significant figures, not identically (abs diff ~1.4e-14 at full double precision -- two different TMB templates converging to the same optimum, not one falling back to the other), because the toy K used (compound-symmetric) is mathematically equivalent to an exchangeable random intercept, confirming the fit is genuine, not a fallback
- **Native DRM.jl**: REFUSES -- DRM.jl #624 comment 2, 16-refuse list: "Gaussian mean-only structured markers (phylo/relmat) with no sd() submodel"
- **Bridge (`engine = "julia"`)**: REFUSES -- measured this run: `engine="julia" cannot fit structured-effect models by REML=TRUE` (bridge_reml_probe.R, row 7) -- drm_julia_has_structured_term() (relmat/animal/spatial) is checked BEFORE any family dispatch and refuses unconditionally
- **Agree?**: NO -- Same asymmetry as gaussian_phylo_mean (DRM.jl #624 item (c)-adjacent), for relmat instead of phylo. Not a new issue number; recorded alongside item (c).

### general_covariance_structured -- poisson

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + relmat(1|g, K=K)), poisson()`
- **Native TMB**: REFUSES -- shared code branch (model_type not in {gaussian,binomial}); not re-run separately
- **Native DRM.jl**: FITS -- DRM.jl's OWN test suite, test_cox_reid_poisson_phylo.jl lines 134-142, testset "relmat/animal share the same spine" (#450): `drm(bf(y~x+relmat(1|id)), Poisson(); A=K, method=:REML)` fits and `estimation_method(fit_rel) === :REML`
- **Bridge (`engine = "julia"`)**: REFUSES -- RE-VERIFIED live this run (bridge_reml_probe2.R): `engine="julia" cannot fit structured-effect models by REML=TRUE`. drm_julia_has_structured_term() intercepts relmat/animal/spatial for EVERY family, before drm_julia_reml_supported()'s Poisson-specific branch is ever consulted.
- **Agree?**: NO -- NEW FINDING (drmTMB #1142 / DRM.jl #624, opposite direction from the fe_poisson gap below): the bridge UNDER-admits relative to DRM.jl's own real capability. DRM.jl natively fits Poisson+relmat/animal REML (#450, its own passing test), but engine="julia" can never reach it because the R-side structured-term gate fires first for every family, not just the ones (Gaussian) that gate is meant to police. Not filed as a GitHub issue in this leaf; flagged for A11/follow-up.

### general_covariance_structured -- nbinom2

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + relmat(1|g, K=K)), nbinom2()`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not among DRM.jl's two Cox-Reid REML families (Poisson only, even for relmat)
- **Bridge (`engine = "julia"`)**: REFUSES -- double-refused: structured-term gate fires first, and nbinom2 is not in the poisson-only gate either
- **Agree?**: YES

### general_covariance_structured -- gamma

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + relmat(1|g, K=K)), Gamma(link="log")`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- double-refused (structured-term gate, and family not in gate)
- **Agree?**: YES

### cross_family_latent

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(mu1=y1~x, mu2=y2~x), c(gaussian(), poisson())`
- **Native TMB**: N/A -- measured this run: the model itself does not exist natively under ANY estimator -- `Mixed-response bivariate families are not implemented yet` (native_reml_probe.R, row 11); this is not a REML-specific refusal
- **Native DRM.jl**: N/A -- cross-family is a bridge-only construct (drm_julia_is_cross_family()); no single native drm() call corresponds to it, so DRM.jl-native REML does not apply here either
- **Bridge (`engine = "julia"`)**: REFUSES -- measured this run: `engine="julia" cannot fit cross-family models by REML=TRUE`, refused BEFORE dispatch, unconditionally (bridge_reml_probe.R, row 9)
- **Agree?**: YES -- Both sides land on "no REML", but for structurally different reasons (TMB: the model doesn't exist at all; bridge: an explicit design refusal per its permanent claim_boundary, D-179 #3). Not a gap on the REML axis specifically.

### engine_control_surface

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `drm_control(optimizer = list(g_tol = ..., algorithm = ...))`
- **Native TMB**: N/A -- orthogonal to the estimator choice -- optimizer control knobs apply identically under ML and REML
- **Native DRM.jl**: N/A -- same
- **Bridge (`engine = "julia"`)**: N/A -- same
- **Agree?**: N/A -- Not classified: this route is not a REML/ML fork.

### plain_binomial_nonphylo

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(cbind(succ, fail) ~ x), stats::binomial()`
- **Native TMB**: REFUSES -- measured this run: `Binomial REML requires exactly one admitted ordinary unlabelled mu random-effect term... no ordinary mu random intercept` (native_reml_probe.R, row 10)
- **Native DRM.jl**: REFUSES -- DRM.jl test_cox_reid_poisson_phylo.jl testset "Binomial still rejects :REML"
- **Bridge (`engine = "julia"`)**: REFUSES -- measured this run: `engine="julia" cannot fit non-Gaussian (binomial) models by REML=TRUE` (bridge_reml_probe.R, row 8)
- **Agree?**: YES

### location_scale_scale

- **Source**: TSV (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x + phylo(1|sp, tree=tr), sigma ~ x, sd_phylo(sp) ~ x)`
- **Native TMB**: FITS -- RE-RUN this session: testthat::test_file("tests/testthat/test-reml-direct-sd-phylo.R") -- 8/8 pass, 0 failures, NOT_CRAN=true
- **Native DRM.jl**: FITS -- DRM.jl #624 census (14-fit list): "LSS sd_phylo dense and sparse (#551)"
- **Bridge (`engine = "julia"`)**: FITS -- commit 79e8f0951 ("widen Julia REML support for LSS models & promote Capability Row 12 to covered"): drm_julia_reml_supported() widened for sd()/sd_phylo(); tests/testthat/test-julia-sigma-phylo-reml.R
- **Agree?**: YES

### fe_student

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ 1, nu ~ 1), student(), fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch (measured live for fe_gamma/fe_poisson, identical branch: model_type not in {gaussian,binomial})
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family; also has zero random effects
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "student" not in drm_julia_reml_supported()'s gate
- **Agree?**: YES

### fe_lognormal

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ 1), lognormal(), fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "lognormal" not in gate
- **Agree?**: YES

### fe_gamma

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x), Gamma(link="log"), fixed-effect only`
- **Native TMB**: REFUSES -- measured this run: `REML is implemented for univariate/bivariate Gaussian and binomial models` (native_reml_probe.R, row 12)
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "Gamma" not in gate
- **Agree?**: YES

### fe_poisson

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x), poisson(), fixed-effect only (ZERO random effects)`
- **Native TMB**: REFUSES -- measured this run: `REML is implemented for univariate/bivariate Gaussian and binomial models` (native_reml_probe.R, row 13)
- **Native DRM.jl**: REFUSES -- DRM.jl's OWN test suite, test_cox_reid_poisson_ranef.jl: "Fixed-effects-only Poisson has no variance component to restrict" -> ArgumentError
- **Bridge (`engine = "julia"`)**: REFUSES (via a RAW Julia stack trace, not the polished refusal) -- measured this run (bridge_reml_probe.R, rows 6-7): `ArgumentError: drm (Poisson): method = :REML is not available with no random effect (fixed-effects-only / zi / hu)` surfaces as "Error happens in Julia" plus the full Julia stacktrace
- **Agree?**: YES -- OUTCOME agrees (all three refuse) but the INTERFACE does not: NEW FINDING, honesty-of-interface class (cf. #625's own fixes). drm_julia_reml_supported()'s poisson_reml branch checks only `!sigma_phylo && !has_sd` -- it never checks whether the model has ANY random effect at all -- so a fixed-effect-only Poisson REML request is forwarded to Julia instead of being caught by the R-side drm_julia_refuse_reml_unsupported() cli_abort. DRM.jl itself then throws, and the user sees a raw ArgumentError + full Julia stacktrace instead of the clean, actionable refusal every other cell in this table gets. Not filed as a GitHub issue in this leaf; flagged for A11/follow-up.

### fe_nbinom2

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x), nbinom2(), fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "nbinom2" not in the poisson-only gate; refused cleanly (no raw stack trace -- unlike fe_poisson, nbinom2 never reaches Julia at all)
- **Agree?**: YES

### fe_beta

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ 1), beta(), fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "beta" not in gate
- **Agree?**: YES

### zi_poisson

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, zi ~ x), poisson(), ZERO mu random effects`
- **Native TMB**: REFUSES -- shared code branch (model_type check runs before any dpar-specific logic)
- **Native DRM.jl**: REFUSES -- DRM.jl's own ArgumentError names `zi`/`hu` explicitly alongside fixed-effects-only as having no variance component to restrict
- **Bridge (`engine = "julia"`)**: REFUSES (via a RAW Julia stack trace) -- measured this run (bridge_reml_probe.R, row 8): identical ArgumentError/stacktrace class as fe_poisson -- family_type is still "poisson", so drm_julia_reml_supported() forwards it
- **Agree?**: YES -- Same honesty-of-interface gap as fe_poisson (see that row); not a second distinct defect, just a second cell hitting the identical unguarded branch.

### zi_nbinom2

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ 1, zi ~ 1), nbinom2(), fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch, not re-run separately
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "nbinom2" not in the poisson-only gate -- refused CLEANLY (the polished cli_abort fires; nbinom2 is not swept up in the same over-admission as poisson)
- **Agree?**: YES

### hurdle_nbinom2

- **Source**: TSV (A3, PR #1168) (`inst/extdata/julia-capabilities.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ 1, hu ~ 1), nbinom2() [bridge spelling], fixed-effect only`
- **Native TMB**: REFUSES -- shared code branch (native fits the same target via truncated_nbinom2()+hu, also non-Gaussian/non-binomial, also refuses REML identically)
- **Native DRM.jl**: REFUSES -- not a Cox-Reid REML family
- **Bridge (`engine = "julia"`)**: REFUSES -- family_type "nbinom2" not in the poisson-only gate -- refused cleanly
- **Agree?**: YES

### gaussian_random_intercept

- **Source**: A5-census (`PR #1170, docs/dev-log/evidence/julia-r-parity/ordinary-re-census/census.tsv`)
- **Formula**: `bf(y ~ x + (1|g), sigma ~ 1), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-51.6093435424 (native_reml_probe.R, row 3)
- **Native DRM.jl**: FITS -- A5 census.tsv, layer=engine-direct: ml_loglik=-171.636217965634 reml_loglik=-174.437057568359 (cited verbatim, a5-census-verbatim.tsv row 6)
- **Bridge (`engine = "julia"`)**: FITS -- A5 census.tsv, layer=shipped: identical ml_loglik/reml_loglik (a5-census-verbatim.tsv row 2)
- **Agree?**: YES

### gaussian_random_slope

- **Source**: A5-census (`PR #1170, docs/dev-log/evidence/julia-r-parity/ordinary-re-census/census.tsv`)
- **Formula**: `bf(y ~ x + (1+x|g), sigma ~ 1), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-50.4273340933 (native_reml_probe.R, row 4); validated Gaussian ordinary random effects, including slopes, are an admitted REML route (R/drmTMB.R comments, recovery ladders cited there)
- **Native DRM.jl**: REFUSES -- A5 census.tsv, layer=engine-direct: REFUSED/UNSUPPORTED (a5-census-verbatim.tsv row 10)
- **Bridge (`engine = "julia"`)**: REFUSES (via a RAW Julia stack trace) -- A5 census.tsv, layer=shipped (a5-census-verbatim.tsv row 4): `ArgumentError: drm: method = :REML is not implemented for this model on the generic univariate Gaussian route (random slopes, ...)`, surfaced as "Error happens in Julia" -- the SAME honesty-of-interface class as fe_poisson: drm_julia_reml_supported()'s Gaussian branch only checks phylo/sigma_phylo/has_sd, never whether the mu-side random effect is a plain single intercept vs a slope, so it forwards a slope model to Julia instead of refusing it on the R side
- **Agree?**: NO -- drmTMB #1142 / DRM.jl #624's OWN central example (item 2, 16-refuse list: "Gaussian random slopes"). Not new as a capability gap, but the INTERFACE half (raw Julia stack trace instead of drm_julia_refuse_reml_unsupported()) is the same unfiled defect class as fe_poisson/zi_poisson/general_covariance_structured-poisson above -- four cells now, all sharing one root cause: drm_julia_reml_supported() only screens for phylo/sd/structured-term presence, never for "is the random-effect shape itself one DRM.jl actually restricts."

### gaussian_sigma_random_intercept

- **Source**: A5-census (`PR #1170, docs/dev-log/evidence/julia-r-parity/ordinary-re-census/census.tsv`)
- **Formula**: `bf(y ~ x, sigma ~ (1|g)), gaussian()`
- **Native TMB**: FITS -- measured this run: logLik=-53.3331996169 (native_reml_probe.R, row 5); native TMB admits ordinary sigma-side random intercepts under REML (R/drmTMB.R comment, 2026-07-08)
- **Native DRM.jl**: REFUSES -- A5 census.tsv, layer=engine-direct: REFUSED/UNSUPPORTED
- **Bridge (`engine = "julia"`)**: REFUSES -- A5 census.tsv, layer=shipped: `engine="julia" does not support method="REML" with a random intercept on sigma` -- the POLISHED cli_abort DOES fire here (drm_julia_check_ordinary_sigma_ranef_route_limits() catches this case explicitly, unlike the mu-side slope case above)
- **Agree?**: NO -- drmTMB #1142's own motivating example (the ArgumentError quoted in the issue body is this exact cell). Not new; the interface here is HONEST (clean R-side refusal, no raw stack trace) because this is the one case the maintainers already special-cased ("Night question 14" comment in R/julia-bridge.R).

## Gaps (every disagreement, with its issue)

1. **gaussian_phylo_mean**: DRM.jl #624 item (c) -- explicitly OUT OF SCOPE for this leaf per the task brief; recorded verbatim, not re-litigated.
2. **phylo_count_large_p (poisson)**: Not a defect: this is "REML wherever possible" working -- DRM.jl/the bridge offer a genuine REML route (Cox-Reid Laplace) that native TMB's REML implementation (Gaussian/binomial only) does not have. Documents the asymmetric REML surface drmTMB #1142 item 4 asks about.
3. **general_covariance_structured (gaussian)**: Same asymmetry as gaussian_phylo_mean (DRM.jl #624 item (c)-adjacent), for relmat instead of phylo. Not a new issue number; recorded alongside item (c).
4. **general_covariance_structured (poisson)**: NEW FINDING (drmTMB #1142 / DRM.jl #624, opposite direction from the fe_poisson gap below): the bridge UNDER-admits relative to DRM.jl's own real capability. DRM.jl natively fits Poisson+relmat/animal REML (#450, its own passing test), but engine="julia" can never reach it because the R-side structured-term gate fires first for every family, not just the ones (Gaussian) that gate is meant to police. Not filed as a GitHub issue in this leaf; flagged for A11/follow-up.
5. **gaussian_random_slope**: drmTMB #1142 / DRM.jl #624's OWN central example (item 2, 16-refuse list: "Gaussian random slopes"). Not new as a capability gap, but the INTERFACE half (raw Julia stack trace instead of drm_julia_refuse_reml_unsupported()) is the same unfiled defect class as fe_poisson/zi_poisson/general_covariance_structured-poisson above -- four cells now, all sharing one root cause: drm_julia_reml_supported() only screens for phylo/sd/structured-term presence, never for "is the random-effect shape itself one DRM.jl actually restricts."
6. **gaussian_sigma_random_intercept**: drmTMB #1142's own motivating example (the ArgumentError quoted in the issue body is this exact cell). Not new; the interface here is HONEST (clean R-side refusal, no raw stack trace) because this is the one case the maintainers already special-cased ("Night question 14" comment in R/julia-bridge.R).

## Not covered by this leaf

- The q4_vcov-on-REML question (DRM.jl #624 item 3): SE/vcov correctness
  on the one route where all three columns already agree the model FITS.
- DRM.jl #624 item (c), mean-only phylogenetic Gaussian REML: recorded as a
  gap on two rows above (`gaussian_phylo_mean`, and the Gaussian sub-row of
  `general_covariance_structured`); not implemented here.
- The four cells sharing the drm_julia_reml_supported() honesty-of-interface
  gap (`fe_poisson`, `zi_poisson`, `general_covariance_structured`/poisson,
  `gaussian_random_slope`) are recorded, not fixed. No GitHub issue was
  filed for this in this leaf; it is flagged for A11 or a follow-up leaf.
- No REML implementation changed anywhere in this leaf (scope: MEASURE and
  TABLE only).


# Plan vs actual — drmTMB 0.7 issue sweep

## Planned outcome

Audit the exact 29-issue inventory against current main, publish one closed
disposition and action per issue, make only verified status-maintenance
comments, and report a finite issue-derived candidate-blocker set without
advancing a release rung.

## Actual outcome

The ledger accounts for all 29 issues exactly. It identifies #61 as the sole
issue-derived candidate blocker and #870 as the sole owner-policy decision;
the remaining 27 issues are non-blocking post-0.7 or backlog work. Five
status-correcting comments were posted to #3, #5, #33, #61, and #531. No issue
was closed, no implementation began, and no release rung changed.

## Material deviations

1. PR #951 merged while the plan was under review, so G0 used a fresh worktree
   from `origin/main@efb5af4f` instead of the earlier handover base. This was the
   plan's documented live-state repair, not scope expansion.
2. V1 found citation-audit defects in the first draft: informal paths, one
   reversed line range, absent action totals, an unsupported fixed-effect phrase,
   and two extra EOF blank lines. The ledger was repaired and the final Luna
   rerun passed.
3. The GitHub connector could read threads but returned HTTP 403 on comments.
   It created no comments. Authenticated `gh issue comment --body-file` posted
   the same five verified bodies, and connector read-back confirmed every URL.
4. No issue met the evidence bar for closure, so the planned closure path was
   unused. This is an evidence result, not incomplete execution.

## Verification and reviews

- R0 Luna repo prefilter plus three independent issue batches covered all 29
  issues before root consolidation.
- Root re-read all expensive or ambiguous candidates.
- V1 Luna final verdict: PASS.
- V2 Rose verdict: PASS.
- V2 Grace's wording repair was applied: predecessor `ad475cc39` / SHA-256
  `2e5234bd4bf819663e9ef95f10a1944d51c90ce64ffd5dd7a9641b69fa50c5ea`
  remains historical proof; `main@efb5af4` is neither `tarball-clean` nor
  `platform-clean`; candidate preparation cannot discharge D-93 or D-117.

## Boundary reconciliation

The actual diff remains limited to the plan, G0 receipt, authoritative audit
ledger, check log, after-task report, plan-actual report, and two pointer
updates. There are no package-code, test, DESCRIPTION, `cran-comments.md`,
platform, D-43, simulation, or foreign-lane changes.

# OU v1 R3 likelihood geometry checkpoint

## Status

**LANE: START A FRESH TASK.** R3 completed its approved profile and one-point
small-tree integration diagnostics. It does not authorise G15 or full OU v1
grammar expansion.

## Evidence

The 50-point profile grid (two retained R2 data sets times a 5 by 5 rate grid)
was measured first: one point projected the full grid at 125.32 seconds.
Every profile point was finite and re-optimised intercepts, field SDs, and
latent modes with both rates fixed at the grid value.

The first stored R2 data set has a high-rate optimum `(8, 8)` and its truth
`(0.7, 1.3)` is 2.885 objective units worse. Fitted sigma-field SD collapses
across a broad alpha-sigma direction. The complementary data set has a
shallower but still weak scale-rate direction: truth is only 0.040 objective
units above its grid minimum.

At one 3-tip plug-in point, independent 5/7-node Gauss--Hermite quadrature
matches the native TMB Laplace objective to `4.4e-10`, with quadrature
refinement `8.6e-14`. This is a small fixed-point validation only.

## Independent verdict and next action

Fisher concludes that the profile evidence is a real rate--amplitude /
scale-boundary geometry problem, while the one small integration comparison
does not exclude a high-dimensional Laplace contribution. G15 remains a
no-go. The detailed review is
`docs/dev-log/evidence/ou-v1-r3/INFERENCE-REVIEW.md`.

R3.1 needs separate approval: exact marginal comparisons at a retained
interior truth point and high-alpha/low-SD ridge point. Do not interpret R3
as proof of a broad Laplace guarantee or as an OU v1 recovery result.

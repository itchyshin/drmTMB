# Plan vs actual — drmTMB 0.6.0 CRAN-RC gate (Phase 20)

**Lane:** drmTMB_final (single continuous Claude Code lane). **Merged:** RC PR #804 → `origin/main`
`b852851b` (2026-07-20). **Deliverable met:** the **tarball-clean + local-UBSAN** rung.

## Scope — planned vs actual

| | Planned (ultra-plan S0–S10 + MV/RC) | Actual |
|---|---|---|
| Deliverable | ONE RC PR to tarball-clean + local UBSAN | ✅ PR #804 merged at that rung |
| Slices | S0–S10, MV, RC | ✅ all run; S8 expanded to **3 panel rounds** (see deltas) |
| Human gates | G3 (version, prior), G4 (merge) | ✅ G3 prior; **G4 approved + merged this session** |
| Compute (G5) | none | ✅ none |
| Manifest ceiling | not exceeded | ✅ only factual corrections (version, #710 status, basis) |

## The five deltas from plan (honest)

1. **S8 took THREE rounds, not one.** Plan assumed one D-43 panel on one frozen artifact. Actual: r1
   (afd4600a) 3× NOT-READY → r2 (e818e165) 2 READY + 1 NOT-READY → r3 (9ca4d07c) 3× READY. The panel
   worked exactly as intended (it caught real defects), but the plan under-budgeted the iteration.
2. **THREE re-freezes, not one.** Each surface-honesty fix that touched a shipped vignette forced a
   rebuild + `--as-cran` re-run. The second re-freeze was avoidable: my r1 cross-family fix patched only
   the extractor table cell and missed two prose siblings in the same file (the Rose-principle failure).
   Pat's r2 dissent caught it; r3 swept the whole vignette. **Lesson logged in the after-task (§9/§11).**
3. **Round-3 Grace/Rose re-dispatch skipped (Shinichi's call).** Only Pat (the r2 dissenter) was
   re-dispatched fresh in r3; Grace/Rose carried their r2 READY plus a round-3 delta the orchestrator
   machine-verified (hash/inventory/lane/smoke/UBSAN/gate). Recorded transparently in the ledger `panel`.
4. **Install-smoke evidence was dishonest at r1 and got corrected.** The r1 frozen `install-smoke.log`
   ended `SMOKE_EXIT=1` (an ad-hoc `coef(fit)[1]` idiom vs `coef.drmTMB`'s list return) yet was labelled
   "PASSED". Fixed by re-running a correct-API smoke; the handover claim was corrected in place.
5. **ROADMAP.md was missed by the S2 reader-surface sweep** (still recommended v0.5.0 / showed
   0.6.0.9000) and was caught by the panel, not the sweep. Confirms the plan's own trap #3
   ("focused/source evidence hides neighbours") — the version-bump commit was scoped to N files and the
   sweep didn't re-diff every file the manifest claimed as "reconciled."

## Evidence — as planned, plus extra

Beyond the plan, the re-freezes **froze the lane-proof evidence** round-1 had left only in a working file
(`cran-lane-testthat.Rout` + `laneproof-invert-devmode.log` now live in the frozen dir) — closing Grace's
r1 concern that the CRAN-lane count `PASS 12011` lived only in `drmTMB.Rcheck`. Also corrected a loose
round-1 claim that the dev-mode invert count "== 12011": it is `PASS 13476` (NOT_CRAN=true dev mode) and is
a corroboration, **not** a scalar match to the CRAN lane.

## Routing

Read-only review sub-agents (sonnet): the D-43 trio × (r1 full + r2 full + r3 Pat). Orchestrator (Opus)
did the builds/checks/freezes/ledger/reconcile. No compute agents. Vault (local-only, D-37): LOOP
checkpoint + Mission Control updated at each slice/stop.

## Claims / rung honesty

Reported rung = **tarball-clean + local-UBSAN** throughout — never "ready", never platform-clean. The
declared next gates (remote platform matrix; CRAN submission) were kept explicitly out of lane. Zero-one-beta
mc-0575 carried as the fenced generator-qualified 2-1-split claim (Noether WITHHELD), unchanged.

## Handoff / residuals

- **G2 follow-ups still to file** (propose-first): Julia xfam extractor issue (+ Pat's r3 items: NEWS
  entry, `predict.drmTMB_julia.Rd` xfam caveat, README direct link), and the pre-existing set
  (worm/qq figure, coef() doc, animal-models h², README jargon, phase18 workflow D-50, #59, #710.2).
- **#61 kept OPEN** (its DoD includes paper-prep/comparator work beyond this lane).
- **Next lane (separate):** the remote platform matrix (win-builder / R-hub UBSAN·valgrind·rchk / 3-OS)
  then the CRAN submission (G6). 0.6.0 is a first/new submission.

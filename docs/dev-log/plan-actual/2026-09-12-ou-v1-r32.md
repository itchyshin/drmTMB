# Plan versus actual — OU v1 R3.2 exact relative profile

## Approved scope

R3.2 was approved to determine whether the R3.1 Laplace discrepancy changes
relative ordering in a fixed, tiny rate--amplitude grid. It did not authorize
recovery, an estimator rewrite, or a public capability claim.

## What ran

An isolated worktree from `8e52fdad1` received a scoped path lease before
edits. The four native fixed-hyperparameter cells were compared to independent
six-dimensional stationary-tip product quadrature. A one-cell timed core of
0.989 seconds projected 3.956 seconds for the four-cell nine-node grid. The
actual retained grid completed locally in 29.8 seconds including loading.

## Result and reconciliation

At nine nodes, the direct and Laplace ranks match. A strengthening check then
retained five/seven/nine nodes for all cells and eleven/thirteen nodes for the
problematic ridge/interior cell. That integral remains moving by 0.0413 NLL
from eleven to thirteen nodes. Fisher rejected an exact-rank conclusion. The
planned decision is therefore unresolved rather than silently promoted.

## Handoff

No estimator repair or recovery redesign follows. The sole proposed next task
is a fresh direct-integration convergence arc with an error-bound criterion
for both unresolved cells, or an independently error-estimated adaptive or
importance integral. It requires new approval, a fresh worktree, a lease, and
a ledger before edits.

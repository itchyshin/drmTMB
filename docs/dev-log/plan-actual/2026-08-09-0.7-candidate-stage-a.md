# Plan vs actual — 0.7.0 candidate preparation, Stage A

**Date:** 2026-08-09 · **Reconciler:** Melissa · **Lane:** Claude task 1
**Plan:** `~/.claude/plans/snazzy-purring-wall.md` · **Commit:** `8eac0846f`
**Axes:** scope · evidence/verification · model routing · safety gates · public claims · handoff

Material deviations only. Cosmetic wording and ordering changes are not drift.

## Slice-level outcome

| Planned slice | Status | Artifact |
| --- | --- | --- |
| R0 rehydrate/classify | done | lane created, handover cherry-picked (`88768389f`, `0c297dee8`) |
| A1 gate orientation | done | `2026-08-09-07-gate-orientation.md` |
| A2 instrument salvage | done | `2026-08-09-07-instrument-salvage.md` + `STALE-EVIDENCE-QUARANTINE.md` |
| A3 product contract + rights | done | `2026-08-09-07-product-contract.md`, `…-rights-ledger.md` |
| A4 ledger instantiation | done | `2026-08-09-07-cran-release-ledger.json` + `…-ledger-gate-receipt.md` |
| A5 #870 analysis | done | `2026-08-09-07-870-offset-analysis.md` |
| A6 Stage-B byte fixes | done | `2026-08-09-07-stage-b-byte-fixes.md` |
| V mechanical verify | done | V1–V7 below |
| M reconcile | this file | — |
| H Gate H hold | done | `2026-08-09-07-gate-h-checkpoint.md` |

No slice was silently dropped.

## Deviations

### D1 · scope · **adaptive**
A5 and A6 were **not in the original plan**. Both were added by explicit owner decision
mid-planning (AskUserQuestion, 2026-08-09): "#870 — I'll decide now, give me the analysis"
and "known NOTE fixes — prepare in Stage A, land in Stage B". Recorded in the plan file
under *Owner decisions taken 2026-08-09* before execution. **Justified.**

### D2 · scope · **adaptive**
A four-lens pre-release capability-gap review (`…-pre-release-user-gap-review.md`) was added
mid-arc on the owner's request ("give me some good pictures of what is missing … send a few
team members to discuss"). It is outside Stage A's original slice table but inside Gate 0's
remit (product contract / overclaim search), so it was filed as a release audit rather than
as a separate lane. **Justified.**

### D3 · model routing · **adaptive**
The plan recorded `FAN-OUT: 0 sub-agents`, because the session's operating guidance forbids
the Agent tool unless the user requests it. The user then explicitly requested it. Four
sub-agents ran, **all Sonnet** (Pat, Darwin, Boole, Fisher) — consistent with the
default-to-Sonnet routing rule; no Opus child, no ceiling escalation. Child budget 4/6,
ceiling 0/1. **Justified and within budget.**

### D4 · evidence/verification · **adaptive, with a quality cost worth recording**
Three of four sub-agents had no `Write` tool and two (Pat, Boole) had no `Bash`. Their
findings were returned in-message and captured by the orchestrator rather than written to
the briefed output paths. Consequence: **two of four lenses produced source traces, not
executed observations.** The single HIGH-severity claim resting purely on reading — Boole's
"bivariate `(1|id)` silently fits uncorrelated" — was **refuted** when the orchestrator ran
it (`scratchpad/biv-corr-probe.R`); the guard exists and fails closed. Fisher's executed
findings all held.

Recorded rather than smoothed over: the brief specified an output path the agents could not
satisfy, and the tool mismatch selectively degraded the lenses whose claims were behavioural.
**Process fix for Rose:** when a sub-agent brief asks for behavioural findings, verify the
agent type actually has Bash/Write before dispatch, or state up front that findings will be
static traces.

### D5 · safety gates · **adaptive**
The goal block specified `status_claim = "source-clean-pending"`. The gate rejects it —
`RUNGS` admits only the ten canonical rungs. Resolved by claiming the nearest real rung
(`source-clean`) with the evidence genuinely absent, so the gate **fails closed (exit 1)**
and names the two missing rows. This is stronger than the intended state: it converts an
informal "pending" label into a machine-checked checklist. The gate additionally refused a
`rationale` that excused a *deferred* row rather than a `NOT_APPLICABLE:` one — correct, and
flagged in the receipt as the Stage B temptation point.

### D6 · safety gates · **adaptive**
A1 **deliberately deferred** the protocol's "refresh current CRAN policy at release time"
step to Stage B, on the grounds that a read taken now would be stale by the time the
candidate is built after the separation disposition. This is a departure from running every
Gate -1 step in order; it is stated in A1 §4 and is one of the two rows the gate now blocks
on, so it cannot be silently forgotten. **Justified.**

### D7 · public claims · **no drift**
Nothing advanced. Verdict held at **NOT READY**; `DESCRIPTION` held at `0.6.0`;
`platform-clean` not written; D-43 not fired; D-93/D-117 untouched; no rung claimed above
its evidence. The four overclaims found (offset docs, README v0.5.0 install, stale
spatial-REML prose, CITATION fallback) were **recorded, not fixed** — correctly deferred to
Stage B as installed-byte changes.

### D8 · handoff state · **as planned**
Committed to `claude/07-candidate-preparation-staged` (`8eac0846f`), **not pushed, no PR** —
Stage A is an internal checkpoint and the lane waits at Gate H. No foreign lane touched:
#858, #937, historical #947, the five stashes, the dirty primary, and the separation
worktree are all untouched. Never used `git add -A`.

## Verification receipts

| # | Check | Result |
| --- | --- | --- |
| V1 | drift `ad475cc39 → ac363cadb` over build-included paths | **15** — unchanged from plan |
| V2 | `DESCRIPTION` version | **0.6.0** |
| V3 | lane vs `origin/main` | 0 behind / 3 ahead |
| V4 | source-tree edits | **none** — only new dev-log docs |
| V5 | files added | 10, all docs |
| V6 | forbidden-claim scan (`platform-clean`, `submission-ready`, "CRAN ready", "READY FOR") | all hits are non-goal lists or quoted tool output demonstrating the trap |
| V7 | gate selftest | 14/14 negative controls fail closed |
| V8 | Stage A ledger vs gate | **NOT READY, exit 1**, naming the two owed rows |

## Unresolved / routed

| Item | Owner |
| --- | --- |
| Separation disposition (hard freeze gate) | Claude task 2 |
| #870 Option 1 vs 2 | Shinichi — recommendation: Option 1 |
| Tier surfacing before or after 0.7 | Shinichi — narrow candidate is the bootstrap-boundary warning |
| `source_commit_is_current` gate control | Shinichi — brain `tools/`, not this repo |
| D-93 / D-117 publish-discharge | Shinichi |

## Drift ledger

**No `drift` and no `unclear` entries.** All eight deviations are `adaptive` and recorded
with justification. D4 carries a process fix routed to Rose; it is a brief-construction
defect, not a scope or claim deviation.

# Arc 6 F1M — plan versus actual

## Planned capacity

The approved programme allocated 480 minutes across reconnaissance, independent review, focused test work, coordinate adjudication, a fresh F1 panel, conditional F3 packet, and closeout.  It explicitly required an early stop for any unresolved or mismatched finite fixture.

## Actual outcome

The complete F1 matrix passed without changing tolerances, production controls, or runtime statuses.  The only mathematical extension was the planned independent CDF-scale zero-count formulation, which preserves the `-Inf` endpoint rather than approximating it.  Fresh mathematical, inference, and scope reviews all accepted the deterministic gate.

## Deliberate non-work

F3 was not run.  No refit runner, simulation, remote computation, F4/F5 work, public interface, ledger, or Arc D/F5 change was made.  The drafted F3 packet is conditional on a later, separately written owner approval.

## Disposition

F1M is complete as `F1_PASS`; the next task is not authorized until the owner explicitly approves the exact one-cell F3 protocol at the final post-F1M SHA.

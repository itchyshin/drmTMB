# C2 plan-versus-actual reconciliation

## Delivered

- Rebased cleanly, preserving foreign C3/C9 untracked artifacts.
- Admitted exactly the three approved ordinary-Poisson labelled provider q2
  forms and extended their visible-but-not-profile-ready target status.
- Added provider-order dense-oracle, gradient, reduction, liveness,
  extractor, and neighbour tests.
- Retained three passing three-seed fixtures plus IID controls and updated
  `mc-0446`, `mc-0450`, and `mc-0454` to `point_fit_recovery`.
- Regenerated the ledger surface: 38 -> 35 not implemented.

## Deviation

The first spatial fixture invocation failed at DGP construction because the
new independent precision helper used incompatible dimnames. The error receipt
is retained. The runner was fixed, committed, and rerun under a distinct
receipt directory; all planned model attempts then passed. The closeout also
made minimal grammar/vignette/README/limitations wording updates after review
identified stale public rejection language.

## Not Delivered

No interval/profile computation, bootstrap, coverage, remote compute,
association work, bivariate expansion, zero-one-beta work, or broader provider
grammar was performed.

# Plan vs. actual — D-117 discharge (100k re-gate)

**Plan:** `~/.claude/plans/fancy-kindling-scone.md` · **Arc:** `claude/d117-discharge` off
`origin/main @ a2695a788` · **Date:** 2026-08-09 · **Reconciler:** Melissa (light,
receipt-based; not a re-review of the statistics — a D-43 panel already did that)

Twelve commits (`a2695a788..HEAD`, ending `13c2519e4`), branch not pushed, no PR. This
reconciles what the plan committed to against what the receipts show actually happened,
along six axes. Only material deviations are tagged; exact 1:1 matches are not listed as rows.

---

## 1. Scope

| planned | actual | tag | note |
|---|---|---|---|
| Pre-registration predicted the worst cell BORDERLINE → overall no-discharge (`PREREGISTRATION.md`); plan's decision block frames the failure fork ("Hold + cost both paths") as the live branch | All four cells PASS at 100k; the predicted-FAIL premise did not hold | **adaptive** | Falsification condition was pre-committed and explicitly named as wrong the moment it was met (`PREDICTION-OUTCOME.md`); nothing was reverse-engineered |
| S4: "the two costed paths" — (a) estimand write-up, (b) document-and-ship package, framed against an anticipated FAIL | Delivered as a "case FOR discharge" / "case AGAINST discharge" packet given the actual PASS | **adaptive** | Functionally serves the same "both options priced" goal; the pivot itself is not narrated anywhere as a deliberate premise change — a reader has to infer it |
| Slice table: `S3 dep: S1` (findings review depends on the 100k results) | S3 (`8b962c16b`, Fisher/`PANEL-FINDINGS-2-3-4.md`) was committed before S1's 100k output existed — the doc itself states "the 100,000-replicate re-run… had produced no output at the time of this review… nothing below uses it" | **adaptive** | Self-disclosed at the top of the artifact, no conclusion rests on missing data, and finding #3's z-recomputation is correctly redone against the real numbers later in `VERDICT-100K.md §5`. Still, the literal dependency edge in the plan was not honoured, and no document states *why* it was run early (parallelization is the plausible reason but is not written down) |
| S5 named "in scope" in the plan itself (member Rose, part of the "IN PARALLEL" list) | Executed exactly as planned (`fcdab58f2`) | **adaptive** (plan matched) | The later D-43 panel's Rose seat called this same commit "off-scope for a D-117 lane" (after-task §7a, Rose finding #5, "MINOR — accepted; content accurate, lane wrong"). That is a retrospective disagreement about whether the *plan* should have included it, not an execution deviation — actual matches planned. Flagged for whoever scopes the next D-117-style lane |
| Not in the S0–S8 table | A twelfth commit (`13c2519e4`) repairs every panel-blocking finding after the panel reported | **adaptive** | Reasonable consequence of a 2/3 NOT-DONE verdict; explicitly and repeatedly stated as **not re-adjudicated** (`VERDICT-100K.md §8`, after-task §7a) rather than presented as a fix that reopens the claim |

## 2. Evidence / verification

No material deviations. Every planned check ran: smoke gate (S0, cell 4 `--nrep=50`), pre-registration
committed before dispatch, the byte-identical scorer, the pre-registered cross-checks (one-sided LCB,
exact `binom.test`), prefix reproduction against the banked 2026-08-04 numbers, mechanical verify (S6),
and the D-43 panel (S7) — all present with receipts (`campaign100k.log`, `CROSSCHECKS.csv`,
`results/SUMMARY.csv`, after-task §5). One thing added beyond the plan's `PREREGISTRATION.md` cross-check
list — re-running lme4 at 100k — was correctly scoped as **not done** and stated as a residual
(`VERDICT-100K.md §6` scope note, after-task §10), not silently dropped.

## 3. Model routing

| planned | actual | tag | note |
|---|---|---|---|
| S0 → Curie/Haiku/low; S1 → Curie/Sonnet/medium | Both run inline by the orchestrator ("main") — no `Curie-S0`/`Curie-S1` subagent appears in the dispatch roster | **drift** | Functionally reasonable (the pre-registration's integrity arguably benefits from the same actor authoring and committing it before dispatch), and obliquely stated in the after-task header ("Claude ran the live R/TMB and Totoro compute"). But it is never reconciled against the plan's explicit Curie assignment anywhere in the artifacts — this reconciliation is the first place it is named as a routing change |
| S2→Gauss/Sonnet, S3→Fisher/Sonnet, S4→Fisher/Sonnet, S5→Rose/Haiku, S6→Curie/Haiku | Matches exactly (`Gauss-S2`, `Fisher-S3`, `Fisher-S4`, `Rose-S5`, `Curie-S6`) | — | compliant, no row needed |
| Panel: 2×Sonnet + 1×Opus | Noether/Sonnet, Grace/Sonnet, Rose/Opus | — | compliant, matches the separately-budgeted composition exactly |

No planned Haiku slice was silently upgraded (S5, S6 stayed Haiku). Dispatched-subagent headcount (5) stayed under the 6-producer budget; the two absorbed-inline slices did not add a new named dispatch.

## 4. Safety gates

| planned | actual | tag | note |
|---|---|---|---|
| Phase 0.25 sweep receipt | Present, seven-row table with concrete commands/greps/findings | — | compliant, non-vacuous |
| Smoke-first (S0 hard gate before scale) | Cell 4, `--nrep=50`, one fit inspected past its guards, before any 100k dispatch | — | compliant |
| Pre-registration committed before results | `ab83638f5` at 16:04:54; campaign start 16:09:52 (`campaign100k.log`) — verified independently by the panel's Rose seat too | — | compliant |
| Seed ceiling `nrep ≤ 100000` | Log confirms exactly 100,000, no collision | — | compliant |
| D-43 panel fired exactly once, 2 build + 1 ceiling, told to refute | Fired once 2026-08-09; Noether(Sonnet)/Grace(Sonnet)/Rose(Opus) | — | compliant |
| "Freeze the tree before dispatching reviewers" (the arc's own stated discipline, `after-task §11`) | The lme4-scope fix (`423b30ac6`) was committed **while the panel was actively reviewing**; Noether's own seat noticed HEAD advancing mid-review, and the dispatch brief undercounted commits (said 9, actually 11) | **drift** | Self-disclosed in `VERDICT-100K.md §8` and after-task §9/§11 as "two process faults of mine." Disclosed, but not a decision that was made deliberately in advance — it is a genuine violation of the review-isolation discipline the arc itself states, caught by the panel rather than prevented by process |

## 5. Public claims

| planned/expected | actual | tag | note |
|---|---|---|---|
| Composite claim WITHHELD (2/3 NOT-DONE) should read consistently everywhere it is cited | `VERDICT-100K.md`, the decision packet, and the after-task report all state the withholding plainly and repeatedly | — | compliant across those three documents |
| Same, for `check-log.md` — the project's most visible cross-session record | The 2026-08-09 top entry (lines 4–47) reports the PASS, the pre-registration, every check, and the caveats — but **never mentions the D-43 panel or that the composite claim was withheld**. (Confirmed by grep: the only `D-43`/`NOT-DONE`/`withheld` hits in the file belong to the *older* 2026-08-04 entry, not this one.) | **drift** | Material: a reader of `check-log.md` alone — the first place most sessions look — would see an unqualified PASS narrative with no indication that a fresh panel returned 2/3 NOT-DONE the same day |
| Recovery figures superseded by `VERDICT-100K.md §6b` (−15.76/−9.31/−10.26/−8.34%) should not be cited as −16.90/−9.12/−9.05/−9.16% without a pointer | The decision packet §1 (`2026-08-09-d117-discharge-decision-packet.md`, "For Shinichi. He decides.") still cites the stale n=1,000 figures, with **no internal reference to §6b anywhere in the packet** (grepped: no hits for "6b" or "superseded") | **drift** | The supersession is stated only in a *different* document (after-task §8: "the decision packet… should be read against §6b; it was written before they were recomputed"). The decision-facing document itself was never revised or annotated — a reader of the packet alone would not know its own headline number is stale |

## 6. Handoff state

No deviations. Branch `claude/d117-discharge` is 12 commits ahead of `origin/main`, unpushed, no PR — stated consistently in the plan's status block, `VERDICT-100K.md §9`, and after-task §10. Carried-over items are named explicitly (fresh re-adjudication of panel repairs, lme4 comparator not re-run at 100k, decision-packet correction, `check-log.md` D-43 mention, `ss_floor` design question, `package_commit = NA`). No merge, tag, CRAN action, or PR — matches the plan's fences.

---

## Drift routed to

| # | drift | routed to | why |
|---|---|---|---|
| 1 | S0/S1 run inline instead of the planned Curie dispatch, never reconciled against the plan in any artifact | **Ada** | scope/routing decision |
| 2 | Mid-panel document edit (`423b30ac6`) while the D-43 panel was actively reviewing | **Rose** | closeout/review-integrity discipline |
| 3 | `check-log.md` top entry omits the D-43 panel and the WITHHELD verdict | **Rose** | closeout/claims consistency |
| 4 | Decision packet (`2026-08-09-d117-discharge-decision-packet.md`) still carries stale n=1,000 recovery figures with no internal pointer to `§6b` | **Fisher** | method-evidence document, Fisher's own artifact |

## Summary

The arc executed close to plan: every safety gate fired, the pre-registration was genuinely
adversarial and correctly falsified, and the panel's WITHHELD verdict is stated consistently in
the three documents that matter most for the statistical claim (`VERDICT-100K.md`, the decision
packet, the after-task report). The four drift items are process/documentation gaps, not
statistical ones — an un-reconciled routing choice, a review-isolation lapse the panel itself
caught, and two places where the withheld/stale status did not propagate into a document a reader
might consult in isolation (`check-log.md`, the decision packet). None of them touch the coverage
number itself, which two of three fresh reviewers independently reproduced bit-for-bit.

`adaptive=4 drift=4 unclear=0`

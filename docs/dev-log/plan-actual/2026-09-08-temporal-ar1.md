# Temporal AR1 plan versus actual

The approved implementation slice is complete: parser, native likelihood,
same-ID ordinary intercept, full-Hessian fixed-effect Wald interface,
deterministic references, reader documentation, final-source recovery, pilot,
and package check were delivered locally. G1--G14 are recorded met in the
ignored Unlazy ledger.

The measured final-source pilot took 68.22 seconds wall time, used about
565 MiB maximum resident memory, and projects roughly 3.8 serial CPU-hours
for 5,000 datasets before overhead. Its C1 interval availability was 4/5.
That is evidence against moving directly to the calibration campaign, not a
reason to change seeds or thresholds. G16, G17, and G18 remain pending; G0 and
G15 are documented manual closure evidence in the after-task report.

The final local implementation commit is recorded after this closure note.

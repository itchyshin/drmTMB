# Plan versus actual — R6 Ayumi fixed-alpha receipt

The first BM timing fit took 75.865 seconds and projected the six-fit grid
well below the three-hour boundary. The first grid used raw alpha values and
revealed a unit error: the source tree has depth 66.787 after pruning, while
R5's grid was depth-one. The raw grid was not interpreted. R6 corrected the
grid by dividing each normalized strength by this depth and reran all fits in
the same attached local session class. The corrected six-fit receipt completed
cleanly, with all convergence and Hessian checks passing.

This closes the initial Ayumi empirical acceptance example for fixed-alpha
location-side OU sensitivity. It does not broaden public syntax or resolve the
larger free-alpha OU-v1 programme.

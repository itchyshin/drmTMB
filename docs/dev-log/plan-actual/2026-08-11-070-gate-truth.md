# Plan-vs-actual reconciliation — 070 CRAN gate truth arc (2026-08-11)

Commits reconciled: `079b66523` (and predecessor `885f260d1`) on `claude/07-candidate-freeze-2`.
Sources: `git log`/`git diff` on both commits, `git status`, `gh pr view 996`,
`docs/dev-log/after-task/2026-08-11-070-gate-truth.md`, S4 verdict file
(`/private/tmp/.../2d578235.../scratchpad/s4-verdict.md`).

| axis | planned | actual | tag | decision-owner |
| --- | --- | --- | --- | --- |
| scope | S1–S6 slices; fences: no DESCRIPTION/NEWS, no re-freeze, no upload, no PR #996 merge | All fences held — verified `git diff --name-only` on both commits touches only `cran-comments.md`, `docs/dev-log/**`; `git ls-files "*.tar.gz"` empty; `gh pr view 996` → `state: OPEN, mergedAt: null`. Two items **not in the slice table** were added and disclosed in after-task §3a: (1) a durable evidence root `~/drmTMB-release-artifacts/0.7.0/` created *outside* version control, with `repo_path` back-references, because the gate fails closed on missing paths; (2) negative-control/falsification testing of the gate itself. | adaptive | Ada (scope) — recorded with rationale in §3a, no fence crossed |
| evidence/verification | S4: one fresh adversarial reviewer, default NOT READY, two NOT-READY votes withhold | Only one reviewer ran (Opus), not two independent votes — the plan's "two NOT-READY votes withhold" language describes a panel that never existed here; S4 was a single lens used twice (first pass fresh, NOT READY with 4 blocking defects; same instance re-verified after fixes → READY tarball-clean-only, with two falsification attempts: git-blob hash match on all 6 durable copies, and an active GPL-3/GPL(>=3) compatibility test). A Haiku recon scout (S1) misreported `cran_release_gate.py` and the ledger JSON as *absent* when both exist (after-task §9) — caught by direct `git grep`, not propagated into the plan. The task brief's "prior-work sweep receipt (8 surfaces)" does not match anything found in the repo: after-task §8 "Consistency audit" checks ~3 concrete items (the 0.6.0 ledger, `RUNG-REPORT-0.7.0.md`, a "CRAN ready" string search), not an enumerated 8. | drift (scout misreport, caught) / unclear (the "8 surfaces" figure) | Rose (closeout/claims) |
| model routing | Haiku scout (S1) / Sonnet (S2) / Sonnet (S3) / Opus (S4, single verify) — max 6 children, exactly 1 Opus, 1 Haiku | Matches on model tiers and count (4 children + Melissa = 5, within budget of 6; exactly 1 Opus, 1 Haiku used). The deviation is *reuse*, not tier: S4 was not re-spawned as a fresh child for the re-verification pass, it was continued via a follow-up message in the same session/context. This keeps independence from the S2/S3 implementers (the "own-the-verifier" bar) but forfeits a second, contextually-uncontaminated look at the fixed artifact — mitigated in practice by the reviewer running active falsification rather than trusting the fix description. | adaptive | Ada (routing) |
| safety gates | D-49 rung fence (no platform-clean without explicit authorisation); no fabricated authority | Held. `status_claim` in both ledgers is `tarball-clean`; `platform-clean` appears only as "NOT claimed" / mechanically blocked (raising it in the negative-control test returns NOT READY). A security classifier fired on an orchestrator-authored coordination-board phrase ("lives legitimately") that overstated authority; self-corrected pre-commit to a fence-respecting, non-approving statement (after-task §9). This is the safety mechanism working as intended, not a bypass. | adaptive | Rose (claims) — near-miss worth naming, not a defect |
| public claims | cran-comments.md should not overstate what evidence covers | S3/S2 work initially shipped a false claim — "Every check result below was run against these exact bytes" — while listing CI/R-hub runs that build their own tarball and never see the frozen bytes (S4 blocking defect #1). Caught by the planned verify step exactly as designed, fixed before commit; re-verification confirms the sentence is gone and the file now correctly labels Exact-bytes vs Same-source evidence. No overstated claim reached the committed record. | adaptive (caught by the planned gate) | Rose (claims) |
| handoff state | S5: after-task + check-log + push | After-task report and check-log entry both exist and are substantive (`docs/dev-log/after-task/2026-08-11-070-gate-truth.md`, check-log 2026-08-11 section); branch is pushed and matches `origin/claude/07-candidate-freeze-2` (0 ahead/0 behind). PR #996 remains open, unmerged (confirmed via `gh`), consistent with the fence. No dedicated handover doc was written even though the arc explicitly hands unfinished rungs (win-builder, submission-ready) to Shinichi/a future session — the plan's S5 didn't call for one, but the hub protocol (`AGENTS.md` "Finishing work" step 2) expects a handover when work continues elsewhere. Two untracked helper scripts (`scratchpad/freeze.sh`, `scratchpad/freeze-ready.sh`) sit undeclared in the worktree, flagged by S4 as non-blocking. | unclear | Rose (closeout) |

## Verdict

The arc's substance matches its plan closely: every explicit fence (DESCRIPTION/NEWS/R/src untouched,
no upload, no re-freeze, PR #996 unmerged, no platform-clean claim) held under direct verification, and
the one real public-claims defect (the exact-bytes overstatement in `cran-comments.md`) was caught and
fixed by the planned verify step rather than slipping through. The unplanned durable-artifact-store
addition is disclosed and justified, not silent scope creep. Two things fall short of a clean
reconciliation and are routed below: a scout's factual misreport that happened to get caught, and a
sweep-receipt count in the task brief ("8 surfaces") that this reconciler could not locate anywhere in
the repo — the after-task's actual consistency audit is real but covers roughly 3 items, not 8.

## Drift/unclear routed

- **Rose (closeout/claims):** S1 Haiku scout reported `cran_release_gate.py` and the ledger JSON as
  *absent* when both exist in the repo (after-task §9). It did not propagate into the plan because it
  was caught by a direct `git grep`, but a scout hallucinating the absence of load-bearing tooling is
  worth watching for recurrence across arcs.
- **Rose (closeout/claims):** the task brief's claimed "prior-work sweep receipt (8 surfaces)" has no
  matching artifact in the repo. The after-task §8 "Consistency audit" is real and non-vacuous but
  enumerates roughly 3 checks. Either the count belongs to an ephemeral plan-phase sweep never committed,
  or the figure is inflated relative to what landed — worth a one-line correction wherever "8 surfaces"
  is asserted upstream of this repo.
- **Rose (closeout/claims):** no handover doc was written despite PR #996 remaining open with
  win-builder/submission-ready work still pending for a future session; confirm whether S5's
  after-task+check-log satisfies the hub's handover expectation or whether one is still owed.

# Plan vs actual — drmTMB 0.7 capability-truth reconciliation

## Planned outcome

Reconcile the capability ledger, runtime gates, reader documentation, generated
surfaces, and Mission Control after the 29-issue sweep. The exact public change
was to retain only ordinary unlabelled binomial `mu` random intercepts and one
independent slope under `REML = TRUE`, at `diagnostic_only`; fixed-only,
correlated, labelled, structured, and missing-response variants were to fail
early. The cumulative-logit O3 study was to remain internal technical evidence,
not a callable or reportable `drmTMB()` estimator.

## Actual outcome

The runtime now rejects fixed-only binomial REML before `MakeADFun()` and keeps
the two admitted ordinary O2 routes. Deterministic intercept and slope fixtures
match `glmmTMB(REML = TRUE)`, converge with positive-definite reported Hessians,
and return finite covariance matrices. The five target ledger cells now carry
their exact public status and evidence tier, generated surfaces agree, and the
reader documentation names both the admitted routes and their exclusions.

`mc-0227` retains its independently earned public ML-Laplace
`point_fit_recovery` tier. The O3 AGHQ25 plus Cox--Reid study remains preserved
as package-private technical provenance and grants no public estimator,
interval, or reporting permission.

## Material deviations

1. PR #951 had already merged. The plan's G0 contingency therefore used that
   merged base, then merged PR #952 unchanged before opening the fresh
   capability-truth lane at `main@31da19f28`.
2. Editing `R/drmTMB.R` correctly tripped the C17/C14 current-source blob gate
   even though the authenticated zero-one-beta semantic fingerprint remained
   unchanged. The existing runner was rerun against current source into a new
   immutable dated receipt; the historical receipt was not rewritten.
3. The first full package test exposed two stale substring assertions for the
   Julia REML fallback warning. The runtime warning was correct; the tests were
   updated to assert the new bounded Gaussian-bridge and native diagnostic-only
   binomial wording, then the focused test passed.
4. No simulation campaign was needed or run. The new evidence is a bounded
   deterministic comparator and rejection matrix, not recovery or coverage.

## Boundary reconciliation

There is no DESCRIPTION change, release-rung change, platform-clean claim,
D-43 panel, compute campaign, `cran-comments.md` finalization, tag, candidate
freeze, or CRAN upload. Draft PRs #858 and #937, historical PR #947, all foreign
worktrees and stashes, the dirty primary checkout, and the dirty MSPL checkout
remain outside this lane.

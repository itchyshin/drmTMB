# Plan versus actual — Lane C Z7 spatial sigma q1

The spatial pilot began with a passing but invalid DGP ordering. Fisher caught
the mismatch before any ledger move. The corrected run-2 aligns named
coordinates and latent draw, retains all four attempts, and passes. The old
run remains a durable invalid-runner receipt. No Future-extension, profile,
interval, coverage, remote-compute, or other cell change occurred.

# AOI-2 final local preflight — additive, n = 360

## Exact source

This final source-loaded preflight ran from campaign SHA
`d565ea940e7e7429deb8d8cf5273f5523a7d45ce`, the same SHA staged at the
Rorqual run root for array job `17779204`.

## Result

One retained additive `n = 360` attempt completed with association status
`interior` and `pdHess = TRUE` for both refitted margins.  The raw output
retains the encoded design fingerprint, alpha truth/estimates, fixed-newdata
link truth/predictions, elapsed time, session information, and source SHA.

It is a one-replicate execution-path preflight only; it supplies neither a
point-recovery claim nor any uncertainty result.

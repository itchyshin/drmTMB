diff --git a/R/profile.R b/R/profile.R
index 46256e97..35030c5d 100644
--- a/R/profile.R
+++ b/R/profile.R
@@ -1986,6 +1986,56 @@ structured_sd_group_count <- function(object) {
   as.numeric(g)
 }

+structured_sd_term_matches_label <- function(term, label) {
+  if (
+    !is.character(term) ||
+      !is.character(label) ||
+      length(term) != 1L ||
+      length(label) != 1L ||
+      is.na(term) ||
+      is.na(label)
+  ) {
+    return(FALSE)
+  }
+  term <- trimws(term)
+  label <- trimws(label)
+  if (!nzchar(term) || !nzchar(label)) {
+    return(FALSE)
+  }
+  if (identical(term, label) || endsWith(term, label)) {
+    return(TRUE)
+  }
+  term_sig <- structured_sd_term_signature(term)
+  label_sig <- structured_sd_term_signature(label)
+  if (is.null(term_sig) || is.null(label_sig)) {
+    return(FALSE)
+  }
+  identical(term_sig$type, label_sig$type) &&
+    identical(term_sig$group, label_sig$group)
+}
+
+structured_sd_term_signature <- function(term) {
+  match <- regexec(
+    "^\\s*([[:alnum:]_]+)\\s*\\((.*)\\)\\s*$",
+    term,
+    perl = TRUE
+  )
+  parts <- regmatches(term, match)[[1L]]
+  if (length(parts) != 3L) {
+    return(NULL)
+  }
+  pieces <- strsplit(parts[[3L]], "|", fixed = TRUE)[[1L]]
+  if (length(pieces) < 2L) {
+    return(NULL)
+  }
+  group <- trimws(paste(pieces[-1L], collapse = "|"))
+  group <- trimws(sub(",.*$", "", group))
+  if (!nzchar(group)) {
+    return(NULL)
+  }
+  list(type = parts[[2L]], group = group)
+}
+
 # Resolve the group count for one SD target row. Tries the structured location
 # block first (matched on the target term's structured label), then a labelled
 # covariance-block registry match. Returns NA_real_ when no unique match exists.
@@ -2020,8 +2070,7 @@ wald_sd_target_group_count <- function(
       is.character(label) &&
         length(label) == 1L &&
         nzchar(label) &&
-        !is.na(term) &&
-        endsWith(term, label)
+        structured_sd_term_matches_label(term, label)
     ) {
       return(structured_g)
     }
@@ -2854,7 +2903,8 @@ drm_profile_endpoint_result <- function(
       max_bracket_steps = max_bracket_steps,
       target_name = target$parm,
       curvature_se = curvature_se,
-      max_eval = max_eval
+      max_eval = max_eval,
+      allow_lower_boundary = identical(target$transformation, "exp")
     )
   }
   crossings <- profile_lapply(c(-1L, 1L), endpoint_worker, endpoint_plan)
@@ -2983,7 +3033,8 @@ profile_endpoint_crossing <- function(
   max_bracket_steps,
   target_name,
   curvature_se = NA_real_,
-  max_eval = NULL
+  max_eval = NULL,
+  allow_lower_boundary = FALSE
 ) {
   n_eval <- 0L
   last_free <- evaluator$start_free
@@ -3019,7 +3070,31 @@ profile_endpoint_crossing <- function(
   initial_step <- step
   n_bracket_step <- 0L
   outer <- theta_hat + direction * step
-  outer_value <- eval_root(outer)
+  lower_boundary_result <- function() {
+    list(
+      theta = -Inf,
+      root_error = NA_real_,
+      n_eval = n_eval,
+      initial_step = initial_step,
+      bracket_step = step,
+      n_bracket_step = n_bracket_step,
+      step_source = paste(step_info$source, "lower_boundary", sep = "_")
+    )
+  }
+  eval_or_boundary <- function(theta) {
+    out <- tryCatch(eval_root(theta), error = function(err) err)
+    if (inherits(out, "error")) {
+      if (direction < 0 && isTRUE(allow_lower_boundary)) {
+        return(lower_boundary_result())
+      }
+      stop(out)
+    }
+    out
+  }
+  outer_value <- eval_or_boundary(outer)
+  if (is.list(outer_value)) {
+    return(outer_value)
+  }
   for (i in seq_len(max_bracket_steps)) {
     if (is.finite(outer_value) && outer_value >= 0) {
       break
@@ -3027,7 +3102,17 @@ profile_endpoint_crossing <- function(
     step <- step * 1.6
     n_bracket_step <- i
     outer <- theta_hat + direction * step
-    outer_value <- eval_root(outer)
+    outer_value <- eval_or_boundary(outer)
+    if (is.list(outer_value)) {
+      return(outer_value)
+    }
+  }
+  if (
+    direction < 0 &&
+      isTRUE(allow_lower_boundary) &&
+      (!is.finite(outer_value) || outer_value < 0)
+  ) {
+    return(lower_boundary_result())
   }
   if (!is.finite(outer_value) || outer_value < 0) {
     cli::cli_abort(c(
@@ -3084,6 +3169,10 @@ profile_endpoint_initial_step <- function(
   ) {
     step <- sqrt(2 * cutoff) * curvature_se * 1.1
     if (is.finite(step) && step > sqrt(.Machine$double.eps)) {
+      step_cap <- max(2, abs(theta_hat) * 0.5)
+      if (step > step_cap) {
+        return(list(step = step_cap, source = "curvature_capped"))
+      }
       return(list(step = step, source = "curvature"))
     }
   }
diff --git a/docs/dev-log/check-log.md b/docs/dev-log/check-log.md
index fda90664..5d1b9107 100644
--- a/docs/dev-log/check-log.md
+++ b/docs/dev-log/check-log.md
@@ -2,6 +2,620 @@

 Record meaningful development checks here.

+## 2026-06-29: Q-Series Gaussian q1 mu-slope SR475 top-up
+
+Goal:
+
+- Top up the Gaussian q1 `mu` one-slope phylo, relmat, and spatial rows from
+  SR150 to an SR475 retained hybrid denominator, then decide whether the rows
+  are promotable or still blocked.
+
+Result:
+
+- Ran the generated-seed top-up for replicate indices 151-475 with animal
+  excluded.
+- Ran endpoint-profile repair on the 27 top-up boundary/non-Wald rows.
+- Added `tools/summarize-structured-re-gaussian-mu-slope-hybrid-sr475.R`.
+- Wrote
+  `docs/dev-log/dashboard/structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv`
+  and the six-row target summary under
+  `docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-topup-sr475-local/`.
+- Updated the widget to display phylo, relmat, and spatial as
+  `mcse_met_upper_tail_blocked`.
+- Bumped the dashboard build to `r95`.
+
+Evidence:
+
+- Top-up pregrid: 325 generated seeds, 975 fit-status rows, 1950 retained
+  target-replicate rows, six target summaries, and three provider summaries.
+  All 975 fits converged with `pdHess = TRUE`.
+- Top-up boundary profiles: 27 boundary/non-Wald rows profiled; phylo 18/18
+  finite with 5 covered and 13 upper misses, relmat 2/2 finite with 0 covered
+  and 2 upper misses, spatial 6/7 finite with 0 covered, 6 upper misses, and
+  one profile failure.
+- Combined SR475 hybrid audit: phylo 913/950 covered, MCSE 0.006277, misses
+  5/31; relmat 926/950 covered, MCSE 0.005091, misses 3/20; spatial 912/950
+  covered, MCSE 0.006358, misses 15/22.
+- All six target summaries meet MCSE `<= 0.01`; worst target is phylo `mu:x`
+  with coverage 0.9537, MCSE 0.009643, and lower/upper misses 1/20.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok` with three Gaussian mu-slope hybrid-SR475 audit rows.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+
+Boundary:
+
+- This is MCSE-qualified negative/routing evidence, not a status promotion. The
+  support-cell TSV remains `fit_status = point_fit`, `interval_status =
+  planned`, and `coverage_status = planned` for the three SR475 rows. No
+  `inference_ready`, `supported`, q2/q4/q8, sigma, non-Gaussian, REML,
+  AI-REML, bridge support, or public support is promoted.
+
+## 2026-06-29: Q-Series Gaussian q1 mu-slope top-up runner
+
+Goal:
+
+- Make the phylo, relmat, and spatial Gaussian q1 `mu` one-slope top-up lane
+  executable from generated non-overlapping seeds while keeping animal excluded
+  and preventing smoke runs from overwriting the dashboard sidecar.
+
+Result:
+
+- Extended `tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R` with
+  `--seed-start`, `--seed-base`, `--providers`, `--write-dashboard`, copied
+  seed manifests, and a help path.
+- Extended
+  `tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R`
+  with `--source-replicates` and a help path so future top-up artifacts can use
+  the same endpoint-profile boundary repair channel.
+- Wrote a one-replicate top-up smoke artifact under
+  `docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-topup-smoke-local/`.
+
+Evidence:
+
+- `air format tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R`:
+  passed.
+- `air format
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R`:
+  passed.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R --help`:
+  passed and printed the new runner options.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R
+  --help`: passed and printed the new source-override options.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R
+  --n-rep=1 --seed-start=151 --providers=phylo,relmat,spatial
+  --output-dir=docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-topup-smoke-local
+  --write-dashboard=false --overwrite=true`: passed, writing one generated
+  seed, three fit rows, six retained target-replicate rows, six target
+  summaries, three provider summaries, `sessionInfo.txt`, `git-sha.txt`, and a
+  run log with `dashboard_results = not_written`.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R
+  --source-replicates=docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-coverage-pregrid-local/structured-re-gaussian-mu-slope-coverage-pregrid-replicates.tsv
+  --max-rows=1 --output-dir=/tmp/drmtmb-gaussian-mu-slope-boundary-source-smoke
+  --write-dashboard=false --overwrite=true`: passed, writing one diagnostic
+  detail row and one summary row.
+
+Boundary:
+
+- This is executable-runner evidence only. It does not promote
+  `inference_ready`, `supported`, q2/q4/q8, sigma, non-Gaussian, REML,
+  AI-REML, bridge support, or public support; the support-cell TSV is unchanged.
+
+## 2026-06-29: Q-Series Gaussian q1 mu-slope hybrid boundary audit
+
+Goal:
+
+- Overlay the repaired endpoint-profile boundary rows onto the original SR150
+  Gaussian q1 `mu` slope pregrid denominator and update the widget routing
+  without promoting interval or coverage status.
+
+Result:
+
+- Added
+  `tools/summarize-structured-re-gaussian-mu-slope-hybrid-boundary.R`.
+- Wrote
+  `docs/dev-log/dashboard/structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv`.
+- Updated the widget to load the hybrid sidecar as the current q1 `mu` slope
+  admission overlay. Animal remains `mu_slope_pregrid_blocked`; phylo, relmat,
+  and spatial move to `topup_required`.
+- Bumped the mission-control dashboard build to `r94`.
+
+Evidence:
+
+- Hybrid audit split: animal 132/150 covered, 147/150 usable intervals,
+  coverage 0.880, lower/upper misses 0/15, hard blocked; phylo 284/300
+  covered, 299/300 usable, coverage 0.947, misses 3/12, top-up candidate;
+  relmat 289/300 covered, 299/300 usable, coverage 0.963, misses 2/8, top-up
+  candidate; spatial 286/300 covered, 300/300 usable, coverage 0.953, misses
+  6/8, top-up candidate.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file
+  tools/summarize-structured-re-gaussian-mu-slope-hybrid-boundary.R
+  --overwrite=true`: passed, writing four dashboard rows.
+- `air format
+  tools/summarize-structured-re-gaussian-mu-slope-hybrid-boundary.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok` with four Gaussian mu-slope hybrid-boundary audit rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `git diff --check`: passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background` plus
+  `curl -fsS http://127.0.0.1:8765/version.txt`: dashboard listening, served
+  build `r94`.
+- `curl -fsS
+  http://127.0.0.1:8765/structured-re-q-series-support-cells.tsv | wc -l`:
+  105 lines, meaning header plus 104 Q-Series rows.
+
+Boundary:
+
+- This is SR150 routing evidence only. It does not promote
+  `inference_ready`, `supported`, q2/q4/q8, sigma, non-Gaussian, REML,
+  AI-REML, bridge support, or public support; all linked support cells remain
+  `interval_status = planned` and `coverage_status = planned`.
+
+## 2026-06-29: Q-Series profile endpoint lower-boundary fix
+
+Goal:
+
+- Fix the scalar endpoint-profile path for positive SD targets whose lower
+  confidence endpoint should land on the zero boundary instead of failing after
+  an unstable first step near the ML boundary.
+
+Result:
+
+- Updated `R/profile.R` so positive-scale endpoint profiles cap absurd
+  curvature-derived initial steps and permit a lower-side zero endpoint for
+  `exp`-transformed targets when the likelihood endpoint is on the boundary.
+- Added unit coverage for the capped initial-step source and for lower-boundary
+  endpoint handling.
+- Added a regression fixture for the failing animal q1 `mu` slope replicate
+  (`seed = 791004`, replicate 4, `sd:mu:animal(0 + x | id)`).
+- Reran the 42-row Gaussian q1 `mu` one-slope boundary-profile diagnostic.
+  The fix partially rescued finite lower endpoints but did not unblock the
+  rows: animal has 25/27 finite profile intervals, 10 covered, 15 upper misses,
+  and two profile failures; phylo has 8/9 finite, one covered, seven upper
+  misses, and one profile failure; relmat has 2/3 finite, zero covered, two
+  upper misses, and one profile failure; spatial has 3/3 finite, zero covered,
+  and three upper misses.
+- The four linked support cells remain `fit_status = point_fit`,
+  `interval_status = planned`, and `coverage_status = planned`.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "phase18-animal-mu-slope")'`: 58 PASS / 0 FAIL /
+  0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "profile-targets")'`: 797 PASS / 0 FAIL / 0 WARN /
+  0 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R
+  --overwrite=true`: passed, rewriting 42 detail rows and four dashboard rows.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-gaussian-mu-slope-boundary-profile-diagnostic.md');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-profile-endpoint-lower-boundary-fix.md')"`:
+  both after-task structure checks passed.
+- `sed -n '/<script>/,/<\/script>/p'
+  docs/dev-log/dashboard/index.html | sed '1d;$d' | node --check -`: passed.
+- `git diff --check`: passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background`: dashboard already listening at
+  `http://127.0.0.1:8765/` after a passing mission-control check.
+- `curl -fsS http://127.0.0.1:8765/version.txt`: served build `r93`.
+- `curl -fsS
+  http://127.0.0.1:8765/structured-re-q-series-support-cells.tsv | wc -l`:
+  105 lines, meaning header plus 104 Q-Series rows.
+- `curl -fsS
+  http://127.0.0.1:8765/structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv
+  | head -n 5`: served the four updated boundary-profile diagnostic rows.
+- `air format R/profile.R tests/testthat/test-profile-targets.R
+  tests/testthat/test-phase18-animal-mu-slope.R`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test()'`: 19754 PASS / 0 FAIL / 17 WARN / 43 SKIP. The warnings
+  are existing warning-path tests for non-convergence, clamp, and missing-data
+  edge cases; no endpoint/profile regression failed.
+
+Boundary:
+
+- This is a profile-domain repair and negative diagnostic update. It does not
+  promote Gaussian q1 `mu` one-slope rows to top-up readiness,
+  `inference_ready`, or `supported`, and it does not claim sigma, q2/q4/q8,
+  non-Gaussian, REML, AI-REML, bridge support, or public support.
+
+## 2026-06-29: Q-Series q1 mu-slope boundary profile diagnostic
+
+Goal:
+
+- Diagnose whether endpoint profiles rescue the 42 SR150 Gaussian q1 `mu`
+  one-slope boundary/non-Wald interval rows before any top-up or promotion work.
+
+Result:
+
+- Added
+  `tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R`.
+- Wrote 42 detail rows and four dashboard summary rows under
+  `docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-boundary-profile-diagnostic-local/`
+  and
+  `docs/dev-log/dashboard/structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv`.
+- The first run showed that the exact 42 boundary rows refit with convergence
+  and `pdHess = TRUE`, but the endpoint profile failed for every row.
+- After the follow-up endpoint lower-boundary fix, the rerun still keeps all
+  four rows blocked: animal has 25/27 finite profile intervals, 10 covered, 15
+  upper misses, and two profile failures; phylo has 8/9 finite, one covered,
+  seven upper misses, and one profile failure; relmat has 2/3 finite, zero
+  covered, two upper misses, and one profile failure; spatial has 3/3 finite,
+  zero covered, and three upper misses.
+- The Q-Series widget now shows this separate boundary-profile blocker beside
+  the four Gaussian q1 `mu` one-slope rows. Their support-cell statuses remain
+  `interval_status = planned` and `coverage_status = planned`.
+- Bumped the mission-control dashboard build to `r93`.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R
+  --max-rows=2 --output-dir=/tmp/drmtmb-mu-boundary-profile-smoke
+  --overwrite=true --write-dashboard=false`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file
+  tools/run-structured-re-gaussian-mu-slope-boundary-profile-diagnostic.R
+  --overwrite=true`: passed, writing 42 detail rows and four dashboard rows.
+- `python3 -m py_compile tools/validate-mission-control.py` plus
+  `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `git diff --check`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-gaussian-mu-slope-boundary-profile-diagnostic.md')"`:
+  after-task structure check passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background` plus
+  `curl -fsS http://127.0.0.1:8765/version.txt`: dashboard listening, served
+  build `r93`.
+
+Boundary:
+
+- This is negative interval-geometry evidence only. It does not promote
+  top-up readiness, interval coverage, `inference_ready`, `supported`, REML,
+  AI-REML, bridge support, q2/q4/q8, sigma, non-Gaussian, or public-support
+  wording.
+
+## 2026-06-29: Q-Series q1 mu-slope small-sample correction fix
+
+Goal:
+
+- Fix the q1 structured `mu` slope Wald default so decomposed component rows
+  from `provider(1 + x | group)` resolve the structured group count and receive
+  the documented location-axis t-width plus `log(g/(g-1))` centre shift.
+
+Result:
+
+- Updated `R/profile.R` so `provider(1 | group)` and `provider(0 + x | group)`
+  target terms match the parent structured block label `provider(1 + x |
+  group)` by provider type and grouping variable.
+- Added a regression test in `test-wald-small-sample-default.R` for decomposed
+  structured slope SD components.
+- Reran the Gaussian q1 `mu` one-slope SR150 pregrid. The correction moved
+  phylo/relmat/spatial retained coverage close to nominal, but all four rows
+  remain `mu_slope_pregrid_blocked` and `planned/planned`.
+- Bumped the mission-control dashboard build to `r92`.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "wald-small-sample-default")'`: 21 PASS / 0 FAIL /
+  0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R
+  --n-rep=150 --overwrite=true`: passed, writing the corrected SR150 pregrid
+  artifacts.
+- Corrected SR150 summary: animal 122/150 usable intervals for its eligible
+  slope target plus one visible holdout; phylo 291/300 usable intervals with
+  retained coverage 0.940-0.947 and 3 lower / 5 upper misses; relmat 297/300
+  usable intervals with retained coverage 0.953-0.973 and 2 lower / 6 upper
+  misses; spatial 297/300 usable intervals with retained coverage 0.947-0.960
+  and 6 lower / 5 upper misses.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`.
+- `git diff --check`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-mu-slope-small-sample-correction-fix.md');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-gaussian-mu-slope-coverage-pregrid.md')"`:
+  both after-task structure checks passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background` plus
+  `curl -fsS http://127.0.0.1:8765/version.txt`: dashboard listening, served
+  build `r92`.
+
+Boundary:
+
+- This fixes the documented default correction for q1 structured location
+  slope components and updates negative SR150 evidence. It does not promote
+  interval coverage, `inference_ready`, `supported`, REML, AI-REML, bridge
+  support, q2/q4/q8, sigma, non-Gaussian, or public-support wording.
+
+## 2026-06-29: Q-Series Gaussian q1 mu-slope SR150 pregrid
+
+Goal:
+
+- Execute the retained-outcome SR150 pregrid for the Gaussian q1 `mu`
+  one-slope provider rows, without changing interval, coverage,
+  `inference_ready`, `supported`, REML, AI-REML, bridge, q2/q4/q8, sigma,
+  non-Gaussian, or public-support claims.
+
+Result:
+
+- Added `tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R`.
+- Wrote artifacts under
+  `docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-coverage-pregrid-local/`:
+  600 fit-status rows, 1200 retained target-replicate rows, eight target
+  summary rows, four provider summary rows, `sessionInfo`, `git-sha`, and a
+  run log.
+- Added
+  `docs/dev-log/dashboard/structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv`.
+- Updated the Q-Series widget so all four Gaussian q1 `mu` one-slope rows now
+  display `mu_slope_pregrid_blocked`.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript --no-init-file tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R --n-rep=2 --output-dir=/tmp/drmtmb-gaussian-mu-slope-pregrid-smoke --overwrite=true`:
+  passed.
+- `Rscript --no-init-file -e 'parse("tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R")'`:
+  passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript --no-init-file tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R --n-rep=150 --overwrite=true`:
+  passed, writing the SR150 pregrid artifacts.
+- SR150 summary after the q1 mu-slope small-sample correction fix: animal
+  122/150 usable intervals for its eligible slope target plus one holdout;
+  phylo 291/300 usable intervals with retained coverage 0.940-0.947 and
+  3 lower / 5 upper misses; relmat 297/300 usable intervals with retained
+  coverage 0.953-0.973 and 2 lower / 6 upper misses; spatial 297/300 usable
+  intervals with retained coverage 0.947-0.960 and 6 lower / 5 upper misses.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`.
+- `git diff --check`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-gaussian-mu-slope-coverage-pregrid.md')"`:
+  after-task structure check passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background` plus
+  `curl -fsS http://127.0.0.1:8765/version.txt`: dashboard listening, served
+  build `r91`.
+
+Boundary:
+
+- This is negative SR150 pregrid evidence only. It does not promote interval
+  coverage, `inference_ready`, `supported`, REML, AI-REML, bridge support,
+  q2/q4/q8, sigma, non-Gaussian, or public-support wording.
+
+## 2026-06-29: Q-Series Gaussian q1 mu-slope interval probe
+
+Goal:
+
+- Move the four Gaussian q1 `mu` one-slope provider rows from smoke-only
+  evidence to a local interval-admission rung, while keeping interval,
+  coverage, `inference_ready`, `supported`, REML, AI-REML, bridge, q2/q4/q8,
+  sigma, non-Gaussian, and public-support claims unpromoted.
+
+Result:
+
+- Added `tools/run-structured-re-gaussian-mu-slope-interval-probe.R`, a
+  rerunnable local probe for the direct location-axis structured-SD targets.
+- Wrote artifacts under
+  `docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-interval-probe-local/`:
+  16 interval-probe result rows, eight target summary rows, a seed manifest, a
+  150-seed pregrid manifest, a 1050-row pregrid cell manifest, `sessionInfo`,
+  `git-sha`, and a run log.
+- Added `structured-re-gaussian-mu-slope-admission-audit.tsv` and
+  `structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv`.
+- Updated the Q-Series widget so phylo, spatial, and relmat q1 `mu`
+  one-slope rows display `mu_slope_pregrid_planned`; animal displays
+  `admission_blocked` because one `sd:mu:animal(1 | id)` interval hit a
+  boundary Wald status.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript --no-init-file tools/run-structured-re-gaussian-mu-slope-interval-probe.R`:
+  passed, writing the four-row admission audit, eight-row pregrid dry-run
+  ledger, 16-row probe results, and 1050-row pregrid cell manifest.
+- Probe summary: phylo, spatial, and relmat each have 4/4 finite default
+  intervals and zero boundary Wald statuses; animal has 3/4 finite default
+  intervals and one boundary Wald status at `sd:mu:animal(1 | id)`.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R'); main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-gaussian-mu-slope-interval-probe.md')"`:
+  passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'devtools::test(filter = "structured-re-conversion-contracts")'`:
+  passed, 6353 PASS / 0 FAIL / 0 WARN / 0 SKIP.
+- `git diff --check`: passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh tools/start-mission-control.sh --background`:
+  passed; the served dashboard reports build `r90` at
+  `http://127.0.0.1:8765/version.txt`.
+- Data-side widget check: 104 Q-Series rows; three q1 `mu` rows are
+  `mu_slope_pregrid_planned`; `qseries_animal_q1_mu_one_slope` is
+  `admission_blocked`; all four q1 `mu` rows keep `interval_status = planned`
+  and `coverage_status = planned`.
+
+Boundary:
+
+- This is local interval-admission evidence only. It does not promote
+  interval coverage, `inference_ready`, `supported`, REML, AI-REML, bridge
+  support, q2/q4/q8, sigma, non-Gaussian, or public-support wording.
+
+## 2026-06-29: Q-Series count-intercept caveat diagnostic
+
+Goal:
+
+- Explain the three non-Gaussian count-intercept recovery caveats from the
+  80-rep local recovery grid without changing interval, coverage,
+  `inference_ready`, `supported`, REML, AI-REML, bridge, q2/q4, high-q, or
+  public-support claims.
+
+Result:
+
+- Added `tools/summarize-structured-re-count-intercept-caveat-diagnostic.R`, a
+  rerunnable summarizer that derives condition-level diagnostics from
+  `docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local/tables/count-intercept-recovery-replicates.csv`.
+- Added
+  `docs/dev-log/dashboard/structured-re-count-intercept-caveat-diagnostic.tsv`
+  with 12 condition rows for the three caveated count-intercept cells.
+- Updated the mission-control widget to show the caveat diagnostic link and
+  worst near-zero condition for recovery-caveated count-intercept rows.
+- Updated `tools/validate-mission-control.py` and the focused
+  `structured-re-conversion-contracts` test so the 12-row sidecar, exact
+  verdict classes, no-promotion boundary, and unsupported interval/coverage
+  statuses are guarded.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file tools/summarize-structured-re-count-intercept-caveat-diagnostic.R`:
+  passed, writing 12 condition-level caveat diagnostic rows.
+- Diagnostic split: phylo Poisson has four
+  `condition_near_zero_caveat` rows; phylo NB2 has four
+  `condition_pdhess_caveat` rows; spatial NB2 has two weak-signal
+  `condition_near_zero_caveat` rows and two stronger-signal
+  `condition_recovery_ok` rows.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`, including 12 structured RE count-intercept
+  caveat-diagnostic rows.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R'); main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-count-intercept-caveat-diagnostic.md')"`:
+  passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'devtools::test(filter = "structured-re-conversion-contracts")'`:
+  passed, 6307 PASS / 0 FAIL / 0 WARN / 0 SKIP.
+- `git diff --check`: passed.
+
+Boundary:
+
+- This is condition-level blocker evidence only. It does not promote
+  non-Gaussian recovery wording, interval readiness, coverage readiness,
+  `inference_ready`, `supported`, REML, AI-REML, q2/q4 count covariance,
+  high-q, bridge support, or public support.
+
+## 2026-06-29: Q-Series non-Gaussian count-intercept recovery grid
+
+Goal:
+
+- Move the ten non-Gaussian q1 count `mu` intercept rows from local smoke
+  evidence to row-level recovery evidence, without changing interval,
+  coverage, `inference_ready`, `supported`, REML, AI-REML, bridge, q2/q4, or
+  public-support claims.
+
+Result:
+
+- Added `tools/run-structured-re-count-intercept-recovery-grid.R`, a
+  reproducible local runner that executes the ordinary structured count q1
+  action task, the exact phylo Poisson/NB2 formal shards, and the exact
+  `phylo_interaction()` Poisson/NB2 count fits.
+- Wrote artifacts under
+  `docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local/`
+  with raw replicate rows, summary rows, failures, manifest, seed manifest,
+  logs, `sessionInfo.txt`, `git-sha.txt`, and `module-list.txt`.
+- Added
+  `docs/dev-log/dashboard/structured-re-count-intercept-recovery-results.tsv`
+  with ten row-level recovery summaries.
+- Updated `structured-re-nongaussian-status-audit.tsv` and the mission-control
+  widget so seven intercept rows are `non_gaussian_recovery_only` and three
+  rows are `non_gaussian_recovery_caveat`: phylo NB2 because 13/320
+  structured-SD rows had `pdHess = FALSE`, and phylo Poisson plus spatial NB2
+  because at least 25% of structured-SD estimates fell below the near-zero
+  threshold `1e-4`.
+- The support-cell TSV remains unchanged: all ten linked rows keep
+  `interval_status = unsupported`, `coverage_status = planned`, and
+  `authority_status = source`.
+
+Evidence:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file tools/run-structured-re-count-intercept-recovery-grid.R --n_rep=80 --seed_start=2026062901 --cores=4 --backend=multicore --overwrite`: passed.
+- Recovery summary: 7 count-intercept `non_gaussian_recovery_only`, 3
+  count-intercept `non_gaussian_recovery_caveat`; the all-up non-Gaussian
+  audit is 15 recovery-only, 3 recovery-caveat, 18 rejected, and 1 planned.
+  No interval or coverage row was promoted.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check -`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`:
+  `mission_control_ok`, including 10 structured RE count-intercept
+  recovery-results rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'devtools::test(filter = "structured-re-conversion-contracts")'`:
+  passed before the near-zero diagnostic refinement with 6292 PASS / 0 FAIL,
+  then passed after the refinement with 6295 PASS / 0 FAIL.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R'); main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-count-intercept-recovery-grid.md')"`:
+  passed.
+- `git diff --check`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'devtools::test()'`:
+  passed after the near-zero diagnostic refinement, 19674 PASS / 0 FAIL /
+  17 WARN / 43 SKIP in 529.9s.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'devtools::check()'`:
+  passed after the near-zero diagnostic refinement, 0 errors / 0 warnings /
+  0 notes in 11m 15.2s.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e 'pkgdown::check_pkgdown()'`:
+  passed, no problems found.
+- Near-zero diagnostic refinement after the first full check: the recovery
+  runner, sidecar, widget, validator, audit TSV, README, check-log, and
+  after-task report now report near-zero structured-SD estimates at threshold
+  `1e-4`; `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`,
+  dashboard `node --check -`, focused `structured-re-conversion-contracts`,
+  full `devtools::test()`, full `devtools::check()`, `pkgdown::check_pkgdown()`,
+  and `git diff --check` all passed.
+
+Boundary:
+
+- This is recovery evidence only. It is not interval-ready, coverage-ready,
+  `inference_ready`, `supported`, REML, AI-REML, bridge support, q2/q4 count
+  covariance support, high-q evidence, or public support.
+- `qseries_phylo_nbinom2_q1_mu_intercept`,
+  `qseries_phylo_poisson_q1_mu_intercept`, and
+  `qseries_spatial_nbinom2_q1_mu_intercept` need the pdHess or near-zero
+  lower-tail caveat diagnosed before clean recovery wording.
+
 ## 2026-06-28: q-series support-cell table in mission-control widget

 Goal:
@@ -70032,6 +70646,62 @@ Evidence:
   11m 59.9s.
 - `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
   'pkgdown::check_pkgdown()'`: no problems found.
+
+
+## 2026-06-29: Q-Series count-intercept denominator diagnostic
+
+Goal:
+
+- Test whether the three non-Gaussian q1 count `mu` intercept recovery caveats
+  persist under larger denominators and stronger structured-SD signals, without
+  changing support-cell status.
+
+Result:
+
+- Added `tools/run-structured-re-count-intercept-denominator-diagnostic.R`.
+- Added
+  `docs/dev-log/dashboard/structured-re-count-intercept-denominator-diagnostic.tsv`
+  with 12 condition rows: four stronger-denominator conditions each for
+  `qseries_phylo_poisson_q1_mu_intercept`,
+  `qseries_phylo_nbinom2_q1_mu_intercept`, and
+  `qseries_spatial_nbinom2_q1_mu_intercept`.
+- The local 30-rep diagnostic cleared all 12 conditions: 30/30 fits, zero
+  `pdHess = FALSE`, and zero structured-SD estimates below `1e-4`.
+- Updated the Q-Series widget to show a separate "NG denominator conditions"
+  tile plus a per-row denominator-diagnostic link and verdict note for the
+  three recovery-caveated count-intercept cells.
+
+Boundary:
+
+- No Q-Series support-cell row changed.
+- The three rows remain `non_gaussian_recovery_caveat`; the new sidecar only
+  shows that the caveats are design-sensitive under stronger local conditions.
+- The sidecar does not promote non-Gaussian intervals, coverage,
+  `inference_ready`, `supported`, REML, AI-REML, bridge support, q2/q4 count
+  covariance, high-q evidence, or public support.
+
+Validation:
+
+- 2-rep smoke run to `/tmp`: wrote 12 denominator diagnostic rows.
+- 30-rep local run: wrote 12 denominator diagnostic rows to the dashboard
+  sidecar and raw artifacts under
+  `docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-denominator-diagnostic-local/`.
+- `/opt/homebrew/bin/air format
+  tools/run-structured-re-count-intercept-denominator-diagnostic.R
+  tests/testthat/test-structured-re-conversion-contracts.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript syntax check with `node --check`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`: passed
+  with `mission_control_ok`, including 12 structured RE count-intercept
+  denominator-diagnostic rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6319 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::check()'`: passed in 11m 11.3s with 0 errors / 0 warnings /
+  0 notes.
+
+
 - PR #684 R-CMD-check CI on consolidation head `45b4e9d8`: macOS, Ubuntu, and
   Windows completed successfully; the PR was marked ready for review and has
   merge state `CLEAN`.
@@ -70050,6 +70720,79 @@ Boundary:
   derivation work are v1.1+ arcs.


+## 2026-06-29: Q-Series count-intercept top-up recovery
+
+Goal:
+
+- Test whether the three formerly recovery-caveated non-Gaussian q1 count
+  `mu` intercept rows can move to recovery-only display state under an 80-seed
+  stronger-denominator local top-up, without changing support-cell interval or
+  coverage status.
+
+Result:
+
+- Added `tools/summarize-structured-re-count-intercept-topup-recovery.R`.
+- Ran the stronger-denominator runner at 80 seeds x four conditions for each of
+  `qseries_phylo_poisson_q1_mu_intercept`,
+  `qseries_phylo_nbinom2_q1_mu_intercept`, and
+  `qseries_spatial_nbinom2_q1_mu_intercept`.
+- Added
+  `docs/dev-log/dashboard/structured-re-count-intercept-topup-recovery-results.tsv`
+  with three row-level top-up recovery summaries.
+- All three rows passed the local top-up recovery-only gate: 320/320 fit
+  success, zero `pdHess = FALSE`, zero finite-estimate losses, and zero
+  structured-SD estimates below `1e-4`.
+- Updated `structured-re-nongaussian-status-audit.tsv` so non-Gaussian
+  recovery-only rows move from 15 to 18 and recovery-caveated rows move from 3
+  to 0. The original weak-denominator caveat sidecars remain visible as
+  provenance.
+- Bumped the mission-control widget to `r85`; it now reads
+  `structured-re-count-intercept-topup-recovery-results.tsv` after the original
+  count-intercept recovery sidecar.
+
+Boundary:
+
+- No Q-Series support-cell row changed.
+- The sidecar is recovery-only evidence. It does not promote non-Gaussian
+  intervals, coverage, `inference_ready`, `supported`, REML, AI-REML, bridge
+  support, q2/q4 count covariance, high-q evidence, or public support.
+- Primary-cluster confirmation remains the next gate before public recovery
+  wording.
+
+Validation:
+
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file tools/run-structured-re-count-intercept-denominator-diagnostic.R
+  --output_dir=docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-local
+  --dashboard_output=/tmp/drmtmb-count-intercept-topup-condition-diagnostic.tsv
+  --n_rep=80 --seed_start=2026062921 --overwrite`: wrote 12 condition rows
+  and 960 structured-SD replicate rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file
+  tools/summarize-structured-re-count-intercept-topup-recovery.R`: wrote three
+  top-up recovery rows.
+- `/opt/homebrew/bin/air format
+  tools/summarize-structured-re-count-intercept-topup-recovery.R
+  tests/testthat/test-structured-re-conversion-contracts.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- Dashboard JavaScript extracted from `docs/dev-log/dashboard/index.html` and
+  checked with `node --check`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`: passed
+  with `mission_control_ok`, including three structured RE count-intercept
+  top-up recovery-result rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6335 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file -e 'devtools::test()'`: 19714 PASS / 0 FAIL / 17 WARN /
+  43 SKIP.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file -e 'devtools::check()'`: passed in 11m 9.7s with 0 errors /
+  0 warnings / 0 notes.
+
+
 ## 2026-06-28: q1 sigma phylo/relmat rows to inference_ready

 Goal:
@@ -71001,3 +71744,197 @@ Validation:
   'devtools::check()'`: 0 errors / 0 warnings / 0 notes.
 - `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
   'pkgdown::check_pkgdown()'`: no problems found.
+## 2026-06-29: Q-Series high-q diagnostic status sync and Rorqual dispatch retry
+
+Goal:
+
+- Make the 104-row Q-Series widget show existing q4/q8-shaped interval
+  diagnostics as `diagnostic_only` interval signals while keeping high-q
+  coverage and inference promotions blocked.
+- Complete the count-intercept top-up primary-cluster confirmation as
+  recovery-only evidence, not as interval, coverage, or support evidence.
+
+Result:
+
+- Updated exactly twelve high-q or q8-shaped support-cell rows from
+  `interval_status = planned` to `interval_status = diagnostic_only`.
+- The twelve rows are the four q4 location one-slope rows, four q4 all-four
+  intercept rows, and four q8-shaped all-four one-slope rows. All twelve keep
+  `coverage_status = planned`, `fit_status = point_fit`,
+  `bridge_status = fixture_parity`, and `denominator_policy =
+  fixture_not_coverage`.
+- Synced `structured-re-high-q-status-audit.tsv` so the linked interval status
+  matches the support-cell table and the high-q audit explains the diagnostic
+  signal.
+- Added a validator/test allow-list for those exact twelve diagnostic interval
+  cells. The allow-list only applies to `interval_status`; high-q coverage
+  remains blocked.
+- Bumped the mission-control widget to `r89`. The served dashboard at
+  `http://127.0.0.1:8765/` reports interval counts: 40 unsupported, 37 planned,
+  18 diagnostic-only, 5 inference-ready, and 4 interval-feasible; coverage
+  counts remain 78 planned, 21 unsupported, and 5 inference-ready.
+- Patched `tools/slurm/count-intercept-topup-rorqual.sbatch` to use Rorqual's
+  module-backed `r-bundle-bioconductor/3.21` dependency stack, fixed the
+  dependency-version logger, added source-cleaning for stale local build
+  products after the `14896924` retry failed with an invalid ELF header, and
+  added `cd "$REPO"` after the `14896996` retry installed successfully but
+  launched repo-relative simulation helpers from the campaign root.
+- SLURM job `14897050` completed on Rorqual with `runner_exit_code=0` and
+  `summary_exit_code=0`. Fetched the result TSV and metadata into
+  `docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual/`.
+  The three-row Rorqual summary matches the local top-up sidecar on row
+  identity, retained denominators, `pdHess`, near-zero counts, recovery
+  verdicts, and numeric summaries.
+- Updated `structured-re-count-intercept-topup-cluster-dispatch.tsv` to
+  `job_state = completed_passed` and changed the top-up recovery sidecar wording
+  to cluster-confirmed recovery-only evidence.
+
+Boundary:
+
+- This promotes no q4, q6, q8, non-Gaussian, REML, AI-REML, bridge, or public
+  support claim.
+- No high-q row is `inference_ready`; no high-q row has coverage evidence.
+- The Rorqual confirmation is recovery-only evidence. It still promotes no
+  non-Gaussian interval, coverage, `inference_ready`, `supported`, REML,
+  AI-REML, bridge, or public-support claim.
+
+Validation:
+
+- `/opt/homebrew/bin/air format tools/validate-mission-control.py
+  tests/testthat/test-structured-re-conversion-contracts.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`: passed
+  with `mission_control_ok`.
+- `node --check /tmp/drmtmb-dashboard-script.js`: passed.
+- `git diff --check`: passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6353 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background`: dashboard already listening;
+  served copy updated to `r89`.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-high-q-diagnostic-status-sync.md')"`:
+  after-task structure check passed.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true OMP_NUM_THREADS=1
+  OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 TMB_NTHREADS=1 Rscript
+  --no-init-file -e 'devtools::check()'`: passed in 11m 8.5s with 0 errors,
+  0 warnings, and 0 notes.
+
+## 2026-06-29: Q-Series non-Gaussian recovery rollup and Recovery widget column
+
+Goal:
+
+- Make non-Gaussian count recovery evidence visible as its own row-level signal
+  in the 104-row Q-Series widget, separate from fit stability, interval status,
+  coverage status, and inference readiness.
+- Sync the three Rorqual-confirmed count-intercept top-up rows back into the
+  source support-cell TSV without changing `fit_status`, `interval_status`, or
+  `coverage_status`.
+
+Result:
+
+- Added `structured-re-nongaussian-recovery-rollup.tsv` with exactly 18
+  recovery rows: 3 `cluster_confirmed_recovery_only`, 14
+  `local_recovery_only`, and 1 `local_recovery_hessian_caveat`.
+- Added a separate `Recovery` column to the Q-Series widget beside `Stability`
+  and `Inference`.
+- Updated the three formerly caveated count-intercept source rows
+  (`qseries_phylo_poisson_q1_mu_intercept`,
+  `qseries_phylo_nbinom2_q1_mu_intercept`, and
+  `qseries_spatial_nbinom2_q1_mu_intercept`) so their evidence URL and claim
+  boundary point to the fetched Rorqual top-up artifact. All three remain
+  `fit_status = point_fit`, `interval_status = unsupported`, and
+  `coverage_status = planned`.
+- Synced the local rollup-linked count `mu` support-cell rows so their source
+  text names recovery evidence instead of stale "add recovery evidence" fixture
+  gates; the spatial NB2 one-slope row now carries the retained
+  `2/80 pdHess false` caveat in both source and audit tables.
+- Bumped the served dashboard build to `r96`. The served
+  `structured-re-nongaussian-recovery-rollup.tsv` is reachable at
+  `http://127.0.0.1:8765/`.
+
+Boundary:
+
+- This is recovery-only evidence. It does not promote non-Gaussian intervals,
+  coverage, `inference_ready`, `supported`, REML, AI-REML, q2/q4 covariance,
+  high-q rows, bridge support, count sigma, zero-inflation, or public support.
+- The 104-row support-cell counts are unchanged: 5 `inference_ready` interval
+  rows and 5 `inference_ready` coverage rows remain the only inference-ready
+  rows.
+
+Validation:
+
+- `/opt/homebrew/bin/air format tools/validate-mission-control.py
+  tests/testthat/test-structured-re-conversion-contracts.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `node --check /tmp/drmtmb-dashboard-script.js`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`: passed
+  with `mission_control_ok`, including 18 non-Gaussian recovery-rollup rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6381 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `git diff --check`: passed.
+- Served dashboard verification: `curl -fsS http://127.0.0.1:8765/version.txt`
+  returned `r96`, and the rollup TSV served with the expected header and first
+  rows.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-nongaussian-recovery-rollup.md')"`:
+  after-task structure check passed.
+
+## 2026-06-29: Q-Series q2 bias+t top-up runner contract
+
+Goal:
+
+- Make the next spatial/animal q2 bias+t coverage top-up executable and visible
+  without promoting either linked support row.
+
+Result:
+
+- Added `tools/run-structured-re-q2-bias-t-coverage-topup.R`, an endpoint-only
+  runner for spatial/animal q2 `mu1:x` and `mu2:x` SD endpoints. The runner
+  prefers current source via `devtools::load_all()` for local smoke and supports
+  `--no-load-all` for cluster installed/temp-install execution.
+- Added `tools/slurm/q2-bias-t-topup-rorqual.sbatch` with real Rorqual account
+  routing, pinned BLAS/TMB threads, module-backed R, and shard array 1-4.
+- Added `structured-re-q2-slope-bias-t-topup-runner-contract.tsv` with four
+  smoked endpoint shards: spatial `mu1:x`, spatial `mu2:x`, animal `mu1:x`,
+  and animal `mu2:x`. Each shard plans 525 top-up replicates over seeds
+  730476-731000.
+- Added current-source one-rep smoke artifacts under
+  `docs/dev-log/simulation-artifacts/2026-06-29-q2-bias-t-topup-current-source-smoke-local/`.
+  Each endpoint had one fit, convergence, `pdHess = TRUE`, and finite default
+  bias+t Wald interval output.
+- Synced the q2 spatial/animal admission audit and dashboard README so the next
+  gate points to the top-up contract.
+
+Boundary:
+
+- This is runner-contract and smoke evidence only. It promotes no spatial q2 or
+  animal q2 interval status, coverage status, `inference_ready`, `supported`,
+  correlation target, q4/q8, REML, AI-REML, bridge, or public-support claim.
+
+Validation:
+
+- `/opt/homebrew/bin/air format tools/validate-mission-control.py
+  tests/testthat/test-structured-re-conversion-contracts.R`: passed.
+- `python3 -m py_compile tools/validate-mission-control.py`: passed.
+- `R_PROFILE_USER=/dev/null python3 tools/validate-mission-control.py`: passed
+  with `mission_control_ok`, including 4 q2 slope bias+t top-up
+  runner-contract rows.
+- `R_PROFILE_USER=/dev/null NOT_CRAN=true Rscript --no-init-file -e
+  'devtools::test(filter = "structured-re-conversion-contracts")'`: 6465 PASS /
+  0 FAIL / 0 WARN / 0 SKIP.
+- `git diff --check`: passed.
+- `DRMTMB_DASHBOARD_DIR=/tmp/drm-dashboard DRMTMB_DASHBOARD_PORT=8765 sh
+  tools/start-mission-control.sh --background`: dashboard already listening at
+  `http://127.0.0.1:8765/`.
+- Served dashboard verification: `curl -fsS http://127.0.0.1:8765/version.txt`
+  returned `r96`, and the top-up runner-contract TSV served with the expected
+  header and first rows.
+- `R_PROFILE_USER=/dev/null Rscript --no-init-file -e
+  "source('/Users/z3437171/shinichi-brain/tools/check-after-task.R');
+  main_check_after_task('docs/dev-log/after-task/2026-06-29-q-series-q2-bias-t-topup-runner-contract.md')"`:
+  after-task structure check passed.
diff --git a/docs/dev-log/dashboard/README.md b/docs/dev-log/dashboard/README.md
index 0e95fe1c..13f123a3 100644
--- a/docs/dev-log/dashboard/README.md
+++ b/docs/dev-log/dashboard/README.md
@@ -211,16 +211,38 @@ has been banked, and it joins q4/q6/q8 rows to
 `structured-re-high-q-status-audit.tsv` so high-q diagnostic, q4 gate-required,
 q8 stability-blocked, and high-q planned states are visible separately from
 inference readiness. It also joins all non-Gaussian rows to
-`structured-re-nongaussian-status-audit.tsv` so count recovery-only,
-count-intercept smoke, rejected, and planned family-design rows are visible
-separately from Gaussian interval claims. It also joins the remaining Gaussian low-q rows
-to `structured-re-gaussian-lowq-status-audit.tsv` so ordinary baselines,
+`structured-re-nongaussian-status-audit.tsv` so count recovery-only, rejected,
+and planned family-design rows are visible separately from Gaussian interval
+claims. It also joins the remaining
+Gaussian low-q rows to `structured-re-gaussian-lowq-status-audit.tsv` so ordinary baselines,
 point/fixture gates, diagnostic-only rows, and rejection-contract rows no
 longer collapse into a generic tried bucket. Four Gaussian q1 `mu` one-slope
 rows also join to `structured-re-gaussian-mu-slope-smoke-status.tsv`, which
 records local one-rep smoke evidence for `phylo()`, fixed-covariance
 `spatial()`, `animal()`, and `relmat()`. Ten non-Gaussian q1 count `mu`
 intercept rows also join to
+`structured-re-count-intercept-recovery-results.tsv`, which records the local
+80-rep recovery grid for the ten exact count-intercept rows and flags the
+phylo NB2 intercept pdHess caveat plus the phylo Poisson and spatial NB2
+near-zero lower-tail caveats, while keeping interval and coverage claims
+unsupported/planned. The three caveated count-intercept rows also join to
+`structured-re-count-intercept-caveat-diagnostic.tsv`, which breaks the same
+local 80-rep recovery grid down by condition: phylo Poisson is near-zero
+caveated in all four formal-shard conditions, phylo NB2 keeps pdHess caveats
+in all four formal-shard conditions, and spatial NB2 separates two weak-signal
+near-zero caveats from two stronger-signal recovery-ok conditions. They also
+join to `structured-re-count-intercept-denominator-diagnostic.tsv`, which
+records a 30-rep stronger-denominator diagnostic where all 12 condition rows
+cleared locally with 30/30 fits, zero `pdHess = FALSE`, and zero near-zero SD
+estimates. The same three rows now join to
+`structured-re-count-intercept-topup-recovery-results.tsv`, an 80-seed x
+4-condition stronger-denominator recovery top-up with 320/320 fit success,
+zero `pdHess = FALSE`, and zero near-zero SD estimates per row.
+`structured-re-nongaussian-recovery-rollup.tsv` is the widget-facing recovery
+grade table for the 18 non-Gaussian Poisson/NB2 count `mu` rows: three are
+cluster-confirmed recovery-only rows from the Rorqual top-up, fourteen are
+local recovery-only rows, and one fixed-covariance spatial NB2 slope row keeps
+a local Hessian caveat. The earlier smoke rungs remain available in
 `structured-re-count-intercept-recovery-smoke-status.tsv`, which records local
 recovery-smoke evidence for the exact spatial, animal, and relmat
 Poisson/NB2 intercept formulas, and
@@ -231,19 +253,25 @@ records the exact pair-level `phylo_interaction()` Poisson/NB2 smoke. The suppor
 the route/status contract; the evidence summary is the reader-facing
 denominator for rows already promoted to `inference_ready`, the count recovery
 summary is reader-facing recovery evidence only, and the high-q, non-Gaussian,
+non-Gaussian recovery rollup, count-intercept recovery, count-intercept caveat,
+count-intercept denominator, count-intercept top-up,
+count-intercept top-up cluster dispatch,
 count-intercept smoke, phylo count-intercept smoke, phylo-interaction count
-smoke, Gaussian low-q, and Gaussian q1 `mu` smoke audits are blocker ledgers
-only.
+smoke, Gaussian low-q, Gaussian q1 `mu` smoke/admission/pregrid, and Gaussian
+q1 `mu` boundary-profile audits are blocker ledgers only.

 `structured-re-high-q-status-audit.tsv` records one audit row for each of the
 24 q4/q6/q8 support cells. It assigns eight q4 fixture rows to
 `high_q_gate_required`, five q8 or q8-shaped rows to
 `q8_stability_blocked`, three ordinary/direct-SD high-q comparator rows to
 `high_q_diagnostic`, and eight broader q6/q8 future-design rows to
-`high_q_planned`. Every linked support cell keeps its existing fit, interval,
-and coverage statuses; no high-q row is `inference_ready`, and the audit does
-not promote q4/q6/q8 intervals, coverage, REML, AI-REML, bridge support,
-`supported`, or public support.
+`high_q_planned`. The tried q4 location, q4 all-four intercept, and
+q8-shaped all-four one-slope rows now expose `interval_status =
+diagnostic_only` in the 104-row table because row-specific interval diagnostic
+sidecars exist, but coverage stays `planned`; no high-q row is
+`inference_ready`, and the audit does not promote q4/q6/q8 calibrated
+intervals, coverage, REML, AI-REML, bridge support, `supported`, or public
+support.

 `structured-re-gaussian-lowq-status-audit.tsv` records the 35 remaining
 Gaussian low-q Q-Series cells after the exact `inference_ready`, sigma/q2
@@ -269,12 +297,94 @@ and `coverage_status = planned`, and the sidecar does not promote
 `inference_ready`, REML, AI-REML, bridge support, `supported`, or public
 support. A replicated interval/coverage denominator grid remains the next gate.

+`structured-re-gaussian-mu-slope-admission-audit.tsv` records the next local
+interval-probe rung for those same four Gaussian q1 `mu` one-slope rows. The
+artifacts under
+`docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-interval-probe-local/`
+show two small default-`confint()` probes per provider for the direct
+location-axis structured-SD targets. Phylo, fixed-covariance spatial, and
+relmat each have 4/4 finite default intervals with no boundary Wald statuses
+and therefore move to `mu_slope_pregrid_planned` in the widget. Animal has
+3/4 finite default intervals because the intercept SD hit one boundary Wald
+status, so the row stays `admission_blocked` until that endpoint is diagnosed.
+`structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv` records the
+retained-outcome SR150 pregrid manifest: seven clean target endpoints are
+pregrid targets and the animal intercept endpoint is a visible holdout. These
+sidecars still do not promote interval+coverage readiness, REML, AI-REML,
+bridge support, `supported`, or public support.
+
+`structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv` records the
+executed local SR150 retained-outcome pregrid for those exact Gaussian q1
+`mu` one-slope rows. The current artifact was rerun after fixing the structured
+SD group-count matcher so decomposed slope targets from `provider(1 + x |
+group)` receive the documented location-axis t-width and `log(g/(g-1))` centre
+shift. It remains negative admission evidence, not a promotion: all four rows
+display as `mu_slope_pregrid_blocked`. The target summaries show 1200 retained
+target-replicate rows across the four providers. Animal has only 122/150 usable
+intervals for the eligible slope SD target and keeps the intercept SD as a
+visible holdout; phylo has 291/300 usable intervals, retained coverage
+0.940-0.947, and 3 lower / 5 upper misses; relmat has 297/300 usable intervals,
+retained coverage 0.953-0.973, and 2 lower / 6 upper misses; spatial has
+297/300 usable intervals, retained coverage 0.947-0.960, and 6 lower / 5 upper
+misses. Linked support cells still keep `interval_status = planned` and
+`coverage_status = planned`; the sidecar does not promote `inference_ready`,
+REML, AI-REML, bridge support, `supported`, or public support.
+
+`structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv` records the
+endpoint-profile follow-up for all 42 SR150 boundary/non-Wald rows from that
+pregrid. All 42 rows refit with convergence and `pdHess = TRUE`; the
+zero-lower-boundary endpoint fix rescued finite lower endpoints for many rows,
+but the profile channel is still blocked by upper misses and remaining profile
+failures. Animal has 25/27 finite profile intervals, 10 covered, 15 upper
+misses, and two profile failures; phylo has 8/9 finite, one covered, seven
+upper misses, and one profile failure; relmat has 2/3 finite, zero covered, two
+upper misses, and one profile failure; spatial has 3/3 finite, zero covered,
+and three upper misses. The four linked support cells therefore remain
+`mu_slope_pregrid_blocked` with `interval_status = planned` and
+`coverage_status = planned`. This sidecar is negative interval-geometry
+evidence only; it does not promote top-up readiness, `inference_ready`, REML,
+AI-REML, bridge support, `supported`, or public support.
+
+`structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv` overlays those
+repaired endpoint-profile boundary rows back onto the original SR150 pregrid
+denominator. This routing audit splits the four rows without promoting any of
+them: animal remains `mu_slope_pregrid_blocked` because the eligible slope
+target has 132/150 covered, coverage 0.880, 147/150 usable intervals, and 15
+upper misses; phylo, relmat, and spatial move to `topup_required` because their
+hybrid SR150 coverage remains near nominal but MCSE is still above 0.01. Phylo
+has 284/300 covered, 299/300 usable, coverage 0.947, and lower/upper misses
+3/12; relmat has 289/300 covered, 299/300 usable, coverage 0.963, and misses
+2/8; spatial has 286/300 covered, 300/300 usable, coverage 0.953, and misses
+6/8. These are top-up candidates only; the support-cell TSV still keeps all
+four rows at `interval_status = planned` and `coverage_status = planned`.
+
+The top-up runner is now executable for those three candidate rows without
+touching the visible dashboard. `tools/run-structured-re-gaussian-mu-slope-coverage-pregrid.R`
+accepts generated non-overlapping seed slices, provider selection, and
+`--write-dashboard=false`; a one-replicate smoke under
+`docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-topup-smoke-local/`
+used seed 791151 for phylo, relmat, and spatial only. The boundary-profile
+diagnostic runner also accepts `--source-replicates`, so future top-up
+replicates can be passed through the same endpoint-profile repair channel. This
+is executable-contract evidence only, not a support-cell promotion.
+
+`structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv` records the completed
+SR475 hybrid audit for the three top-up candidates. The stronger denominator
+meets the MCSE gate but does not promote the rows: phylo has 913/950 covered,
+MCSE 0.006277, and lower/upper misses 5/31; relmat has 926/950 covered, MCSE
+0.005091, and misses 3/20; spatial has 912/950 covered, MCSE 0.006358, and
+misses 15/22. Each provider still has one profile-failed row after combining
+the original SR150 boundary profiles and the top-up boundary profiles. The
+widget therefore shows phylo, relmat, and spatial as
+`mcse_met_upper_tail_blocked`; all linked support cells remain
+`interval_status = planned` and `coverage_status = planned`.
+
 `structured-re-nongaussian-status-audit.tsv` records one audit row for each of
-the 37 non-Gaussian Q-Series cells. It assigns eight Poisson/NB2 count
-one-slope rows to `non_gaussian_recovery_only`, ten count-intercept or
-`phylo_interaction()` smoke rows to `non_gaussian_intercept_recovery_smoke`,
-eighteen intentional rejection rows to `non_gaussian_rejected`, and one broader
-family-design row to `non_gaussian_planned`. The table preserves the current
+the 37 non-Gaussian Q-Series cells. It assigns eighteen Poisson/NB2 count
+rows to `non_gaussian_recovery_only`, zero rows to
+`non_gaussian_recovery_caveat`, eighteen intentional rejection rows to
+`non_gaussian_rejected`, and one broader family-design row to
+`non_gaussian_planned`. The table preserves the current
 family distribution: 14 Poisson rows, 15 NB2 rows, two Student rows, two beta
 rows, and one row each for Gamma, cumulative-logit, truncated-NB2, and the
 non-count/extended-count future-design bucket. All linked rows keep
@@ -282,6 +392,91 @@ non-count/extended-count future-design bucket. All linked rows keep
 intervals, coverage, q2/q4 covariance, REML, AI-REML, bridge support,
 `supported`, or public support.

+`structured-re-count-intercept-recovery-results.tsv` records the local
+80-rep recovery grid for the ten non-Gaussian q1 count `mu` intercept rows.
+The artifacts under
+`docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local/`
+include raw replicate tables, summary tables, a seed manifest, run logs,
+`sessionInfo.txt`, `git-sha.txt`, and local module information. Seven rows pass
+the local recovery-only gate. Three rows remain
+`non_gaussian_recovery_caveat`: `qseries_phylo_nbinom2_q1_mu_intercept`
+because the run retained 13/320 `pdHess = FALSE` structured-SD rows, and
+`qseries_phylo_poisson_q1_mu_intercept` plus
+`qseries_spatial_nbinom2_q1_mu_intercept` because at least 25% of
+structured-SD estimates fell below the near-zero threshold `1e-4`. This
+sidecar is recovery evidence only: linked support cells keep
+`interval_status = unsupported` and `coverage_status = planned`, and it does
+not promote non-Gaussian intervals, coverage, q2/q4 covariance, REML,
+AI-REML, bridge support, `supported`, or public support.
+
+`structured-re-count-intercept-caveat-diagnostic.tsv` records 12 condition-level
+diagnostic rows for the three caveated count-intercept recovery cells. It is
+derived from the same local 80-rep recovery grid and explains the caveats
+without changing any support-cell status: phylo Poisson has four
+`condition_near_zero_caveat` rows; phylo NB2 has four
+`condition_pdhess_caveat` rows; spatial NB2 has two weak-signal
+`condition_near_zero_caveat` rows and two stronger-signal
+`condition_recovery_ok` rows. This sidecar points the next gate toward a
+targeted denominator diagnostic with stronger signal and/or larger count
+denominators before public recovery wording; intervals and coverage remain
+unsupported.
+
+`structured-re-count-intercept-denominator-diagnostic.tsv` records the targeted
+30-rep stronger-denominator follow-up for those same three caveated cells. It
+uses larger phylo/spatial denominators, larger count means, and stronger
+structured-SD signals than the caveated local 80-rep grid. All 12 condition
+rows cleared locally: 30/30 fits, zero `pdHess = FALSE`, and zero structured-SD
+estimates below `1e-4`. This makes the three row-level caveats
+design-sensitive recovery blockers, not engine-wide non-Gaussian support
+failures. It still does not promote intervals, coverage, `inference_ready`,
+REML, AI-REML, bridge support, `supported`, or public support.
+
+`structured-re-count-intercept-topup-recovery-results.tsv` records the
+80-seed x four-condition stronger-denominator recovery top-up for the three
+formerly caveated count-intercept cells, now reproduced by Rorqual SLURM job
+`14897050`. The local artifacts under
+`docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-local/`
+and the fetched Rorqual artifacts under
+`docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual/`
+include raw replicate tables, condition manifests, summary tables, a seed
+manifest, run logs, `sessionInfo.txt`, `git-sha.txt`, and module information.
+All three rows pass the cluster-confirmed recovery-only top-up gate:
+`qseries_phylo_poisson_q1_mu_intercept`,
+`qseries_phylo_nbinom2_q1_mu_intercept`, and
+`qseries_spatial_nbinom2_q1_mu_intercept` each have 320/320 fit success,
+zero `pdHess = FALSE`, and zero structured-SD estimates below `1e-4`. This
+sidecar supersedes the original weak-denominator caveat for widget row state
+but preserves the original caveat sidecars as provenance. It does not promote
+non-Gaussian intervals, coverage, q2/q4 covariance, REML, AI-REML, bridge
+support, `supported`, or public support.
+
+`structured-re-nongaussian-recovery-rollup.tsv` records the separate Recovery
+column used by the Q-Series widget for all 18 non-Gaussian Poisson/NB2 count
+`mu` rows with recovery evidence. It deliberately separates recovery grade from
+fit stability, interval status, and coverage status. The three formerly
+caveated count-intercept rows are `cluster_confirmed_recovery_only` from
+Rorqual SLURM job `14897050`; fourteen rows are `local_recovery_only`; and the
+fixed-covariance spatial NB2 one-slope row is
+`local_recovery_hessian_caveat` because the local 80-rep slope grid retained
+2/80 `pdHess = FALSE` fits. All linked support-cell rows keep
+`interval_status = unsupported` and `coverage_status = planned`; the rollup
+does not promote non-Gaussian intervals, coverage, q2/q4 covariance, REML,
+AI-REML, bridge support, `supported`, or public support. Rollup-linked
+support-cell source rows must also name recovery evidence in their
+`claim_boundary` or `next_gate`; mission control rejects stale gates such as
+"add recovery evidence" for rows where recovery is already banked.
+
+`structured-re-count-intercept-topup-cluster-dispatch.tsv` records the
+Rorqual confirmation job for those same three top-up rows. The current dispatch
+row is `completed_passed` for SLURM job `14897050`, under
+`/project/def-snakagaw/snakagaw/drmtmb-qseries/20260629-count-intercept-topup-rorqual-77b634eda91b/`.
+It is cluster confirmation evidence for recovery-only row state: the runner and
+summary both exited 0, and the fetched result TSV matches the local top-up
+sidecar on the three target cells, retained denominators, `pdHess`, near-zero
+counts, and recovery verdicts. It does not promote intervals, coverage,
+`inference_ready`, REML, AI-REML, q2/q4 covariance, bridge support,
+`supported`, or public support.
+
 `structured-re-count-intercept-recovery-smoke-status.tsv` records the first
 local smoke rung for six exact non-Gaussian q1 structured `mu` intercept
 rows: spatial, animal, and relmat Poisson/NB2. The artifacts under
@@ -290,11 +485,10 @@ show 24 total condition-replicates, zero failures, and for each of the six
 cell subsets, 4/4 structured-SD rows with converged fits, `pdHess = TRUE`, and
 finite estimates. The spatial NB2 subset is explicitly flagged because 3/4
 structured-SD rows have lower-boundary warnings. This sidecar does not cover
-the phylo count intercept rows, does not replace the 80-rep count one-slope
-recovery grid, and does not promote non-Gaussian intervals, coverage,
+the phylo count intercept rows, has been superseded for dashboard row state by
+the 80-rep count-intercept recovery grid, and does not promote non-Gaussian intervals, coverage,
 q2/q4 covariance, REML, AI-REML, bridge support, `supported`, or public
-support. A replicated recovery grid with MCSE and a boundary ledger remains
-the next gate.
+support.

 `structured-re-phylo-count-intercept-recovery-smoke-status.tsv` records the
 first local smoke rung for the two exact phylo non-Gaussian q1 structured `mu`
@@ -1338,6 +1532,17 @@ the cell `admission_blocked`. Both linked support cells keep
 not promote range-estimating spatial support, pedigree/Ainv bridge marshalling,
 q4/q8, REML, AI-REML, bridge support, `supported`, or public support.

+`structured-re-q2-slope-bias-t-topup-runner-contract.tsv` records the executable
+top-up contract for those four spatial/animal q2 SD endpoints. Shards 1-4 cover
+spatial `mu1:x`, spatial `mu2:x`, animal `mu1:x`, and animal `mu2:x` with
+525 planned replicates each for seeds 730476-731000. The local current-source
+smoke has one finite fit, `pdHess = TRUE`, and finite default bias+t Wald
+interval output for each endpoint. This is runner-contract evidence only: the
+linked q-series rows remain `interval_status = planned` and
+`coverage_status = planned`, and the contract does not promote inference
+readiness, calibrated coverage, correlation targets, q4/q8, REML, AI-REML,
+bridge support, `supported`, or public support.
+
 `structured-re-q2-slope-coverage-pregrid-dry-run.tsv` records the executable
 manifest shape for that future q2 slope coverage pre-grid without running any
 coverage fits. It links to a 150-row predeclared seed manifest and a 1500-row
diff --git a/docs/dev-log/dashboard/index.html b/docs/dev-log/dashboard/index.html
index c261de03..91ac2fcd 100644
--- a/docs/dev-log/dashboard/index.html
+++ b/docs/dev-log/dashboard/index.html
@@ -179,15 +179,20 @@ h2 { font-size: 18px; margin: 26px 0 12px; letter-spacing: 0; }
 .extractor-ready, .fixture-parity,
 .non-gaussian-recovery-only, .non-gaussian-point-only,
 .non-gaussian-intercept-recovery-smoke,
-.gaussian-mu-slope-smoke-passed { background: var(--blue-bg); color: var(--blue); }
+.gaussian-mu-slope-smoke-passed,
+.local-recovery-only, .cluster-confirmed-recovery-only { background: var(--blue-bg); color: var(--blue); }
 .tried-diagnostic, .diagnostic-only, .topup-required,
 .calibration-required, .high-q-gate-required,
 .high-q-diagnostic, .gaussian-lowq-gate-required,
-.gaussian-lowq-diagnostic { background: var(--gold-bg); color: var(--gold); }
+.gaussian-lowq-diagnostic, .non-gaussian-recovery-caveat,
+.mu-slope-pregrid-planned, .local-recovery-hessian-caveat { background: var(--gold-bg); color: var(--gold); }
 .unsupported-or-blocked, .inference-blocked, .admission-blocked,
 .q8-stability-blocked, .non-gaussian-rejected,
-.gaussian-lowq-rejected { background: var(--rose-bg); color: var(--rose); }
-.high-q-planned, .non-gaussian-planned { background: var(--gray-bg); color: var(--muted); }
+.gaussian-lowq-rejected, .non-gaussian-recovery-blocked,
+.mu-slope-pregrid-blocked, .mcse-met-upper-tail-blocked { background: var(--rose-bg); color: var(--rose); }
+.high-q-planned, .non-gaussian-planned,
+.not-recovery-target, .recovery-unavailable,
+.recovery-planned { background: var(--gray-bg); color: var(--muted); }
 .slices { padding: 0 14px 12px 110px; }
 .slice {
   display: grid;
@@ -301,6 +306,7 @@ h2 { font-size: 18px; margin: 26px 0 12px; letter-spacing: 0; }
     minmax(140px, .65fr)
     minmax(140px, .65fr)
     minmax(140px, .65fr)
+    minmax(130px, .6fr)
     minmax(92px, .45fr)
     minmax(110px, .5fr)
     minmax(110px, .5fr)
@@ -308,13 +314,18 @@ h2 { font-size: 18px; margin: 26px 0 12px; letter-spacing: 0; }
     minmax(290px, 1.35fr);
   gap: 8px;
   align-items: start;
-  min-width: 1640px;
+  min-width: 1760px;
   padding: 10px;
   border: 1px solid var(--line);
   border-radius: 8px;
   background: var(--panel);
   font-size: 12px;
 }
+.qseries-row .cell:last-child {
+  max-height: 96px;
+  overflow-y: auto;
+  padding-right: 4px;
+}
 .qseries-head {
   border: 0;
   background: transparent;
@@ -435,7 +446,7 @@ code { background: var(--soft); border-radius: 5px; padding: 1px 5px; font-size:
 </main>

 <script>
-const BUILD = "r80";
+const BUILD = "r96";
 const FINISH_SECTIONS = [
   "Critical Path",
   "Issue Ledger",
@@ -687,6 +698,9 @@ function qSeriesSupportState(row, recoveryByCell = {}, admissionAuditByCell = {}
   if (row.interval_status === "inference_ready" && row.coverage_status === "inference_ready") {
     return "inference_ready";
   }
+  if (recoveryByCell[row.cell_id] && recoveryByCell[row.cell_id].widget_state) {
+    return recoveryByCell[row.cell_id].widget_state;
+  }
   if (admissionAuditByCell[row.cell_id]) {
     return admissionAuditByCell[row.cell_id].widget_state || "tried_not_inference_ready";
   }
@@ -747,7 +761,27 @@ function qSeriesInference(row) {
   return "planned";
 }

-function qSeriesEvidenceSummary(row, evidenceByCell, recoveryByCell, admissionAuditByCell) {
+function qSeriesRecovery(row, recoveryRollupByCell = {}, recoveryByCell = {}) {
+  const rollup = recoveryRollupByCell[row.cell_id];
+  if (rollup) return rollup.recovery_grade || "recovery_only";
+  const recovery = recoveryByCell[row.cell_id];
+  if (recovery) return recovery.widget_state || "recovery_only";
+  if (row.family_class !== "non_gaussian") return "not_recovery_target";
+  if (row.fit_status === "unsupported") return "recovery_unavailable";
+  if (row.fit_status === "planned") return "recovery_planned";
+  return "point_only";
+}
+
+function qSeriesEvidenceSummary(
+  row,
+  evidenceByCell,
+  recoveryByCell,
+  recoveryRollupByCell,
+  caveatDiagnosticByCell,
+  denominatorDiagnosticByCell,
+  admissionAuditByCell,
+  boundaryProfileByCell
+) {
   const evidence = evidenceByCell[row.cell_id];
   if (evidence) {
     return `<div class="note"><strong>${esc(evidence.interval_channel)}</strong></div>
@@ -757,9 +791,56 @@ function qSeriesEvidenceSummary(row, evidenceByCell, recoveryByCell, admissionAu
   }
   const recovery = recoveryByCell[row.cell_id];
   if (recovery) {
+    const rollup = recoveryRollupByCell[row.cell_id];
+    const bias = recovery.bias_sd || recovery.bias_sd_mu_x || recovery.bias_sd_mu_intercept || "";
+    const rmse = recovery.rmse_sd || recovery.rmse_sd_mu_x || "";
+    const verdict = recovery.recovery_verdict ? `<div class="note">${esc(recovery.recovery_verdict)}</div>` : "";
+    const rollupNote = rollup
+      ? `<div class="note"><strong>${esc(rollup.recovery_grade)}</strong></div>
+        <div class="note">${esc(rollup.fit_signal)}; ${esc(rollup.hessian_signal)}</div>
+        <div class="note">${esc(rollup.finite_signal)}; ${esc(rollup.near_zero_signal)}</div>`
+      : "";
+    const diagnostics = caveatDiagnosticByCell[row.cell_id] || [];
+    const denominatorDiagnostics = denominatorDiagnosticByCell[row.cell_id] || [];
+    const diagnosticNote = diagnostics.length
+      ? (() => {
+        const worst = diagnostics.reduce((best, item) =>
+          Number(item.near_zero_estimate_rate || 0) > Number(best.near_zero_estimate_rate || 0)
+            ? item
+            : best
+        , diagnostics[0]);
+        const verdicts = [...new Set(diagnostics.map((item) => item.diagnostic_verdict).filter(Boolean))].join(", ");
+        return `<div class="note">${esc(diagnostics.length)} caveat diagnostic condition(s); worst near-zero rate ${esc(worst.near_zero_estimate_rate)} (${esc(worst.condition_label)})</div>
+          <div class="note">${esc(verdicts)}</div>`;
+      })()
+      : "";
+    const denominatorNote = denominatorDiagnostics.length
+      ? (() => {
+        const verdicts = [...new Set(denominatorDiagnostics.map((item) => item.denominator_verdict).filter(Boolean))].join(", ");
+        const worst = denominatorDiagnostics.reduce((best, item) =>
+          Number(item.near_zero_estimate_rate || 0) > Number(best.near_zero_estimate_rate || 0)
+            ? item
+            : best
+        , denominatorDiagnostics[0]);
+        return `<div class="note">${esc(denominatorDiagnostics.length)} stronger-denominator condition(s); worst near-zero rate ${esc(worst.near_zero_estimate_rate)} (${esc(worst.condition_label)})</div>
+          <div class="note">${esc(verdicts)}</div>`;
+      })()
+      : "";
+    const nearZero = recovery.near_zero_estimate_rows
+      ? `<div class="note">${esc(recovery.near_zero_estimate_rows)}/${esc(recovery.n_rep)} near-zero SD estimates below ${esc(recovery.near_zero_threshold)}</div>`
+      : "";
+    const conditionNote = recovery.n_internal_conditions
+      ? `<div class="note">${esc(recovery.n_seed_replicates)} seeds x ${esc(recovery.n_internal_conditions)} condition(s)</div>`
+      : "";
     return `<div class="note"><strong>recovery_only</strong></div>
+      ${rollupNote}
       <div class="note">${esc(recovery.fit_ok)}/${esc(recovery.n_rep)} fit_ok; ${esc(recovery.pdhess_false)} pdHess false</div>
-      <div class="note">sd_mu:x bias ${esc(recovery.bias_sd_mu_x)}, RMSE ${esc(recovery.rmse_sd_mu_x)}</div>
+      ${conditionNote}
+      ${nearZero}
+      ${diagnosticNote}
+      ${denominatorNote}
+      <div class="note">structured SD bias ${esc(bias)}, RMSE ${esc(rmse)}</div>
+      ${verdict}
       <div class="note">${esc(recovery.claim_boundary)}</div>`;
   }
   const admission = admissionAuditByCell[row.cell_id];
@@ -768,30 +849,71 @@ function qSeriesEvidenceSummary(row, evidenceByCell, recoveryByCell, admissionAu
     const summary = admission.wald_finite_summary || admission.raw_coverage_summary || admission.evidence_basis || "";
     const denominator = admission.denominator_status || admission.inference_signal || "";
     const stability = admission.stability_signal || "";
+    const boundaryProfile = boundaryProfileByCell[row.cell_id];
+    const boundaryNote = boundaryProfile
+      ? `<div class="note"><strong>boundary profile diagnostic</strong></div>
+        <div class="note">${esc(boundaryProfile.n_profile_finite_interval)}/${esc(boundaryProfile.n_boundary_rows)} finite endpoint-profile intervals; ${esc(boundaryProfile.profile_status_counts)}</div>
+        <div class="note">${esc(boundaryProfile.diagnostic_verdict)}</div>`
+      : "";
     return `<div class="note"><strong>${esc(status)}</strong></div>
       <div class="note">${esc(summary)}</div>
       <div class="note">${esc(denominator)}</div>
       ${stability ? `<div class="note">${esc(stability)}</div>` : ""}
+      ${boundaryNote}
       <div class="note">${esc(admission.claim_boundary)}</div>`;
   }
   return `<div class="note">${esc(row.denominator_policy)}</div>`;
 }

-function renderQSeriesBoard(rows, inferenceEvidence, countRecovery, admissionAudit) {
+function renderQSeriesBoard(
+  rows,
+  inferenceEvidence,
+  countRecovery,
+  recoveryRollup,
+  countTopupRecovery,
+  countTopupClusterDispatch,
+  countCaveatDiagnostics,
+  countDenominatorDiagnostics,
+  admissionAudit,
+  boundaryProfileDiagnostic
+) {
   const evidenceByCell = Object.fromEntries((inferenceEvidence || []).map((row) => [row.cell_id, row]));
   const recoveryByCell = Object.fromEntries((countRecovery || []).map((row) => [row.linked_cell_id || row.cell_id, row]));
+  const recoveryRollupByCell = Object.fromEntries((recoveryRollup || []).map((row) => [row.cell_id, row]));
+  const recoveryGradeCounts = (recoveryRollup || []).reduce((acc, row) => {
+    acc[row.recovery_grade] = (acc[row.recovery_grade] || 0) + 1;
+    return acc;
+  }, {});
+  const caveatDiagnosticByCell = (countCaveatDiagnostics || []).reduce((acc, row) => {
+    const cell = row.cell_id;
+    if (!acc[cell]) acc[cell] = [];
+    acc[cell].push(row);
+    return acc;
+  }, {});
+  const denominatorDiagnosticByCell = (countDenominatorDiagnostics || []).reduce((acc, row) => {
+    const cell = row.cell_id;
+    if (!acc[cell]) acc[cell] = [];
+    acc[cell].push(row);
+    return acc;
+  }, {});
   const admissionAuditByCell = Object.fromEntries((admissionAudit || []).map((row) => [row.cell_id, row]));
+  const boundaryProfileByCell = Object.fromEntries((boundaryProfileDiagnostic || []).map((row) => [row.cell_id, row]));
   const orderedStates = [
     "inference_ready",
     "gaussian_baseline_comparator",
     "fit_supported_baseline",
     "non_gaussian_recovery_only",
+    "non_gaussian_recovery_caveat",
+    "non_gaussian_recovery_blocked",
     "recovery_only",
     "non_gaussian_point_only",
     "non_gaussian_intercept_recovery_smoke",
     "gaussian_mu_slope_smoke_passed",
+    "mu_slope_pregrid_planned",
     "gaussian_lowq_gate_required",
+    "mu_slope_pregrid_blocked",
     "topup_required",
+    "mcse_met_upper_tail_blocked",
     "calibration_required",
     "admission_blocked",
     "high_q_gate_required",
@@ -823,12 +945,26 @@ function renderQSeriesBoard(rows, inferenceEvidence, countRecovery, admissionAud
     ["Gaussian baselines", stateCounts.gaussian_baseline_comparator || 0, "Ordinary comparators"],
     ["Fit baseline", stateCounts.fit_supported_baseline || 0, "Supported fit comparators only"],
     ["NG recovery", stateCounts.non_gaussian_recovery_only || 0, "Count recovery only"],
+    ["Recovery graded", (recoveryRollup || []).length, "Separate recovery-grade rows"],
+    ["Cluster recovery", recoveryGradeCounts.cluster_confirmed_recovery_only || 0, "Rorqual-confirmed"],
+    ["Local recovery", recoveryGradeCounts.local_recovery_only || 0, "Local recovery grid"],
+    ["Recovery caveat", recoveryGradeCounts.local_recovery_hessian_caveat || 0, "Local Hessian caveat"],
+    ["NG caveat", stateCounts.non_gaussian_recovery_caveat || 0, "Recovery with caveat"],
+    ["NG top-up", (countTopupRecovery || []).length, "Stronger-denominator recovery"],
+    ["NG cluster dispatch", (countTopupClusterDispatch || []).length, "Primary-cluster confirmation"],
+    ["NG caveat conditions", (countCaveatDiagnostics || []).length, "Condition-level blocker diagnostics"],
+    ["NG denominator conditions", (countDenominatorDiagnostics || []).length, "Stronger-denominator diagnostics"],
+    ["NG blocked", stateCounts.non_gaussian_recovery_blocked || 0, "Recovery attempted, blocked"],
     ["Recovery-only", stateCounts.recovery_only || 0, "Point recovery, no intervals"],
     ["NG point", stateCounts.non_gaussian_point_only || 0, "Point/extractor only"],
     ["NG intercept smoke", stateCounts.non_gaussian_intercept_recovery_smoke || 0, "Count intercept local smoke"],
     ["Mu-slope smoke", stateCounts.gaussian_mu_slope_smoke_passed || 0, "Gaussian q1 local smoke"],
+    ["Mu pregrid", stateCounts.mu_slope_pregrid_planned || 0, "Gaussian q1 interval probe"],
+    ["Mu SR150 blocked", stateCounts.mu_slope_pregrid_blocked || 0, "Retained pregrid blockers"],
+    ["Mu boundary profiles", (boundaryProfileDiagnostic || []).reduce((sum, row) => sum + Number(row.n_boundary_rows || 0), 0), "Endpoint-profile attempts"],
     ["Low-q gate", stateCounts.gaussian_lowq_gate_required || 0, "Gaussian point/fixture only"],
     ["Top-up needed", stateCounts.topup_required || 0, "Coverage measured, not enough"],
+    ["Mu SR475 blocked", stateCounts.mcse_met_upper_tail_blocked || 0, "MCSE met, miss-balance blocked"],
     ["Calibration needed", stateCounts.calibration_required || 0, "Correction not row-signed"],
     ["Admission blocked", stateCounts.admission_blocked || 0, "Denominator gap first"],
     ["High-q gate", stateCounts.high_q_gate_required || 0, "q4 fixture, no inference"],
@@ -863,6 +999,7 @@ function renderQSeriesBoard(rows, inferenceEvidence, countRecovery, admissionAud
     "Provider/q",
     "Endpoint/slope",
     "Stability",
+    "Recovery",
     "Inference",
     "Fit",
     "Interval",
@@ -877,18 +1014,19 @@ function renderQSeriesBoard(rows, inferenceEvidence, countRecovery, admissionAud
       <div class="cell">${esc(row.structure_provider)}<div class="note">${esc(row.dimension_pattern)} ${esc(row.covariance_layout)}</div></div>
       <div class="cell">${esc(row.endpoint_set)}<div class="note">${esc(row.slope_class)}</div></div>
       <div class="cell">${pill(qSeriesStability(row))}<div class="note">${esc(row.extractor_status)} / ${esc(row.bridge_status)}</div></div>
+      <div class="cell">${pill(qSeriesRecovery(row, recoveryRollupByCell, recoveryByCell))}</div>
       <div class="cell">${pill(qSeriesInference(row))}</div>
       <div class="cell">${pill(row.fit_status)}</div>
       <div class="cell">${pill(row.interval_status)}</div>
       <div class="cell">${pill(row.coverage_status)}</div>
-      <div class="cell">${link(row.evidence_url, "evidence")}${evidenceByCell[row.cell_id] ? ` ${link(evidenceByCell[row.cell_id].evidence_url, "summary")}` : ""}${recoveryByCell[row.cell_id] ? ` ${link(recoveryByCell[row.cell_id].evidence_url, "recovery")}` : ""}${admissionAuditByCell[row.cell_id] ? ` ${link(admissionAuditByCell[row.cell_id].evidence_url, "admission")}` : ""}<div class="note">${esc(row.next_gate)}</div>${qSeriesEvidenceSummary(row, evidenceByCell, recoveryByCell, admissionAuditByCell)}</div>
+      <div class="cell">${link(row.evidence_url, "evidence")}${evidenceByCell[row.cell_id] ? ` ${link(evidenceByCell[row.cell_id].evidence_url, "summary")}` : ""}${recoveryByCell[row.cell_id] ? ` ${link(recoveryByCell[row.cell_id].evidence_url, "recovery")}` : ""}${recoveryRollupByCell[row.cell_id] ? ` ${link(recoveryRollupByCell[row.cell_id].evidence_url, "rollup")}` : ""}${caveatDiagnosticByCell[row.cell_id] ? ` ${link(caveatDiagnosticByCell[row.cell_id][0].evidence_url, "caveat diagnostic")}` : ""}${denominatorDiagnosticByCell[row.cell_id] ? ` ${link(denominatorDiagnosticByCell[row.cell_id][0].evidence_url, "denominator diagnostic")}` : ""}${admissionAuditByCell[row.cell_id] ? ` ${link(admissionAuditByCell[row.cell_id].evidence_url, "admission")}` : ""}${boundaryProfileByCell[row.cell_id] ? ` ${link(boundaryProfileByCell[row.cell_id].evidence_url, "boundary profile")}` : ""}<div class="note">${esc(row.next_gate)}</div>${qSeriesEvidenceSummary(row, evidenceByCell, recoveryByCell, recoveryRollupByCell, caveatDiagnosticByCell, denominatorDiagnosticByCell, admissionAuditByCell, boundaryProfileByCell)}</div>
     </article>`
   ).join("");
   document.getElementById("q-series-board").innerHTML =
     `<div class="qseries-summary">${summary.map(([label, value, note]) =>
       `<article class="status-cell"><div class="label">${esc(label)}</div><div class="value">${esc(value)}</div><div class="note">${esc(note)}</div></article>`
     ).join("")}</div>
-    <div class="note">Rows are sorted by evidence state. Fit stability, recovery evidence, local smoke evidence, admission state, inference readiness, interval status, and coverage status are separate signals; a tried, fit-stable, recovery-only, non-Gaussian point-only, count-intercept-smoke, local-smoke-passed, low-q-gated, top-up-required, calibration-required, admission-blocked, or high-q-gated row is not inference-ready unless both interval and coverage are <code>inference_ready</code>. Promoted rows show their interval channel, denominator, coverage, and miss-balance summary from <code>structured-re-q-series-inference-evidence-summary.tsv</code>; count recovery-only rows show recovery metrics from <code>structured-re-count-slope-recovery-results.tsv</code>; count intercept smoke, spatial/animal sigma, q2, high-q, non-Gaussian, Gaussian mu-slope smoke, and Gaussian low-q status rows show blocker audits from their sidecars; other rows show the support-cell denominator policy.</div>
+      <div class="note">Rows are sorted by evidence state. Fit stability, recovery grade, local smoke evidence, admission state, inference readiness, interval status, and coverage status are separate signals; a tried, fit-stable, recovery-only, recovery-caveated, non-Gaussian point-only, count-intercept-smoke, local-smoke-passed, mu-slope-pregrid-planned, mu-slope-pregrid-blocked, low-q-gated, top-up-required, MCSE-met-upper-tail-blocked, calibration-required, admission-blocked, or high-q-gated row is not inference-ready unless both interval and coverage are <code>inference_ready</code>. Promoted rows show their interval channel, denominator, coverage, and miss-balance summary from <code>structured-re-q-series-inference-evidence-summary.tsv</code>; count recovery rows show recovery metrics from <code>structured-re-count-slope-recovery-results.tsv</code>, <code>structured-re-count-intercept-recovery-results.tsv</code>, <code>structured-re-count-intercept-topup-recovery-results.tsv</code>, and the separate grade rollup <code>structured-re-nongaussian-recovery-rollup.tsv</code>; primary-cluster dispatch for the count-intercept top-up is tracked in <code>structured-re-count-intercept-topup-cluster-dispatch.tsv</code> and the fetched result TSV is validated as recovery-only evidence; recovery-caveated count intercept rows also show condition-level blocker diagnostics from <code>structured-re-count-intercept-caveat-diagnostic.tsv</code> and stronger-denominator diagnostics from <code>structured-re-count-intercept-denominator-diagnostic.tsv</code>; Gaussian mu-slope SR150 boundary profile failures are tracked separately in <code>structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv</code>, the SR150 Wald-plus-boundary-profile routing audit is in <code>structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv</code>, and the SR475 top-up audit is in <code>structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv</code>; count intercept smoke, spatial/animal sigma, q2, high-q, non-Gaussian, Gaussian mu-slope smoke/admission/pregrid, and Gaussian low-q status rows show blocker audits from their sidecars; other rows show the support-cell denominator policy.</div>
     <div class="qseries-table">
       <article class="qseries-row qseries-head">${head.map((x) => `<div>${esc(x)}</div>`).join("")}</article>
       ${table}
@@ -1071,9 +1209,29 @@ function render(data, gates, capabilities, sidecars) {
     ...(sidecars.phyloCountInterceptRecoverySmokeStatus || []),
     ...(sidecars.phyloInteractionCountRecoverySmokeStatus || []),
     ...(sidecars.gaussianLowQStatusAudit || []),
-    ...(sidecars.gaussianMuSlopeSmokeStatus || [])
+    ...(sidecars.gaussianMuSlopeSmokeStatus || []),
+    ...(sidecars.gaussianMuSlopeAdmissionAudit || []),
+    ...(sidecars.gaussianMuSlopePregridResults || []),
+    ...(sidecars.gaussianMuSlopeHybridBoundaryAudit || []),
+    ...(sidecars.gaussianMuSlopeHybridSR475Audit || [])
+  ];
+  const countRecoveryResults = [
+    ...(sidecars.countSlopeRecoveryResults || []),
+    ...(sidecars.countInterceptRecoveryResults || []),
+    ...(sidecars.countInterceptTopupRecoveryResults || [])
   ];
-  renderQSeriesBoard(sidecars.qSeriesCells || [], sidecars.qSeriesInferenceEvidence || [], sidecars.countSlopeRecoveryResults || [], admissionAudits);
+  renderQSeriesBoard(
+    sidecars.qSeriesCells || [],
+    sidecars.qSeriesInferenceEvidence || [],
+    countRecoveryResults,
+    sidecars.nongaussianRecoveryRollup || [],
+    sidecars.countInterceptTopupRecoveryResults || [],
+    sidecars.countInterceptTopupClusterDispatch || [],
+    sidecars.countInterceptCaveatDiagnostics || [],
+    sidecars.countInterceptDenominatorDiagnostics || [],
+    admissionAudits,
+    sidecars.gaussianMuSlopeBoundaryProfileDiagnostic || []
+  );
   renderFinishBoard(data);
   renderRepos(data);
   renderPhases(data);
@@ -1109,7 +1267,13 @@ async function tick() {
       assignments: parseTsv(await readText("member-wave-assignments.tsv")),
       qSeriesCells: parseTsv(await readText("structured-re-q-series-support-cells.tsv")),
       qSeriesInferenceEvidence: parseTsv(await readText("structured-re-q-series-inference-evidence-summary.tsv")),
+      nongaussianRecoveryRollup: parseTsv(await readText("structured-re-nongaussian-recovery-rollup.tsv")),
       countSlopeRecoveryResults: parseTsv(await readText("structured-re-count-slope-recovery-results.tsv")),
+      countInterceptRecoveryResults: parseTsv(await readText("structured-re-count-intercept-recovery-results.tsv")),
+      countInterceptTopupRecoveryResults: parseTsv(await readText("structured-re-count-intercept-topup-recovery-results.tsv")),
+      countInterceptTopupClusterDispatch: parseTsv(await readText("structured-re-count-intercept-topup-cluster-dispatch.tsv")),
+      countInterceptCaveatDiagnostics: parseTsv(await readText("structured-re-count-intercept-caveat-diagnostic.tsv")),
+      countInterceptDenominatorDiagnostics: parseTsv(await readText("structured-re-count-intercept-denominator-diagnostic.tsv")),
       sigmaSlopeSpatialAnimalAdmissionAudit: parseTsv(await readText("structured-re-sigma-slope-spatial-animal-admission-audit.tsv")),
       q2SlopeSpatialAnimalAdmissionAudit: parseTsv(await readText("structured-re-q2-slope-spatial-animal-admission-audit.tsv")),
       highQStatusAudit: parseTsv(await readText("structured-re-high-q-status-audit.tsv")),
@@ -1119,6 +1283,12 @@ async function tick() {
       phyloInteractionCountRecoverySmokeStatus: parseTsv(await readText("structured-re-phylo-interaction-count-recovery-smoke-status.tsv")),
       gaussianLowQStatusAudit: parseTsv(await readText("structured-re-gaussian-lowq-status-audit.tsv")),
       gaussianMuSlopeSmokeStatus: parseTsv(await readText("structured-re-gaussian-mu-slope-smoke-status.tsv")),
+      gaussianMuSlopeAdmissionAudit: parseTsv(await readText("structured-re-gaussian-mu-slope-admission-audit.tsv")),
+      gaussianMuSlopePregridDryRun: parseTsv(await readText("structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv")),
+      gaussianMuSlopePregridResults: parseTsv(await readText("structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv")),
+      gaussianMuSlopeBoundaryProfileDiagnostic: parseTsv(await readText("structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv")),
+      gaussianMuSlopeHybridBoundaryAudit: parseTsv(await readText("structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv")),
+      gaussianMuSlopeHybridSR475Audit: parseTsv(await readText("structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv")),
       balanceSlices: parseTsv(await readText("structured-re-balance-100-slices.tsv")),
       finishSlices: parseTsv(await readText("structured-re-finish-100-slices.tsv")),
       conversionSlices: parseTsv(await readText("structured-re-conversion-200-slices.tsv")),
diff --git a/docs/dev-log/dashboard/structured-re-high-q-status-audit.tsv b/docs/dev-log/dashboard/structured-re-high-q-status-audit.tsv
index 284683a9..38a9ff00 100644
--- a/docs/dev-log/dashboard/structured-re-high-q-status-audit.tsv
+++ b/docs/dev-log/dashboard/structured-re-high-q-status-audit.tsv
@@ -2,24 +2,24 @@ audit_id	cell_id	dimension_pattern	high_q_scope	widget_state	evidence_basis	stab
 high_q_ordinary_q4_location_one_slope	qseries_ordinary_q4_location_one_slope	q4	ordinary_location_comparator	high_q_diagnostic	ordinary q4 location route with extractor evidence; comparator only	diagnostic route; not structured q4 support	diagnostic_only interval status and no coverage evidence	point_fit	diagnostic_only	planned	do_not_promote	NEWS.md	Ordinary q4 location one-slope is a comparator and diagnostic route only; it is not structured q4, calibrated interval, coverage, q8, REML, AI-REML, bridge, supported, or public support.	Keep ordinary q4 as comparator; do not use it to promote structured q4 rows.
 high_q_ordinary_q6_location_two_slopes	qseries_ordinary_q6_location_two_slopes	q6	ordinary_location_comparator	high_q_diagnostic	ordinary q6 location route with extractor evidence; diagnostic only	diagnostic route; not structured q6 support	diagnostic_only interval status and no coverage evidence	diagnostic_only	diagnostic_only	planned	do_not_promote	NEWS.md	Ordinary q6 location two-slope is diagnostic-only; it is not structured q6, calibrated interval, coverage, q8, REML, AI-REML, bridge, supported, or public support.	Keep q6 out of structured support wording until row-specific fit, interval, and coverage evidence exists.
 high_q_ordinary_q8_all_endpoint_one_slope	qseries_ordinary_q8_all_endpoint_one_slope	q8	ordinary_all_endpoint_diagnostic	q8_stability_blocked	ordinary q8 all-endpoint diagnostic audit: 38/40 manifests completed, convergence 0.263 and 0.158, zero positive-Hessian fits, no usable Wald intervals	stability-first q8 route; Hessian failure blocks inference	no q8 coverage, power, calibrated interval, or public-support evidence	diagnostic_only	diagnostic_only	planned	do_not_promote	docs/design/161-phase-18-bivariate-q8-recovery-audit.md	Ordinary q8 all-endpoint one-slope remains diagnostic-only with weak-Hessian evidence; it is not structured q8, calibrated interval, coverage, power, REML, AI-REML, bridge, supported, or public support.	Stabilize q8 geometry and positive-Hessian rate before any coverage or power grid.
-high_q_phylo_q4_mu1_mu2_one_slope	qseries_phylo_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Phylo q4 location one-slope has point/fixture evidence only; it is not interval-ready, coverage-ready, q8, REML, AI-REML, bridge-supported, supported, or public support.	Freeze q4 denominator, direct-SD interval, and derived-correlation interval rules before coverage.
-high_q_phylo_q4_all_four_intercept	qseries_phylo_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-parity-acceptance-gate.tsv	Phylo q4 all-four intercept has point/fixture evidence only; it is not interval-ready, coverage-ready, q8, REML, AI-REML, bridge-supported, supported, or public support.	Use q4 Hessian/bootstrap diagnostics to decide denominator admission before coverage.
-high_q_phylo_q8_all_four_one_slope	qseries_phylo_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked in q4-slope interval stability probe; direct SD estimates include lower-bound sigma endpoints	planned interval and coverage; no q8 calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-interval-stability-probe.tsv	Phylo shared-label all-four one-slope q8-shaped cell has point/fixture evidence but stability blocks inference; it is not calibrated interval, coverage, q4 REML, q8 REML, AI-REML, bridge-supported, supported, or public support.	Diagnose q8-shaped Hessian and lower-bound behaviour before denominator accounting.
+high_q_phylo_q4_mu1_mu2_one_slope	qseries_phylo_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Phylo q4 location one-slope has point/fixture evidence plus diagnostic-only interval evidence; it is not interval-ready, coverage-ready, q8, REML, AI-REML, bridge-supported, supported, or public support.	Freeze q4 denominator, direct-SD interval, and derived-correlation interval rules before coverage.
+high_q_phylo_q4_all_four_intercept	qseries_phylo_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-parity-acceptance-gate.tsv	Phylo q4 all-four intercept has point/fixture evidence plus diagnostic-only interval evidence; it is not interval-ready, coverage-ready, q8, REML, AI-REML, bridge-supported, supported, or public support.	Use q4 Hessian/bootstrap diagnostics to decide denominator admission before coverage.
+high_q_phylo_q8_all_four_one_slope	qseries_phylo_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked in q4-slope interval stability probe; direct SD estimates include lower-bound sigma endpoints	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-interval-stability-probe.tsv	Phylo shared-label all-four one-slope q8-shaped cell has point/fixture evidence plus diagnostic-only interval evidence, but stability blocks inference; it is not calibrated interval, coverage, q4 REML, q8 REML, AI-REML, bridge-supported, supported, or public support.	Diagnose q8-shaped Hessian and lower-bound behaviour before denominator accounting.
 high_q_phylo_q6_planned	qseries_phylo_q6_planned	q6	structured_location_q6_planned	high_q_planned	structured q6 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Phylo q6 remains planned; ordinary q6 diagnostics do not transfer to structured q6 and do not promote intervals, coverage, q8, REML, AI-REML, supported, or public support.	Design and fit row-specific q6 before interval or coverage work.
 high_q_phylo_q8_planned	qseries_phylo_q8_planned	q8	structured_q8_planned	high_q_planned	broader structured q8 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader phylo q8 remains planned; ordinary q8 and q8-shaped shared-label fixtures do not transfer to this row or promote intervals, coverage, REML, AI-REML, supported, or public support.	Design broader q8 only after shared-label q8-shaped stability gates pass.
-high_q_spatial_q4_mu1_mu2_one_slope	qseries_spatial_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 fixed-covariance location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Fixed-covariance spatial q4 location one-slope has point/fixture evidence only; it is not range-estimating spatial support, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze fixed-covariance q4 denominator and derived-correlation interval rules before coverage.
-high_q_spatial_q4_all_four_intercept	qseries_spatial_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 fixed-covariance all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Fixed-covariance spatial q4 all-four intercept has point/fixture evidence only; it is not range-estimating spatial support, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 fixed-covariance Hessian/bootstrap diagnostics to decide denominator admission before coverage.
-high_q_spatial_q8_all_four_one_slope	qseries_spatial_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact fixed-covariance shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	planned interval and coverage; no q8 calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Fixed-covariance spatial shared-label all-four one-slope q8-shaped cell has point/fixture evidence but stability blocks inference; it is not range-estimating spatial support, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate spatial fixed-covariance q8-shaped Hessian geometry from range-estimating spatial support before coverage.
+high_q_spatial_q4_mu1_mu2_one_slope	qseries_spatial_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 fixed-covariance location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Fixed-covariance spatial q4 location one-slope has point/fixture evidence plus diagnostic-only interval evidence; it is not range-estimating spatial support, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze fixed-covariance q4 denominator and derived-correlation interval rules before coverage.
+high_q_spatial_q4_all_four_intercept	qseries_spatial_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 fixed-covariance all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Fixed-covariance spatial q4 all-four intercept has point/fixture evidence plus diagnostic-only interval evidence; it is not range-estimating spatial support, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 fixed-covariance Hessian/bootstrap diagnostics to decide denominator admission before coverage.
+high_q_spatial_q8_all_four_one_slope	qseries_spatial_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact fixed-covariance shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Fixed-covariance spatial shared-label all-four one-slope q8-shaped cell has point/fixture evidence plus diagnostic-only interval evidence, but stability blocks inference; it is not range-estimating spatial support, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate spatial fixed-covariance q8-shaped Hessian geometry from range-estimating spatial support before coverage.
 high_q_spatial_q6_planned	qseries_spatial_q6_planned	q6	structured_location_q6_planned	high_q_planned	structured spatial q6 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured spatial q6 remains planned; ordinary q6 diagnostics do not transfer and do not promote intervals, coverage, q8, REML, AI-REML, supported, or public support.	Design and fit row-specific fixed-covariance spatial q6 before interval or coverage work.
 high_q_spatial_q8_planned	qseries_spatial_q8_planned	q8	structured_q8_planned	high_q_planned	broader structured spatial q8 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured spatial q8 remains planned; ordinary q8 and fixed-covariance q8-shaped shared-label fixtures do not transfer or promote intervals, coverage, REML, AI-REML, supported, or public support.	Design broader spatial q8 only after fixed-covariance q8-shaped stability gates pass.
-high_q_animal_q4_mu1_mu2_one_slope	qseries_animal_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 A-matrix location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Animal q4 location one-slope has point/fixture evidence only; it is not pedigree/Ainv bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze A-matrix q4 denominator and derived-correlation interval rules before coverage.
-high_q_animal_q4_all_four_intercept	qseries_animal_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 A-matrix all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; finite Wald/profile but nonfinite bootstrap behaviour remains diagnostic	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Animal q4 all-four intercept has point/fixture evidence only; it is not pedigree/Ainv bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 A-matrix diagnostics to decide denominator admission before coverage.
-high_q_animal_q8_all_four_one_slope	qseries_animal_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact A-matrix shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	planned interval and coverage; no q8 calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Animal shared-label all-four one-slope q8-shaped cell has point/fixture evidence but stability blocks inference; it is not pedigree/Ainv bridge marshalling, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate animal q8-shaped Hessian geometry from pedigree/Ainv bridge work before coverage.
+high_q_animal_q4_mu1_mu2_one_slope	qseries_animal_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 A-matrix location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Animal q4 location one-slope has point/fixture evidence plus diagnostic-only interval evidence; it is not pedigree/Ainv bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze A-matrix q4 denominator and derived-correlation interval rules before coverage.
+high_q_animal_q4_all_four_intercept	qseries_animal_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 A-matrix all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; finite Wald/profile but nonfinite bootstrap behaviour remains diagnostic	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Animal q4 all-four intercept has point/fixture evidence plus diagnostic-only interval evidence; it is not pedigree/Ainv bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 A-matrix diagnostics to decide denominator admission before coverage.
+high_q_animal_q8_all_four_one_slope	qseries_animal_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact A-matrix shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Animal shared-label all-four one-slope q8-shaped cell has point/fixture evidence plus diagnostic-only interval evidence, but stability blocks inference; it is not pedigree/Ainv bridge marshalling, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate animal q8-shaped Hessian geometry from pedigree/Ainv bridge work before coverage.
 high_q_animal_q6_planned	qseries_animal_q6_planned	q6	structured_location_q6_planned	high_q_planned	structured animal q6 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured animal q6 remains planned; ordinary q6 diagnostics do not transfer and do not promote intervals, coverage, q8, REML, AI-REML, supported, or public support.	Design and fit row-specific animal q6 before interval or coverage work.
 high_q_animal_q8_planned	qseries_animal_q8_planned	q8	structured_q8_planned	high_q_planned	broader structured animal q8 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured animal q8 remains planned; ordinary q8 and A-matrix q8-shaped shared-label fixtures do not transfer or promote intervals, coverage, REML, AI-REML, supported, or public support.	Design broader animal q8 only after A-matrix q8-shaped stability gates pass.
-high_q_relmat_q4_mu1_mu2_one_slope	qseries_relmat_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 K-matrix location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Relmat q4 location one-slope has point/fixture evidence only; it is not Q bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze K-matrix q4 denominator and derived-correlation interval rules before coverage.
-high_q_relmat_q4_all_four_intercept	qseries_relmat_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 K-matrix all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	planned interval and coverage; no calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Relmat q4 all-four intercept has point/fixture evidence only; it is not Q bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 K-matrix diagnostics to decide denominator admission before coverage.
-high_q_relmat_q8_all_four_one_slope	qseries_relmat_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact K/Q shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	planned interval and coverage; no q8 calibrated denominator	point_fit	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Relmat shared-label all-four one-slope q8-shaped cell has point/fixture evidence but stability blocks inference; it is not Q bridge marshalling, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate relmat q8-shaped Hessian geometry from Q bridge work before coverage.
+high_q_relmat_q4_mu1_mu2_one_slope	qseries_relmat_q4_mu1_mu2_one_slope	q4	structured_location_q4	high_q_gate_required	q4 K-matrix location point/extractor evidence plus same-target fixture parity	fit stable at fixture level; derived correlations still need interval machinery	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Relmat q4 location one-slope has point/fixture evidence plus diagnostic-only interval evidence; it is not Q bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Freeze K-matrix q4 denominator and derived-correlation interval rules before coverage.
+high_q_relmat_q4_all_four_intercept	qseries_relmat_q4_all_four_intercept	q4	structured_all_four_intercept_q4	high_q_gate_required	q4 K-matrix all-four intercept point/extractor evidence plus same-target fixture parity	fixture parity only; Hessian/bootstrap geometry still diagnostic	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Relmat q4 all-four intercept has point/fixture evidence plus diagnostic-only interval evidence; it is not Q bridge marshalling, interval-ready, coverage-ready, q8, REML, AI-REML, supported, or public support.	Use q4 K-matrix diagnostics to decide denominator admission before coverage.
+high_q_relmat_q8_all_four_one_slope	qseries_relmat_q4_all_four_one_slope_planned	q8	structured_all_four_one_slope_q8_shape	q8_stability_blocked	exact K/Q shared-label all-four one-slope point/extractor and fixture evidence; q8-shaped direct SD stability probe blocks inference	pdHess blocked/nonfinite covariance geometry in q4-slope diagnostics	diagnostic-only interval signal; coverage still planned; no calibrated denominator	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-slope-hessian-geometry.tsv	Relmat shared-label all-four one-slope q8-shaped cell has point/fixture evidence plus diagnostic-only interval evidence, but stability blocks inference; it is not Q bridge marshalling, calibrated interval, coverage, REML, AI-REML, supported, or public support.	Separate relmat q8-shaped Hessian geometry from Q bridge work before coverage.
 high_q_relmat_q6_planned	qseries_relmat_q6_planned	q6	structured_location_q6_planned	high_q_planned	structured relmat q6 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured relmat q6 remains planned; ordinary q6 diagnostics do not transfer and do not promote intervals, coverage, q8, REML, AI-REML, supported, or public support.	Design and fit row-specific relmat q6 before interval or coverage work.
 high_q_relmat_q8_planned	qseries_relmat_q8_planned	q8	structured_q8_planned	high_q_planned	broader structured relmat q8 remains a future design row	no point-fit or fixture denominator yet	no interval or coverage denominator	planned	planned	planned	do_not_promote	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured relmat q8 remains planned; ordinary q8 and K/Q q8-shaped shared-label fixtures do not transfer or promote intervals, coverage, REML, AI-REML, supported, or public support.	Design broader relmat q8 only after K/Q q8-shaped stability and bridge gates pass.
 high_q_phylo_direct_sd_bivariate	qseries_phylo_direct_sd_bivariate	q4	bivariate_direct_sd_targets	high_q_diagnostic	bivariate direct SD target visibility and profile-target bridge map; diagnostic only	direct SD targets visible but derived-correlation intervals are not support evidence	diagnostic_only interval status and no coverage evidence	point_fit	diagnostic_only	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q4-profile-target-bridge-map.tsv	Bivariate direct SD target visibility is diagnostic-only; it is not q4 interval reliability, derived-correlation interval support, calibrated coverage, q8, REML, AI-REML, supported, or public support.	Keep direct SD profile smoke separate from coverage acceptance.
diff --git a/docs/dev-log/dashboard/structured-re-nongaussian-status-audit.tsv b/docs/dev-log/dashboard/structured-re-nongaussian-status-audit.tsv
index 941e0275..0b91ebd8 100644
--- a/docs/dev-log/dashboard/structured-re-nongaussian-status-audit.tsv
+++ b/docs/dev-log/dashboard/structured-re-nongaussian-status-audit.tsv
@@ -1,18 +1,18 @@
 audit_id	cell_id	family	family_group	structure_provider	endpoint_set	widget_state	evidence_basis	stability_signal	inference_signal	linked_fit_status	linked_interval_status	linked_coverage_status	promotion_decision	evidence_url	claim_boundary	next_gate
-nongaussian_status_phylo_interaction_poisson_q1_mu	qseries_phylo_interaction_poisson_q1_mu	poisson()	count_mu_intercept_smoke	phylo_interaction	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local phylo_interaction recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-phylo-interaction-count-recovery-smoke-status.tsv	Non-Gaussian poisson() phylo_interaction mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_phylo_interaction_nbinom2_q1_mu	qseries_phylo_interaction_nbinom2_q1_mu	nbinom2()	count_mu_intercept_smoke	phylo_interaction	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local phylo_interaction recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-phylo-interaction-count-recovery-smoke-status.tsv	Non-Gaussian nbinom2() phylo_interaction mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_phylo_poisson_q1_mu_intercept	qseries_phylo_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	phylo	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local phylo count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-phylo-count-intercept-recovery-smoke-status.tsv	Non-Gaussian poisson() phylo mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_phylo_nbinom2_q1_mu_intercept	qseries_phylo_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	phylo	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local phylo count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-phylo-count-intercept-recovery-smoke-status.tsv	Non-Gaussian nbinom2() phylo mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_spatial_poisson_q1_mu_intercept	qseries_spatial_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	spatial	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian poisson() spatial mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_spatial_nbinom2_q1_mu_intercept	qseries_spatial_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	spatial	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists with boundary warnings; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian nbinom2() spatial mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only with lower-boundary warnings; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_animal_poisson_q1_mu_intercept	qseries_animal_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	animal	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian poisson() animal mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_animal_nbinom2_q1_mu_intercept	qseries_animal_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	animal	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian nbinom2() animal mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_relmat_poisson_q1_mu_intercept	qseries_relmat_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	relmat	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian poisson() relmat mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_relmat_nbinom2_q1_mu_intercept	qseries_relmat_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	relmat	mu	non_gaussian_intercept_recovery_smoke	local recovery-smoke sidecar evidence only	local count-intercept recovery smoke exists; see linked sidecar	local recovery smoke only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/dashboard/structured-re-count-intercept-recovery-smoke-status.tsv	Non-Gaussian nbinom2() relmat mu row is non_gaussian_intercept_recovery_smoke; local recovery smoke only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Run replicated recovery grid with MCSE and boundary ledger before changing non-Gaussian interval, coverage, bridge, q2, q4, or public-support wording.
+nongaussian_status_phylo_interaction_poisson_q1_mu	qseries_phylo_interaction_poisson_q1_mu	poisson()	count_mu_intercept_smoke	phylo_interaction	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (80 fit_ok/80, 0 pdHess false, 1 near-zero estimates at threshold 1e-04).	80/80 fit_ok; 0 pdHess false; 80 finite estimates; 1 near-zero estimates (rate 0.0125); 1 boundary-warning rows; bias -0.05111; RMSE 0.1572.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 80 structured-SD rows for poisson() phylo_interaction q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_phylo_interaction_nbinom2_q1_mu	qseries_phylo_interaction_nbinom2_q1_mu	nbinom2()	count_mu_intercept_smoke	phylo_interaction	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (80 fit_ok/80, 0 pdHess false, 4 near-zero estimates at threshold 1e-04).	80/80 fit_ok; 0 pdHess false; 80 finite estimates; 4 near-zero estimates (rate 0.05); 4 boundary-warning rows; bias -0.05387; RMSE 0.175.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 80 structured-SD rows for nbinom2() phylo_interaction q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_phylo_poisson_q1_mu_intercept	qseries_phylo_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	phylo	mu	non_gaussian_recovery_only	80-rep stronger-denominator count-intercept top-up recovery grid exists; Rorqual-confirmed top-up recovery-only sidecar evidence from SLURM job 14897050 (320 fit_ok/320, 0 pdHess false, 0 near-zero estimates at threshold 1e-04). Original weak-denominator caveat remains documented in caveat sidecars.	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 0 near-zero estimates (rate 0); 0 boundary-warning rows; bias -0.02113; RMSE 0.09728.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Non-Gaussian count-intercept top-up recovery measured locally and reproduced on Rorqual with 320 structured-SD rows for poisson() phylo q1 mu. The top-up sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support. The original weak-denominator caveat remains documented.	Use cluster-confirmed recovery-only wording with original caveat provenance; intervals and coverage remain unsupported until a separate interval route is designed and validated.
+nongaussian_status_phylo_nbinom2_q1_mu_intercept	qseries_phylo_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	phylo	mu	non_gaussian_recovery_only	80-rep stronger-denominator count-intercept top-up recovery grid exists; Rorqual-confirmed top-up recovery-only sidecar evidence from SLURM job 14897050 (320 fit_ok/320, 0 pdHess false, 0 near-zero estimates at threshold 1e-04). Original weak-denominator caveat remains documented in caveat sidecars.	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 0 near-zero estimates (rate 0); 0 boundary-warning rows; bias -0.02820; RMSE 0.09633.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Non-Gaussian count-intercept top-up recovery measured locally and reproduced on Rorqual with 320 structured-SD rows for nbinom2() phylo q1 mu. The top-up sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support. The original weak-denominator caveat remains documented.	Use cluster-confirmed recovery-only wording with original caveat provenance; intervals and coverage remain unsupported until a separate interval route is designed and validated.
+nongaussian_status_spatial_poisson_q1_mu_intercept	qseries_spatial_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	spatial	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (320 fit_ok/320, 0 pdHess false, 63 near-zero estimates at threshold 1e-04).	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 63 near-zero estimates (rate 0.1969); 0 boundary-warning rows; bias -0.08126; RMSE 0.1932.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 320 structured-SD rows for poisson() spatial q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_spatial_nbinom2_q1_mu_intercept	qseries_spatial_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	spatial	mu	non_gaussian_recovery_only	80-rep stronger-denominator count-intercept top-up recovery grid exists; Rorqual-confirmed top-up recovery-only sidecar evidence from SLURM job 14897050 (320 fit_ok/320, 0 pdHess false, 0 near-zero estimates at threshold 1e-04). Original weak-denominator caveat remains documented in caveat sidecars.	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 0 near-zero estimates (rate 0); 0 boundary-warning rows; bias -0.04196; RMSE 0.16157.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Non-Gaussian count-intercept top-up recovery measured locally and reproduced on Rorqual with 320 structured-SD rows for nbinom2() spatial q1 mu. The top-up sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support. The original weak-denominator caveat remains documented.	Use cluster-confirmed recovery-only wording with original caveat provenance; intervals and coverage remain unsupported until a separate interval route is designed and validated.
+nongaussian_status_animal_poisson_q1_mu_intercept	qseries_animal_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	animal	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (320 fit_ok/320, 0 pdHess false, 25 near-zero estimates at threshold 1e-04).	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 25 near-zero estimates (rate 0.07812); 0 boundary-warning rows; bias -0.05439; RMSE 0.1396.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 320 structured-SD rows for poisson() animal q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_animal_nbinom2_q1_mu_intercept	qseries_animal_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	animal	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (320 fit_ok/320, 0 pdHess false, 53 near-zero estimates at threshold 1e-04).	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 53 near-zero estimates (rate 0.1656); 0 boundary-warning rows; bias -0.07418; RMSE 0.1723.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 320 structured-SD rows for nbinom2() animal q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_relmat_poisson_q1_mu_intercept	qseries_relmat_poisson_q1_mu_intercept	poisson()	count_mu_intercept_smoke	relmat	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (320 fit_ok/320, 0 pdHess false, 23 near-zero estimates at threshold 1e-04).	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 23 near-zero estimates (rate 0.07187); 0 boundary-warning rows; bias -0.04868; RMSE 0.1391.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 320 structured-SD rows for poisson() relmat q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
+nongaussian_status_relmat_nbinom2_q1_mu_intercept	qseries_relmat_nbinom2_q1_mu_intercept	nbinom2()	count_mu_intercept_smoke	relmat	mu	non_gaussian_recovery_only	80-rep local count-intercept recovery grid exists; recovery-only sidecar evidence (320 fit_ok/320, 0 pdHess false, 42 near-zero estimates at threshold 1e-04).	320/320 fit_ok; 0 pdHess false; 320 finite estimates; 42 near-zero estimates (rate 0.1312); 0 boundary-warning rows; bias -0.07216; RMSE 0.1656.	recovery evidence only; interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Non-Gaussian count-intercept recovery measured locally with 320 structured-SD rows for nbinom2() relmat q1 mu. The sidecar reports fit, pdHess, finite-estimate, near-zero-estimate, bias, RMSE, and MCSE diagnostics. This is RECOVERY evidence only and is not interval-ready or coverage-ready: it does NOT promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, bridge support, q2/q4 count covariance, high-q, or public support.	Top up or rerun on the primary cluster before public recovery wording; intervals and coverage remain unsupported.
 nongaussian_status_phylo_poisson_q1_mu_one_slope	qseries_phylo_poisson_q1_mu_one_slope	poisson()	count_mu_recovery	phylo	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian poisson() phylo mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 nongaussian_status_phylo_nbinom2_q1_mu_one_slope	qseries_phylo_nbinom2_q1_mu_one_slope	nbinom2()	count_mu_recovery	phylo	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian nbinom2() phylo mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 nongaussian_status_spatial_poisson_q1_mu_one_slope	qseries_spatial_poisson_q1_mu_one_slope	poisson()	count_mu_recovery	spatial	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian poisson() spatial mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
-nongaussian_status_spatial_nbinom2_q1_mu_one_slope	qseries_spatial_nbinom2_q1_mu_one_slope	nbinom2()	count_mu_recovery	spatial	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian nbinom2() spatial mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
+nongaussian_status_spatial_nbinom2_q1_mu_one_slope	qseries_spatial_nbinom2_q1_mu_one_slope	nbinom2()	count_mu_recovery	spatial	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid with Hessian caveat	point fit and extractor ready; 80/80 fit_ok; 2/80 pdHess false; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	docs/dev-log/simulation-artifacts/2026-06-27-count-slope-recovery-grid-local	Non-Gaussian nbinom2() spatial mu row is non_gaussian_recovery_only with a retained 2/80 pdHess false Hessian caveat; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked with 2/80 pdHess false retained; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 nongaussian_status_animal_poisson_q1_mu_one_slope	qseries_animal_poisson_q1_mu_one_slope	poisson()	count_mu_recovery	animal	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian poisson() animal mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 nongaussian_status_animal_nbinom2_q1_mu_one_slope	qseries_animal_nbinom2_q1_mu_one_slope	nbinom2()	count_mu_recovery	animal	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian nbinom2() animal mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 nongaussian_status_relmat_poisson_q1_mu_one_slope	qseries_relmat_poisson_q1_mu_one_slope	poisson()	count_mu_recovery	relmat	mu	non_gaussian_recovery_only	native point/extractor evidence plus 80-rep recovery grid	point fit and extractor ready; recovery-only evidence	interval unsupported and coverage planned only	point_fit	unsupported	planned	do_not_promote	tests/testthat/test-count-structured-mu.R	Non-Gaussian poisson() relmat mu row is non_gaussian_recovery_only; it is not interval-ready, coverage-ready, REML, AI-REML, supported, bridge-supported, q2/q4 covariance, or public support.	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
diff --git a/docs/dev-log/dashboard/structured-re-q-series-support-cells.tsv b/docs/dev-log/dashboard/structured-re-q-series-support-cells.tsv
index e13d2592..09026b13 100644
--- a/docs/dev-log/dashboard/structured-re-q-series-support-cells.tsv
+++ b/docs/dev-log/dashboard/structured-re-q-series-support-cells.tsv
@@ -15,10 +15,10 @@ qseries_phylo_q1_sigma_one_slope	phylo(1 + x | species, tree = tree) in sigma	ga
 qseries_phylo_q1_mu_sigma_one_slope	phylo(1 + x | species, tree = tree) in mu and sigma	gaussian	gaussian()	phylo	q1_plus_q1	mu+sigma	independent_one_slope	separate_structured_scalars	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-mu-sigma-slope-parity-fixture.tsv	Matched phylo mu plus sigma one-slope support has native point-fit and extractor evidence for the exact four endpoint members plus one deterministic same-target fixture; it does not imply broad bridge support, labelled slope covariance, intervals, coverage, REML, or AI-REML.	fixture_not_coverage	add calibrated interval diagnostics before coverage or public support promotion
 qseries_phylo_q2_mu1_mu2_intercept	phylo(1 | p | species, tree = tree) in mu1 and mu2	gaussian	biv_gaussian()	phylo	q2	mu1+mu2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-acceptance-gate.tsv	Phylo q2 bridge evidence is one complete-response exact-Gaussian ML fixture, not broad bridge or q2 REML support.	fixture_not_coverage	keep q2 acceptance scoped to exact fixture rows
 qseries_phylo_q2_mu1_mu2_one_slope	phylo(0 + x | p | species, tree = tree) in mu1 and mu2	gaussian	biv_gaussian()	phylo	q2	mu1+mu2	labelled_slope_covariance	labelled_structured_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	inference_ready	inference_ready	source	docs/dev-log/dashboard/structured-re-q2-slope-parity-fixture.tsv	Inference-ready: the DEFAULT confint(method='wald') now applies the simulation-calibrated bias+t correction to this location-axis structured-RE slope-only SD; engine-validated nominal-in-total coverage at the deployment default g=8 (SR475, ~0.95 across providers) plus a second grid (near-boundary SD=0.35 conservative, extra g, one-sided rates), on native point-fit/extractor evidence plus deterministic same-target fixture parity for the exact slope-only mu1:x+mu2:x cell; interval reliability rung passed. supported is WITHHELD: residual right-tail miss asymmetry (~6:1 at SD~0.9, upper limit mildly anti-conservative) and g-dependent under-correction (e.g. relmat g=12 ~0.93); the log(g/(g-1)) shift is ~2x the leading-order REML SD term, simulation-calibrated per model class (doc 219). REML, AI-REML, intercept-plus-slope q4/q8, and broad bridge support remain separate gates.	fixture_not_coverage	add calibrated interval diagnostics before support promotion
-qseries_phylo_q4_mu1_mu2_one_slope	phylo(1 + x | p | species, tree = tree) in mu1 and mu2	gaussian	biv_gaussian()	phylo	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Phylo intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact q4 location fixture before support promotion
+qseries_phylo_q4_mu1_mu2_one_slope	phylo(1 + x | p | species, tree = tree) in mu1 and mu2	gaussian	biv_gaussian()	phylo	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Phylo intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact q4 location fixture before support promotion
 qseries_phylo_q2_plus_q2_intercept	phylo(1 | pl | species, tree = tree) in mu1 and mu2 plus phylo(1 | ps | species, tree = tree) in sigma1 and sigma2	gaussian	biv_gaussian()	phylo	q2_plus_q2	mu1+mu2;sigma1+sigma2	intercept_only	two_labelled_structured_blocks	native_tmb	ML	ML	point_fit	extractor_ready	planned	planned	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Block-diagonal phylo q2 plus q2 evidence is not all-four q4 covariance evidence.	not_coverage_evidence	add same-target fixture and extractor checks
-qseries_phylo_q4_all_four_intercept	phylo(1 | p | species, tree = tree) in mu1, mu2, sigma1, and sigma2	gaussian	biv_gaussian()	phylo	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-parity-acceptance-gate.tsv	Phylo q4 point parity and extractor evidence do not accept interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, or HSquared AI-REML.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
-qseries_phylo_q4_all_four_one_slope_planned	phylo(1 + x | p | species, tree = tree) in all four endpoints	gaussian	biv_gaussian()	phylo	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured phylo all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact q4 all-four one-slope fixture for interval diagnostics before coverage, q4 REML, AI-REML, or public-support work.
+qseries_phylo_q4_all_four_intercept	phylo(1 | p | species, tree = tree) in mu1, mu2, sigma1, and sigma2	gaussian	biv_gaussian()	phylo	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-parity-acceptance-gate.tsv	Phylo q4 point parity and extractor evidence do not accept interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, or HSquared AI-REML.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
+qseries_phylo_q4_all_four_one_slope_planned	phylo(1 + x | p | species, tree = tree) in all four endpoints	gaussian	biv_gaussian()	phylo	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured phylo all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact q4 all-four one-slope fixture for interval diagnostics before coverage, q4 REML, AI-REML, or public-support work.
 qseries_phylo_q6_planned	phylo(1 + x + z | p | species, tree = tree) in mu1 and mu2	gaussian	biv_gaussian()	phylo	q6	mu1+mu2	multiple_slope	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured q6 remains planned; ordinary q6 diagnostics do not transfer to phylo.	no_denominator_until_fit	leave two-slope support for a later tranche
 qseries_phylo_q8_planned	broader phylo q8 variants beyond the exact shared-label all-four one-slope cell	gaussian	biv_gaussian()	phylo	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured phylo q8 variants remain planned and are not implied by ordinary q8, q4 phylo intercept parity, or the exact shared-label all-four one-slope runtime cell.	no_denominator_until_fit	design broader q8 cells only after shared-label phylo fixture parity and provider runtime gates land
 qseries_spatial_q1_mu_intercept	spatial(1 | site, coords = coords) in mu	gaussian	gaussian()	spatial	q1	mu	intercept_only	structured_scalar	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Spatial q1 mu parity is fixed-covariance fixture evidence; range-estimating, mesh, SPDE, intervals, and coverage remain separate.	fixture_not_coverage	provider contract banked; add calibrated interval and coverage denominator evidence before support promotion
@@ -29,10 +29,10 @@ qseries_spatial_q1_sigma_one_slope	spatial(1 + x | site, coords = coords) in sig
 qseries_spatial_q1_mu_sigma_one_slope	spatial(1 + x | site, coords = coords) in mu and sigma	gaussian	gaussian()	spatial	q1_plus_q1	mu+sigma	independent_one_slope	separate_structured_scalars	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-mu-sigma-slope-parity-fixture.tsv	Matched fixed-covariance spatial mu plus sigma one-slope support has native point-fit and extractor evidence for the exact four endpoint members plus one deterministic same-target fixture; it does not imply range-estimating spatial support, broad bridge support, labelled slope covariance, intervals, or coverage.	fixture_not_coverage	add calibrated interval diagnostics before coverage or public support promotion
 qseries_spatial_q2_mu1_mu2_intercept	spatial(1 | p | site, coords = coords) in mu1 and mu2	gaussian	biv_gaussian()	spatial	q2	mu1+mu2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-acceptance-gate.tsv	Spatial q2 acceptance is one fixed-covariance coordinate native/direct/bridge fixture; range-estimating spatial and broad q2 bridge support remain unsupported.	fixture_not_coverage	add range-estimating spatial design separately
 qseries_spatial_q2_mu1_mu2_one_slope	spatial(0 + x | p | site, coords = coords) in mu1 and mu2	gaussian	biv_gaussian()	spatial	q2	mu1+mu2	labelled_slope_covariance	labelled_structured_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-slope-parity-fixture.tsv	fixed-covariance spatial slope-only q2 location covariance has native point-fit and extractor evidence plus deterministic same-target fixture parity for the exact mu1:x plus mu2:x cell, and measured SR475 default bias+t SD-endpoint diagnostics now block row promotion because mu2:x coverage is 0.9411 with MCSE 0.0108 and the endpoint-only sidecar does not promote the correlation target; no range-estimating spatial route, intercept-plus-slope q4/q8, broad bridge support, interval reliability, coverage, REML, AI-REML, supported, or public support is promoted.	fixture_not_coverage	Use the admission sidecars for interval diagnostics: top up or redesign row-specific spatial q2 default bias+t coverage with retained denominator accounting, one-sided misses, correlation-target handling, and g=32 profile/Wald comparison before any status edit.
-qseries_spatial_q4_mu1_mu2_one_slope	spatial(1 + x | p | site, coords = coords) in mu1 and mu2	gaussian	biv_gaussian()	spatial	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Fixed-covariance spatial intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply range-estimating spatial support, broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact fixed-covariance q4 location fixture before support promotion
+qseries_spatial_q4_mu1_mu2_one_slope	spatial(1 + x | p | site, coords = coords) in mu1 and mu2	gaussian	biv_gaussian()	spatial	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Fixed-covariance spatial intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply range-estimating spatial support, broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact fixed-covariance q4 location fixture before support promotion
 qseries_spatial_q2_plus_q2_sigma_rejected	spatial(1 | ps | site, coords = coords) in sigma1 and sigma2	gaussian	biv_gaussian()	spatial	q2_plus_q2	sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	unsupported	ML	ML	unsupported	unsupported	unsupported	unsupported	unsupported	source	docs/dev-log/dashboard/structured-re-q2-plus-q2-sigma-rejection-contract.tsv	Spatial scale-side q2-plus sibling support has exact pre-optimization rejection evidence; do not infer it from balanced q4, q2 location fixtures, or fixed-covariance spatial point fits.	no_denominator_until_fit	design a supported scale-side route before parser-ready or point-fit promotion
-qseries_spatial_q4_all_four_intercept	spatial(1 | p | site, coords = coords) in all four endpoints	gaussian	biv_gaussian()	spatial	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Fixed-covariance spatial q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; range-estimating spatial support, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track fixed-covariance pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep range-estimating spatial, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
-qseries_spatial_q4_all_four_one_slope_planned	spatial(1 + x | p | site, coords = coords) in all four endpoints	gaussian	biv_gaussian()	spatial	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured fixed-covariance spatial all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; range-estimating spatial support, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact fixed-covariance q4 all-four one-slope fixture for interval diagnostics; keep range-estimating spatial, coverage, q4 REML, AI-REML, and public-support work separate.
+qseries_spatial_q4_all_four_intercept	spatial(1 | p | site, coords = coords) in all four endpoints	gaussian	biv_gaussian()	spatial	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Fixed-covariance spatial q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; range-estimating spatial support, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track fixed-covariance pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep range-estimating spatial, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
+qseries_spatial_q4_all_four_one_slope_planned	spatial(1 + x | p | site, coords = coords) in all four endpoints	gaussian	biv_gaussian()	spatial	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured fixed-covariance spatial all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; range-estimating spatial support, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact fixed-covariance q4 all-four one-slope fixture for interval diagnostics; keep range-estimating spatial, coverage, q4 REML, AI-REML, and public-support work separate.
 qseries_spatial_q6_planned	spatial(1 + x + z | p | site, coords = coords) in mu1 and mu2	gaussian	biv_gaussian()	spatial	q6	mu1+mu2	multiple_slope	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured spatial q6 remains planned.	no_denominator_until_fit	leave two-slope support for a later tranche
 qseries_spatial_q8_planned	broader spatial q8 variants beyond the exact fixed-covariance shared-label all-four one-slope cell	gaussian	biv_gaussian()	spatial	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured spatial q8 variants remain planned and are not implied by ordinary q8, constant spatial q4, or the exact fixed-covariance shared-label all-four one-slope runtime cell.	no_denominator_until_fit	design broader q8 cells only after fixed-covariance spatial fixture parity and animal/relmat runtime gates land
 qseries_animal_q1_mu_intercept	animal(1 | id, A = A) in mu	gaussian	gaussian()	animal	q1	mu	intercept_only	structured_scalar	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Animal q1 mu parity is one A-matrix fixture; pedigree, Ainv, sparse, intervals, and coverage remain separate.	fixture_not_coverage	provider contract banked; add calibrated interval and coverage denominator evidence before support promotion
@@ -43,10 +43,10 @@ qseries_animal_q1_sigma_one_slope	animal(1 + x | id, A = A) in sigma	gaussian	ga
 qseries_animal_q1_mu_sigma_one_slope	animal(1 + x | id, A = A) in mu and sigma	gaussian	gaussian()	animal	q1_plus_q1	mu+sigma	independent_one_slope	separate_structured_scalars	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-mu-sigma-slope-parity-fixture.tsv	Matched animal A-matrix mu plus sigma one-slope support has native point-fit and extractor evidence for the exact four endpoint members plus one deterministic same-target fixture; it does not imply pedigree/Ainv bridge marshalling, broad bridge support, labelled slope covariance, intervals, or coverage.	fixture_not_coverage	add calibrated interval diagnostics before coverage or public support promotion
 qseries_animal_q2_mu1_mu2_intercept	animal(1 | p | id, A = A) in mu1 and mu2	gaussian	biv_gaussian()	animal	q2	mu1+mu2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-acceptance-gate.tsv	Animal q2 acceptance is one complete-response exact-Gaussian ML A-matrix native/direct/bridge fixture; q2 REML and broad bridge support remain unsupported.	fixture_not_coverage	add pedigree and Ainv marshalling evidence separately
 qseries_animal_q2_mu1_mu2_one_slope	animal(0 + x | p | id, A/Ainv = A/Q) in mu1 and mu2	gaussian	biv_gaussian()	animal	q2	mu1+mu2	labelled_slope_covariance	labelled_structured_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-slope-parity-fixture.tsv	Animal A/Ainv slope-only q2 location covariance has native point-fit and extractor evidence plus deterministic A-matrix same-target fixture parity for the exact mu1:x plus mu2:x cell, and measured SR475 default bias+t SD-endpoint diagnostics now block row promotion because mu2:x is borderline at 0.9474 with MCSE 0.0102 and the correlation target has no coverage-grid row after the denominator holdout; no pedigree/Ainv bridge marshalling, intercept-plus-slope q4/q8, broad bridge support, interval reliability, coverage, REML, AI-REML, supported, or public support is promoted.	fixture_not_coverage	Use the admission sidecars for interval diagnostics: reconcile the animal q2 correlation endpoint denominator holdout, then rerun retained-denominator default bias+t coverage for the exact row with one-sided misses before any status edit.
-qseries_animal_q4_mu1_mu2_one_slope	animal(1 + x | p | id, A = A) in mu1 and mu2	gaussian	biv_gaussian()	animal	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Animal A-matrix intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply pedigree/Ainv bridge marshalling, broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact A-matrix q4 location fixture before support promotion
+qseries_animal_q4_mu1_mu2_one_slope	animal(1 + x | p | id, A = A) in mu1 and mu2	gaussian	biv_gaussian()	animal	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Animal A-matrix intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; it does not imply pedigree/Ainv bridge marshalling, broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact A-matrix q4 location fixture before support promotion
 qseries_animal_q2_plus_q2_sigma_rejected	animal(1 | ps | id, A = A) in sigma1 and sigma2	gaussian	biv_gaussian()	animal	q2_plus_q2	sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	unsupported	ML	ML	unsupported	unsupported	unsupported	unsupported	unsupported	source	docs/dev-log/dashboard/structured-re-q2-plus-q2-sigma-rejection-contract.tsv	Animal scale-side q2-plus sibling support has exact pre-optimization rejection evidence; do not infer it from q2 location fixtures, q4 point rows, or A-matrix all-four cells.	no_denominator_until_fit	design a supported scale-side route before parser-ready or point-fit promotion
-qseries_animal_q4_all_four_intercept	animal(1 | p | id, A = A) in all four endpoints	gaussian	biv_gaussian()	animal	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Animal A-matrix q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; pedigree/Ainv bridge marshalling, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to keep finite Wald/profile but nonfinite bootstrap behavior out of denominator accounting and coverage-grid design; keep pedigree/Ainv, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
-qseries_animal_q4_all_four_one_slope_planned	animal(1 + x | p | id, A = A) in all four endpoints	gaussian	biv_gaussian()	animal	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured animal A-matrix all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; pedigree/Ainv bridge marshalling, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact A-matrix q4 all-four one-slope fixture for interval diagnostics; keep pedigree/Ainv, coverage, q4 REML, AI-REML, and public-support work separate.
+qseries_animal_q4_all_four_intercept	animal(1 | p | id, A = A) in all four endpoints	gaussian	biv_gaussian()	animal	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Animal A-matrix q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; pedigree/Ainv bridge marshalling, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to keep finite Wald/profile but nonfinite bootstrap behavior out of denominator accounting and coverage-grid design; keep pedigree/Ainv, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
+qseries_animal_q4_all_four_one_slope_planned	animal(1 + x | p | id, A = A) in all four endpoints	gaussian	biv_gaussian()	animal	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured animal A-matrix all-four one-slope cell has native ML point-fit, extractor evidence, and deterministic same-target fixture parity for the exact eight-member endpoint map; pedigree/Ainv bridge marshalling, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact A-matrix q4 all-four one-slope fixture for interval diagnostics; keep pedigree/Ainv, coverage, q4 REML, AI-REML, and public-support work separate.
 qseries_animal_q6_planned	animal(1 + x + z | p | id, A = A) in mu1 and mu2	gaussian	biv_gaussian()	animal	q6	mu1+mu2	multiple_slope	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured animal q6 remains planned.	no_denominator_until_fit	leave two-slope support for a later tranche
 qseries_animal_q8_planned	broader animal q8 variants beyond the exact A-matrix shared-label all-four one-slope cell	gaussian	biv_gaussian()	animal	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured animal q8 variants remain planned and are not implied by ordinary q8, constant animal q4, or the exact A-matrix shared-label all-four one-slope runtime cell.	no_denominator_until_fit	design broader q8 cells only after animal fixture parity and larger-provider boundaries land
 qseries_relmat_q1_mu_intercept	relmat(1 | id, K = K) in mu	gaussian	gaussian()	relmat	q1	mu	intercept_only	structured_scalar	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Relmat q1 mu parity is one K-matrix fixture; Q precision, intervals, REML, and coverage remain separate.	fixture_not_coverage	provider contract banked; add calibrated interval and coverage denominator evidence before support promotion
@@ -57,29 +57,29 @@ qseries_relmat_q1_sigma_one_slope	relmat(1 + x | id, K = K) in sigma	gaussian	ga
 qseries_relmat_q1_mu_sigma_one_slope	relmat(1 + x | id, K/Q = K/Q) in mu and sigma	gaussian	gaussian()	relmat	q1_plus_q1	mu+sigma	independent_one_slope	separate_structured_scalars	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-mu-sigma-slope-parity-fixture.tsv	Matched relmat K/Q mu plus sigma one-slope support has native point-fit and extractor evidence for the exact four endpoint members plus one deterministic K-matrix same-target fixture; it does not imply Q bridge marshalling, broad bridge support, labelled slope covariance, intervals, or coverage.	fixture_not_coverage	add calibrated interval diagnostics before coverage or public support promotion
 qseries_relmat_q2_mu1_mu2_intercept	relmat(1 | p | id, K = K) in mu1 and mu2	gaussian	biv_gaussian()	relmat	q2	mu1+mu2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q2-acceptance-gate.tsv	Relmat q2 acceptance is one complete-response exact-Gaussian ML K-matrix native/direct/bridge fixture; Q precision, q2 REML, and broad bridge support remain unsupported.	fixture_not_coverage	add precision-source marshalling evidence separately
 qseries_relmat_q2_mu1_mu2_one_slope	relmat(0 + x | p | id, K/Q = K/Q) in mu1 and mu2	gaussian	biv_gaussian()	relmat	q2	mu1+mu2	labelled_slope_covariance	labelled_structured_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	inference_ready	inference_ready	source	docs/dev-log/dashboard/structured-re-q2-slope-parity-fixture.tsv	Inference-ready: the DEFAULT confint(method='wald') now applies the simulation-calibrated bias+t correction to this relmat K/Q location-axis structured-RE slope-only SD; engine-validated nominal-in-total coverage at g=8 (SR475) plus a second grid (near-boundary conservative, extra g, one-sided rates), on native K/Q point-fit/extractor evidence plus deterministic same-target fixture parity for the exact slope-only mu1:x+mu2:x cell; interval reliability rung passed. supported is WITHHELD: residual right-tail miss asymmetry and g-dependent under-correction (g=12 ~0.93); the shift is ~2x the leading-order REML SD term, simulation-calibrated per class. Q bridge marshalling, REML, AI-REML, intercept-plus-slope q4/q8, and broad bridge support remain separate gates.	fixture_not_coverage	add calibrated interval diagnostics before support promotion
-qseries_relmat_q4_mu1_mu2_one_slope	relmat(1 + x | p | id, K = K) in mu1 and mu2	gaussian	biv_gaussian()	relmat	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Relmat K-matrix intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; Q precision marshalling remains separate, and this does not imply broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact K-matrix q4 location fixture before support promotion
+qseries_relmat_q4_mu1_mu2_one_slope	relmat(1 + x | p | id, K = K) in mu1 and mu2	gaussian	biv_gaussian()	relmat	q4	mu1+mu2	labelled_slope_covariance	labelled_structured_location_intercept_slope_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-location-slope-parity-fixture.tsv	Relmat K-matrix intercept-plus-slope structured location q4 has native ML point-fit/extractor evidence plus same-target fixture parity for the exact four-member q4 location endpoint map; Q precision marshalling remains separate, and this does not imply broad bridge support, partial location-scale support, interval reliability, coverage, q4 REML, AI-REML, public support, or broader q8 support.	fixture_not_coverage	add calibrated interval diagnostics for the exact K-matrix q4 location fixture before support promotion
 qseries_relmat_q2_plus_q2_sigma_rejected	relmat(1 | ps | id, K = K) in sigma1 and sigma2	gaussian	biv_gaussian()	relmat	q2_plus_q2	sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	unsupported	ML	ML	unsupported	unsupported	unsupported	unsupported	unsupported	source	docs/dev-log/dashboard/structured-re-q2-plus-q2-sigma-rejection-contract.tsv	Relmat scale-side q2-plus sibling support has exact pre-optimization rejection evidence; do not infer it from q2 location fixtures, q4 point rows, K/Q parity, or K-matrix all-four cells.	no_denominator_until_fit	design a supported scale-side route before parser-ready or point-fit promotion
-qseries_relmat_q4_all_four_intercept	relmat(1 | p | id, K = K) in all four endpoints	gaussian	biv_gaussian()	relmat	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Relmat K-matrix q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; Q bridge marshalling, Q precision marshalling, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track K-matrix pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep Q bridge marshalling, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
-qseries_relmat_q4_all_four_one_slope_planned	relmat(1 + x | p | id, K = K) in all four endpoints	gaussian	biv_gaussian()	relmat	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	planned	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured relmat K-matrix all-four one-slope cell has native ML point-fit, extractor evidence, runtime K/Q same-target parity, and deterministic same-target fixture parity for the exact eight-member endpoint map; Q bridge marshalling, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact K-matrix q4 all-four one-slope fixture for interval diagnostics; keep Q bridge marshalling, coverage, q4 REML, AI-REML, and public-support work separate.
+qseries_relmat_q4_all_four_intercept	relmat(1 | p | id, K = K) in all four endpoints	gaussian	biv_gaussian()	relmat	q4	mu1+mu2+sigma1+sigma2	intercept_only	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-intercept-parity-fixture.tsv	Relmat K-matrix q4 all-four intercept has native ML point-fit/extractor evidence plus deterministic same-target fixture parity for the exact four-endpoint q4 map; Q bridge marshalling, Q precision marshalling, broad bridge support, interval reliability, interval coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, AI-REML, public support, and coverage remain planned.	fixture_not_coverage	Use structured-re-q4-intercept-hessian-bootstrap-diagnostic.tsv to track K-matrix pdHess/indefinite-covariance geometry before denominator accounting and coverage-grid design; keep Q bridge marshalling, coverage, q4 REML, native-TMB q4 REML, q4 AI-REML, and HSquared AI-REML separate.
+qseries_relmat_q4_all_four_one_slope_planned	relmat(1 + x | p | id, K = K) in all four endpoints	gaussian	biv_gaussian()	relmat	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	native_direct_bridge_fixture	ML	ML	point_fit	extractor_ready	fixture_parity	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-slope-parity-fixture.tsv	Structured relmat K-matrix all-four one-slope cell has native ML point-fit, extractor evidence, runtime K/Q same-target parity, and deterministic same-target fixture parity for the exact eight-member endpoint map; Q bridge marshalling, broad bridge support, interval reliability, coverage, q4 REML, AI-REML, and public support remain planned.	fixture_not_coverage	Use the exact K-matrix q4 all-four one-slope fixture for interval diagnostics; keep Q bridge marshalling, coverage, q4 REML, AI-REML, and public-support work separate.
 qseries_relmat_q6_planned	relmat(1 + x + z | p | id, K = K) in mu1 and mu2	gaussian	biv_gaussian()	relmat	q6	mu1+mu2	multiple_slope	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Structured relmat q6 remains planned.	no_denominator_until_fit	leave two-slope support for a later tranche
 qseries_relmat_q8_planned	broader relmat q8 variants beyond the exact K/Q shared-label all-four one-slope cell	gaussian	biv_gaussian()	relmat	q8	mu1+mu2+sigma1+sigma2	labelled_slope_covariance	labelled_structured_endpoint_covariance	planned	ML	ML	planned	planned	planned	planned	planned	source	docs/design/59-structural-slope-and-non-gaussian-map.md	Broader structured relmat q8 variants remain planned and are not implied by ordinary q8, constant relmat q4, or the exact K/Q shared-label all-four one-slope runtime cell.	no_denominator_until_fit	design broader q8 cells only after relmat fixture parity and Q bridge boundaries land
 qseries_phylo_interaction_q1_mu	phylo_interaction(1 | pair, tree1 = tree1, tree2 = tree2) in mu	gaussian	gaussian()	phylo_interaction	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML	point_fit	extractor_ready	unsupported	planned	planned	source	tests/testthat/test-structured-effects.R	Phylo interaction is a q1 pair-level field, not a q2 or q4 endpoint covariance family.	not_coverage_evidence	provider contract banked; keep q2/q4 endpoint covariance out of scope
-qseries_phylo_interaction_poisson_q1_mu	phylo_interaction(1 | pair, tree1 = tree1, tree2 = tree2) in mu	non_gaussian	poisson()	phylo_interaction	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-phylo-interaction.R	Poisson phylo_interaction q1 mean point support is a single pair-level Kronecker field; it is not bridge, REML, AI-REML, interval, coverage, q2, q4, slope, additive partner-main, binary incidence, or public-support evidence.	not_coverage_evidence	keep non-Gaussian phylo_interaction expansion cell-specific; add count fixture and recovery evidence before promotion
-qseries_phylo_interaction_nbinom2_q1_mu	phylo_interaction(1 | pair, tree1 = tree1, tree2 = tree2) in mu	non_gaussian	nbinom2()	phylo_interaction	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-phylo-interaction.R	NB2 phylo_interaction q1 mean point support is a single pair-level Kronecker field; it is not bridge, REML, AI-REML, interval, coverage, q2, q4, slope, additive partner-main, binary incidence, structured sigma, or public-support evidence.	not_coverage_evidence	keep overdispersion-aware phylo_interaction recovery separate from Poisson; add count fixture and recovery evidence before promotion
-qseries_phylo_poisson_q1_mu_intercept	phylo(1 | species, tree = tree) in mu	non_gaussian	poisson()	phylo	q1	mu	intercept_only	structured_scalar	native_direct_bridge_fixture	ML	ML_Laplace	point_fit	extractor_ready	fixture_parity	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Poisson phylo q1 bridge evidence is approximate ML/Laplace fixture evidence only; NB2, q2, q4, REML, intervals, and broad bridge support remain separate.	fixture_not_coverage	add NB2 and calibrated count diagnostics separately
-qseries_phylo_nbinom2_q1_mu_intercept	phylo(1 | species, tree = tree) in mu	non_gaussian	nbinom2()	phylo	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	NB2 phylo q1 mean point support is not bridge, REML, interval, q2, or q4 support.	not_coverage_evidence	add NB2 fixture evidence before bridge wording
-qseries_spatial_poisson_q1_mu_intercept	spatial(1 | site, coords = coords) in mu	non_gaussian	poisson()	spatial	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Poisson spatial q1 mean point support is not bridge, REML, interval, range-estimating, q2, or q4 support.	not_coverage_evidence	add fixed-covariance count fixture evidence
-qseries_spatial_nbinom2_q1_mu_intercept	spatial(1 | site, coords = coords) in mu	non_gaussian	nbinom2()	spatial	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	NB2 spatial q1 mean point support is not bridge, REML, interval, range-estimating, q2, or q4 support.	not_coverage_evidence	add fixed-covariance count fixture evidence
-qseries_animal_poisson_q1_mu_intercept	animal(1 | id, A = A) in mu	non_gaussian	poisson()	animal	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Poisson animal q1 mean point support is not bridge, REML, interval, q2, or q4 support.	not_coverage_evidence	add A-matrix count fixture evidence
-qseries_animal_nbinom2_q1_mu_intercept	animal(1 | id, A = A) in mu	non_gaussian	nbinom2()	animal	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	NB2 animal q1 mean point support is not bridge, REML, interval, q2, or q4 support.	not_coverage_evidence	add A-matrix count fixture evidence
-qseries_relmat_poisson_q1_mu_intercept	relmat(1 | id, K = K) in mu	non_gaussian	poisson()	relmat	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	Poisson relmat q1 mean point support is not bridge, REML, interval, q2, or q4 support.	not_coverage_evidence	add K-matrix count fixture evidence
-qseries_relmat_nbinom2_q1_mu_intercept	relmat(1 | id, K = K) in mu	non_gaussian	nbinom2()	relmat	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/dashboard/structured-re-balance-matrix.tsv	NB2 relmat q1 mean point support is not bridge, REML, interval, q2, or q4 support.	not_coverage_evidence	add K-matrix count fixture evidence
+qseries_phylo_interaction_poisson_q1_mu	phylo_interaction(1 | pair, tree1 = tree1, tree2 = tree2) in mu	non_gaussian	poisson()	phylo_interaction	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-phylo-interaction.R	Poisson phylo_interaction q1 mean point support is a single pair-level Kronecker field with local recovery-only sidecar evidence: 80/80 fit_ok, 0 pdHess false, 1/80 near-zero structured-SD estimate, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, slope, additive partner-main, binary incidence, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep non-Gaussian phylo_interaction expansion cell-specific; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_phylo_interaction_nbinom2_q1_mu	phylo_interaction(1 | pair, tree1 = tree1, tree2 = tree2) in mu	non_gaussian	nbinom2()	phylo_interaction	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-phylo-interaction.R	NB2 phylo_interaction q1 mean point support is a single pair-level Kronecker field with local recovery-only sidecar evidence: 80/80 fit_ok, 0 pdHess false, 4/80 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, slope, additive partner-main, binary incidence, structured sigma, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep overdispersion-aware phylo_interaction recovery separate from Poisson; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_phylo_poisson_q1_mu_intercept	phylo(1 | species, tree = tree) in mu	non_gaussian	poisson()	phylo	q1	mu	intercept_only	structured_scalar	native_direct_bridge_fixture	ML	ML_Laplace	point_fit	extractor_ready	fixture_parity	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Cluster-confirmed recovery-only evidence from Rorqual SLURM job 14897050: 320/320 stronger-denominator count-intercept fits, zero pdHess false, zero near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. This does not promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, q2/q4 covariance, broad bridge support, count sigma, zero-inflation, or public support.	fixture_not_coverage	keep as recovery-only; design count-specific interval and coverage route before any inference claim
+qseries_phylo_nbinom2_q1_mu_intercept	phylo(1 | species, tree = tree) in mu	non_gaussian	nbinom2()	phylo	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Cluster-confirmed recovery-only evidence from Rorqual SLURM job 14897050: 320/320 stronger-denominator count-intercept fits, zero pdHess false, zero near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. This does not promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, q2/q4 covariance, broad bridge support, count sigma, zero-inflation, or public support.	not_coverage_evidence	keep as recovery-only; design count-specific interval and coverage route before any inference claim
+qseries_spatial_poisson_q1_mu_intercept	spatial(1 | site, coords = coords) in mu	non_gaussian	poisson()	spatial	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Poisson fixed-covariance spatial q1 mean point support has local recovery-only sidecar evidence: 320/320 fit_ok, 0 pdHess false, 63/320 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, range-estimating, q2, q4, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep as local fixed-covariance recovery-only; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_spatial_nbinom2_q1_mu_intercept	spatial(1 | site, coords = coords) in mu	non_gaussian	nbinom2()	spatial	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-topup-recovery-rorqual	Cluster-confirmed recovery-only evidence from Rorqual SLURM job 14897050: 320/320 stronger-denominator count-intercept fits, zero pdHess false, zero near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. This does not promote interval_status, coverage_status, inference_ready, supported, REML, AI-REML, q2/q4 covariance, broad bridge support, count sigma, zero-inflation, range-estimating spatial support, or public support.	not_coverage_evidence	keep as fixed-covariance recovery-only; design count-specific interval and coverage route before any inference claim
+qseries_animal_poisson_q1_mu_intercept	animal(1 | id, A = A) in mu	non_gaussian	poisson()	animal	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Poisson animal A-matrix q1 mean point support has local recovery-only sidecar evidence: 320/320 fit_ok, 0 pdHess false, 25/320 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep as local A-matrix recovery-only; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_animal_nbinom2_q1_mu_intercept	animal(1 | id, A = A) in mu	non_gaussian	nbinom2()	animal	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	NB2 animal A-matrix q1 mean point support has local recovery-only sidecar evidence: 320/320 fit_ok, 0 pdHess false, 53/320 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep as local A-matrix recovery-only; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_relmat_poisson_q1_mu_intercept	relmat(1 | id, K = K) in mu	non_gaussian	poisson()	relmat	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	Poisson relmat K-matrix q1 mean point support has local recovery-only sidecar evidence: 320/320 fit_ok, 0 pdHess false, 23/320 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep as local K-matrix recovery-only; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
+qseries_relmat_nbinom2_q1_mu_intercept	relmat(1 | id, K = K) in mu	non_gaussian	nbinom2()	relmat	q1	mu	intercept_only	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	docs/dev-log/simulation-artifacts/2026-06-29-count-intercept-recovery-grid-local	NB2 relmat K-matrix q1 mean point support has local recovery-only sidecar evidence: 320/320 fit_ok, 0 pdHess false, 42/320 near-zero structured-SD estimates, and bias/RMSE/MCSE diagnostics reported. It is not bridge, REML, AI-REML, interval, coverage, q2, q4, inference_ready, supported, or public-support evidence.	not_coverage_evidence	keep as local K-matrix recovery-only; top up or confirm recovery on the primary cluster and design a count-specific interval route before any interval or coverage claim
 qseries_phylo_direct_sd_univariate	sd_phylo() or sd(species) direct target	gaussian	gaussian()	phylo	q1	sd_mu_or_sd_sigma	not_applicable	direct_sd_target	native_tmb	ML	ML	point_fit	extractor_ready	unsupported	interval_feasible	planned	source	docs/dev-log/dashboard/phylo-profile-loglik-status.tsv	Direct univariate SD profile feasibility does not imply derived correlation interval support or q4 coverage.	profile_denominator_required	keep direct SD and derived correlation interval rows separate
 qseries_phylo_direct_sd_bivariate	sd_phylo1(), sd_phylo2(), sd_sigma1(), and sd_sigma2() direct targets	gaussian	biv_gaussian()	phylo	q4	sd_mu1+sd_mu2+sd_sigma1+sd_sigma2	not_applicable	direct_sd_target	native_tmb	ML	ML	point_fit	extractor_ready	unsupported	diagnostic_only	planned	source	docs/dev-log/dashboard/structured-re-q4-profile-target-bridge-map.tsv	Bivariate direct SD target visibility is not q4 interval reliability, derived-correlation interval support, or coverage evidence.	finite_interval_denominator_required	keep direct SD profile smoke separate from coverage acceptance
 qseries_phylo_poisson_q1_mu_one_slope	phylo(1 + x | species, tree = tree) in mu	non_gaussian	poisson()	phylo	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	Poisson phylo tree structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 qseries_phylo_nbinom2_q1_mu_one_slope	phylo(1 + x | species, tree = tree) in mu	non_gaussian	nbinom2()	phylo	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	NB2 phylo tree structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 qseries_spatial_poisson_q1_mu_one_slope	spatial(1 + x | site, coords = coords) in mu	non_gaussian	poisson()	spatial	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	Poisson fixed-covariance spatial coordinates structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
-qseries_spatial_nbinom2_q1_mu_one_slope	spatial(1 + x | site, coords = coords) in mu	non_gaussian	nbinom2()	spatial	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	NB2 fixed-covariance spatial coordinates structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
+qseries_spatial_nbinom2_q1_mu_one_slope	spatial(1 + x | site, coords = coords) in mu	non_gaussian	nbinom2()	spatial	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	NB2 fixed-covariance spatial coordinates structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged, 2/80 pdHess false, and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only with a Hessian caveat retained: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, inference_ready, supported, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked with 2/80 pdHess false retained; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 qseries_animal_poisson_q1_mu_one_slope	animal(1 + x | id, A/Ainv = A/Ainv) in mu	non_gaussian	poisson()	animal	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	Poisson animal A/Ainv relatedness structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 qseries_animal_nbinom2_q1_mu_one_slope	animal(1 + x | id, A/Ainv = A/Ainv) in mu	non_gaussian	nbinom2()	animal	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	NB2 animal A/Ainv relatedness structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
 qseries_relmat_poisson_q1_mu_one_slope	relmat(1 + x | id, K/Q = K/Q) in mu	non_gaussian	poisson()	relmat	q1	mu	independent_one_slope	structured_scalar	native_tmb	ML	ML_Laplace	point_fit	extractor_ready	unsupported	unsupported	planned	source	tests/testthat/test-count-structured-mu.R	Poisson relmat K/Q relatedness structured mu one-slope support has native TMB ML/Laplace point-fit and extractor evidence for the exact unlabelled intercept-plus-one-slope count cell, plus an 80-rep local recovery grid with all fits converged and g=8 structured-SD downward shrinkage recorded. This is recovery evidence only: zero-inflated structured effects, labelled count covariance, multiple structured count slopes, structured count sigma or shape effects, simultaneous structured types, bridge support, REML, AI-REML, intervals, coverage, q2, q4, and public-support promotion remain planned or unsupported.	not_coverage_evidence	Recovery grid banked; design count-specific interval/coverage evidence and larger MCSE-calibrated recovery/interval grids before changing interval, coverage, bridge, q2, q4, or public-support wording.
diff --git a/docs/dev-log/dashboard/structured-re-q2-slope-spatial-animal-admission-audit.tsv b/docs/dev-log/dashboard/structured-re-q2-slope-spatial-animal-admission-audit.tsv
index e897a0a9..1caf5f7a 100644
--- a/docs/dev-log/dashboard/structured-re-q2-slope-spatial-animal-admission-audit.tsv
+++ b/docs/dev-log/dashboard/structured-re-q2-slope-spatial-animal-admission-audit.tsv
@@ -1,3 +1,3 @@
 audit_id	cell_id	provider	endpoint_scope	source_raw_coverage_artifact	source_bias_correction_evidence	source_denominator_rule	raw_coverage_summary	bias_correction_status	denominator_status	admission_status	widget_state	linked_interval_status	linked_coverage_status	promotion_decision	evidence_url	claim_boundary	next_gate
-q2_slope_admission_spatial_mu1_mu2_one_slope	qseries_spatial_q2_mu1_mu2_one_slope	spatial	mu1:x;mu2:x;mu1:x+mu2:x	docs/dev-log/dashboard/structured-re-slope-coverage-results.tsv	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	docs/dev-log/dashboard/structured-re-q2-slope-replicated-denominator-rule.tsv	raw SR475 under-covers: mu1:x Wald 0.8968 MCSE 0.014; mu2:x Wald 0.8779 MCSE 0.015; cor Wald 0.8905 MCSE 0.0143	default bias+t SD endpoints were measured at SR475: mu1:x 0.9663 MCSE 0.0083, mu2:x 0.9411 MCSE 0.0108 with 24 upper-tail misses; endpoint-only evidence blocks promotion until top-up/sign-off and correlation-target handling.	all three targets eligible_for_pregrid_with_retention; coverage_evaluable remains FALSE in denominator-rule sidecar	row_specific_bias_t_revalidation_required	calibration_required	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	Spatial q2 mu1+mu2 one-slope has native point-fit/fixture evidence plus raw SR475 and default bias+t SD-endpoint diagnostics, but it is not inference_ready: raw Wald/profile coverage under-covers, mu2:x bias+t remains below nominal with MCSE above the 0.01 gate, and the endpoint-only sidecar does not promote the correlation target; this does not promote range-estimating spatial support, q4/q8, REML, AI-REML, bridge, supported, or public support.	Top up or redesign row-specific spatial q2 default bias+t coverage with retained denominator accounting, one-sided misses, correlation-target handling, and g=32 profile/Wald comparison; require Fisher/Rose sign-off before any status edit.
-q2_slope_admission_animal_mu1_mu2_one_slope	qseries_animal_q2_mu1_mu2_one_slope	animal	mu1:x;mu2:x;mu1:x+mu2:x	docs/dev-log/dashboard/structured-re-slope-coverage-results.tsv	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	docs/dev-log/dashboard/structured-re-q2-slope-replicated-denominator-rule.tsv	raw SR475 under-covers for SD endpoints: mu1:x Wald 0.9053 MCSE 0.0134; mu2:x Wald 0.88 MCSE 0.0149; correlation target absent from coverage grid	default bias+t SD endpoints were measured at SR475: mu1:x 0.9663 MCSE 0.0083, mu2:x 0.9474 MCSE 0.0102; correlation target has no coverage-grid row after denominator holdout, so the row remains blocked.	mu1:x and mu2:x eligible_for_pregrid_with_retention; mu1:x+mu2:x visible_holdout_until_smoke_profile_reconciled; coverage_evaluable remains FALSE	animal_correlation_denominator_holdout	admission_blocked	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	Animal q2 mu1+mu2 one-slope has native point-fit/fixture evidence plus raw SR475 and default bias+t SD-endpoint diagnostics, but it is not inference_ready: raw coverage under-covers, mu2:x bias+t remains borderline with MCSE above the 0.01 gate, the correlation target has no coverage-grid row, and the correlation target remains a denominator holdout after smoke endpoint-profile failure; this does not promote pedigree/Ainv bridge marshalling, q4/q8, REML, AI-REML, bridge, supported, or public support.	Reconcile animal q2 correlation endpoint-profile admission, then run retained-denominator default bias+t coverage for the exact row with one-sided misses and Fisher/Rose sign-off before any status edit.
+q2_slope_admission_spatial_mu1_mu2_one_slope	qseries_spatial_q2_mu1_mu2_one_slope	spatial	mu1:x;mu2:x;mu1:x+mu2:x	docs/dev-log/dashboard/structured-re-slope-coverage-results.tsv	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	docs/dev-log/dashboard/structured-re-q2-slope-replicated-denominator-rule.tsv	raw SR475 under-covers: mu1:x Wald 0.8968 MCSE 0.014; mu2:x Wald 0.8779 MCSE 0.015; cor Wald 0.8905 MCSE 0.0143	default bias+t SD endpoints were measured at SR475: mu1:x 0.9663 MCSE 0.0083, mu2:x 0.9411 MCSE 0.0108 with 24 upper-tail misses; endpoint-only evidence blocks promotion until top-up/sign-off and correlation-target handling.	all three targets eligible_for_pregrid_with_retention; coverage_evaluable remains FALSE in denominator-rule sidecar	row_specific_bias_t_revalidation_required	calibration_required	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	Spatial q2 mu1+mu2 one-slope has native point-fit/fixture evidence plus raw SR475 and default bias+t SD-endpoint diagnostics, but it is not inference_ready: raw Wald/profile coverage under-covers, mu2:x bias+t remains below nominal with MCSE above the 0.01 gate, and the endpoint-only sidecar does not promote the correlation target; this does not promote range-estimating spatial support, q4/q8, REML, AI-REML, bridge, supported, or public support.	Use structured-re-q2-slope-bias-t-topup-runner-contract.tsv to run shards 1-2 for row-specific spatial q2 default bias+t coverage top-up with retained denominator accounting, one-sided misses, correlation-target handling, and g=32 profile/Wald comparison; require Fisher/Rose sign-off before any status edit.
+q2_slope_admission_animal_mu1_mu2_one_slope	qseries_animal_q2_mu1_mu2_one_slope	animal	mu1:x;mu2:x;mu1:x+mu2:x	docs/dev-log/dashboard/structured-re-slope-coverage-results.tsv	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	docs/dev-log/dashboard/structured-re-q2-slope-replicated-denominator-rule.tsv	raw SR475 under-covers for SD endpoints: mu1:x Wald 0.9053 MCSE 0.0134; mu2:x Wald 0.88 MCSE 0.0149; correlation target absent from coverage grid	default bias+t SD endpoints were measured at SR475: mu1:x 0.9663 MCSE 0.0083, mu2:x 0.9474 MCSE 0.0102; correlation target has no coverage-grid row after denominator holdout, so the row remains blocked.	mu1:x and mu2:x eligible_for_pregrid_with_retention; mu1:x+mu2:x visible_holdout_until_smoke_profile_reconciled; coverage_evaluable remains FALSE	animal_correlation_denominator_holdout	admission_blocked	planned	planned	do_not_promote	docs/dev-log/dashboard/structured-re-q2-slope-bias-t-coverage-evidence.tsv	Animal q2 mu1+mu2 one-slope has native point-fit/fixture evidence plus raw SR475 and default bias+t SD-endpoint diagnostics, but it is not inference_ready: raw coverage under-covers, mu2:x bias+t remains borderline with MCSE above the 0.01 gate, the correlation target has no coverage-grid row, and the correlation target remains a denominator holdout after smoke endpoint-profile failure; this does not promote pedigree/Ainv bridge marshalling, q4/q8, REML, AI-REML, bridge, supported, or public support.	Reconcile animal q2 correlation endpoint-profile admission, then use structured-re-q2-slope-bias-t-topup-runner-contract.tsv to run shards 3-4 for retained-denominator default bias+t SD-endpoint coverage with one-sided misses and Fisher/Rose sign-off before any status edit.
diff --git a/docs/dev-log/dashboard/version.txt b/docs/dev-log/dashboard/version.txt
index 9312a7e5..29f0ec93 100644
--- a/docs/dev-log/dashboard/version.txt
+++ b/docs/dev-log/dashboard/version.txt
@@ -1 +1 @@
-r80
+r96
diff --git a/tests/testthat/test-phase18-animal-mu-slope.R b/tests/testthat/test-phase18-animal-mu-slope.R
index 1fdd581f..b51d4332 100644
--- a/tests/testthat/test-phase18-animal-mu-slope.R
+++ b/tests/testthat/test-phase18-animal-mu-slope.R
@@ -69,6 +69,42 @@ test_that("Phase 18 animal mu slope DGP is seeded and self-describing", {
   )
 })

+test_that("Phase 18 animal mu slope boundary profiles use zero lower endpoint", {
+  source_phase18_animal_mu_slope()
+
+  conditions <- phase18_animal_mu_slope_conditions(
+    n_id = 8L,
+    n_each = 7L
+  )[1L, , drop = FALSE]
+  dat <- phase18_dgp_animal_mu_slope_cell(
+    conditions,
+    seed = 791004,
+    cell_id = "animal_mu_slope_boundary_profile",
+    replicate = 4L
+  )
+  fit <- phase18_fit_animal_mu_slope(dat, conditions)
+  parm <- "sd:mu:animal(0 + x | id)"
+
+  ci <- suppressWarnings(stats::confint(
+    fit,
+    parm = parm,
+    method = "profile",
+    profile_engine = "endpoint",
+    profile_endpoint_max_eval = 80L
+  ))
+
+  expect_equal(fit$opt$convergence, 0L)
+  expect_true(fit$sdr$pdHess)
+  expect_equal(ci$parm, parm)
+  expect_equal(ci$profile.engine, "endpoint")
+  expect_equal(ci$conf.status, "profile")
+  expect_true(ci$profile.boundary)
+  expect_equal(ci$profile.message, "near_sd_boundary")
+  expect_equal(ci$lower, 0)
+  expect_true(is.finite(ci$upper))
+  expect_gt(ci$upper, attr(dat, "truth")$sd[[sub("^sd:mu:", "", parm)]])
+})
+
 test_that("Phase 18 animal mu slope smoke runner summarises output", {
   source_phase18_animal_mu_slope()

diff --git a/tests/testthat/test-profile-targets.R b/tests/testthat/test-profile-targets.R
index e53b5454..ec2ef7c4 100644
--- a/tests/testthat/test-profile-targets.R
+++ b/tests/testthat/test-profile-targets.R
@@ -1957,11 +1957,60 @@ test_that("endpoint initial steps use curvature when available", {
     cutoff = cutoff,
     curvature_se = NA_real_
   )
+  capped <- drmTMB:::profile_endpoint_initial_step(
+    theta_hat = -12,
+    direction = -1,
+    cutoff = cutoff,
+    curvature_se = 10000
+  )

   expect_equal(seeded$source, "curvature")
   expect_equal(seeded$step, sqrt(2 * cutoff) * 0.2 * 1.1)
   expect_equal(fallback$source, "fixed")
   expect_equal(fallback$step, 0.25)
+  expect_equal(capped$source, "curvature_capped")
+  expect_equal(capped$step, 6)
+})
+
+test_that("endpoint positive-scale lower side can land on the zero boundary", {
+  evaluator <- list(
+    start_free = numeric(),
+    evaluate = function(theta, start) {
+      list(nll = 0, par = start)
+    }
+  )
+
+  bounded <- drmTMB:::profile_endpoint_crossing(
+    evaluator = evaluator,
+    theta_hat = 0,
+    nll_hat = 0,
+    cutoff = 1,
+    direction = -1,
+    root_tol = 1e-4,
+    max_bracket_steps = 2L,
+    target_name = "sd:mu:(0 + x | id)",
+    curvature_se = NA_real_,
+    allow_lower_boundary = TRUE
+  )
+
+  expect_equal(bounded$theta, -Inf)
+  expect_true(is.na(bounded$root_error))
+  expect_match(bounded$step_source, "lower_boundary")
+  expect_error(
+    drmTMB:::profile_endpoint_crossing(
+      evaluator = evaluator,
+      theta_hat = 0,
+      nll_hat = 0,
+      cutoff = 1,
+      direction = -1,
+      root_tol = 1e-4,
+      max_bracket_steps = 2L,
+      target_name = "sd:mu:(0 + x | id)",
+      curvature_se = NA_real_,
+      allow_lower_boundary = FALSE
+    ),
+    "Could not bracket profile endpoint"
+  )
 })

 test_that("NULL workers use a bounded automatic multicore default", {
diff --git a/tests/testthat/test-structured-re-conversion-contracts.R b/tests/testthat/test-structured-re-conversion-contracts.R
index e015ba45..3285dad9 100644
--- a/tests/testthat/test-structured-re-conversion-contracts.R
+++ b/tests/testthat/test-structured-re-conversion-contracts.R
@@ -64,11 +64,29 @@ structured_re_artifact_path <- function(...) {
   "qseries_relmat_q1_sigma_one_slope",
   "qseries_relmat_q2_mu1_mu2_one_slope"
 )
+.diagnostic_interval_cells <- c(
+  "qseries_phylo_q4_mu1_mu2_one_slope",
+  "qseries_spatial_q4_mu1_mu2_one_slope",
+  "qseries_animal_q4_mu1_mu2_one_slope",
+  "qseries_relmat_q4_mu1_mu2_one_slope",
+  "qseries_phylo_q4_all_four_intercept",
+  "qseries_spatial_q4_all_four_intercept",
+  "qseries_animal_q4_all_four_intercept",
+  "qseries_relmat_q4_all_four_intercept",
+  "qseries_phylo_q4_all_four_one_slope_planned",
+  "qseries_spatial_q4_all_four_one_slope_planned",
+  "qseries_animal_q4_all_four_one_slope_planned",
+  "qseries_relmat_q4_all_four_one_slope_planned"
+)
 .expected_interval <- function(ids) {
   ifelse(
     ids %in% .ir_cells,
     "inference_ready",
-    ifelse(ids %in% .if_cells, "interval_feasible", "planned")
+    ifelse(
+      ids %in% .if_cells,
+      "interval_feasible",
+      ifelse(ids %in% .diagnostic_interval_cells, "diagnostic_only", "planned")
+    )
   )
 }
 .expected_coverage <- function(ids) {
@@ -459,7 +477,10 @@ test_that("q-series support-cell dashboard owns exact structured rows", {
     native_q4_location_slope$bridge_status,
     rep("fixture_parity", 4L)
   )
-  expect_equal(native_q4_location_slope$interval_status, rep("planned", 4L))
+  expect_equal(
+    native_q4_location_slope$interval_status,
+    .expected_interval(native_q4_location_slope$cell_id)
+  )
   expect_equal(
     native_q4_location_slope$coverage_status,
     .expected_coverage(native_q4_location_slope$cell_id)
@@ -638,10 +659,34 @@ test_that("phylo_interaction count q1 support cells stay family-specific", {
   expect_match(nb2_row$claim_boundary, "structured sigma", fixed = TRUE)
 })

-test_that("non-Gaussian audit mirrors count intercept smoke sidecars", {
+test_that("non-Gaussian audit mirrors count intercept recovery results", {
   audit <- structured_re_read_dashboard_tsv(
     "structured-re-nongaussian-status-audit.tsv"
   )
+  qseries <- structured_re_read_dashboard_tsv(
+    "structured-re-q-series-support-cells.tsv"
+  )
+  rollup <- structured_re_read_dashboard_tsv(
+    "structured-re-nongaussian-recovery-rollup.tsv"
+  )
+  slope_recovery <- structured_re_read_dashboard_tsv(
+    "structured-re-count-slope-recovery-results.tsv"
+  )
+  recovery <- structured_re_read_dashboard_tsv(
+    "structured-re-count-intercept-recovery-results.tsv"
+  )
+  topup_recovery <- structured_re_read_dashboard_tsv(
+    "structured-re-count-intercept-topup-recovery-results.tsv"
+  )
+  topup_dispatch <- structured_re_read_dashboard_tsv(
+    "structured-re-count-intercept-topup-cluster-dispatch.tsv"
+  )
+  caveat_diagnostic <- structured_re_read_dashboard_tsv(
+    "structured-re-count-intercept-caveat-diagnostic.tsv"
+  )
+  denominator_diagnostic <- structured_re_read_dashboard_tsv(
+    "structured-re-count-intercept-denominator-diagnostic.tsv"
+  )
   count_smoke <- structured_re_read_dashboard_tsv(
     "structured-re-count-intercept-recovery-smoke-status.tsv"
   )
@@ -657,48 +702,456 @@ test_that("non-Gaussian audit mirrors count intercept smoke sidecars", {
     phylo_smoke$cell_id,
     interaction_smoke$cell_id
   )
-  smoke_audit <- audit[match(smoke_cells, audit$cell_id), , drop = FALSE]
+  recovery_audit <- audit[
+    match(recovery$cell_id, audit$cell_id),
+    ,
+    drop = FALSE
+  ]

   expect_equal(length(smoke_cells), 10L)
-  expect_false(anyNA(smoke_audit$audit_id))
+  expect_setequal(recovery$cell_id, smoke_cells)
+  expect_equal(nrow(recovery), 10L)
+  expect_false(anyNA(recovery_audit$audit_id))
+  expected_audit_state <- recovery$widget_state
+  expected_audit_state[
+    recovery$cell_id %in% topup_recovery$cell_id
+  ] <- "non_gaussian_recovery_only"
+  expect_equal(
+    recovery_audit$widget_state,
+    expected_audit_state
+  )
+  expect_equal(
+    recovery_audit$linked_interval_status,
+    rep("unsupported", length(recovery$cell_id))
+  )
+  expect_equal(
+    recovery_audit$linked_coverage_status,
+    rep("planned", length(recovery$cell_id))
+  )
+  expect_equal(
+    recovery_audit$promotion_decision,
+    rep("do_not_promote", length(recovery$cell_id))
+  )
+  expect_equal(
+    grepl(
+      "80-rep local count-intercept recovery grid|80-rep stronger-denominator count-intercept top-up",
+      recovery_audit$evidence_basis
+    ),
+    rep(TRUE, length(recovery$cell_id))
+  )
+  structured_re_expect_all_match(
+    recovery_audit$claim_boundary,
+    "RECOVERY evidence only"
+  )
+  structured_re_expect_all_match(
+    recovery_audit$claim_boundary,
+    "near-zero-estimate"
+  )
+  structured_re_expect_all_match(
+    recovery_audit$next_gate,
+    "intervals and coverage remain unsupported"
+  )
+  expect_named(
+    rollup,
+    c(
+      "rollup_id",
+      "cell_id",
+      "family",
+      "structure_provider",
+      "endpoint_set",
+      "slope_class",
+      "recovery_grade",
+      "evidence_basis",
+      "fit_signal",
+      "hessian_signal",
+      "finite_signal",
+      "near_zero_signal",
+      "bias_signal",
+      "rmse_signal",
+      "mcse_signal",
+      "linked_fit_status",
+      "linked_interval_status",
+      "linked_coverage_status",
+      "evidence_url",
+      "claim_boundary",
+      "next_gate"
+    )
+  )
+  expect_equal(nrow(rollup), 18L)
+  expect_setequal(
+    rollup$cell_id,
+    unique(c(slope_recovery$cell_id, recovery$cell_id))
+  )
+  expect_equal(
+    as.integer(table(rollup$recovery_grade)[
+      c(
+        "cluster_confirmed_recovery_only",
+        "local_recovery_only",
+        "local_recovery_hessian_caveat"
+      )
+    ]),
+    c(3L, 14L, 1L)
+  )
+  expect_equal(rollup$linked_fit_status, rep("point_fit", nrow(rollup)))
+  expect_equal(rollup$linked_interval_status, rep("unsupported", nrow(rollup)))
+  expect_equal(rollup$linked_coverage_status, rep("planned", nrow(rollup)))
+  structured_re_expect_all_match(rollup$claim_boundary, "RECOVERY-ONLY")
+  structured_re_expect_all_match(rollup$claim_boundary, "does NOT promote")
+  structured_re_expect_all_match(rollup$claim_boundary, "interval_status")
+  structured_re_expect_all_match(rollup$claim_boundary, "coverage_status")
+  rollup_support <- qseries[
+    match(rollup$cell_id, qseries$cell_id),
+    ,
+    drop = FALSE
+  ]
+  expect_false(anyNA(rollup_support$cell_id))
+  structured_re_expect_all_match(
+    paste(rollup_support$claim_boundary, rollup_support$next_gate),
+    "recovery"
+  )
+  structured_re_expect_all_match(
+    paste(rollup_support$claim_boundary, rollup_support$next_gate),
+    "interval"
+  )
+  structured_re_expect_all_match(
+    paste(rollup_support$claim_boundary, rollup_support$next_gate),
+    "coverage"
+  )
+  stale_recovery_gates <- c(
+    "add count fixture",
+    "add fixed-covariance count fixture evidence",
+    "add A-matrix count fixture evidence",
+    "add K-matrix count fixture evidence",
+    "add recovery evidence"
+  )
+  expect_false(any(vapply(
+    stale_recovery_gates,
+    function(gate) any(grepl(gate, rollup_support$next_gate, fixed = TRUE)),
+    logical(1)
+  )))
+  hessian_cell <- "qseries_spatial_nbinom2_q1_mu_one_slope"
+  expect_equal(
+    rollup$recovery_grade[match(hessian_cell, rollup$cell_id)],
+    "local_recovery_hessian_caveat"
+  )
+  hessian_support <- qseries[
+    qseries$cell_id == hessian_cell,
+    ,
+    drop = FALSE
+  ]
+  hessian_audit <- audit[
+    audit$cell_id == hessian_cell,
+    ,
+    drop = FALSE
+  ]
+  expect_equal(nrow(hessian_support), 1L)
+  expect_equal(nrow(hessian_audit), 1L)
+  expect_match(
+    paste(hessian_support$claim_boundary, hessian_support$next_gate),
+    "2/80 pdHess false",
+    fixed = TRUE
+  )
+  expect_match(
+    paste(
+      hessian_audit$evidence_basis,
+      hessian_audit$stability_signal,
+      hessian_audit$claim_boundary,
+      hessian_audit$next_gate
+    ),
+    "2/80 pdHess false",
+    fixed = TRUE
+  )
+  caveat_cells <- c(
+    "qseries_phylo_nbinom2_q1_mu_intercept",
+    "qseries_phylo_poisson_q1_mu_intercept",
+    "qseries_spatial_nbinom2_q1_mu_intercept"
+  )
+  expect_setequal(
+    recovery$cell_id[recovery$widget_state == "non_gaussian_recovery_caveat"],
+    caveat_cells
+  )
+  expect_setequal(
+    topup_recovery$cell_id,
+    caveat_cells
+  )
+  expect_setequal(
+    rollup$cell_id[rollup$recovery_grade == "cluster_confirmed_recovery_only"],
+    caveat_cells
+  )
+  structured_re_expect_all_match(
+    rollup$evidence_basis[
+      rollup$recovery_grade == "cluster_confirmed_recovery_only"
+    ],
+    "Rorqual SLURM job 14897050"
+  )
+  qseries_caveat <- qseries[
+    match(caveat_cells, qseries$cell_id),
+    ,
+    drop = FALSE
+  ]
+  expect_equal(
+    qseries_caveat$interval_status,
+    rep("unsupported", length(caveat_cells))
+  )
+  expect_equal(
+    qseries_caveat$coverage_status,
+    rep("planned", length(caveat_cells))
+  )
+  structured_re_expect_all_match(
+    qseries_caveat$evidence_url,
+    "2026-06-29-count-intercept-topup-recovery-rorqual"
+  )
+  structured_re_expect_all_match(
+    qseries_caveat$claim_boundary,
+    "Cluster-confirmed recovery-only evidence"
+  )
+  structured_re_expect_all_match(
+    qseries_caveat$claim_boundary,
+    "does not promote interval_status"
+  )
+  expect_equal(
+    unique(topup_recovery$widget_state),
+    "non_gaussian_recovery_only"
+  )
+  expect_equal(
+    unique(topup_recovery$recovery_verdict),
+    "topup_recovery_only_passed"
+  )
+  expect_equal(
+    unique(as.integer(topup_recovery$n_rep)),
+    320L
+  )
   expect_equal(
-    smoke_audit$widget_state,
-    rep("non_gaussian_intercept_recovery_smoke", length(smoke_cells))
+    unique(as.integer(topup_recovery$n_seed_replicates)),
+    80L
   )
   expect_equal(
-    smoke_audit$linked_interval_status,
-    rep("unsupported", length(smoke_cells))
+    unique(as.integer(topup_recovery$n_internal_conditions)),
+    4L
   )
   expect_equal(
-    smoke_audit$linked_coverage_status,
-    rep("planned", length(smoke_cells))
+    unique(as.integer(topup_recovery$fit_ok)),
+    320L
   )
   expect_equal(
-    smoke_audit$promotion_decision,
-    rep("do_not_promote", length(smoke_cells))
+    unique(as.integer(topup_recovery$pdhess_false)),
+    0L
+  )
+  expect_equal(
+    unique(as.integer(topup_recovery$near_zero_estimate_rows)),
+    0L
+  )
+  structured_re_expect_all_match(
+    topup_recovery$claim_boundary,
+    "RECOVERY TOP-UP evidence only"
   )
   structured_re_expect_all_match(
-    smoke_audit$evidence_basis,
-    "local recovery-smoke sidecar evidence only"
+    topup_recovery$claim_boundary,
+    "Rorqual SLURM job 14897050"
   )
   structured_re_expect_all_match(
-    smoke_audit$claim_boundary,
-    "local recovery smoke only"
+    topup_recovery$claim_boundary,
+    "does NOT promote"
   )
   structured_re_expect_all_match(
-    smoke_audit$next_gate,
-    "replicated recovery grid"
+    topup_recovery$claim_boundary,
+    "original weak-denominator caveat"
+  )
+  structured_re_expect_all_match(
+    topup_recovery$next_gate,
+    "cluster-confirmed recovery-only board evidence"
+  )
+  expect_equal(nrow(topup_dispatch), 1L)
+  expect_equal(
+    topup_dispatch$dispatch_id,
+    "count_intercept_topup_rorqual_20260629"
+  )
+  expect_equal(topup_dispatch$host, "rorqual")
+  expect_equal(topup_dispatch$scheduler, "slurm")
+  expect_equal(as.character(topup_dispatch$job_id), "14897050")
+  expect_equal(topup_dispatch$job_state, "completed_passed")
+  expect_equal(as.integer(topup_dispatch$n_rep), 80L)
+  expect_equal(as.integer(topup_dispatch$n_internal_conditions), 4L)
+  expect_setequal(
+    strsplit(topup_dispatch$target_cells, ";", fixed = TRUE)[[1]],
+    caveat_cells
+  )
+  expect_match(
+    topup_dispatch$run_root,
+    "/project/def-snakagaw/snakagaw/drmtmb-qseries/",
+    fixed = TRUE
+  )
+  expect_match(
+    topup_dispatch$expected_result_path,
+    "structured-re-count-intercept-topup-recovery-results.tsv",
+    fixed = TRUE
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$claim_boundary,
+    "Cluster confirmation evidence only"
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$claim_boundary,
+    "runner_exit_code=0"
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$claim_boundary,
+    "summary_exit_code=0"
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$claim_boundary,
+    "does not promote"
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$next_gate,
+    "Fetched result TSV and metadata"
+  )
+  structured_re_expect_all_match(
+    topup_dispatch$next_gate,
+    "separate interval route"
+  )
+  expect_equal(
+    recovery_audit$widget_state[match(caveat_cells, recovery_audit$cell_id)],
+    rep("non_gaussian_recovery_only", length(caveat_cells))
+  )
+  structured_re_expect_all_match(
+    recovery_audit$evidence_basis[match(caveat_cells, recovery_audit$cell_id)],
+    "top-up recovery-only sidecar evidence"
+  )
+  expect_equal(
+    recovery$recovery_verdict[
+      match(
+        c(
+          "qseries_phylo_nbinom2_q1_mu_intercept",
+          "qseries_phylo_poisson_q1_mu_intercept",
+          "qseries_spatial_nbinom2_q1_mu_intercept"
+        ),
+        recovery$cell_id
+      )
+    ],
+    c(
+      "recovery_caveat_pdhess_rate",
+      "recovery_caveat_near_zero_rate",
+      "recovery_caveat_near_zero_rate"
+    )
+  )
+  expect_equal(
+    as.integer(recovery$pdhess_false[
+      recovery$cell_id == "qseries_phylo_nbinom2_q1_mu_intercept"
+    ]),
+    13L
+  )
+  expect_equal(
+    as.integer(recovery$near_zero_estimate_rows[
+      match(caveat_cells, recovery$cell_id)
+    ]),
+    c(136L, 121L, 100L)
+  )
+  expect_equal(nrow(caveat_diagnostic), 12L)
+  expect_setequal(caveat_diagnostic$cell_id, caveat_cells)
+  expect_equal(
+    as.integer(table(caveat_diagnostic$cell_id)[caveat_cells]),
+    c(4L, 4L, 4L)
+  )
+  expect_equal(
+    unique(as.numeric(caveat_diagnostic$near_zero_threshold)),
+    1e-4
+  )
+  expect_equal(
+    unique(as.integer(caveat_diagnostic$n_rep)),
+    80L
+  )
+  expect_equal(
+    unique(caveat_diagnostic$diagnostic_verdict[
+      caveat_diagnostic$cell_id == "qseries_phylo_poisson_q1_mu_intercept"
+    ]),
+    "condition_near_zero_caveat"
+  )
+  expect_equal(
+    unique(caveat_diagnostic$diagnostic_verdict[
+      caveat_diagnostic$cell_id == "qseries_phylo_nbinom2_q1_mu_intercept"
+    ]),
+    "condition_pdhess_caveat"
+  )
+  expect_setequal(
+    caveat_diagnostic$diagnostic_verdict[
+      caveat_diagnostic$cell_id == "qseries_spatial_nbinom2_q1_mu_intercept"
+    ],
+    c("condition_near_zero_caveat", "condition_recovery_ok")
+  )
+  expect_equal(
+    max(as.numeric(caveat_diagnostic$near_zero_estimate_rate[
+      caveat_diagnostic$cell_id == "qseries_spatial_nbinom2_q1_mu_intercept"
+    ])),
+    0.6
+  )
+  structured_re_expect_all_match(
+    caveat_diagnostic$claim_boundary,
+    "Condition-level diagnostic only"
+  )
+  structured_re_expect_all_match(
+    caveat_diagnostic$claim_boundary,
+    "does NOT promote"
+  )
+  structured_re_expect_all_match(
+    caveat_diagnostic$next_gate,
+    "intervals and coverage remain unsupported"
+  )
+  expect_equal(nrow(denominator_diagnostic), 12L)
+  expect_setequal(denominator_diagnostic$cell_id, caveat_cells)
+  expect_equal(
+    as.integer(table(denominator_diagnostic$cell_id)[caveat_cells]),
+    c(4L, 4L, 4L)
+  )
+  expect_equal(
+    unique(as.integer(denominator_diagnostic$n_rep)),
+    30L
+  )
+  expect_equal(
+    unique(as.integer(denominator_diagnostic$fit_ok)),
+    30L
+  )
+  expect_equal(
+    unique(as.integer(denominator_diagnostic$pdhess_false)),
+    0L
+  )
+  expect_equal(
+    unique(as.integer(denominator_diagnostic$near_zero_estimate_rows)),
+    0L
+  )
+  expect_equal(
+    unique(denominator_diagnostic$denominator_verdict),
+    "denominator_cleared_locally"
+  )
+  structured_re_expect_all_match(
+    denominator_diagnostic$condition_label,
+    "mean_count"
+  )
+  structured_re_expect_all_match(
+    denominator_diagnostic$claim_boundary,
+    "Targeted stronger-denominator diagnostic only"
+  )
+  structured_re_expect_all_match(
+    denominator_diagnostic$claim_boundary,
+    "does NOT promote"
+  )
+  structured_re_expect_all_match(
+    denominator_diagnostic$next_gate,
+    "intervals and coverage remain unsupported"
   )
   expect_equal(
     as.integer(table(audit$widget_state)[
       c(
-        "non_gaussian_intercept_recovery_smoke",
         "non_gaussian_recovery_only",
         "non_gaussian_rejected",
         "non_gaussian_planned"
       )
     ]),
-    c(10L, 8L, 18L, 1L)
+    c(18L, 18L, 1L)
+  )
+  expect_equal(
+    sum(audit$widget_state == "non_gaussian_recovery_caveat"),
+    0L
   )
 })

@@ -5647,6 +6100,9 @@ test_that("q2 spatial/animal bias+t evidence blocks row promotion", {
   evidence <- structured_re_read_dashboard_tsv(
     "structured-re-q2-slope-bias-t-coverage-evidence.tsv"
   )
+  topup <- structured_re_read_dashboard_tsv(
+    "structured-re-q2-slope-bias-t-topup-runner-contract.tsv"
+  )
   admission <- structured_re_read_dashboard_tsv(
     "structured-re-q2-slope-spatial-animal-admission-audit.tsv"
   )
@@ -5772,6 +6228,146 @@ test_that("q2 spatial/animal bias+t evidence blocks row promotion", {
     admission_status$claim_boundary,
     "not inference_ready"
   )
+  structured_re_expect_all_match(
+    admission_status$next_gate,
+    "structured-re-q2-slope-bias-t-topup-runner-contract.tsv"
+  )
+
+  expect_named(
+    topup,
+    c(
+      "contract_id",
+      "linked_cell_id",
+      "provider",
+      "endpoint_member",
+      "shard",
+      "source_runner",
+      "smoke_artifact",
+      "planned_topup_reps",
+      "seed_start",
+      "seed_end",
+      "interval_channel",
+      "smoke_fit_ok",
+      "smoke_pdhess",
+      "smoke_bias_t_finite",
+      "execution_status",
+      "linked_interval_status",
+      "linked_coverage_status",
+      "promotion_decision",
+      "evidence_url",
+      "claim_boundary",
+      "next_gate"
+    )
+  )
+  expect_equal(nrow(topup), 4L)
+  expect_equal(anyDuplicated(topup$contract_id), 0L)
+  expect_equal(
+    topup$contract_id,
+    c(
+      "q2_bias_t_topup_spatial_mu1_x",
+      "q2_bias_t_topup_spatial_mu2_x",
+      "q2_bias_t_topup_animal_mu1_x",
+      "q2_bias_t_topup_animal_mu2_x"
+    )
+  )
+  expect_equal(
+    topup$linked_cell_id,
+    c(
+      "qseries_spatial_q2_mu1_mu2_one_slope",
+      "qseries_spatial_q2_mu1_mu2_one_slope",
+      "qseries_animal_q2_mu1_mu2_one_slope",
+      "qseries_animal_q2_mu1_mu2_one_slope"
+    )
+  )
+  expect_equal(topup$provider, c("spatial", "spatial", "animal", "animal"))
+  expect_equal(topup$endpoint_member, c("mu1:x", "mu2:x", "mu1:x", "mu2:x"))
+  expect_equal(topup$shard, 1:4)
+  expect_equal(
+    topup$source_runner,
+    rep("tools/run-structured-re-q2-bias-t-coverage-topup.R", 4L)
+  )
+  expect_true(file.exists(
+    structured_re_artifact_path(strsplit(topup$source_runner[[1L]], "/")[[1L]])
+  ))
+  expect_equal(topup$planned_topup_reps, rep(525L, 4L))
+  expect_equal(topup$seed_start, rep(730476L, 4L))
+  expect_equal(topup$seed_end, rep(731000L, 4L))
+  expect_equal(
+    topup$interval_channel,
+    rep("default_bias_t_location_wald", 4L)
+  )
+  expect_equal(topup$smoke_fit_ok, rep("1/1", 4L))
+  expect_equal(topup$smoke_pdhess, rep("1/1", 4L))
+  expect_equal(topup$smoke_bias_t_finite, rep("1/1", 4L))
+  expect_equal(
+    topup$execution_status,
+    rep("smoke_passed_ready_for_cluster_topup", 4L)
+  )
+  expect_equal(topup$linked_interval_status, rep("planned", 4L))
+  expect_equal(topup$linked_coverage_status, rep("planned", 4L))
+  expect_equal(topup$promotion_decision, rep("do_not_promote", 4L))
+
+  linked_status <- qseries[
+    match(unique(topup$linked_cell_id), qseries$cell_id),
+    ,
+    drop = FALSE
+  ]
+  expect_false(anyNA(linked_status$cell_id))
+  expect_equal(linked_status$interval_status, rep("planned", 2L))
+  expect_equal(linked_status$coverage_status, rep("planned", 2L))
+  expect_false(any(linked_status$authority_status == "supported"))
+
+  for (phrase in c(
+    "top-up runner contract only",
+    "devtools_load_all",
+    "does not promote interval_status",
+    "coverage_status",
+    "inference_ready",
+    "supported",
+    "correlation targets",
+    "q4/q8",
+    "REML",
+    "AI-REML",
+    "public support"
+  )) {
+    structured_re_expect_all_match(topup$claim_boundary, phrase)
+  }
+  structured_re_expect_all_match(
+    topup$claim_boundary[topup$provider == "spatial"],
+    "range-estimating spatial"
+  )
+  structured_re_expect_all_match(
+    topup$claim_boundary[topup$provider == "animal"],
+    "pedigree/Ainv"
+  )
+  structured_re_expect_all_match(topup$next_gate, "seeds 730476-731000")
+
+  for (i in seq_len(nrow(topup))) {
+    smoke <- utils::read.delim(
+      structured_re_artifact_path(strsplit(topup$smoke_artifact[[i]], "/")[[
+        1L
+      ]]),
+      sep = "\t",
+      quote = "",
+      check.names = FALSE,
+      stringsAsFactors = FALSE
+    )
+    expect_equal(nrow(smoke), 1L)
+    expect_equal(smoke$linked_cell_id, topup$linked_cell_id[[i]])
+    expect_equal(smoke$provider, topup$provider[[i]])
+    expect_equal(smoke$target, topup$endpoint_member[[i]])
+    expect_equal(smoke$planned_reps, 1L)
+    expect_equal(smoke$n_fit_ok, 1L)
+    expect_equal(smoke$n_converged, 1L)
+    expect_equal(smoke$n_pdhess, 1L)
+    expect_equal(smoke$n_bias_t_finite, 1L)
+    expect_equal(smoke$summary_status, "topup_endpoint_only_no_promotion")
+    expect_match(
+      smoke$claim_boundary,
+      "does not promote interval_status",
+      fixed = TRUE
+    )
+  }
 })

 test_that("q2 slope-only replicated denominator rule keeps coverage blocked", {
@@ -9977,7 +10573,10 @@ test_that("q4 all-four intercept parity fixture records provider bridge fixture"
   expect_equal(provider_rows$fit_status, rep("point_fit", 3L))
   expect_equal(provider_rows$extractor_status, rep("extractor_ready", 3L))
   expect_equal(provider_rows$bridge_status, rep("fixture_parity", 3L))
-  expect_equal(provider_rows$interval_status, rep("planned", 3L))
+  expect_equal(
+    provider_rows$interval_status,
+    .expected_interval(provider_rows$cell_id)
+  )
   expect_equal(provider_rows$coverage_status, rep("planned", 3L))
   expect_equal(
     provider_rows$evidence_url,
diff --git a/tests/testthat/test-wald-small-sample-default.R b/tests/testthat/test-wald-small-sample-default.R
index 4b025cc7..6888a81c 100644
--- a/tests/testthat/test-wald-small-sample-default.R
+++ b/tests/testthat/test-wald-small-sample-default.R
@@ -86,6 +86,80 @@ test_that("location-axis structured SD is corrected by the wald DEFAULT", {
   expect_false(isTRUE(all.equal(default$lower, raw$lower)))
 })

+test_that("location-axis structured slope SD components resolve group count", {
+  set.seed(20260629)
+  g <- 10L
+  n_each <- 10L
+  site_levels <- paste0("site", seq_len(g))
+  theta <- seq(0, 1.6 * pi, length.out = g)
+  coords <- data.frame(
+    x = cos(theta) + seq_len(g) / (4 * g),
+    y = sin(theta)
+  )
+  rownames(coords) <- site_levels
+  site <- rep(site_levels, each = n_each)
+  x <- stats::rnorm(length(site))
+  site_intercept <- seq(-0.5, 0.5, length.out = g)
+  site_slope <- seq(-1, 1, length.out = g)
+  names(site_intercept) <- site_levels
+  names(site_slope) <- site_levels
+  y <- 0.5 +
+    0.3 * x +
+    site_intercept[site] +
+    site_slope[site] * x +
+    stats::rnorm(length(site), 0, 0.4)
+  dat <- data.frame(y = y, x = x, site = site)
+  fit <- drmTMB(
+    bf(y ~ x + spatial(1 + x | site, coords = coords), sigma ~ 1),
+    family = gaussian(),
+    data = dat,
+    control = drm_control(keep_tmb_object = TRUE)
+  )
+  level <- 0.95
+  p <- (1 + level) / 2
+  shift <- log(g / (g - 1))
+  tq <- stats::qt(p, g - 1)
+  parms <- c(
+    "sd:mu:spatial(1 | site)",
+    "sd:mu:spatial(0 + x | site)"
+  )
+
+  targets <- drmTMB:::drm_profile_targets(fit)
+  target_rows <- targets[targets$parm %in% parms, , drop = FALSE]
+  expect_equal(
+    drmTMB:::wald_target_df(fit, target_rows, location_only = TRUE),
+    rep(g - 1, length(parms))
+  )
+  expect_equal(
+    drmTMB:::wald_target_log_bias(fit, target_rows, location_only = TRUE),
+    rep(shift, length(parms))
+  )
+
+  raw <- drmTMB:::drm_wald_confint(
+    fit,
+    parm = parms,
+    level = level,
+    small_sample_df = "none",
+    bias_correct = "none"
+  )
+  default <- drmTMB:::drm_wald_confint(fit, parm = parms, level = level)
+
+  for (parm in parms) {
+    es <- wald_small_sample_eta_se(raw[raw$parm == parm, ], level)
+    row <- default[default$parm == parm, ]
+    expect_equal(
+      row$lower,
+      exp((es[["eta"]] + shift) - tq * es[["se"]]),
+      tolerance = 1e-9
+    )
+    expect_equal(
+      row$upper,
+      exp((es[["eta"]] + shift) + tq * es[["se"]]),
+      tolerance = 1e-9
+    )
+  }
+})
+
 test_that("dispersion-axis structured SD keeps the raw z-interval by default", {
   sim <- wald_small_sample_spatial_data()
   coords <- sim$coords
diff --git a/tools/validate-mission-control.py b/tools/validate-mission-control.py
index 7fedc39a..ff35944e 100755
--- a/tools/validate-mission-control.py
+++ b/tools/validate-mission-control.py
@@ -109,6 +109,21 @@ STRUCTURED_RE_SLOPE_COVERAGE_RESULTS = (
 STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS = (
     DASHBOARD / "structured-re-count-slope-recovery-results.tsv"
 )
+STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_RESULTS = (
+    DASHBOARD / "structured-re-count-intercept-recovery-results.tsv"
+)
+STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_RECOVERY_RESULTS = (
+    DASHBOARD / "structured-re-count-intercept-topup-recovery-results.tsv"
+)
+STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_CLUSTER_DISPATCH = (
+    DASHBOARD / "structured-re-count-intercept-topup-cluster-dispatch.tsv"
+)
+STRUCTURED_RE_COUNT_INTERCEPT_CAVEAT_DIAGNOSTIC = (
+    DASHBOARD / "structured-re-count-intercept-caveat-diagnostic.tsv"
+)
+STRUCTURED_RE_COUNT_INTERCEPT_DENOMINATOR_DIAGNOSTIC = (
+    DASHBOARD / "structured-re-count-intercept-denominator-diagnostic.tsv"
+)
 STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_SMOKE_STATUS = (
     DASHBOARD / "structured-re-count-intercept-recovery-smoke-status.tsv"
 )
@@ -197,18 +212,63 @@ STRUCTURED_RE_Q2_SLOPE_SPATIAL_ANIMAL_ADMISSION_AUDIT = (
 STRUCTURED_RE_Q2_SLOPE_BIAS_T_COVERAGE_EVIDENCE = (
     DASHBOARD / "structured-re-q2-slope-bias-t-coverage-evidence.tsv"
 )
+STRUCTURED_RE_Q2_SLOPE_BIAS_T_TOPUP_RUNNER_CONTRACT = (
+    DASHBOARD / "structured-re-q2-slope-bias-t-topup-runner-contract.tsv"
+)
 STRUCTURED_RE_HIGH_Q_STATUS_AUDIT = (
     DASHBOARD / "structured-re-high-q-status-audit.tsv"
 )
 STRUCTURED_RE_NONGAUSSIAN_STATUS_AUDIT = (
     DASHBOARD / "structured-re-nongaussian-status-audit.tsv"
 )
+STRUCTURED_RE_NONGAUSSIAN_RECOVERY_ROLLUP = (
+    DASHBOARD / "structured-re-nongaussian-recovery-rollup.tsv"
+)
 STRUCTURED_RE_GAUSSIAN_LOWQ_STATUS_AUDIT = (
     DASHBOARD / "structured-re-gaussian-lowq-status-audit.tsv"
 )
 STRUCTURED_RE_GAUSSIAN_MU_SLOPE_SMOKE_STATUS = (
     DASHBOARD / "structured-re-gaussian-mu-slope-smoke-status.tsv"
 )
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_ADMISSION_AUDIT = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-admission-audit.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_DRY_RUN = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_RESULTS = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_BOUNDARY_AUDIT = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_SR475_AUDIT = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC = (
+    DASHBOARD / "structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR = (
+    ROOT
+    / "docs"
+    / "dev-log"
+    / "simulation-artifacts"
+    / "2026-06-29-gaussian-mu-slope-coverage-pregrid-local"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_ARTIFACT_DIR = (
+    ROOT
+    / "docs"
+    / "dev-log"
+    / "simulation-artifacts"
+    / "2026-06-29-gaussian-mu-slope-boundary-profile-diagnostic-local"
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR = (
+    ROOT
+    / "docs"
+    / "dev-log"
+    / "simulation-artifacts"
+    / "2026-06-29-gaussian-mu-slope-interval-probe-local"
+)
 STRUCTURED_RE_Q2_SLOPE_COVERAGE_PREGRID_DRY_RUN = (
     DASHBOARD / "structured-re-q2-slope-coverage-pregrid-dry-run.tsv"
 )
@@ -1963,6 +2023,29 @@ STRUCTURED_RE_Q2_SLOPE_BIAS_T_COVERAGE_EVIDENCE_FIELDS = (
     "promotion_status",
     "claim_boundary",
 )
+STRUCTURED_RE_Q2_SLOPE_BIAS_T_TOPUP_RUNNER_CONTRACT_FIELDS = (
+    "contract_id",
+    "linked_cell_id",
+    "provider",
+    "endpoint_member",
+    "shard",
+    "source_runner",
+    "smoke_artifact",
+    "planned_topup_reps",
+    "seed_start",
+    "seed_end",
+    "interval_channel",
+    "smoke_fit_ok",
+    "smoke_pdhess",
+    "smoke_bias_t_finite",
+    "execution_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
 STRUCTURED_RE_HIGH_Q_STATUS_AUDIT_FIELDS = (
     "audit_id",
     "cell_id",
@@ -1999,6 +2082,29 @@ STRUCTURED_RE_NONGAUSSIAN_STATUS_AUDIT_FIELDS = (
     "claim_boundary",
     "next_gate",
 )
+STRUCTURED_RE_NONGAUSSIAN_RECOVERY_ROLLUP_FIELDS = (
+    "rollup_id",
+    "cell_id",
+    "family",
+    "structure_provider",
+    "endpoint_set",
+    "slope_class",
+    "recovery_grade",
+    "evidence_basis",
+    "fit_signal",
+    "hessian_signal",
+    "finite_signal",
+    "near_zero_signal",
+    "bias_signal",
+    "rmse_signal",
+    "mcse_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
 STRUCTURED_RE_GAUSSIAN_LOWQ_STATUS_AUDIT_FIELDS = (
     "audit_id",
     "cell_id",
@@ -2037,6 +2143,206 @@ STRUCTURED_RE_GAUSSIAN_MU_SLOPE_SMOKE_STATUS_FIELDS = (
     "claim_boundary",
     "next_gate",
 )
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_ADMISSION_AUDIT_FIELDS = (
+    "audit_id",
+    "cell_id",
+    "provider",
+    "source_task",
+    "formula_cell",
+    "source_smoke_status",
+    "source_interval_probe",
+    "n_targets",
+    "n_probe_replicates",
+    "n_interval_rows",
+    "n_converged_rows",
+    "n_pdhess_rows",
+    "n_finite_intervals",
+    "n_wald_boundary",
+    "n_covered",
+    "n_lower_miss",
+    "n_upper_miss",
+    "widget_state",
+    "admission_status",
+    "evidence_basis",
+    "stability_signal",
+    "inference_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_DRY_RUN_FIELDS = (
+    "pregrid_id",
+    "cell_id",
+    "formula_cell",
+    "provider",
+    "target_kind",
+    "endpoint_member",
+    "direct_sd_target",
+    "source_probe",
+    "source_seed_manifest",
+    "source_cell_manifest",
+    "current_denominator_action",
+    "denominator_role",
+    "planned_replicates",
+    "planned_cells",
+    "seed_manifest_rows",
+    "target_cell_manifest_rows",
+    "total_cell_manifest_rows",
+    "nominal_coverage",
+    "nominal_mcse_at_150",
+    "replicates_for_mcse_threshold",
+    "mcse_threshold",
+    "mcse_threshold_status",
+    "interval_methods",
+    "retention_policy",
+    "execution_status",
+    "coverage_evaluable",
+    "coverage_status",
+    "interval_claim_status",
+    "status",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_RESULTS_FIELDS = (
+    "result_id",
+    "cell_id",
+    "provider",
+    "n_targets",
+    "n_eligible_targets",
+    "n_holdout_targets",
+    "n_rep",
+    "n_total_target_replicates",
+    "n_usable_intervals",
+    "min_finite_interval_rate",
+    "min_coverage_all",
+    "max_coverage_all",
+    "max_coverage_mcse",
+    "n_lower_miss",
+    "n_upper_miss",
+    "target_gate",
+    "widget_state",
+    "evidence_basis",
+    "stability_signal",
+    "inference_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "artifact_dir",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC_FIELDS = (
+    "diagnostic_id",
+    "cell_id",
+    "provider",
+    "n_boundary_rows",
+    "n_profile_attempted",
+    "n_profile_converged_fit",
+    "n_profile_pdhess_fit",
+    "n_profile_confint_ok",
+    "n_profile_finite_interval",
+    "n_profile_failed",
+    "n_profile_covered",
+    "n_profile_lower_miss",
+    "n_profile_upper_miss",
+    "profile_status_counts",
+    "profile_verdict_counts",
+    "profile_message_examples",
+    "diagnostic_verdict",
+    "widget_state",
+    "stability_signal",
+    "inference_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "artifact_dir",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_BOUNDARY_AUDIT_FIELDS = (
+    "audit_id",
+    "cell_id",
+    "provider",
+    "source_pregrid",
+    "source_boundary_profile",
+    "n_eligible_target_replicates",
+    "n_usable_hybrid_intervals",
+    "finite_interval_rate",
+    "n_profile_boundary_rows",
+    "n_profile_finite",
+    "n_profile_failed",
+    "n_profile_covered",
+    "n_covered",
+    "hybrid_coverage_all",
+    "hybrid_coverage_usable",
+    "hybrid_coverage_mcse",
+    "n_lower_miss",
+    "n_upper_miss",
+    "miss_balance",
+    "widget_state",
+    "admission_status",
+    "evidence_basis",
+    "stability_signal",
+    "inference_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "artifact_dir",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_SR475_AUDIT_FIELDS = (
+    "audit_id",
+    "cell_id",
+    "provider",
+    "source_pregrid",
+    "source_boundary_profile",
+    "source_topup_pregrid",
+    "source_topup_boundary_profile",
+    "n_seed_replicates",
+    "n_eligible_target_replicates",
+    "n_usable_hybrid_intervals",
+    "finite_interval_rate",
+    "n_profile_boundary_rows",
+    "n_profile_finite",
+    "n_profile_failed",
+    "n_profile_covered",
+    "n_covered",
+    "hybrid_coverage_all",
+    "hybrid_coverage_usable",
+    "hybrid_coverage_mcse",
+    "n_lower_miss",
+    "n_upper_miss",
+    "miss_balance",
+    "worst_target",
+    "worst_target_coverage",
+    "worst_target_mcse",
+    "worst_target_miss_balance",
+    "widget_state",
+    "admission_status",
+    "evidence_basis",
+    "stability_signal",
+    "inference_signal",
+    "linked_fit_status",
+    "linked_interval_status",
+    "linked_coverage_status",
+    "promotion_decision",
+    "evidence_url",
+    "artifact_dir",
+    "claim_boundary",
+    "next_gate",
+)
 STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_SMOKE_STATUS_FIELDS = (
     "smoke_id",
     "cell_id",
@@ -2112,6 +2418,132 @@ STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS_FIELDS = (
     "evidence_url",
     "claim_boundary",
 )
+STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_RESULTS_FIELDS = (
+    "recovery_id",
+    "cell_id",
+    "family",
+    "structured_type",
+    "n_rep",
+    "n_seed_replicates",
+    "n_internal_conditions",
+    "fit_ok",
+    "nonconverged",
+    "pdhess_false",
+    "finite_estimate_rows",
+    "near_zero_threshold",
+    "near_zero_estimate_rows",
+    "near_zero_estimate_rate",
+    "boundary_warning_rows",
+    "true_sd",
+    "mean_sd",
+    "bias_sd",
+    "rmse_sd",
+    "bias_mcse",
+    "rmse_mcse",
+    "recovery_verdict",
+    "widget_state",
+    "linked_cell_id",
+    "linked_coverage_status",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_RECOVERY_RESULTS_FIELDS = (
+    "recovery_id",
+    "cell_id",
+    "family",
+    "structured_type",
+    "topup_design",
+    "n_rep",
+    "n_seed_replicates",
+    "n_internal_conditions",
+    "fit_ok",
+    "nonconverged",
+    "pdhess_false",
+    "finite_estimate_rows",
+    "near_zero_threshold",
+    "near_zero_estimate_rows",
+    "near_zero_estimate_rate",
+    "boundary_warning_rows",
+    "true_sd_min",
+    "true_sd_max",
+    "mean_sd",
+    "bias_sd",
+    "rmse_sd",
+    "bias_mcse",
+    "rmse_mcse",
+    "recovery_verdict",
+    "widget_state",
+    "linked_cell_id",
+    "linked_coverage_status",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_CLUSTER_DISPATCH_FIELDS = (
+    "dispatch_id",
+    "lane",
+    "host",
+    "scheduler",
+    "job_id",
+    "job_state",
+    "run_root",
+    "source_snapshot",
+    "git_sha",
+    "n_rep",
+    "n_internal_conditions",
+    "target_cells",
+    "expected_result_path",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_COUNT_INTERCEPT_CAVEAT_DIAGNOSTIC_FIELDS = (
+    "diagnostic_id",
+    "cell_id",
+    "family",
+    "provider",
+    "condition_label",
+    "n_rep",
+    "fit_ok",
+    "nonconverged",
+    "pdhess_false",
+    "near_zero_threshold",
+    "near_zero_estimate_rows",
+    "near_zero_estimate_rate",
+    "boundary_warning_rows",
+    "true_sd",
+    "mean_sd",
+    "bias_sd",
+    "rmse_sd",
+    "diagnostic_verdict",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
+STRUCTURED_RE_COUNT_INTERCEPT_DENOMINATOR_DIAGNOSTIC_FIELDS = (
+    "diagnostic_id",
+    "cell_id",
+    "family",
+    "provider",
+    "condition_label",
+    "n_rep",
+    "fit_ok",
+    "nonconverged",
+    "pdhess_false",
+    "finite_estimate_rows",
+    "near_zero_threshold",
+    "near_zero_estimate_rows",
+    "near_zero_estimate_rate",
+    "boundary_warning_rows",
+    "true_sd",
+    "mean_sd",
+    "bias_sd",
+    "rmse_sd",
+    "denominator_verdict",
+    "evidence_url",
+    "claim_boundary",
+    "next_gate",
+)
 STRUCTURED_RE_SLOPE_COVERAGE_GSWEEP_FIELDS = (
     "gsweep_id",
     "lane",
@@ -5606,8 +6038,9 @@ STRUCTURED_RE_Q_SERIES_STATUSES = {
 # `interval_feasible` on 2026-06-27 (Fisher/Rose/Emmy/Pat/Darwin + maintainer
 # sign-off) on the strength of the g=32 profile certification + interval-
 # reliability rung. The anti-over-promotion guards in the diagnostic/denominator
-# sidecars allow `interval_feasible` ONLY for these cells; every other linked
-# cell still must read `planned`, and coverage_status stays `planned` for all.
+# sidecars allow `interval_feasible` ONLY for these cells. A separate high-q
+# diagnostic allow-list below permits `diagnostic_only` for exact tried q4/q8
+# interval-smoke rows without accepting coverage or inference readiness.
 CERTIFIED_INTERVAL_FEASIBLE_CELLS = {
     "qseries_phylo_q1_sigma_one_slope",
     "qseries_phylo_q2_mu1_mu2_one_slope",
@@ -5631,15 +6064,29 @@ CERTIFIED_INFERENCE_READY_CELLS = {
     "qseries_relmat_q1_sigma_one_slope",
     "qseries_relmat_q2_mu1_mu2_one_slope",
 }
-
-
-def _promoted_status_ok(qrow, field):
-    """True if `field` on linked q-series cell `qrow` is `planned`, or an
-    explicitly-signed-off elevated status for that exact cell: interval_status may
-    be `interval_feasible` (CERTIFIED_INTERVAL_FEASIBLE_CELLS) or `inference_ready`
-    (CERTIFIED_INFERENCE_READY_CELLS); coverage_status may be `inference_ready`
-    (CERTIFIED_INFERENCE_READY_CELLS). Cell-id-keyed: every other field, value, and
-    cell still requires `planned`, and non-status fields fall through to exact
+CERTIFIED_DIAGNOSTIC_INTERVAL_CELLS = {
+    "qseries_phylo_q4_mu1_mu2_one_slope",
+    "qseries_spatial_q4_mu1_mu2_one_slope",
+    "qseries_animal_q4_mu1_mu2_one_slope",
+    "qseries_relmat_q4_mu1_mu2_one_slope",
+    "qseries_phylo_q4_all_four_intercept",
+    "qseries_spatial_q4_all_four_intercept",
+    "qseries_animal_q4_all_four_intercept",
+    "qseries_relmat_q4_all_four_intercept",
+    "qseries_phylo_q4_all_four_one_slope_planned",
+    "qseries_spatial_q4_all_four_one_slope_planned",
+    "qseries_animal_q4_all_four_one_slope_planned",
+    "qseries_relmat_q4_all_four_one_slope_planned",
+}
+
+
+def _promoted_status_ok(qrow, field):
+    """True if `field` on linked q-series cell `qrow` is `planned`, or an
+    explicitly-signed-off elevated status for that exact cell: interval_status may
+    be `interval_feasible` (CERTIFIED_INTERVAL_FEASIBLE_CELLS) or `inference_ready`
+    (CERTIFIED_INFERENCE_READY_CELLS); coverage_status may be `inference_ready`
+    (CERTIFIED_INFERENCE_READY_CELLS). Cell-id-keyed: every other field, value, and
+    cell still requires `planned`, and non-status fields fall through to exact
     matching, so sidecar-own rows and all other cells are unaffected."""
     value = qrow.get(field)
     if value == "planned":
@@ -5650,6 +6097,8 @@ def _promoted_status_ok(qrow, field):
             return True
         if value == "inference_ready" and cell in CERTIFIED_INFERENCE_READY_CELLS:
             return True
+        if value == "diagnostic_only" and cell in CERTIFIED_DIAGNOSTIC_INTERVAL_CELLS:
+            return True
     elif field == "coverage_status":
         if value == "inference_ready" and cell in CERTIFIED_INFERENCE_READY_CELLS:
             return True
@@ -6029,6 +6478,21 @@ def main() -> int:
     structured_re_count_slope_recovery_results_rows = read_tsv(
         STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS
     )
+    structured_re_count_intercept_recovery_results_rows = read_tsv(
+        STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_RESULTS
+    )
+    structured_re_count_intercept_topup_recovery_results_rows = read_tsv(
+        STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_RECOVERY_RESULTS
+    )
+    structured_re_count_intercept_topup_cluster_dispatch_rows = read_tsv(
+        STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_CLUSTER_DISPATCH
+    )
+    structured_re_count_intercept_caveat_diagnostic_rows = read_tsv(
+        STRUCTURED_RE_COUNT_INTERCEPT_CAVEAT_DIAGNOSTIC
+    )
+    structured_re_count_intercept_denominator_diagnostic_rows = read_tsv(
+        STRUCTURED_RE_COUNT_INTERCEPT_DENOMINATOR_DIAGNOSTIC
+    )
     structured_re_count_intercept_recovery_smoke_status_rows = read_tsv(
         STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_SMOKE_STATUS
     )
@@ -6107,18 +6571,42 @@ def main() -> int:
     structured_re_q2_slope_bias_t_coverage_evidence_rows = read_tsv(
         STRUCTURED_RE_Q2_SLOPE_BIAS_T_COVERAGE_EVIDENCE
     )
+    structured_re_q2_slope_bias_t_topup_runner_contract_rows = read_tsv(
+        STRUCTURED_RE_Q2_SLOPE_BIAS_T_TOPUP_RUNNER_CONTRACT
+    )
     structured_re_high_q_status_audit_rows = read_tsv(
         STRUCTURED_RE_HIGH_Q_STATUS_AUDIT
     )
     structured_re_nongaussian_status_audit_rows = read_tsv(
         STRUCTURED_RE_NONGAUSSIAN_STATUS_AUDIT
     )
+    structured_re_nongaussian_recovery_rollup_rows = read_tsv(
+        STRUCTURED_RE_NONGAUSSIAN_RECOVERY_ROLLUP
+    )
     structured_re_gaussian_lowq_status_audit_rows = read_tsv(
         STRUCTURED_RE_GAUSSIAN_LOWQ_STATUS_AUDIT
     )
     structured_re_gaussian_mu_slope_smoke_status_rows = read_tsv(
         STRUCTURED_RE_GAUSSIAN_MU_SLOPE_SMOKE_STATUS
     )
+    structured_re_gaussian_mu_slope_admission_audit_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_ADMISSION_AUDIT
+    )
+    structured_re_gaussian_mu_slope_coverage_pregrid_dry_run_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_DRY_RUN
+    )
+    structured_re_gaussian_mu_slope_coverage_pregrid_results_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_RESULTS
+    )
+    structured_re_gaussian_mu_slope_hybrid_boundary_audit_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_BOUNDARY_AUDIT
+    )
+    structured_re_gaussian_mu_slope_hybrid_sr475_audit_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_SR475_AUDIT
+    )
+    structured_re_gaussian_mu_slope_boundary_profile_diagnostic_rows = read_tsv(
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC
+    )
     structured_re_q2_slope_coverage_pregrid_dry_run_rows = read_tsv(
         STRUCTURED_RE_Q2_SLOPE_COVERAGE_PREGRID_DRY_RUN
     )
@@ -7915,7 +8403,7 @@ def main() -> int:
                 "fit_status": "point_fit",
                 "extractor_status": "extractor_ready",
                 "bridge_status": "fixture_parity",
-                "interval_status": "planned",
+                "interval_status": "diagnostic_only",
                 "coverage_status": "planned",
                 "authority_status": "source",
                 "evidence_url": (
@@ -13358,6 +13846,168 @@ def main() -> int:
             + ", ".join(sorted(expected_q2_bias_t_evidence))
         )

+    # --- structured-re q2-slope bias+t top-up runner contract ---
+    # EXECUTION contract only: the four endpoint shards are smoked and ready for
+    # cluster top-up, but linked q2 spatial/animal rows remain planned/planned.
+    expected_q2_bias_t_topup_contract = {
+        "q2_bias_t_topup_spatial_mu1_x": {
+            "linked_cell_id": "qseries_spatial_q2_mu1_mu2_one_slope",
+            "provider": "spatial",
+            "endpoint_member": "mu1:x",
+            "shard": "1",
+            "smoke_artifact": (
+                "docs/dev-log/simulation-artifacts/"
+                "2026-06-29-q2-bias-t-topup-current-source-smoke-local/"
+                "01-spatial-mu1_x-summary.tsv"
+            ),
+            "provider_phrase": "range-estimating spatial",
+        },
+        "q2_bias_t_topup_spatial_mu2_x": {
+            "linked_cell_id": "qseries_spatial_q2_mu1_mu2_one_slope",
+            "provider": "spatial",
+            "endpoint_member": "mu2:x",
+            "shard": "2",
+            "smoke_artifact": (
+                "docs/dev-log/simulation-artifacts/"
+                "2026-06-29-q2-bias-t-topup-current-source-smoke-local/"
+                "02-spatial-mu2_x-summary.tsv"
+            ),
+            "provider_phrase": "range-estimating spatial",
+        },
+        "q2_bias_t_topup_animal_mu1_x": {
+            "linked_cell_id": "qseries_animal_q2_mu1_mu2_one_slope",
+            "provider": "animal",
+            "endpoint_member": "mu1:x",
+            "shard": "3",
+            "smoke_artifact": (
+                "docs/dev-log/simulation-artifacts/"
+                "2026-06-29-q2-bias-t-topup-current-source-smoke-local/"
+                "03-animal-mu1_x-summary.tsv"
+            ),
+            "provider_phrase": "pedigree/Ainv",
+        },
+        "q2_bias_t_topup_animal_mu2_x": {
+            "linked_cell_id": "qseries_animal_q2_mu1_mu2_one_slope",
+            "provider": "animal",
+            "endpoint_member": "mu2:x",
+            "shard": "4",
+            "smoke_artifact": (
+                "docs/dev-log/simulation-artifacts/"
+                "2026-06-29-q2-bias-t-topup-current-source-smoke-local/"
+                "04-animal-mu2_x-summary.tsv"
+            ),
+            "provider_phrase": "pedigree/Ainv",
+        },
+    }
+    seen_q2_bias_t_topup_contract_ids: set[str] = set()
+    if len(structured_re_q2_slope_bias_t_topup_runner_contract_rows) != 4:
+        errors.append(
+            "structured-re-q2-slope-bias-t-topup-runner-contract.tsv: "
+            "expected 4 endpoint rows"
+        )
+    for row in structured_re_q2_slope_bias_t_topup_runner_contract_rows:
+        row_id = row.get("contract_id", "<q2 bias+t top-up contract>")
+        if set(row.keys()) != set(STRUCTURED_RE_Q2_SLOPE_BIAS_T_TOPUP_RUNNER_CONTRACT_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-q2-slope-bias-t-topup-runner-contract.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_Q2_SLOPE_BIAS_T_TOPUP_RUNNER_CONTRACT_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_q2_bias_t_topup_contract_ids:
+            errors.append(f"duplicate q2 bias+t top-up contract id: {row_id}")
+        seen_q2_bias_t_topup_contract_ids.add(row_id)
+        expected = expected_q2_bias_t_topup_contract.get(row_id)
+        if expected is None:
+            errors.append(f"{row_id}: unexpected q2 bias+t top-up contract row")
+            continue
+        exact_values = {
+            "linked_cell_id": expected["linked_cell_id"],
+            "provider": expected["provider"],
+            "endpoint_member": expected["endpoint_member"],
+            "shard": expected["shard"],
+            "source_runner": "tools/run-structured-re-q2-bias-t-coverage-topup.R",
+            "smoke_artifact": expected["smoke_artifact"],
+            "planned_topup_reps": "525",
+            "seed_start": "730476",
+            "seed_end": "731000",
+            "interval_channel": "default_bias_t_location_wald",
+            "smoke_fit_ok": "1/1",
+            "smoke_pdhess": "1/1",
+            "smoke_bias_t_finite": "1/1",
+            "execution_status": "smoke_passed_ready_for_cluster_topup",
+            "linked_interval_status": "planned",
+            "linked_coverage_status": "planned",
+            "promotion_decision": "do_not_promote",
+            "evidence_url": (
+                "docs/dev-log/simulation-artifacts/"
+                "2026-06-29-q2-bias-t-topup-current-source-smoke-local"
+            ),
+        }
+        for field, expected_value in exact_values.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        for reference_field in ("source_runner", "smoke_artifact", "evidence_url"):
+            if not evidence_reference_exists(row.get(reference_field, "")):
+                errors.append(f"{row_id}: {reference_field} does not resolve locally")
+        linked = qseries_by_cell.get(row.get("linked_cell_id", ""))
+        if linked is None:
+            errors.append(f"{row_id}: linked q-series support cell is missing")
+        else:
+            for field, expected_value in {
+                "interval_status": "planned",
+                "coverage_status": "planned",
+            }.items():
+                if linked.get(field) != expected_value:
+                    errors.append(f"{row_id}: linked {field} must remain {expected_value}")
+            if linked.get("authority_status") == "supported":
+                errors.append(f"{row_id}: linked cell must not be supported")
+        smoke_rows = read_tsv(ROOT / row.get("smoke_artifact", "missing"))
+        if len(smoke_rows) != 1:
+            errors.append(f"{row_id}: smoke_artifact must contain exactly one summary row")
+        else:
+            smoke = smoke_rows[0]
+            for field, expected_value in {
+                "linked_cell_id": expected["linked_cell_id"],
+                "provider": expected["provider"],
+                "target": expected["endpoint_member"],
+                "planned_reps": "1",
+                "n_fit_ok": "1",
+                "n_converged": "1",
+                "n_pdhess": "1",
+                "n_bias_t_finite": "1",
+                "summary_status": "topup_endpoint_only_no_promotion",
+            }.items():
+                if smoke.get(field) != expected_value:
+                    errors.append(
+                        f"{row_id}: smoke summary {field} must be {expected_value!r}"
+                    )
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "top-up runner contract only",
+            "devtools_load_all",
+            "does not promote interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "correlation targets",
+            "q4/q8",
+            "REML",
+            "AI-REML",
+            "public support",
+            expected["provider_phrase"],
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "seeds 730476-731000" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must name the top-up seed range")
+    if seen_q2_bias_t_topup_contract_ids != set(expected_q2_bias_t_topup_contract):
+        errors.append(
+            "structured-re-q2-slope-bias-t-topup-runner-contract.tsv row ids must be "
+            + ", ".join(sorted(expected_q2_bias_t_topup_contract))
+        )
+
     # --- structured-re high-q status audit ---
     # DISPLAY/AUDIT evidence only: the 24 q4/q6/q8 Q-Series cells get explicit
     # board states, but none are promoted to inference_ready or supported.
@@ -13501,8 +14151,8 @@ def main() -> int:
     # point/recovery/rejection/planned state, but intervals remain unsupported
     # and no coverage/support wording is promoted.
     expected_nongaussian_state_counts = {
-        "non_gaussian_recovery_only": 8,
-        "non_gaussian_intercept_recovery_smoke": 10,
+        "non_gaussian_recovery_only": 18,
+        "non_gaussian_recovery_caveat": 0,
         "non_gaussian_rejected": 18,
         "non_gaussian_planned": 1,
     }
@@ -13548,6 +14198,18 @@ def main() -> int:
         errors.append(
             "non-Gaussian intercept smoke sidecars must cover exactly 10 cells"
         )
+    nongaussian_intercept_recovery_by_cell = {
+        row.get("linked_cell_id", row.get("cell_id", "")): row
+        for row in structured_re_count_intercept_recovery_results_rows
+    }
+    nongaussian_intercept_topup_by_cell = {
+        row.get("linked_cell_id", row.get("cell_id", "")): row
+        for row in structured_re_count_intercept_topup_recovery_results_rows
+    }
+    nongaussian_recovery_result_cells = {
+        recovery.get("linked_cell_id", recovery.get("cell_id", ""))
+        for recovery in structured_re_count_slope_recovery_results_rows
+    } | set(nongaussian_intercept_recovery_by_cell) | set(nongaussian_intercept_topup_by_cell)
     for row in structured_re_nongaussian_status_audit_rows:
         row_id = row.get("audit_id", "<non-Gaussian status audit>")
         cell_id = row.get("cell_id", "")
@@ -13595,6 +14257,14 @@ def main() -> int:
             expected_widget_state = "non_gaussian_rejected"
         elif cell_id == "qseries_nongaussian_structured_slope_neighbors_planned":
             expected_widget_state = "non_gaussian_planned"
+        elif cell_id in nongaussian_intercept_topup_by_cell:
+            expected_widget_state = nongaussian_intercept_topup_by_cell[cell_id].get(
+                "widget_state", ""
+            )
+        elif cell_id in nongaussian_intercept_recovery_by_cell:
+            expected_widget_state = nongaussian_intercept_recovery_by_cell[cell_id].get(
+                "widget_state", ""
+            )
         elif cell_id in nongaussian_intercept_smoke_cell_ids:
             expected_widget_state = "non_gaussian_intercept_recovery_smoke"
         elif (
@@ -13625,13 +14295,30 @@ def main() -> int:
             if phrase not in claim_boundary:
                 errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
         if row.get("widget_state") == "non_gaussian_recovery_only":
-            if "80-rep recovery grid" not in row.get("evidence_basis", ""):
+            if (
+                "80-rep recovery grid" not in row.get("evidence_basis", "")
+                and "80-rep local count-intercept recovery grid"
+                not in row.get("evidence_basis", "")
+                and "80-rep stronger-denominator count-intercept top-up"
+                not in row.get("evidence_basis", "")
+            ):
                 errors.append(f"{row_id}: recovery row must cite the 80-rep recovery grid")
-            if cell_id not in {
-                recovery.get("linked_cell_id", recovery.get("cell_id", ""))
-                for recovery in structured_re_count_slope_recovery_results_rows
-            }:
+            if cell_id not in nongaussian_recovery_result_cells:
                 errors.append(f"{row_id}: recovery row lacks a recovery-results match")
+        if row.get("widget_state") == "non_gaussian_recovery_caveat":
+            recovery_row = nongaussian_intercept_recovery_by_cell.get(cell_id)
+            if recovery_row is None:
+                errors.append(f"{row_id}: recovery caveat row lacks intercept recovery results")
+            elif recovery_row.get("widget_state") != "non_gaussian_recovery_caveat":
+                errors.append(f"{row_id}: recovery caveat row must match recovery sidecar state")
+            if "caveat" not in row.get("evidence_basis", ""):
+                errors.append(f"{row_id}: recovery caveat row must name the caveat")
+            if "pdHess" not in row.get("stability_signal", ""):
+                errors.append(f"{row_id}: recovery caveat row must name pdHess in stability_signal")
+            if "near-zero" not in row.get("stability_signal", ""):
+                errors.append(f"{row_id}: recovery caveat row must name near-zero estimates in stability_signal")
+            if "unsupported" not in row.get("next_gate", ""):
+                errors.append(f"{row_id}: recovery caveat next_gate must keep intervals unsupported")
         if row.get("widget_state") == "non_gaussian_intercept_recovery_smoke":
             if "local recovery-smoke sidecar evidence only" not in row.get("evidence_basis", ""):
                 errors.append(
@@ -13654,6 +14341,8 @@ def main() -> int:
             "structured-re-nongaussian-status-audit.tsv cell_ids must match all "
             "non-Gaussian q-series support-cell rows"
         )
+    for state in expected_nongaussian_state_counts:
+        nongaussian_state_counts.setdefault(state, 0)
     if nongaussian_state_counts != expected_nongaussian_state_counts:
         errors.append(
             "structured-re-nongaussian-status-audit.tsv widget_state counts "
@@ -13667,6 +14356,190 @@ def main() -> int:
             f"{nongaussian_family_counts}"
         )

+    # --- structured-re non-Gaussian recovery rollup ---
+    # DISPLAY evidence only: separates point-estimation recovery grade from
+    # interval/coverage inference. All linked rows remain unsupported/planned
+    # for interval/coverage status.
+    nongaussian_audit_by_cell = {
+        row.get("cell_id", ""): row for row in structured_re_nongaussian_status_audit_rows
+    }
+    expected_recovery_rollup_grade_counts = {
+        "cluster_confirmed_recovery_only": 3,
+        "local_recovery_only": 14,
+        "local_recovery_hessian_caveat": 1,
+    }
+    expected_cluster_recovery_cells = {
+        "qseries_phylo_poisson_q1_mu_intercept",
+        "qseries_phylo_nbinom2_q1_mu_intercept",
+        "qseries_spatial_nbinom2_q1_mu_intercept",
+    }
+    if len(structured_re_nongaussian_recovery_rollup_rows) != 18:
+        errors.append(
+            "structured-re-nongaussian-recovery-rollup.tsv: expected 18 "
+            "recovery rows; got "
+            f"{len(structured_re_nongaussian_recovery_rollup_rows)}"
+        )
+    seen_recovery_rollup_ids: set[str] = set()
+    seen_recovery_rollup_cells: set[str] = set()
+    recovery_rollup_grade_counts: dict[str, int] = {}
+    for row in structured_re_nongaussian_recovery_rollup_rows:
+        row_id = row.get("rollup_id", "<non-Gaussian recovery rollup>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_NONGAUSSIAN_RECOVERY_ROLLUP_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-nongaussian-recovery-rollup.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_NONGAUSSIAN_RECOVERY_ROLLUP_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_recovery_rollup_ids:
+            errors.append(f"duplicate non-Gaussian recovery rollup id: {row_id}")
+        seen_recovery_rollup_ids.add(row_id)
+        if cell_id in seen_recovery_rollup_cells:
+            errors.append(f"duplicate non-Gaussian recovery rollup cell_id: {cell_id}")
+        seen_recovery_rollup_cells.add(cell_id)
+        recovery_rollup_grade_counts[row.get("recovery_grade", "")] = (
+            recovery_rollup_grade_counts.get(row.get("recovery_grade", ""), 0) + 1
+        )
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+            continue
+        for rollup_field, support_field in (
+            ("family", "family"),
+            ("structure_provider", "structure_provider"),
+            ("endpoint_set", "endpoint_set"),
+            ("slope_class", "slope_class"),
+            ("linked_fit_status", "fit_status"),
+            ("linked_interval_status", "interval_status"),
+            ("linked_coverage_status", "coverage_status"),
+        ):
+            if row.get(rollup_field) != support_row.get(support_field):
+                errors.append(
+                    f"{row_id}: {rollup_field} must match support-cell "
+                    f"{support_field}={support_row.get(support_field)!r}"
+                )
+        if support_row.get("family_class") != "non_gaussian":
+            errors.append(f"{row_id}: linked support cell must be non_gaussian")
+        if support_row.get("fit_status") != "point_fit":
+            errors.append(f"{row_id}: rollup rows must link to point_fit cells")
+        if support_row.get("interval_status") != "unsupported":
+            errors.append(f"{row_id}: interval_status must remain unsupported")
+        if support_row.get("coverage_status") != "planned":
+            errors.append(f"{row_id}: coverage_status must remain planned")
+        if cell_id not in nongaussian_recovery_result_cells:
+            errors.append(f"{row_id}: rollup cell lacks a recovery result sidecar")
+        if not evidence_reference_exists(row.get("evidence_url", "")):
+            errors.append(f"{row_id}: evidence_url does not resolve")
+        support_source_text = (
+            support_row.get("evidence_url", "")
+            + " "
+            + support_row.get("claim_boundary", "")
+            + " "
+            + support_row.get("next_gate", "")
+        )
+        if "recovery" not in support_source_text:
+            errors.append(f"{row_id}: support-cell source text must mention recovery evidence")
+        for phrase in ("interval", "coverage"):
+            if phrase not in support_source_text:
+                errors.append(f"{row_id}: support-cell source text must mention {phrase}")
+        for stale_phrase in (
+            "add count fixture",
+            "add fixed-covariance count fixture evidence",
+            "add A-matrix count fixture evidence",
+            "add K-matrix count fixture evidence",
+            "add recovery evidence",
+        ):
+            if stale_phrase in support_row.get("next_gate", ""):
+                errors.append(
+                    f"{row_id}: support-cell next_gate still has stale text "
+                    f"{stale_phrase!r} after recovery rollup"
+                )
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "RECOVERY-ONLY",
+            "does NOT promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        recovery_grade = row.get("recovery_grade", "")
+        if cell_id in expected_cluster_recovery_cells:
+            if recovery_grade != "cluster_confirmed_recovery_only":
+                errors.append(
+                    f"{row_id}: formerly caveated top-up cells must be "
+                    "cluster_confirmed_recovery_only"
+                )
+            if "Rorqual SLURM job 14897050" not in row.get("evidence_basis", ""):
+                errors.append(f"{row_id}: cluster row must cite Rorqual job 14897050")
+            for source_phrase in (
+                "2026-06-29-count-intercept-topup-recovery-rorqual",
+                "Rorqual SLURM job 14897050",
+                "recovery-only",
+            ):
+                if source_phrase not in support_source_text:
+                    errors.append(
+                        f"{row_id}: support-cell source text must mention "
+                        f"{source_phrase!r}"
+                    )
+        elif recovery_grade == "cluster_confirmed_recovery_only":
+            errors.append(
+                f"{row_id}: only the three Rorqual top-up cells may be "
+                "cluster_confirmed_recovery_only"
+            )
+        if recovery_grade == "local_recovery_hessian_caveat":
+            if "pdHess false" not in row.get("hessian_signal", ""):
+                errors.append(f"{row_id}: Hessian caveat must name pdHess false")
+            audit_row = nongaussian_audit_by_cell.get(cell_id)
+            audit_text = ""
+            if audit_row is None:
+                errors.append(f"{row_id}: Hessian caveat lacks non-Gaussian audit row")
+            else:
+                audit_text = (
+                    audit_row.get("evidence_basis", "")
+                    + " "
+                    + audit_row.get("stability_signal", "")
+                    + " "
+                    + audit_row.get("claim_boundary", "")
+                    + " "
+                    + audit_row.get("next_gate", "")
+                )
+            if "2/80 pdHess false" not in support_source_text:
+                errors.append(
+                    f"{row_id}: support-cell source text must retain 2/80 pdHess false"
+                )
+            if "2/80 pdHess false" not in audit_text:
+                errors.append(
+                    f"{row_id}: non-Gaussian audit row must retain 2/80 pdHess false"
+                )
+        elif recovery_grade == "local_recovery_only":
+            if "local 80-rep" not in row.get("evidence_basis", ""):
+                errors.append(f"{row_id}: local recovery row must cite local 80-rep evidence")
+        elif recovery_grade not in expected_recovery_rollup_grade_counts:
+            errors.append(f"{row_id}: unexpected recovery_grade {recovery_grade!r}")
+        if "interval" not in row.get("next_gate", "") or "coverage" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must keep interval/coverage work separate")
+    if seen_recovery_rollup_cells != nongaussian_recovery_result_cells:
+        errors.append(
+            "structured-re-nongaussian-recovery-rollup.tsv cell_ids must "
+            "match the 18 non-Gaussian recovery-result cells"
+        )
+    for grade in expected_recovery_rollup_grade_counts:
+        recovery_rollup_grade_counts.setdefault(grade, 0)
+    if recovery_rollup_grade_counts != expected_recovery_rollup_grade_counts:
+        errors.append(
+            "structured-re-nongaussian-recovery-rollup.tsv recovery_grade "
+            f"counts must be {expected_recovery_rollup_grade_counts}; saw "
+            f"{recovery_rollup_grade_counts}"
+        )
+
     # --- structured-re count intercept local recovery smoke status ---
     # SMOKE/RECOVERY evidence only: exact q1 structured count mu intercept
     # fits for spatial/animal/relmat Poisson and NB2. No interval or coverage
@@ -14812,34 +15685,1196 @@ def main() -> int:
             "the four q1 mu one-slope provider rows"
         )

-    # --- structured-re count-slope recovery-results (local 80-rep recovery) ---
-    # RECOVERY evidence (convergence + SD bias/RMSE), NOT coverage: linked cells
-    # stay at coverage_status 'planned'.
-    seen_count_recovery_ids: set[str] = set()
-    if len(structured_re_count_slope_recovery_results_rows) != 8:
+    # --- structured-re Gaussian q1 mu one-slope interval admission/pregrid ---
+    # INTERVAL PROBE evidence only: these rows prove a tiny default-confint
+    # admission rung, not coverage or inference readiness. Animal keeps a
+    # visible boundary caveat.
+    expected_gaussian_mu_slope_admission = {
+        "gaussian_mu_slope_admission_phylo": {
+            "cell_id": "qseries_phylo_q1_mu_one_slope",
+            "provider": "phylo",
+            "source_task": "phylo_mu_slope",
+            "widget_state": "mu_slope_pregrid_planned",
+            "admission_status": "interval_probe_passed_pregrid_planned",
+        },
+        "gaussian_mu_slope_admission_spatial": {
+            "cell_id": "qseries_spatial_q1_mu_one_slope",
+            "provider": "spatial",
+            "source_task": "spatial_mu_slope",
+            "widget_state": "mu_slope_pregrid_planned",
+            "admission_status": "interval_probe_passed_pregrid_planned",
+        },
+        "gaussian_mu_slope_admission_animal": {
+            "cell_id": "qseries_animal_q1_mu_one_slope",
+            "provider": "animal",
+            "source_task": "animal_mu_slope",
+            "widget_state": "admission_blocked",
+            "admission_status": "boundary_caveat_before_denominator",
+        },
+        "gaussian_mu_slope_admission_relmat": {
+            "cell_id": "qseries_relmat_q1_mu_one_slope",
+            "provider": "relmat",
+            "source_task": "relmat_mu_slope",
+            "widget_state": "mu_slope_pregrid_planned",
+            "admission_status": "interval_probe_passed_pregrid_planned",
+        },
+    }
+    probe_summary_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-interval-probe-summary.tsv"
+    )
+    probe_results_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-interval-probe-results.tsv"
+    )
+    probe_seed_manifest_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-interval-probe-seed-manifest.tsv"
+    )
+    pregrid_seed_manifest_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-pregrid-seed-manifest.tsv"
+    )
+    pregrid_cell_manifest_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-pregrid-cell-manifest.tsv"
+    )
+    for path in (
+        probe_summary_path,
+        probe_results_path,
+        probe_seed_manifest_path,
+        pregrid_seed_manifest_path,
+        pregrid_cell_manifest_path,
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "sessionInfo.txt",
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_INTERVAL_PROBE_ARTIFACT_DIR
+        / "git-sha.txt",
+    ):
+        if not path.exists():
+            errors.append(
+                "Gaussian mu-slope interval probe artifact is missing: "
+                f"{path.relative_to(ROOT)}"
+            )
+    probe_summary_rows = read_tsv(probe_summary_path) if probe_summary_path.exists() else []
+    probe_results_rows = read_tsv(probe_results_path) if probe_results_path.exists() else []
+    pregrid_seed_manifest_rows = (
+        read_tsv(pregrid_seed_manifest_path)
+        if pregrid_seed_manifest_path.exists()
+        else []
+    )
+    pregrid_cell_manifest_rows = (
+        read_tsv(pregrid_cell_manifest_path)
+        if pregrid_cell_manifest_path.exists()
+        else []
+    )
+    if len(probe_results_rows) != 16:
         errors.append(
-            "structured-re-count-slope-recovery-results.tsv: expected 8 rows "
-            f"(4 providers x 2 families); got "
-            f"{len(structured_re_count_slope_recovery_results_rows)}"
+            "structured-re Gaussian mu-slope interval probe results must have "
+            f"16 rows; saw {len(probe_results_rows)}"
         )
-    for row in structured_re_count_slope_recovery_results_rows:
-        row_id = row.get("recovery_id", "<count recovery row>")
-        if set(row.keys()) != set(STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS_FIELDS):
+    if len(probe_summary_rows) != 8:
+        errors.append(
+            "structured-re Gaussian mu-slope interval probe summary must have "
+            f"8 rows; saw {len(probe_summary_rows)}"
+        )
+    if len(pregrid_seed_manifest_rows) != 150:
+        errors.append(
+            "structured-re Gaussian mu-slope pregrid seed manifest must have "
+            f"150 rows; saw {len(pregrid_seed_manifest_rows)}"
+        )
+    if len(pregrid_cell_manifest_rows) != 1050:
+        errors.append(
+            "structured-re Gaussian mu-slope pregrid cell manifest must have "
+            f"1050 rows; saw {len(pregrid_cell_manifest_rows)}"
+        )
+    expected_probe_source = (
+        "docs/dev-log/simulation-artifacts/"
+        "2026-06-29-gaussian-mu-slope-interval-probe-local/"
+        "structured-re-gaussian-mu-slope-interval-probe-summary.tsv"
+    )
+    observed_probe_by_cell: dict[str, dict[str, int]] = {}
+    for summary in probe_summary_rows:
+        cell_id = summary.get("cell_id", "")
+        acc = observed_probe_by_cell.setdefault(
+            cell_id,
+            {
+                "n_targets": 0,
+                "n_probe_replicates": 0,
+                "n_interval_rows": 0,
+                "n_converged_rows": 0,
+                "n_pdhess_rows": 0,
+                "n_finite_intervals": 0,
+                "n_wald_boundary": 0,
+                "n_covered": 0,
+                "n_lower_miss": 0,
+                "n_upper_miss": 0,
+            },
+        )
+        acc["n_targets"] += 1
+        try:
+            acc["n_probe_replicates"] = max(
+                acc["n_probe_replicates"],
+                int(summary.get("n_probe_replicates", "")),
+            )
+        except ValueError:
             errors.append(
-                f"{row_id}: structured-re-count-slope-recovery-results.tsv "
+                "structured-re Gaussian mu-slope probe summary has "
+                f"non-integer 'n_probe_replicates' for {cell_id}"
+            )
+        numeric_map = {
+            "n_interval_rows": "n_probe_replicates",
+            "n_converged_rows": "converged",
+            "n_pdhess_rows": "pdHess",
+            "n_finite_intervals": "finite_interval",
+            "n_wald_boundary": "n_wald_boundary",
+            "n_covered": "covered",
+            "n_lower_miss": "lower_miss",
+            "n_upper_miss": "upper_miss",
+        }
+        for out_field, source_field in numeric_map.items():
+            try:
+                acc[out_field] += int(summary.get(source_field, ""))
+            except ValueError:
+                errors.append(
+                    "structured-re Gaussian mu-slope probe summary has "
+                    f"non-integer {source_field!r} for {cell_id}"
+                )
+    if len(structured_re_gaussian_mu_slope_admission_audit_rows) != 4:
+        errors.append(
+            "structured-re-gaussian-mu-slope-admission-audit.tsv: expected 4 rows"
+        )
+    seen_gaussian_mu_slope_admission_ids: set[str] = set()
+    seen_gaussian_mu_slope_admission_cells: set[str] = set()
+    for row in structured_re_gaussian_mu_slope_admission_audit_rows:
+        row_id = row.get("audit_id", "<Gaussian mu-slope admission audit>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_ADMISSION_AUDIT_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-admission-audit.tsv "
                 "fields do not match the contract"
             )
-        for field in STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS_FIELDS:
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_ADMISSION_AUDIT_FIELDS:
             if not row.get(field):
                 errors.append(f"{row_id}: {field} is empty")
-        if row_id in seen_count_recovery_ids:
-            errors.append(f"duplicate count recovery id: {row_id}")
-        seen_count_recovery_ids.add(row_id)
-        if row.get("linked_coverage_status") != "planned":
-            errors.append(
-                f"{row_id}: linked_coverage_status must be 'planned' "
-                "(recovery evidence does not promote coverage_status)"
-            )
+        if row_id in seen_gaussian_mu_slope_admission_ids:
+            errors.append(f"duplicate Gaussian mu-slope admission id: {row_id}")
+        seen_gaussian_mu_slope_admission_ids.add(row_id)
+        if cell_id in seen_gaussian_mu_slope_admission_cells:
+            errors.append(f"duplicate Gaussian mu-slope admission cell_id: {cell_id}")
+        seen_gaussian_mu_slope_admission_cells.add(cell_id)
+        expected = expected_gaussian_mu_slope_admission.get(row_id)
+        if expected is None:
+            errors.append(f"{row_id}: unexpected Gaussian mu-slope admission row")
+            continue
+        for field, expected_value in expected.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+            continue
+        for field, expected_value in {
+            "family_class": "gaussian",
+            "family": "gaussian()",
+            "dimension_pattern": "q1",
+            "endpoint_set": "mu",
+            "slope_class": "independent_one_slope",
+            "fit_status": "point_fit",
+            "interval_status": "planned",
+            "coverage_status": "planned",
+        }.items():
+            if support_row.get(field) != expected_value:
+                errors.append(f"{row_id}: linked support-cell {field} must be {expected_value!r}")
+        if support_row.get("structure_provider") != row.get("provider"):
+            errors.append(
+                f"{row_id}: provider must match support-cell structure_provider"
+            )
+        if row.get("source_interval_probe") != expected_probe_source:
+            errors.append(f"{row_id}: source_interval_probe must be {expected_probe_source!r}")
+        observed = observed_probe_by_cell.get(cell_id)
+        if observed is None:
+            errors.append(f"{row_id}: missing probe summary rows for {cell_id}")
+        else:
+            for field, observed_value in observed.items():
+                try:
+                    actual = int(row.get(field, ""))
+                except ValueError:
+                    errors.append(f"{row_id}: {field} must be an integer")
+                    continue
+                if actual != observed_value:
+                    errors.append(
+                        f"{row_id}: {field} must match probe summary {observed_value}"
+                    )
+        if row.get("linked_interval_status") != "planned" or row.get("linked_coverage_status") != "planned":
+            errors.append(f"{row_id}: linked interval/coverage statuses must remain planned")
+        if row.get("promotion_decision") != "do_not_promote":
+            errors.append(f"{row_id}: promotion_decision must be do_not_promote")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "interval probe only",
+            "no coverage-evaluable denominator evidence",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+    if seen_gaussian_mu_slope_admission_ids != set(expected_gaussian_mu_slope_admission):
+        errors.append(
+            "structured-re-gaussian-mu-slope-admission-audit.tsv row ids must be "
+            + ", ".join(sorted(expected_gaussian_mu_slope_admission))
+        )
+
+    expected_pregrid_ids = {
+        f"gaussian_mu_slope_pregrid_{provider}_{endpoint}"
+        for provider in ("phylo", "spatial", "animal", "relmat")
+        for endpoint in ("mu_intercept", "mu_x")
+    }
+    seen_pregrid_ids: set[str] = set()
+    eligible_pregrid_rows = 0
+    visible_holdout_rows = 0
+    planned_pregrid_cells = 0
+    for row in structured_re_gaussian_mu_slope_coverage_pregrid_dry_run_rows:
+        row_id = row.get("pregrid_id", "<Gaussian mu-slope pregrid>")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_DRY_RUN_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_DRY_RUN_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        seen_pregrid_ids.add(row_id)
+        if row.get("source_probe") != expected_probe_source:
+            errors.append(f"{row_id}: source_probe must be {expected_probe_source!r}")
+        if row.get("interval_methods") != "confint_default_bias_t_location_sd":
+            errors.append(f"{row_id}: interval_methods must use default location-SD confint")
+        if row.get("execution_status") != "not_executed":
+            errors.append(f"{row_id}: execution_status must be not_executed")
+        if row.get("coverage_evaluable") != "FALSE":
+            errors.append(f"{row_id}: coverage_evaluable must remain FALSE")
+        if row.get("coverage_status") != "not_evaluated":
+            errors.append(f"{row_id}: coverage_status must be not_evaluated")
+        if row.get("interval_claim_status") != "diagnostic_only":
+            errors.append(f"{row_id}: interval_claim_status must be diagnostic_only")
+        if row.get("nominal_mcse_at_150") != "0.017795":
+            errors.append(f"{row_id}: nominal_mcse_at_150 must be 0.017795")
+        if row.get("replicates_for_mcse_threshold") != "475":
+            errors.append(f"{row_id}: replicates_for_mcse_threshold must be 475")
+        try:
+            planned_cells = int(row.get("planned_cells", ""))
+        except ValueError:
+            errors.append(f"{row_id}: planned_cells must be an integer")
+            planned_cells = 0
+        planned_pregrid_cells += planned_cells
+        if row.get("current_denominator_action") == "eligible_for_pregrid_with_retention":
+            eligible_pregrid_rows += 1
+            if row.get("denominator_role") != "pregrid_target" or planned_cells != 150:
+                errors.append(f"{row_id}: eligible target must have 150 planned cells")
+        elif row.get("current_denominator_action") == "visible_holdout_until_boundary_reconciled":
+            visible_holdout_rows += 1
+            if row.get("denominator_role") != "visible_holdout" or planned_cells != 0:
+                errors.append(f"{row_id}: visible holdout must have 0 planned cells")
+        else:
+            errors.append(f"{row_id}: unexpected current_denominator_action")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "coverage pre-grid dry-run only",
+            "no coverage-evaluable denominator evidence",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "DRAC execution",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+    if len(structured_re_gaussian_mu_slope_coverage_pregrid_dry_run_rows) != 8:
+        errors.append(
+            "structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv: "
+            f"expected 8 rows; got {len(structured_re_gaussian_mu_slope_coverage_pregrid_dry_run_rows)}"
+        )
+    if seen_pregrid_ids != expected_pregrid_ids:
+        errors.append(
+            "structured-re-gaussian-mu-slope-coverage-pregrid-dry-run.tsv row ids "
+            "must cover four providers x two mu targets"
+        )
+    if eligible_pregrid_rows != 7 or visible_holdout_rows != 1:
+        errors.append(
+            "Gaussian mu-slope pregrid must have seven eligible targets and "
+            f"one visible holdout; saw {eligible_pregrid_rows} eligible and "
+            f"{visible_holdout_rows} holdout"
+        )
+    if planned_pregrid_cells != 1050:
+        errors.append(
+            "Gaussian mu-slope pregrid planned_cells must total 1050; saw "
+            f"{planned_pregrid_cells}"
+        )
+
+    # --- structured-re Gaussian q1 mu one-slope executed SR150 pregrid ---
+    # NEGATIVE admission evidence: the retained-outcome pregrid ran and all
+    # four cells remain blocked; linked support cells stay planned/planned.
+    pregrid_replicates_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-coverage-pregrid-replicates.tsv"
+    )
+    pregrid_target_summary_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-coverage-pregrid-target-summary.tsv"
+    )
+    pregrid_provider_summary_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-coverage-pregrid-provider-summary.tsv"
+    )
+    pregrid_fit_status_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-coverage-pregrid-fit-status.tsv"
+    )
+    for path in (
+        pregrid_replicates_path,
+        pregrid_target_summary_path,
+        pregrid_provider_summary_path,
+        pregrid_fit_status_path,
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "sessionInfo.txt",
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_ARTIFACT_DIR
+        / "git-sha.txt",
+    ):
+        if not path.exists():
+            errors.append(
+                "Gaussian mu-slope SR150 pregrid artifact is missing: "
+                f"{path.relative_to(ROOT)}"
+            )
+    pregrid_replicate_rows = (
+        read_tsv(pregrid_replicates_path) if pregrid_replicates_path.exists() else []
+    )
+    pregrid_target_summary_rows = (
+        read_tsv(pregrid_target_summary_path)
+        if pregrid_target_summary_path.exists()
+        else []
+    )
+    pregrid_provider_summary_rows = (
+        read_tsv(pregrid_provider_summary_path)
+        if pregrid_provider_summary_path.exists()
+        else []
+    )
+    pregrid_fit_status_rows = (
+        read_tsv(pregrid_fit_status_path) if pregrid_fit_status_path.exists() else []
+    )
+    if len(pregrid_fit_status_rows) != 600:
+        errors.append(
+            "Gaussian mu-slope SR150 pregrid fit-status artifact must have "
+            f"600 rows; saw {len(pregrid_fit_status_rows)}"
+        )
+    if len(pregrid_replicate_rows) != 1200:
+        errors.append(
+            "Gaussian mu-slope SR150 pregrid replicate artifact must have "
+            f"1200 rows; saw {len(pregrid_replicate_rows)}"
+        )
+    if len(pregrid_target_summary_rows) != 8:
+        errors.append(
+            "Gaussian mu-slope SR150 pregrid target-summary artifact must have "
+            f"8 rows; saw {len(pregrid_target_summary_rows)}"
+        )
+    if len(pregrid_provider_summary_rows) != 4:
+        errors.append(
+            "Gaussian mu-slope SR150 pregrid provider-summary artifact must have "
+            f"4 rows; saw {len(pregrid_provider_summary_rows)}"
+        )
+    expected_pregrid_result_ids = {
+        f"gaussian_mu_slope_pregrid_result_{provider}"
+        for provider in ("phylo", "spatial", "animal", "relmat")
+    }
+    expected_corrected_mu_slope_pregrid = {
+        "animal": {
+            "n_usable_intervals": "122",
+            "min_finite_interval_rate": "0.8133",
+            "min_coverage_all": "0.8133",
+            "max_coverage_all": "0.8133",
+            "n_lower_miss": "0",
+            "n_upper_miss": "0",
+        },
+        "phylo": {
+            "n_usable_intervals": "291",
+            "min_finite_interval_rate": "0.9400",
+            "min_coverage_all": "0.9400",
+            "max_coverage_all": "0.9467",
+            "n_lower_miss": "3",
+            "n_upper_miss": "5",
+        },
+        "relmat": {
+            "n_usable_intervals": "297",
+            "min_finite_interval_rate": "0.9800",
+            "min_coverage_all": "0.9533",
+            "max_coverage_all": "0.9733",
+            "n_lower_miss": "2",
+            "n_upper_miss": "6",
+        },
+        "spatial": {
+            "n_usable_intervals": "297",
+            "min_finite_interval_rate": "0.9800",
+            "min_coverage_all": "0.9467",
+            "max_coverage_all": "0.9600",
+            "n_lower_miss": "6",
+            "n_upper_miss": "5",
+        },
+    }
+    provider_summary_by_cell = {
+        row.get("cell_id", ""): row for row in pregrid_provider_summary_rows
+    }
+    if len(structured_re_gaussian_mu_slope_coverage_pregrid_results_rows) != 4:
+        errors.append(
+            "structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv: "
+            f"expected 4 rows; got {len(structured_re_gaussian_mu_slope_coverage_pregrid_results_rows)}"
+        )
+    seen_pregrid_result_ids: set[str] = set()
+    for row in structured_re_gaussian_mu_slope_coverage_pregrid_results_rows:
+        row_id = row.get("result_id", "<Gaussian mu-slope SR150 result>")
+        cell_id = row.get("cell_id", "")
+        provider = row.get("provider", "")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_RESULTS_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_COVERAGE_PREGRID_RESULTS_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        seen_pregrid_result_ids.add(row_id)
+        expected = expected_gaussian_mu_slope_admission.get(
+            row_id.replace("pregrid_result", "admission")
+        )
+        if expected is None:
+            errors.append(f"{row_id}: unexpected Gaussian mu-slope SR150 result row")
+            continue
+        if cell_id != expected["cell_id"] or provider != expected["provider"]:
+            errors.append(f"{row_id}: cell_id/provider must match the q1 mu provider row")
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+            continue
+        for field, expected_value in {
+            "family_class": "gaussian",
+            "family": "gaussian()",
+            "dimension_pattern": "q1",
+            "endpoint_set": "mu",
+            "slope_class": "independent_one_slope",
+            "fit_status": "point_fit",
+            "interval_status": "planned",
+            "coverage_status": "planned",
+        }.items():
+            if support_row.get(field) != expected_value:
+                errors.append(f"{row_id}: linked support-cell {field} must be {expected_value!r}")
+        if row.get("widget_state") != "mu_slope_pregrid_blocked":
+            errors.append(f"{row_id}: widget_state must be mu_slope_pregrid_blocked")
+        if row.get("target_gate") != "pregrid_blocked":
+            errors.append(f"{row_id}: target_gate must be pregrid_blocked")
+        if row.get("n_rep") != "150":
+            errors.append(f"{row_id}: n_rep must be 150")
+        if row.get("promotion_decision") != "do_not_promote":
+            errors.append(f"{row_id}: promotion_decision must be do_not_promote")
+        if row.get("linked_interval_status") != "planned" or row.get("linked_coverage_status") != "planned":
+            errors.append(f"{row_id}: linked interval/coverage statuses must remain planned")
+        corrected_expected = expected_corrected_mu_slope_pregrid.get(provider)
+        if corrected_expected is None:
+            errors.append(f"{row_id}: provider is not in corrected pregrid expectation map")
+        else:
+            for field, expected_value in corrected_expected.items():
+                if row.get(field) != expected_value:
+                    errors.append(
+                        f"{row_id}: {field} must be {expected_value!r} "
+                        "after the q1 mu-slope small-sample correction fix"
+                    )
+        if row.get("artifact_dir") != "docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-coverage-pregrid-local":
+            errors.append(f"{row_id}: artifact_dir must point to the SR150 local artifact directory")
+        provider_summary = provider_summary_by_cell.get(cell_id)
+        if provider_summary is None:
+            errors.append(f"{row_id}: missing provider-summary artifact row")
+        else:
+            field_map = {
+                "n_targets": "n_targets",
+                "n_eligible_targets": "n_eligible_targets",
+                "n_holdout_targets": "n_holdout_targets",
+                "n_total_target_replicates": "n_total_target_replicates",
+                "n_usable_intervals": "n_usable_intervals",
+                "n_lower_miss": "n_lower_miss",
+                "n_upper_miss": "n_upper_miss",
+                "target_gate": "target_gate",
+                "widget_state": "widget_state",
+            }
+            for result_field, summary_field in field_map.items():
+                if row.get(result_field) != provider_summary.get(summary_field):
+                    errors.append(
+                        f"{row_id}: {result_field} must match provider-summary "
+                        f"{provider_summary.get(summary_field)!r}"
+                    )
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "SR150 coverage pregrid only",
+            "no MCSE-qualified coverage",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "Resolve finite-interval, boundary, or low retained-coverage failures" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must name finite/boundary/coverage failures")
+    if seen_pregrid_result_ids != expected_pregrid_result_ids:
+        errors.append(
+            "structured-re-gaussian-mu-slope-coverage-pregrid-results.tsv row ids "
+            "must cover the four q1 mu provider rows"
+        )
+
+    # --- structured-re Gaussian q1 mu one-slope hybrid boundary audit ---
+    # ROUTING evidence only: combines the SR150 Wald rows with the repaired
+    # endpoint-profile boundary rows. It can move clean rows to a top-up gate,
+    # but it cannot promote interval/coverage status.
+    expected_hybrid_boundary = {
+        "gaussian_mu_slope_hybrid_boundary_animal": {
+            "cell_id": "qseries_animal_q1_mu_one_slope",
+            "provider": "animal",
+            "n_eligible_target_replicates": "150",
+            "n_usable_hybrid_intervals": "147",
+            "finite_interval_rate": "0.9800",
+            "n_profile_boundary_rows": "27",
+            "n_profile_finite": "25",
+            "n_profile_failed": "2",
+            "n_profile_covered": "10",
+            "n_covered": "132",
+            "hybrid_coverage_all": "0.8800",
+            "hybrid_coverage_usable": "0.8980",
+            "hybrid_coverage_mcse": "0.026533",
+            "n_lower_miss": "0",
+            "n_upper_miss": "15",
+            "miss_balance": "lower=0 upper=15",
+            "widget_state": "mu_slope_pregrid_blocked",
+            "admission_status": "hybrid_boundary_hard_blocked",
+            "inference_signal": "not_inference_ready_hybrid_coverage_hard_negative",
+        },
+        "gaussian_mu_slope_hybrid_boundary_phylo": {
+            "cell_id": "qseries_phylo_q1_mu_one_slope",
+            "provider": "phylo",
+            "n_eligible_target_replicates": "300",
+            "n_usable_hybrid_intervals": "299",
+            "finite_interval_rate": "0.9967",
+            "n_profile_boundary_rows": "9",
+            "n_profile_finite": "8",
+            "n_profile_failed": "1",
+            "n_profile_covered": "1",
+            "n_covered": "284",
+            "hybrid_coverage_all": "0.9467",
+            "hybrid_coverage_usable": "0.9498",
+            "hybrid_coverage_mcse": "0.012973",
+            "n_lower_miss": "3",
+            "n_upper_miss": "12",
+            "miss_balance": "lower=3 upper=12",
+            "widget_state": "topup_required",
+            "admission_status": "hybrid_boundary_topup_candidate",
+            "inference_signal": "not_inference_ready_hybrid_sr150_mcse_above_0.01",
+        },
+        "gaussian_mu_slope_hybrid_boundary_relmat": {
+            "cell_id": "qseries_relmat_q1_mu_one_slope",
+            "provider": "relmat",
+            "n_eligible_target_replicates": "300",
+            "n_usable_hybrid_intervals": "299",
+            "finite_interval_rate": "0.9967",
+            "n_profile_boundary_rows": "3",
+            "n_profile_finite": "2",
+            "n_profile_failed": "1",
+            "n_profile_covered": "0",
+            "n_covered": "289",
+            "hybrid_coverage_all": "0.9633",
+            "hybrid_coverage_usable": "0.9666",
+            "hybrid_coverage_mcse": "0.010851",
+            "n_lower_miss": "2",
+            "n_upper_miss": "8",
+            "miss_balance": "lower=2 upper=8",
+            "widget_state": "topup_required",
+            "admission_status": "hybrid_boundary_topup_candidate",
+            "inference_signal": "not_inference_ready_hybrid_sr150_mcse_above_0.01",
+        },
+        "gaussian_mu_slope_hybrid_boundary_spatial": {
+            "cell_id": "qseries_spatial_q1_mu_one_slope",
+            "provider": "spatial",
+            "n_eligible_target_replicates": "300",
+            "n_usable_hybrid_intervals": "300",
+            "finite_interval_rate": "1.0000",
+            "n_profile_boundary_rows": "3",
+            "n_profile_finite": "3",
+            "n_profile_failed": "0",
+            "n_profile_covered": "0",
+            "n_covered": "286",
+            "hybrid_coverage_all": "0.9533",
+            "hybrid_coverage_usable": "0.9533",
+            "hybrid_coverage_mcse": "0.012178",
+            "n_lower_miss": "6",
+            "n_upper_miss": "8",
+            "miss_balance": "lower=6 upper=8",
+            "widget_state": "topup_required",
+            "admission_status": "hybrid_boundary_topup_candidate",
+            "inference_signal": "not_inference_ready_hybrid_sr150_mcse_above_0.01",
+        },
+    }
+    expected_hybrid_pregrid_source = (
+        "docs/dev-log/simulation-artifacts/"
+        "2026-06-29-gaussian-mu-slope-coverage-pregrid-local/"
+        "structured-re-gaussian-mu-slope-coverage-pregrid-replicates.tsv"
+    )
+    expected_hybrid_boundary_source = (
+        "docs/dev-log/simulation-artifacts/"
+        "2026-06-29-gaussian-mu-slope-boundary-profile-diagnostic-local/"
+        "structured-re-gaussian-mu-slope-boundary-profile-diagnostic-detail.tsv"
+    )
+    if len(structured_re_gaussian_mu_slope_hybrid_boundary_audit_rows) != 4:
+        errors.append(
+            "structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv: "
+            f"expected 4 rows; got {len(structured_re_gaussian_mu_slope_hybrid_boundary_audit_rows)}"
+        )
+    seen_hybrid_boundary_ids: set[str] = set()
+    for row in structured_re_gaussian_mu_slope_hybrid_boundary_audit_rows:
+        row_id = row.get("audit_id", "<Gaussian mu-slope hybrid boundary audit>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_BOUNDARY_AUDIT_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_BOUNDARY_AUDIT_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        seen_hybrid_boundary_ids.add(row_id)
+        expected = expected_hybrid_boundary.get(row_id)
+        if expected is None:
+            errors.append(f"{row_id}: unexpected Gaussian mu-slope hybrid boundary row")
+            continue
+        for field, expected_value in expected.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        for field, expected_value in {
+            "source_pregrid": expected_hybrid_pregrid_source,
+            "source_boundary_profile": expected_hybrid_boundary_source,
+            "stability_signal": "hybrid_wald_plus_endpoint_profile_boundary_accounting",
+            "linked_fit_status": "point_fit",
+            "linked_interval_status": "planned",
+            "linked_coverage_status": "planned",
+            "promotion_decision": "do_not_promote",
+            "artifact_dir": "docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-boundary-profile-diagnostic-local",
+        }.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            for field, expected_value in {
+                "family_class": "gaussian",
+                "family": "gaussian()",
+                "dimension_pattern": "q1",
+                "endpoint_set": "mu",
+                "slope_class": "independent_one_slope",
+                "fit_status": "point_fit",
+                "interval_status": "planned",
+                "coverage_status": "planned",
+            }.items():
+                if support_row.get(field) != expected_value:
+                    errors.append(f"{row_id}: linked support-cell {field} must be {expected_value!r}")
+        if row.get("widget_state") == "topup_required" and "Top up the hybrid" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: top-up candidate next_gate must name the hybrid top-up")
+        if row.get("widget_state") == "mu_slope_pregrid_blocked" and "hard negative" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: hard-blocked next_gate must name the hard negative")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "hybrid boundary audit only",
+            "SR150 evidence only",
+            "no MCSE-qualified coverage",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+    if seen_hybrid_boundary_ids != set(expected_hybrid_boundary):
+        errors.append(
+            "structured-re-gaussian-mu-slope-hybrid-boundary-audit.tsv row ids "
+            "must cover the four q1 mu provider rows"
+        )
+
+    # --- structured-re Gaussian q1 mu one-slope hybrid SR475 audit ---
+    # STRONGER NEGATIVE evidence: top-up reaches the MCSE gate for phylo,
+    # relmat, and spatial, but upper-tail miss imbalance and one profile
+    # failure per row block inference-ready promotion.
+    expected_hybrid_sr475 = {
+        "gaussian_mu_slope_hybrid_sr475_phylo": {
+            "cell_id": "qseries_phylo_q1_mu_one_slope",
+            "provider": "phylo",
+            "n_seed_replicates": "475",
+            "n_eligible_target_replicates": "950",
+            "n_usable_hybrid_intervals": "949",
+            "finite_interval_rate": "0.9989",
+            "n_profile_boundary_rows": "27",
+            "n_profile_finite": "26",
+            "n_profile_failed": "1",
+            "n_profile_covered": "6",
+            "n_covered": "913",
+            "hybrid_coverage_all": "0.9611",
+            "hybrid_coverage_usable": "0.9621",
+            "hybrid_coverage_mcse": "0.006277",
+            "n_lower_miss": "5",
+            "n_upper_miss": "31",
+            "miss_balance": "lower=5 upper=31",
+            "worst_target": "mu:x",
+            "worst_target_coverage": "0.9537",
+            "worst_target_mcse": "0.009643",
+            "worst_target_miss_balance": "lower=1 upper=20",
+        },
+        "gaussian_mu_slope_hybrid_sr475_relmat": {
+            "cell_id": "qseries_relmat_q1_mu_one_slope",
+            "provider": "relmat",
+            "n_seed_replicates": "475",
+            "n_eligible_target_replicates": "950",
+            "n_usable_hybrid_intervals": "949",
+            "finite_interval_rate": "0.9989",
+            "n_profile_boundary_rows": "5",
+            "n_profile_finite": "4",
+            "n_profile_failed": "1",
+            "n_profile_covered": "0",
+            "n_covered": "926",
+            "hybrid_coverage_all": "0.9747",
+            "hybrid_coverage_usable": "0.9758",
+            "hybrid_coverage_mcse": "0.005091",
+            "n_lower_miss": "3",
+            "n_upper_miss": "20",
+            "miss_balance": "lower=3 upper=20",
+            "worst_target": "mu:(Intercept)",
+            "worst_target_coverage": "0.9621",
+            "worst_target_mcse": "0.008761",
+            "worst_target_miss_balance": "lower=2 upper=16",
+        },
+        "gaussian_mu_slope_hybrid_sr475_spatial": {
+            "cell_id": "qseries_spatial_q1_mu_one_slope",
+            "provider": "spatial",
+            "n_seed_replicates": "475",
+            "n_eligible_target_replicates": "950",
+            "n_usable_hybrid_intervals": "949",
+            "finite_interval_rate": "0.9989",
+            "n_profile_boundary_rows": "10",
+            "n_profile_finite": "9",
+            "n_profile_failed": "1",
+            "n_profile_covered": "0",
+            "n_covered": "912",
+            "hybrid_coverage_all": "0.9600",
+            "hybrid_coverage_usable": "0.9610",
+            "hybrid_coverage_mcse": "0.006358",
+            "n_lower_miss": "15",
+            "n_upper_miss": "22",
+            "miss_balance": "lower=15 upper=22",
+            "worst_target": "mu:(Intercept)",
+            "worst_target_coverage": "0.9600",
+            "worst_target_mcse": "0.008991",
+            "worst_target_miss_balance": "lower=8 upper=11",
+        },
+    }
+    expected_hybrid_sr475_sources = {
+        "source_pregrid": expected_hybrid_pregrid_source,
+        "source_boundary_profile": expected_hybrid_boundary_source,
+        "source_topup_pregrid": (
+            "docs/dev-log/simulation-artifacts/"
+            "2026-06-29-gaussian-mu-slope-topup-sr475-local/"
+            "structured-re-gaussian-mu-slope-coverage-pregrid-replicates.tsv"
+        ),
+        "source_topup_boundary_profile": (
+            "docs/dev-log/simulation-artifacts/"
+            "2026-06-29-gaussian-mu-slope-topup-boundary-profile-local/"
+            "structured-re-gaussian-mu-slope-boundary-profile-diagnostic-detail.tsv"
+        ),
+        "artifact_dir": (
+            "docs/dev-log/simulation-artifacts/"
+            "2026-06-29-gaussian-mu-slope-topup-sr475-local"
+        ),
+    }
+    if len(structured_re_gaussian_mu_slope_hybrid_sr475_audit_rows) != 3:
+        errors.append(
+            "structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv: "
+            f"expected 3 rows; got {len(structured_re_gaussian_mu_slope_hybrid_sr475_audit_rows)}"
+        )
+    seen_hybrid_sr475_ids: set[str] = set()
+    for row in structured_re_gaussian_mu_slope_hybrid_sr475_audit_rows:
+        row_id = row.get("audit_id", "<Gaussian mu-slope hybrid SR475 audit>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_SR475_AUDIT_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_HYBRID_SR475_AUDIT_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        seen_hybrid_sr475_ids.add(row_id)
+        expected = expected_hybrid_sr475.get(row_id)
+        if expected is None:
+            errors.append(f"{row_id}: unexpected Gaussian mu-slope hybrid SR475 row")
+            continue
+        for field, expected_value in expected.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        for field, expected_value in expected_hybrid_sr475_sources.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        for field, expected_value in {
+            "widget_state": "mcse_met_upper_tail_blocked",
+            "admission_status": "hybrid_sr475_upper_tail_blocked",
+            "stability_signal": "hybrid_sr475_all_fits_converged_pdhess_true",
+            "inference_signal": "not_inference_ready_hybrid_sr475_upper_tail_imbalance_or_profile_failure",
+            "linked_fit_status": "point_fit",
+            "linked_interval_status": "planned",
+            "linked_coverage_status": "planned",
+            "promotion_decision": "do_not_promote",
+        }.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            for field, expected_value in {
+                "family_class": "gaussian",
+                "family": "gaussian()",
+                "dimension_pattern": "q1",
+                "endpoint_set": "mu",
+                "slope_class": "independent_one_slope",
+                "fit_status": "point_fit",
+                "interval_status": "planned",
+                "coverage_status": "planned",
+            }.items():
+                if support_row.get(field) != expected_value:
+                    errors.append(f"{row_id}: linked support-cell {field} must be {expected_value!r}")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "hybrid SR475 audit only",
+            "MCSE-qualified retained denominator",
+            "upper-tail miss imbalance remains",
+            "no inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "Fisher/Rose review" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must require Fisher/Rose review")
+    if seen_hybrid_sr475_ids != set(expected_hybrid_sr475):
+        errors.append(
+            "structured-re-gaussian-mu-slope-hybrid-sr475-audit.tsv row ids "
+            "must cover phylo, relmat, and spatial q1 mu rows"
+        )
+
+    hybrid_sr475_target_summary_path = (
+        ROOT
+        / "docs"
+        / "dev-log"
+        / "simulation-artifacts"
+        / "2026-06-29-gaussian-mu-slope-topup-sr475-local"
+        / "structured-re-gaussian-mu-slope-hybrid-sr475-target-summary.tsv"
+    )
+    if not hybrid_sr475_target_summary_path.exists():
+        errors.append("Gaussian mu-slope hybrid SR475 target summary artifact is missing")
+    else:
+        hybrid_sr475_target_rows = read_tsv(hybrid_sr475_target_summary_path)
+        if len(hybrid_sr475_target_rows) != 6:
+            errors.append(
+                "Gaussian mu-slope hybrid SR475 target summary must have 6 rows; "
+                f"got {len(hybrid_sr475_target_rows)}"
+            )
+        for row in hybrid_sr475_target_rows:
+            row_id = f"{row.get('provider')}:{row.get('endpoint_member')}"
+            if row.get("target_signal") != "mcse_met":
+                errors.append(f"{row_id}: SR475 target_signal must be mcse_met")
+            if row.get("n_eligible_target_replicates") != "475":
+                errors.append(f"{row_id}: SR475 target denominator must be 475")
+
+    # --- structured-re Gaussian q1 mu one-slope boundary profile diagnostic ---
+    # NEGATIVE interval evidence: retained SR150 boundary/non-Wald rows were
+    # retried with endpoint profiles. The zero-lower-boundary fix rescues many
+    # lower endpoints, but upper misses and remaining profile failures block
+    # promotion.
+    boundary_profile_detail_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-boundary-profile-diagnostic-detail.tsv"
+    )
+    boundary_profile_summary_path = (
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_ARTIFACT_DIR
+        / "structured-re-gaussian-mu-slope-boundary-profile-diagnostic-summary.tsv"
+    )
+    for path in (
+        boundary_profile_detail_path,
+        boundary_profile_summary_path,
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_ARTIFACT_DIR
+        / "sessionInfo.txt",
+        STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_ARTIFACT_DIR
+        / "git-sha.txt",
+    ):
+        if not path.exists():
+            errors.append(
+                "Gaussian mu-slope boundary profile diagnostic artifact is missing: "
+                f"{path.relative_to(ROOT)}"
+            )
+    boundary_profile_detail_rows = (
+        read_tsv(boundary_profile_detail_path)
+        if boundary_profile_detail_path.exists()
+        else []
+    )
+    boundary_profile_summary_rows = (
+        read_tsv(boundary_profile_summary_path)
+        if boundary_profile_summary_path.exists()
+        else []
+    )
+    expected_boundary_profile_counts = {
+        "qseries_animal_q1_mu_one_slope": {
+            "provider": "animal",
+            "n_boundary_rows": "27",
+            "n_profile_finite_interval": "25",
+            "n_profile_failed": "2",
+            "n_profile_covered": "10",
+            "n_profile_upper_miss": "15",
+            "profile_status_counts": "profile=25; profile_failed=2",
+            "profile_verdict_counts": "profile_finite_upper_miss=15; profile_rescued_boundary_covered=10; profile_failed_or_nonfinite_boundary=2",
+            "diagnostic_verdict": "boundary_profile_partial_rescue_still_blocked",
+            "inference_signal": "not_inference_ready_boundary_profile_upper_miss_or_failure",
+        },
+        "qseries_phylo_q1_mu_one_slope": {
+            "provider": "phylo",
+            "n_boundary_rows": "9",
+            "n_profile_finite_interval": "8",
+            "n_profile_failed": "1",
+            "n_profile_covered": "1",
+            "n_profile_upper_miss": "7",
+            "profile_status_counts": "profile=8; profile_failed=1",
+            "profile_verdict_counts": "profile_finite_upper_miss=7; profile_failed_or_nonfinite_boundary=1; profile_rescued_boundary_covered=1",
+            "diagnostic_verdict": "boundary_profile_partial_rescue_still_blocked",
+            "inference_signal": "not_inference_ready_boundary_profile_upper_miss_or_failure",
+        },
+        "qseries_relmat_q1_mu_one_slope": {
+            "provider": "relmat",
+            "n_boundary_rows": "3",
+            "n_profile_finite_interval": "2",
+            "n_profile_failed": "1",
+            "n_profile_covered": "0",
+            "n_profile_upper_miss": "2",
+            "profile_status_counts": "profile=2; profile_failed=1",
+            "profile_verdict_counts": "profile_finite_upper_miss=2; profile_failed_or_nonfinite_boundary=1",
+            "diagnostic_verdict": "boundary_profile_partial_rescue_still_blocked",
+            "inference_signal": "not_inference_ready_boundary_profile_upper_miss_or_failure",
+        },
+        "qseries_spatial_q1_mu_one_slope": {
+            "provider": "spatial",
+            "n_boundary_rows": "3",
+            "n_profile_finite_interval": "3",
+            "n_profile_failed": "0",
+            "n_profile_covered": "0",
+            "n_profile_upper_miss": "3",
+            "profile_status_counts": "profile=3",
+            "profile_verdict_counts": "profile_finite_upper_miss=3",
+            "diagnostic_verdict": "boundary_profile_finite_miss_still_blocked",
+            "inference_signal": "not_inference_ready_boundary_profile_upper_miss_or_failure",
+        },
+    }
+    if len(boundary_profile_detail_rows) != 42:
+        errors.append(
+            "Gaussian mu-slope boundary profile detail artifact must have "
+            f"42 rows; saw {len(boundary_profile_detail_rows)}"
+        )
+    if len(boundary_profile_summary_rows) != 4:
+        errors.append(
+            "Gaussian mu-slope boundary profile summary artifact must have "
+            f"4 rows; saw {len(boundary_profile_summary_rows)}"
+        )
+    detail_counts_by_cell: dict[str, int] = {}
+    for row in boundary_profile_detail_rows:
+        row_id = row.get("diagnostic_id", "<Gaussian mu-slope boundary profile detail>")
+        cell_id = row.get("cell_id", "")
+        detail_counts_by_cell[cell_id] = detail_counts_by_cell.get(cell_id, 0) + 1
+        if row.get("wald_interval_status") != "boundary_or_nonwald_status":
+            errors.append(f"{row_id}: wald_interval_status must be boundary_or_nonwald_status")
+        for field in ("fit_ok", "converged", "pdHess", "profile_ok"):
+            if row.get(field) != "TRUE":
+                errors.append(f"{row_id}: {field} must be TRUE for this diagnostic")
+        if row.get("profile_conf_status") not in {"profile", "profile_failed"}:
+            errors.append(f"{row_id}: profile_conf_status must be profile or profile_failed")
+        if row.get("profile_lower_miss") != "FALSE":
+            errors.append(f"{row_id}: profile_lower_miss must remain FALSE")
+        if row.get("profile_conf_status") == "profile":
+            for field in ("profile_finite_interval", "profile_ok"):
+                if row.get(field) != "TRUE":
+                    errors.append(f"{row_id}: {field} must be TRUE for finite profile rows")
+            if row.get("profile_lower") != "0":
+                errors.append(f"{row_id}: finite boundary profile rows must use lower endpoint 0")
+            if row.get("profile_message") != "near_sd_boundary":
+                errors.append(f"{row_id}: finite boundary profile rows must report near_sd_boundary")
+            if row.get("diagnostic_verdict") not in {
+                "profile_rescued_boundary_covered",
+                "profile_finite_upper_miss",
+            }:
+                errors.append(f"{row_id}: unexpected finite-profile diagnostic_verdict")
+        else:
+            if row.get("profile_finite_interval") != "FALSE":
+                errors.append(f"{row_id}: failed profile rows must not be finite")
+            if row.get("diagnostic_verdict") != "profile_failed_or_nonfinite_boundary":
+                errors.append(f"{row_id}: failed diagnostic_verdict must be profile_failed_or_nonfinite_boundary")
+            if "Endpoint profile root" not in row.get("profile_message", ""):
+                errors.append(f"{row_id}: failed profile_message must retain the endpoint-root failure")
+    for cell_id, expected in expected_boundary_profile_counts.items():
+        expected_count = int(expected["n_boundary_rows"])
+        if detail_counts_by_cell.get(cell_id) != expected_count:
+            errors.append(
+                f"Gaussian mu-slope boundary profile detail count for {cell_id} "
+                f"must be {expected_count}; saw {detail_counts_by_cell.get(cell_id, 0)}"
+            )
+    if len(structured_re_gaussian_mu_slope_boundary_profile_diagnostic_rows) != 4:
+        errors.append(
+            "structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv: "
+            f"expected 4 rows; got {len(structured_re_gaussian_mu_slope_boundary_profile_diagnostic_rows)}"
+        )
+    summary_artifact_by_cell = {
+        row.get("cell_id", ""): row for row in boundary_profile_summary_rows
+    }
+    seen_boundary_profile_ids: set[str] = set()
+    for row in structured_re_gaussian_mu_slope_boundary_profile_diagnostic_rows:
+        row_id = row.get("diagnostic_id", "<Gaussian mu-slope boundary profile>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        seen_boundary_profile_ids.add(row_id)
+        expected = expected_boundary_profile_counts.get(cell_id)
+        if expected is None:
+            errors.append(f"{row_id}: unexpected boundary profile support cell {cell_id!r}")
+            continue
+        provider = expected["provider"]
+        expected_count = expected["n_boundary_rows"]
+        if row.get("provider") != provider:
+            errors.append(f"{row_id}: provider must be {provider!r}")
+        expected_id = f"gaussian_mu_slope_boundary_profile_{provider}"
+        if row_id != expected_id:
+            errors.append(f"{row_id}: diagnostic_id must be {expected_id!r}")
+        summary_artifact = summary_artifact_by_cell.get(cell_id)
+        if summary_artifact is None:
+            errors.append(f"{row_id}: matching summary artifact row is missing")
+        else:
+            for field in STRUCTURED_RE_GAUSSIAN_MU_SLOPE_BOUNDARY_PROFILE_DIAGNOSTIC_FIELDS:
+                if row.get(field) != summary_artifact.get(field):
+                    errors.append(
+                        f"{row_id}: dashboard field {field} must match the summary artifact"
+                    )
+        for field, expected_value in {
+            "n_boundary_rows": expected_count,
+            "n_profile_attempted": expected_count,
+            "n_profile_converged_fit": expected_count,
+            "n_profile_pdhess_fit": expected_count,
+            "n_profile_confint_ok": expected_count,
+            "n_profile_finite_interval": expected["n_profile_finite_interval"],
+            "n_profile_failed": expected["n_profile_failed"],
+            "n_profile_covered": expected["n_profile_covered"],
+            "n_profile_lower_miss": "0",
+            "n_profile_upper_miss": expected["n_profile_upper_miss"],
+            "profile_status_counts": expected["profile_status_counts"],
+            "profile_verdict_counts": expected["profile_verdict_counts"],
+            "diagnostic_verdict": expected["diagnostic_verdict"],
+            "widget_state": "mu_slope_boundary_profile_blocked",
+            "inference_signal": expected["inference_signal"],
+            "linked_fit_status": "point_fit",
+            "linked_interval_status": "planned",
+            "linked_coverage_status": "planned",
+            "promotion_decision": "do_not_promote",
+            "artifact_dir": "docs/dev-log/simulation-artifacts/2026-06-29-gaussian-mu-slope-boundary-profile-diagnostic-local",
+        }.items():
+            if row.get(field) != expected_value:
+                errors.append(f"{row_id}: {field} must be {expected_value!r}")
+        if "near_sd_boundary" not in row.get("profile_message_examples", ""):
+            errors.append(f"{row_id}: profile_message_examples must mention near_sd_boundary")
+        if int(expected["n_profile_failed"]) > 0 and "Endpoint profile root" not in row.get("profile_message_examples", ""):
+            errors.append(f"{row_id}: profile_message_examples must retain endpoint-root failures")
+        support_row = q_series_cell_map.get(cell_id)
+        if support_row is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            for field, expected_value in {
+                "family_class": "gaussian",
+                "family": "gaussian()",
+                "dimension_pattern": "q1",
+                "endpoint_set": "mu",
+                "slope_class": "independent_one_slope",
+                "fit_status": "point_fit",
+                "interval_status": "planned",
+                "coverage_status": "planned",
+            }.items():
+                if support_row.get(field) != expected_value:
+                    errors.append(f"{row_id}: linked support-cell {field} must be {expected_value!r}")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "boundary profile diagnostic only",
+            "planned/planned",
+            "no MCSE-qualified coverage",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "upper misses" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must name boundary-profile upper misses")
+    expected_boundary_profile_ids = {
+        f"gaussian_mu_slope_boundary_profile_{expected['provider']}"
+        for expected in expected_boundary_profile_counts.values()
+    }
+    if seen_boundary_profile_ids != expected_boundary_profile_ids:
+        errors.append(
+            "structured-re-gaussian-mu-slope-boundary-profile-diagnostic.tsv row ids "
+            "must cover the four q1 mu provider rows"
+        )
+
+    # --- structured-re count-slope recovery-results (local 80-rep recovery) ---
+    # RECOVERY evidence (convergence + SD bias/RMSE), NOT coverage: linked cells
+    # stay at coverage_status 'planned'.
+    seen_count_recovery_ids: set[str] = set()
+    if len(structured_re_count_slope_recovery_results_rows) != 8:
+        errors.append(
+            "structured-re-count-slope-recovery-results.tsv: expected 8 rows "
+            f"(4 providers x 2 families); got "
+            f"{len(structured_re_count_slope_recovery_results_rows)}"
+        )
+    for row in structured_re_count_slope_recovery_results_rows:
+        row_id = row.get("recovery_id", "<count recovery row>")
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-slope-recovery-results.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_SLOPE_RECOVERY_RESULTS_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_count_recovery_ids:
+            errors.append(f"duplicate count recovery id: {row_id}")
+        seen_count_recovery_ids.add(row_id)
+        if row.get("linked_coverage_status") != "planned":
+            errors.append(
+                f"{row_id}: linked_coverage_status must be 'planned' "
+                "(recovery evidence does not promote coverage_status)"
+            )
         linked = qseries_by_cell.get(row.get("linked_cell_id", ""))
         if linked is None:
             errors.append(
@@ -14874,6 +16909,541 @@ def main() -> int:
                 "the pdHess clean claim explicit"
             )

+    # --- structured-re count-intercept recovery-results (local 80-rep recovery) ---
+    # RECOVERY evidence only: these rows replace the earlier intercept smoke
+    # display state, but linked cells keep interval_status unsupported and
+    # coverage_status planned.
+    expected_count_intercept_recovery_cells = {
+        "qseries_phylo_interaction_poisson_q1_mu",
+        "qseries_phylo_interaction_nbinom2_q1_mu",
+        "qseries_phylo_poisson_q1_mu_intercept",
+        "qseries_phylo_nbinom2_q1_mu_intercept",
+        "qseries_spatial_poisson_q1_mu_intercept",
+        "qseries_spatial_nbinom2_q1_mu_intercept",
+        "qseries_animal_poisson_q1_mu_intercept",
+        "qseries_animal_nbinom2_q1_mu_intercept",
+        "qseries_relmat_poisson_q1_mu_intercept",
+        "qseries_relmat_nbinom2_q1_mu_intercept",
+    }
+    seen_count_intercept_recovery_ids: set[str] = set()
+    seen_count_intercept_recovery_cells: set[str] = set()
+    if len(structured_re_count_intercept_recovery_results_rows) != 10:
+        errors.append(
+            "structured-re-count-intercept-recovery-results.tsv: expected "
+            "10 rows (count intercept + phylo_interaction); got "
+            f"{len(structured_re_count_intercept_recovery_results_rows)}"
+        )
+    for row in structured_re_count_intercept_recovery_results_rows:
+        row_id = row.get("recovery_id", "<count intercept recovery row>")
+        cell_id = row.get("linked_cell_id", row.get("cell_id", ""))
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_RESULTS_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-intercept-recovery-results.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_INTERCEPT_RECOVERY_RESULTS_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_count_intercept_recovery_ids:
+            errors.append(f"duplicate count intercept recovery id: {row_id}")
+        seen_count_intercept_recovery_ids.add(row_id)
+        if cell_id in seen_count_intercept_recovery_cells:
+            errors.append(f"duplicate count intercept recovery cell_id: {cell_id}")
+        seen_count_intercept_recovery_cells.add(cell_id)
+        if row.get("cell_id") != cell_id:
+            errors.append(f"{row_id}: cell_id and linked_cell_id must match")
+        if row.get("linked_coverage_status") != "planned":
+            errors.append(f"{row_id}: linked_coverage_status must remain planned")
+        linked = qseries_by_cell.get(cell_id)
+        if linked is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            if linked.get("family_class") != "non_gaussian":
+                errors.append(f"{row_id}: linked support cell must be non_gaussian")
+            if linked.get("interval_status") != "unsupported":
+                errors.append(f"{row_id}: linked interval_status must remain unsupported")
+            if linked.get("coverage_status") != "planned":
+                errors.append(f"{row_id}: linked coverage_status must remain planned")
+        try:
+            n_rep = int(row.get("n_rep", ""))
+            fit_ok = int(row.get("fit_ok", ""))
+            pdhess_false = int(row.get("pdhess_false", ""))
+            finite_estimate_rows = int(row.get("finite_estimate_rows", ""))
+            near_zero_estimate_rows = int(row.get("near_zero_estimate_rows", ""))
+            near_zero_estimate_rate = float(row.get("near_zero_estimate_rate", ""))
+        except ValueError:
+            errors.append(
+                f"{row_id}: n_rep/fit_ok/pdhess_false/finite/near-zero rows "
+                "and near-zero rate must be numeric"
+            )
+            n_rep = fit_ok = pdhess_false = finite_estimate_rows = near_zero_estimate_rows = -1
+            near_zero_estimate_rate = -1.0
+        if n_rep < 80:
+            errors.append(f"{row_id}: n_rep must be at least 80")
+        if fit_ok < 0.98 * n_rep:
+            errors.append(f"{row_id}: fit_ok rate is below the recovery gate")
+        if finite_estimate_rows != n_rep:
+            errors.append(f"{row_id}: finite_estimate_rows must retain all rows")
+        if row.get("near_zero_threshold") != "1e-04":
+            errors.append(f"{row_id}: near_zero_threshold must be 1e-04")
+        if near_zero_estimate_rows < 0 or near_zero_estimate_rows > n_rep:
+            errors.append(f"{row_id}: near_zero_estimate_rows must be within [0, n_rep]")
+        if abs(near_zero_estimate_rate - (near_zero_estimate_rows / n_rep)) > 1e-10:
+            errors.append(f"{row_id}: near_zero_estimate_rate must match near_zero_estimate_rows/n_rep")
+        if row.get("widget_state") == "non_gaussian_recovery_caveat":
+            if row.get("recovery_verdict") not in {
+                "recovery_caveat_pdhess_rate",
+                "recovery_caveat_near_zero_rate",
+                "recovery_caveat_boundary_rate",
+            }:
+                errors.append(f"{row_id}: caveat verdict must name the diagnostic caveat")
+            if pdhess_false <= int(0.02 * n_rep) and near_zero_estimate_rate < 0.25:
+                errors.append(
+                    f"{row_id}: recovery caveat requires high pdHess-false "
+                    "or near-zero estimate rate"
+                )
+        elif row.get("widget_state") == "non_gaussian_recovery_only":
+            if row.get("recovery_verdict") != "recovery_only_passed":
+                errors.append(f"{row_id}: recovery-only row must have passed verdict")
+            if pdhess_false > max(1, int(0.02 * n_rep)):
+                errors.append(f"{row_id}: recovery-only pdhess_false exceeds gate")
+            if near_zero_estimate_rate >= 0.25:
+                errors.append(f"{row_id}: recovery-only near-zero rate exceeds caveat gate")
+        else:
+            errors.append(f"{row_id}: unexpected widget_state {row.get('widget_state')!r}")
+        if not evidence_reference_exists(row.get("evidence_url", "")):
+            errors.append(f"{row_id}: evidence_url does not resolve")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "RECOVERY evidence only",
+            "does NOT promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "near-zero-estimate",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+    if seen_count_intercept_recovery_cells != expected_count_intercept_recovery_cells:
+        errors.append(
+            "structured-re-count-intercept-recovery-results.tsv cell_ids "
+            "must match the ten count-intercept/phylo_interaction cells"
+        )
+
+    # --- structured-re count-intercept top-up recovery results (stronger-denominator recovery) ---
+    expected_count_intercept_topup_cells = {
+        "qseries_phylo_poisson_q1_mu_intercept",
+        "qseries_phylo_nbinom2_q1_mu_intercept",
+        "qseries_spatial_nbinom2_q1_mu_intercept",
+    }
+    seen_count_intercept_topup_ids: set[str] = set()
+    seen_count_intercept_topup_cells: set[str] = set()
+    if len(structured_re_count_intercept_topup_recovery_results_rows) != 3:
+        errors.append(
+            "structured-re-count-intercept-topup-recovery-results.tsv: expected "
+            "3 rows (the three formerly caveated count-intercept cells); got "
+            f"{len(structured_re_count_intercept_topup_recovery_results_rows)}"
+        )
+    for row in structured_re_count_intercept_topup_recovery_results_rows:
+        row_id = row.get("recovery_id", "<count intercept top-up recovery row>")
+        cell_id = row.get("linked_cell_id", row.get("cell_id", ""))
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_RECOVERY_RESULTS_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-intercept-topup-recovery-results.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_RECOVERY_RESULTS_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_count_intercept_topup_ids:
+            errors.append(f"duplicate count intercept top-up recovery id: {row_id}")
+        seen_count_intercept_topup_ids.add(row_id)
+        if cell_id in seen_count_intercept_topup_cells:
+            errors.append(f"duplicate count intercept top-up recovery cell_id: {cell_id}")
+        seen_count_intercept_topup_cells.add(cell_id)
+        if cell_id not in expected_count_intercept_topup_cells:
+            errors.append(f"{row_id}: unexpected count-intercept top-up cell {cell_id!r}")
+        if row.get("cell_id") != cell_id:
+            errors.append(f"{row_id}: cell_id and linked_cell_id must match")
+        linked = qseries_by_cell.get(cell_id)
+        if linked is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            if linked.get("family_class") != "non_gaussian":
+                errors.append(f"{row_id}: linked support cell must be non_gaussian")
+            if linked.get("interval_status") != "unsupported":
+                errors.append(f"{row_id}: linked interval_status must remain unsupported")
+            if linked.get("coverage_status") != "planned":
+                errors.append(f"{row_id}: linked coverage_status must remain planned")
+            if linked.get("authority_status") != "source":
+                errors.append(f"{row_id}: linked authority_status must remain source")
+        try:
+            n_rep = int(row.get("n_rep", ""))
+            n_seed_replicates = int(row.get("n_seed_replicates", ""))
+            n_internal_conditions = int(row.get("n_internal_conditions", ""))
+            fit_ok = int(row.get("fit_ok", ""))
+            nonconverged = int(row.get("nonconverged", ""))
+            pdhess_false = int(row.get("pdhess_false", ""))
+            finite_estimate_rows = int(row.get("finite_estimate_rows", ""))
+            near_zero_estimate_rows = int(row.get("near_zero_estimate_rows", ""))
+            near_zero_estimate_rate = float(row.get("near_zero_estimate_rate", ""))
+        except ValueError:
+            errors.append(
+                f"{row_id}: top-up n_rep/fit/pdHess/finite/near-zero fields "
+                "must be numeric"
+            )
+            n_rep = n_seed_replicates = n_internal_conditions = fit_ok = nonconverged = pdhess_false = finite_estimate_rows = near_zero_estimate_rows = -1
+            near_zero_estimate_rate = -1.0
+        if n_rep != 320:
+            errors.append(f"{row_id}: top-up n_rep must be 320")
+        if n_seed_replicates != 80:
+            errors.append(f"{row_id}: top-up n_seed_replicates must be 80")
+        if n_internal_conditions != 4:
+            errors.append(f"{row_id}: top-up n_internal_conditions must be 4")
+        if fit_ok != n_rep or nonconverged != 0:
+            errors.append(f"{row_id}: top-up must retain 320/320 fit_ok and 0 nonconverged")
+        if pdhess_false != 0:
+            errors.append(f"{row_id}: top-up pdhess_false must be 0")
+        if finite_estimate_rows != n_rep:
+            errors.append(f"{row_id}: top-up finite_estimate_rows must retain all rows")
+        if row.get("near_zero_threshold") != "1e-04":
+            errors.append(f"{row_id}: near_zero_threshold must be 1e-04")
+        if near_zero_estimate_rows != 0 or near_zero_estimate_rate != 0:
+            errors.append(f"{row_id}: top-up near-zero count and rate must be 0")
+        if row.get("recovery_verdict") != "topup_recovery_only_passed":
+            errors.append(f"{row_id}: top-up recovery_verdict must be topup_recovery_only_passed")
+        if row.get("widget_state") != "non_gaussian_recovery_only":
+            errors.append(f"{row_id}: top-up widget_state must be non_gaussian_recovery_only")
+        if row.get("linked_coverage_status") != "planned":
+            errors.append(f"{row_id}: linked_coverage_status must remain planned")
+        if row.get("topup_design") != "stronger_denominator_80_seed_x_4_conditions":
+            errors.append(f"{row_id}: unexpected topup_design")
+        if not evidence_reference_exists(row.get("evidence_url", "")):
+            errors.append(f"{row_id}: evidence_url does not resolve")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "RECOVERY TOP-UP evidence only",
+            "Rorqual SLURM job 14897050",
+            "does NOT promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+            "original weak-denominator caveat",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        next_gate = row.get("next_gate", "")
+        for phrase in (
+            "cluster-confirmed recovery-only board evidence",
+            "Intervals and coverage remain unsupported",
+        ):
+            if phrase not in next_gate:
+                errors.append(f"{row_id}: next_gate must mention {phrase!r}")
+    if seen_count_intercept_topup_cells != expected_count_intercept_topup_cells:
+        errors.append(
+            "structured-re-count-intercept-topup-recovery-results.tsv cell_ids "
+            "must match exactly the three formerly caveated cells"
+        )
+
+    # --- structured-re count-intercept top-up cluster confirmation (recovery-only evidence) ---
+    if len(structured_re_count_intercept_topup_cluster_dispatch_rows) != 1:
+        errors.append(
+            "structured-re-count-intercept-topup-cluster-dispatch.tsv: expected "
+            "one Rorqual dispatch row; got "
+            f"{len(structured_re_count_intercept_topup_cluster_dispatch_rows)}"
+        )
+    for row in structured_re_count_intercept_topup_cluster_dispatch_rows:
+        row_id = row.get("dispatch_id", "<count intercept top-up dispatch row>")
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_CLUSTER_DISPATCH_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-intercept-topup-cluster-dispatch.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_INTERCEPT_TOPUP_CLUSTER_DISPATCH_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        expected_dispatch = {
+            "dispatch_id": "count_intercept_topup_rorqual_20260629",
+            "lane": "count_intercept_topup_recovery",
+            "host": "rorqual",
+            "scheduler": "slurm",
+            "job_id": "14897050",
+            "job_state": "completed_passed",
+            "n_rep": "80",
+            "n_internal_conditions": "4",
+        }
+        for field, expected in expected_dispatch.items():
+            if row.get(field) != expected:
+                errors.append(f"{row_id}: {field} must be {expected!r}")
+        target_cells = set(row.get("target_cells", "").split(";"))
+        if target_cells != expected_count_intercept_topup_cells:
+            errors.append(f"{row_id}: target_cells must match the three top-up cells")
+        run_root = row.get("run_root", "")
+        expected_run_root = (
+            "/project/def-snakagaw/snakagaw/drmtmb-qseries/"
+            "20260629-count-intercept-topup-rorqual-77b634eda91b"
+        )
+        if run_root != expected_run_root:
+            errors.append(f"{row_id}: run_root must record the Rorqual campaign root")
+        expected_result_path = row.get("expected_result_path", "")
+        if not expected_result_path.startswith(run_root + "/results/"):
+            errors.append(f"{row_id}: expected_result_path must live under run_root/results")
+        if not expected_result_path.endswith(
+            "structured-re-count-intercept-topup-recovery-results.tsv"
+        ):
+            errors.append(f"{row_id}: expected_result_path must name the top-up result TSV")
+        if "77b634eda91b" not in row.get("git_sha", ""):
+            errors.append(f"{row_id}: git_sha must retain the source snapshot SHA")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "Cluster confirmation evidence only",
+            "runner_exit_code=0",
+            "summary_exit_code=0",
+            "does not promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        next_gate = row.get("next_gate", "")
+        for phrase in (
+            "Fetched result TSV and metadata",
+            "keep recovery-only wording",
+            "separate interval route",
+        ):
+            if phrase not in next_gate:
+                errors.append(f"{row_id}: next_gate must mention {phrase!r}")
+
+    # --- structured-re count-intercept caveat diagnostic (condition-level blocker) ---
+    expected_caveat_cells = {
+        "qseries_phylo_poisson_q1_mu_intercept",
+        "qseries_phylo_nbinom2_q1_mu_intercept",
+        "qseries_spatial_nbinom2_q1_mu_intercept",
+    }
+    caveat_rows_by_cell: dict[str, list[dict[str, str]]] = {}
+    seen_caveat_diagnostic_ids: set[str] = set()
+    if len(structured_re_count_intercept_caveat_diagnostic_rows) != 12:
+        errors.append(
+            "structured-re-count-intercept-caveat-diagnostic.tsv: expected "
+            "12 condition rows (4 per caveated count-intercept cell); got "
+            f"{len(structured_re_count_intercept_caveat_diagnostic_rows)}"
+        )
+    for row in structured_re_count_intercept_caveat_diagnostic_rows:
+        row_id = row.get("diagnostic_id", "<count intercept caveat diagnostic row>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_INTERCEPT_CAVEAT_DIAGNOSTIC_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-intercept-caveat-diagnostic.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_INTERCEPT_CAVEAT_DIAGNOSTIC_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_caveat_diagnostic_ids:
+            errors.append(f"duplicate count intercept caveat diagnostic id: {row_id}")
+        seen_caveat_diagnostic_ids.add(row_id)
+        caveat_rows_by_cell.setdefault(cell_id, []).append(row)
+        if cell_id not in expected_caveat_cells:
+            errors.append(f"{row_id}: unexpected caveat diagnostic cell {cell_id!r}")
+        linked = qseries_by_cell.get(cell_id)
+        if linked is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            if linked.get("interval_status") != "unsupported":
+                errors.append(f"{row_id}: linked interval_status must remain unsupported")
+            if linked.get("coverage_status") != "planned":
+                errors.append(f"{row_id}: linked coverage_status must remain planned")
+            if linked.get("authority_status") != "source":
+                errors.append(f"{row_id}: linked authority_status must remain source")
+        try:
+            n_rep = int(row.get("n_rep", ""))
+            fit_ok = int(row.get("fit_ok", ""))
+            nonconverged = int(row.get("nonconverged", ""))
+            pdhess_false = int(row.get("pdhess_false", ""))
+            near_zero_estimate_rows = int(row.get("near_zero_estimate_rows", ""))
+            near_zero_estimate_rate = float(row.get("near_zero_estimate_rate", ""))
+        except ValueError:
+            errors.append(
+                f"{row_id}: n_rep/fit_ok/nonconverged/pdhess_false/near-zero "
+                "fields must be numeric"
+            )
+            n_rep = fit_ok = nonconverged = pdhess_false = near_zero_estimate_rows = -1
+            near_zero_estimate_rate = -1.0
+        if n_rep != 80:
+            errors.append(f"{row_id}: each caveat condition must retain 80 replicates")
+        if fit_ok + nonconverged != n_rep:
+            errors.append(f"{row_id}: fit_ok + nonconverged must equal n_rep")
+        if row.get("near_zero_threshold") != "1e-04":
+            errors.append(f"{row_id}: near_zero_threshold must be 1e-04")
+        if abs(near_zero_estimate_rate - (near_zero_estimate_rows / n_rep)) > 1e-10:
+            errors.append(f"{row_id}: near_zero rate must match count/n_rep")
+        if not evidence_reference_exists(row.get("evidence_url", "")):
+            errors.append(f"{row_id}: evidence_url does not resolve")
+        verdict = row.get("diagnostic_verdict")
+        if cell_id == "qseries_phylo_poisson_q1_mu_intercept":
+            if verdict != "condition_near_zero_caveat":
+                errors.append(f"{row_id}: phylo Poisson conditions must keep near-zero caveat verdict")
+        elif cell_id == "qseries_phylo_nbinom2_q1_mu_intercept":
+            if verdict != "condition_pdhess_caveat":
+                errors.append(f"{row_id}: phylo NB2 conditions must keep pdHess caveat verdict")
+            if pdhess_false <= 1:
+                errors.append(f"{row_id}: phylo NB2 caveat condition must retain pdHess failures")
+        elif cell_id == "qseries_spatial_nbinom2_q1_mu_intercept":
+            if verdict not in {"condition_near_zero_caveat", "condition_recovery_ok"}:
+                errors.append(f"{row_id}: spatial NB2 condition verdict must be caveat or recovery_ok")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "Condition-level diagnostic only",
+            "does NOT promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "intervals and coverage remain unsupported" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must preserve unsupported intervals/coverage")
+    if set(caveat_rows_by_cell) != expected_caveat_cells:
+        errors.append(
+            "structured-re-count-intercept-caveat-diagnostic.tsv cell_ids "
+            "must match exactly the three count-intercept caveat cells"
+        )
+    for cell_id, rows_for_cell in caveat_rows_by_cell.items():
+        if len(rows_for_cell) != 4:
+            errors.append(f"{cell_id}: expected 4 caveat diagnostic condition rows")
+    spatial_verdicts = {
+        row.get("diagnostic_verdict")
+        for row in caveat_rows_by_cell.get("qseries_spatial_nbinom2_q1_mu_intercept", [])
+    }
+    if spatial_verdicts != {"condition_near_zero_caveat", "condition_recovery_ok"}:
+        errors.append(
+            "qseries_spatial_nbinom2_q1_mu_intercept: diagnostic should show "
+            "both weak-signal caveats and stronger-signal recovery_ok conditions"
+        )
+
+    # --- structured-re count-intercept denominator diagnostic (stronger design; no promotion) ---
+    denominator_rows_by_cell: dict[str, list[dict[str, str]]] = {}
+    seen_denominator_diagnostic_ids: set[str] = set()
+    expected_denominator_verdicts = {
+        "denominator_cleared_locally",
+        "denominator_fit_caveat",
+        "denominator_pdhess_caveat",
+        "denominator_near_zero_caveat",
+        "denominator_boundary_caveat",
+    }
+    if len(structured_re_count_intercept_denominator_diagnostic_rows) != 12:
+        errors.append(
+            "structured-re-count-intercept-denominator-diagnostic.tsv: expected "
+            "12 condition rows (4 per caveated count-intercept cell); got "
+            f"{len(structured_re_count_intercept_denominator_diagnostic_rows)}"
+        )
+    for row in structured_re_count_intercept_denominator_diagnostic_rows:
+        row_id = row.get("diagnostic_id", "<count intercept denominator diagnostic row>")
+        cell_id = row.get("cell_id", "")
+        if set(row.keys()) != set(STRUCTURED_RE_COUNT_INTERCEPT_DENOMINATOR_DIAGNOSTIC_FIELDS):
+            errors.append(
+                f"{row_id}: structured-re-count-intercept-denominator-diagnostic.tsv "
+                "fields do not match the contract"
+            )
+        for field in STRUCTURED_RE_COUNT_INTERCEPT_DENOMINATOR_DIAGNOSTIC_FIELDS:
+            if not row.get(field):
+                errors.append(f"{row_id}: {field} is empty")
+        if row_id in seen_denominator_diagnostic_ids:
+            errors.append(f"duplicate count intercept denominator diagnostic id: {row_id}")
+        seen_denominator_diagnostic_ids.add(row_id)
+        denominator_rows_by_cell.setdefault(cell_id, []).append(row)
+        if cell_id not in expected_caveat_cells:
+            errors.append(f"{row_id}: unexpected denominator diagnostic cell {cell_id!r}")
+        linked = qseries_by_cell.get(cell_id)
+        if linked is None:
+            errors.append(f"{row_id}: linked support cell {cell_id!r} is missing")
+        else:
+            if linked.get("interval_status") != "unsupported":
+                errors.append(f"{row_id}: linked interval_status must remain unsupported")
+            if linked.get("coverage_status") != "planned":
+                errors.append(f"{row_id}: linked coverage_status must remain planned")
+            if linked.get("authority_status") != "source":
+                errors.append(f"{row_id}: linked authority_status must remain source")
+        try:
+            n_rep = int(row.get("n_rep", ""))
+            fit_ok = int(row.get("fit_ok", ""))
+            nonconverged = int(row.get("nonconverged", ""))
+            pdhess_false = int(row.get("pdhess_false", ""))
+            finite_estimate_rows = int(row.get("finite_estimate_rows", ""))
+            near_zero_estimate_rows = int(row.get("near_zero_estimate_rows", ""))
+            near_zero_estimate_rate = float(row.get("near_zero_estimate_rate", ""))
+        except ValueError:
+            errors.append(
+                f"{row_id}: n_rep/fit_ok/nonconverged/pdhess_false/finite/near-zero "
+                "fields must be numeric"
+            )
+            n_rep = fit_ok = nonconverged = pdhess_false = finite_estimate_rows = near_zero_estimate_rows = -1
+            near_zero_estimate_rate = -1.0
+        if n_rep != 30:
+            errors.append(f"{row_id}: each denominator diagnostic condition must retain 30 replicates")
+        if fit_ok + nonconverged != n_rep:
+            errors.append(f"{row_id}: fit_ok + nonconverged must equal n_rep")
+        if finite_estimate_rows > n_rep:
+            errors.append(f"{row_id}: finite_estimate_rows cannot exceed n_rep")
+        if row.get("near_zero_threshold") != "1e-04":
+            errors.append(f"{row_id}: near_zero_threshold must be 1e-04")
+        if abs(near_zero_estimate_rate - (near_zero_estimate_rows / n_rep)) > 1e-10:
+            errors.append(f"{row_id}: near-zero rate must match count/n_rep")
+        if row.get("denominator_verdict") not in expected_denominator_verdicts:
+            errors.append(f"{row_id}: unexpected denominator_verdict {row.get('denominator_verdict')!r}")
+        if not evidence_reference_exists(row.get("evidence_url", "")):
+            errors.append(f"{row_id}: evidence_url does not resolve")
+        claim_boundary = row.get("claim_boundary", "")
+        for phrase in (
+            "Targeted stronger-denominator diagnostic only",
+            "does NOT promote",
+            "interval_status",
+            "coverage_status",
+            "inference_ready",
+            "supported",
+            "REML",
+            "AI-REML",
+            "public support",
+        ):
+            if phrase not in claim_boundary:
+                errors.append(f"{row_id}: claim_boundary must mention {phrase!r}")
+        if "intervals and coverage remain unsupported" not in row.get("next_gate", ""):
+            errors.append(f"{row_id}: next_gate must preserve unsupported intervals/coverage")
+    if set(denominator_rows_by_cell) != expected_caveat_cells:
+        errors.append(
+            "structured-re-count-intercept-denominator-diagnostic.tsv cell_ids "
+            "must match exactly the three count-intercept caveat cells"
+        )
+    for cell_id, rows_for_cell in denominator_rows_by_cell.items():
+        if len(rows_for_cell) != 4:
+            errors.append(f"{cell_id}: expected 4 denominator diagnostic condition rows")
+    if {
+        row.get("denominator_verdict")
+        for row in structured_re_count_intercept_denominator_diagnostic_rows
+    } != {"denominator_cleared_locally"}:
+        errors.append(
+            "structured-re-count-intercept-denominator-diagnostic.tsv currently "
+            "records local clearing for all stronger-denominator conditions; "
+            "update the after-task and tests if that evidence changes"
+        )
+
     # --- structured-re slope coverage g-sweep (DIAGNOSTIC; promotes nothing) ---
     seen_gsweep_ids: set[str] = set()
     if len(structured_re_slope_coverage_gsweep_rows) != 18:
@@ -31983,11 +34553,15 @@ def main() -> int:
         f", {len(structured_re_q_series_inference_evidence_summary_rows)} structured RE q-series inference-evidence summary rows"
         f", {len(structured_re_high_q_status_audit_rows)} structured RE high-q status-audit rows"
         f", {len(structured_re_nongaussian_status_audit_rows)} structured RE non-Gaussian status-audit rows"
+        f", {len(structured_re_nongaussian_recovery_rollup_rows)} structured RE non-Gaussian recovery-rollup rows"
         f", {len(structured_re_count_intercept_recovery_smoke_status_rows)} structured RE count-intercept recovery-smoke rows"
         f", {len(structured_re_phylo_count_intercept_recovery_smoke_status_rows)} structured RE phylo count-intercept recovery-smoke rows"
         f", {len(structured_re_phylo_interaction_count_recovery_smoke_status_rows)} structured RE phylo-interaction count recovery-smoke rows"
         f", {len(structured_re_gaussian_lowq_status_audit_rows)} structured RE Gaussian low-q status-audit rows"
         f", {len(structured_re_gaussian_mu_slope_smoke_status_rows)} structured RE Gaussian mu-slope smoke-status rows"
+        f", {len(structured_re_gaussian_mu_slope_boundary_profile_diagnostic_rows)} structured RE Gaussian mu-slope boundary-profile diagnostic rows"
+        f", {len(structured_re_gaussian_mu_slope_hybrid_boundary_audit_rows)} structured RE Gaussian mu-slope hybrid-boundary audit rows"
+        f", {len(structured_re_gaussian_mu_slope_hybrid_sr475_audit_rows)} structured RE Gaussian mu-slope hybrid-SR475 audit rows"
         f", {len(structured_re_count_slope_fixture_recovery_contract_rows)} structured RE count-slope fixture/recovery contract rows"
         f", {len(structured_re_count_slope_native_fixture_status_rows)} structured RE count-slope native-fixture rows"
         f", {len(structured_re_count_slope_recovery_runner_contract_rows)} structured RE count-slope recovery-runner rows"
@@ -32007,6 +34581,11 @@ def main() -> int:
         f", {len(structured_re_count_structured_mu_rejection_contract_rows)} structured RE count structured-mu rejection rows"
         f", {len(structured_re_slope_coverage_results_rows)} structured RE slope coverage-results rows"
         f", {len(structured_re_count_slope_recovery_results_rows)} structured RE count-slope recovery-results rows"
+        f", {len(structured_re_count_intercept_recovery_results_rows)} structured RE count-intercept recovery-results rows"
+        f", {len(structured_re_count_intercept_topup_recovery_results_rows)} structured RE count-intercept top-up recovery-results rows"
+        f", {len(structured_re_count_intercept_topup_cluster_dispatch_rows)} structured RE count-intercept top-up cluster-dispatch rows"
+        f", {len(structured_re_count_intercept_caveat_diagnostic_rows)} structured RE count-intercept caveat-diagnostic rows"
+        f", {len(structured_re_count_intercept_denominator_diagnostic_rows)} structured RE count-intercept denominator-diagnostic rows"
         f", {len(structured_re_slope_coverage_gsweep_rows)} structured RE slope coverage g-sweep rows"
         f", {len(structured_re_slope_coverage_certification_rows)} structured RE slope coverage certification rows"
         f", {len(structured_re_mu_slope_fixture_audit_rows)} structured RE mu-slope audit rows"
@@ -32028,6 +34607,7 @@ def main() -> int:
         f", {len(structured_re_q2_slope_replicated_denominator_rule_rows)} structured RE q2 slope replicated-denominator rule rows"
         f", {len(structured_re_q2_slope_spatial_animal_admission_audit_rows)} structured RE q2 slope spatial/animal admission-audit rows"
         f", {len(structured_re_q2_slope_bias_t_coverage_evidence_rows)} structured RE q2 slope bias+t coverage-evidence rows"
+        f", {len(structured_re_q2_slope_bias_t_topup_runner_contract_rows)} structured RE q2 slope bias+t top-up runner-contract rows"
         f", {len(structured_re_q2_slope_coverage_pregrid_dry_run_rows)} structured RE q2 slope coverage-pregrid dry-run rows"
         f", {len(structured_re_sigma_slope_replicated_denominator_rule_rows)} structured RE sigma-slope replicated-denominator rule rows"
         f", {len(structured_re_sigma_slope_coverage_pregrid_dry_run_rows)} structured RE sigma-slope coverage-pregrid dry-run rows"

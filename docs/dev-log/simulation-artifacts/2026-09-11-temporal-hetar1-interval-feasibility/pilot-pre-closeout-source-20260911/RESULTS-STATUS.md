# Superseded pilot source boundary

This first five-seed pilot retained all fits and intervals and passed its original timing check. It predates the final public R-level diagnostics and the reporting-label repair, so it is retained here while the same frozen C1 seeds are rerun from the final closeout source in `pilot/`. The likelihood, fixture generator, seeds and two-start selection rule did not change.

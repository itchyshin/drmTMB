# GOAL — parity-joint-20260905 (IMMUTABLE — re-read at the top of EVERY arc)
Read this first, every cycle. Auto-compact eats messages, not this file. Unsure after a compaction?
Re-read THIS, then checkpoint.md, then arcs.md, then ultra-plan.md.

## Mission
TRUE R<->Julia parity for drmTMB + DRM.jl, both at 0.7.0 -> prepare 0.7.1, in Shinichi's words (2026-09-05):
"make sure all the models can be run in both, and all the models can be run through engine = julia."
Finish line: THE MATRIX is green — every one of the 43 drmTMB-native capabilities is either fit natively in R,
fit natively in Julia, AND reachable through engine="julia" with same-target coef+logLik (tol 1e-4) and SE
(rtol 1e-3) receipts at the committed pin — or carries a WRITTEN permanent boundary citing its decision.
Then the P/U backlog in the order in ultra-plan.md, until empty or he stops it.

## Headline
The matrix. Two ledgers each pass (DRM.jl parity_ledger CLOSURE: PASS; drmTMB TSV 10/12 rows below covered)
because they measure different things; the cross-join is what makes parity ONE measurable claim.
Centre of mass: A4 — the 9 families BOTH engines fit natively that the bridge alone refuses.

## Invariants (never violated, even after compaction)
- ONE lane, THIS session (drmTMB2), over BOTH repos. The DRM.jl3 session is archived; do not revive, do not message it.
- The pin is what source-pins.json says. A clone is reproducible only because the ref is COMMITTED; never trust a
  clone's identity from its directory name.
- Bridge (engine="julia") work runs LOCALLY ONLY — Totoro's R segfaults on JuliaCall (measured). Totoro = native-R
  campaigns + native-Julia suites, <=150 cores, BLAS pinned, over the existing cm- socket, never a Duo prompt.
- Every DRM.jl edit lands via a PR merged on green. Never --admin unless src/ is byte-identical to base.
- Every negative gate carries a RED control. Every watcher requires >=1 completed check (empty set is NOT success).
- A4 leaf G1: read how the test suite builds a working fit for that family FIRST, then use that call shape
  (today's gaussian() vs biv_gaussian() lesson).
- Never read the main checkout's working tree for source truth: `git show origin/main:<path>`.
- No message to Ayumi or any collaborator (D-221/D-222). No tag, release, CRAN, registration.

## Authoritative WHAT
-> ultra-plan.md (arcs A0..A11, the 24-hour burn schedule, the wayfinder fog + defaults).

## Definition of done
1. Pin advanced to DRM.jl 430ef64cc (or later, recorded), full 37-file sweep clean, tip-identity receipt regenerated last.
2. docs/design/parity-matrix.md exists, is GENERATED by tools/write-parity-matrix.R, regenerates byte-identically, every cell cites a source.
3. The 9 refused families admitted through the bridge, each with parity_fixture + parity_se receipts and a TSV row; the 6 unledgered routes have rows.
4. #1116 #1117 #1118 ported (D-204 both-ways).
5. G3 bridge-side inference qualified on small convergent cells; the 4 fenced TSV rows promoted.
6. parity_ledger.py CLOSURE: PASS at the new pin; capability_ledger.py --check green; both receipt checkers --self-test pass.
7. DESCRIPTION prepared for 0.7.1 on both repos, NEWS written, NOT tagged.
8. Melissa reconcile + Rose after-task on BOTH repos + handover with RESUME line.

## Pre-authorisation (G0, Shinichi 2026-09-05)
Scoped edits both repos on claude/parity-* branches; worktrees; filtered tests; local live-bridge runs <=30 min each
with an estimate in the leaf; PRs on both repos; MERGE ON GREEN on both repos; vault commits; the A10 PRE-RUN on Totoro.
ULTRACODE fan-out is authorised in his words.

## Must stop for
The A10 FULL grid (pre-run receipt + explicit go) · any decide-with-Shinichi fog ticket whose default is irreversible ·
any tag/release/CRAN/registration · any collaborator message · --admin where src/ differs · anything reopening D-179/D-181 ·
a surprise that invalidates the plan (-> back to G0, never improvised).

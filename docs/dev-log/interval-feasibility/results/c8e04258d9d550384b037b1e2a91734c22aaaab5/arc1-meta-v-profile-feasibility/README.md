# Arc 1 `meta_V` pooled-effect profile feasibility

This retained packet promotes only
`mc-0260m::fixef:mu:(Intercept)` under the current-source ML `meta_V` fixture at
`K = 48`. It passed three Totoro seeds (`2026080232:2026080234`) under the same
immutable receipt and semantic-sidecar contract used by the Gaussian fixed
packet.

The between-study residual heterogeneity SD is explicitly outside this result.
The retained `K = 12`, true `tau = 0.10` `[0, Inf]` failure remains binding.
This packet establishes target-specific `interval_feasible`; it supplies no
coverage, calibration, K-wide, coefficient-wide, or meta-route-wide claim.


# Arc 1 Gaussian residual-scale RE profile feasibility

This retained packet promotes only `mc-0266::sd:sigma:(1 | id)` under the
current-source ML Gaussian residual-scale random-intercept fixture with 48
groups and 20 observations per group. It passed three Totoro seeds
(`2026080242:2026080244`) under the immutable receipt and semantic-sidecar
contract.

The result establishes target-specific `interval_feasible`, not coverage or
calibration. It does not widen to location-side random effects, residual-scale
slopes, REML, other information rungs, other families, or public guidance.


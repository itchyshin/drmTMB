# Arc 1 Gaussian REML random-slope profile feasibility

This retained packet promotes only `mc-0269::sd:mu:(0 + x | id)` under the
current-source Gaussian REML independent random-slope fixture with 64 groups
and 12 observations per group. It passed three Totoro seeds
(`2026080252:2026080254`) under the immutable one-profile receipt and semantic
sidecar contract.

The result establishes target-specific `interval_feasible`, not coverage or
calibration. It does not widen to REML fixed effects, random intercepts,
correlated or labelled covariance blocks, scale-side effects, ML, other
information rungs, other families, or public guidance.


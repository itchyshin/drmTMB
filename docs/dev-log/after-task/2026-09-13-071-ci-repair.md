# After Task: 0.7.1 ordinary-Laplace CI repair (PR #1304)

## Goal

Make draft PR #1304 reviewable by repairing its CI failures while leaving the
retained source-pinned parity evidence, pins, fixtures, manifests, and
denominators untouched.  This was the narrow slice the 2026-09-13 Codex
handover assigned to Claude.

## Implemented

Three generated artifacts were regenerated from their sources and one
registry field plus one label guard were corrected in `R/julia-bridge.R`.
The source-tree manifest now lists `test-071-four-fixture-summary.R`, so
`R CMD check` skips it and the blind-spot job runs it.  The env-skip census
carries the test's two skip rows.  The two new bridge-registry rows cite the
retained coverage receipt by a commit-pinned GitHub URL instead of a bare
path.  The bare `resd` label for a mu-only ordinary random intercept is now
emitted only for the three families the scalar Laplace route admits.

## Mathematical Contract

Unchanged.  No likelihood, estimand, target label, or evidence value moved.
The label guard aligns the coefficient-name contract with the existing
admission gate `drm_julia_validate_marginal()`; it does not alter which fits
the bridge accepts.

## Files Changed

`R/julia-bridge.R` (two `evidence_url` cells; family gate on the mu-RI `resd`
label), `tests/testthat/test-julia-bridge-coef-labels.R` (new two-arm test),
`tools/source-tree-tests.txt`, `inst/extdata/env-skip-census.tsv`,
`inst/extdata/julia-capabilities.tsv`,
`docs/dev-log/dashboard/julia-capabilities.tsv`, this report, and
`docs/dev-log/check-log.d/2026-09-13-071-ci-repair.md`.

## Checks Run

`tools/run-source-tree-tests.R --check` (agrees, 37 files); the four narrow
test files under `pkgload::load_all()` with no failures; `git diff --stat
afa88a5b3` over the evidence directories is empty.  CI on the pushed commit is
the remaining check and is recorded on the PR.

## Tests Of The Tests

The new label test asserts both arms: binomial, poisson, and nbinom2 receive
`resd = "g"`, and gaussian, gamma, and a `NULL` family receive no `resd`
label.  Before the guard, the Gaussian arm failed (that is the CI failure at
`test-coefficient-labels.R:869`).  The census and manifest guards are
themselves the tests that failed before regeneration.

## Consistency Audit

The handover called the registry failure "artifact does not match the
registry"; the artifacts were byte-identical to the registry, and the actual
failure was the URL-format assertion at line 176.  The check-log shard states
the corrected diagnosis.

## GitHub Issue Maintenance

None opened or closed.  PR #1304 stays a draft.

## What Did Not Go Smoothly

The handover file lived only on the branch, not on the primary checkout's
branch, so the first read failed until the branch was located.  The mu-RI
label regression sat in retained bridge plumbing the handover described as
finished; fixing it was necessary for CI and is called out here because it is
the one code change beyond generated artifacts.

## Team Learning

A new test that reads `.Rbuildignore`d paths must be added to the source-tree
manifest and the env-skip census in the same commit, or the next CI run fails
in two jobs for one cause.  The two `--update` scripts are pure R and cheap.

## Known Limitations

The Gaussian mu-only route's own labelling is not exercised by a live DRM.jl
fit here; the five live-echo tests skip without `DRM_JL_PATH`.

## Next Actions

Push, confirm the required checks on PR #1304, keep it draft.  Wave 1 of the
Dinnage audit response proceeds on `claude/audit-dinnage-wave1-20260913`.

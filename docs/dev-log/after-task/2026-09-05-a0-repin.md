# A0 — re-pin the DRM.jl clone e0a65f96b → 430ef64cc

Date: 2026-09-05 · Lane: parity-joint-20260905 (the single joint lane over both repos) · Ledger: `.unlazy/parity/gates/leaf-a0.md`

## Why now
The parity plan's machinery scout found the pin 18 commits behind DRM.jl main, and three of those
commits matter: #630 (gradient O(n+G) — timing receipts go pessimistic), **#631 (profile endpoint
NUMBERS change)**, #632 (bridge options + gradient exposed). A receipt taken at the old pin cannot
be trusted for intervals. So the re-pin is arc zero, before any receipt in this programme is written.

## What was measured
f7's protocol, repeated exactly. Probe clone at `430ef64cc` under `~/local-scratch/` (never
`/private/tmp` — the other lane lost 28 gate files to the reaper there on 2026-09-04).
`Project.toml` byte-identical → previous `Manifest.toml` carried over → **dependencies held fixed,
only engine code varies**. Precompiled BEFORE measuring (`PRECOMPILE_OK 0.7.0`).

Full live-Julia suite, all 37 `test-julia*.R`, one session:

**1440 passed · 0 failed · 0 errors · 0 skipped**

Per-file `join` against the `e0a65f96b` sweep: exactly one file differs —
`test-julia-slope-nongaussian.R`, `err=1 → pass=7` — and that is f7's own test conversion (the
earlier sweep started before it), not the pin. **The 18 commits changed no test outcome.**

## A finding, not a pass
#631 changed what profile endpoints return on failure. No test in this suite reached that path,
which is why the sweep could not see the change. That is a coverage gap in the interval suite, handed
to A8 (bridge-side inference qualification) rather than counted as "nothing changed".

## What this arc did NOT do
- No file under `R/` or `src/` changed (gate a0-G7).
- The tip-identity receipt was NOT regenerated in this commit: the registry PR (#1162) was still in
  its merge gate, and it changes `R/julia-bridge.R`. A receipt regenerated before it lands is stale on
  arrival — precisely #1150's failure. The receipt is regenerated once, after #1162 merges, on a
  worktree that has merged main, and committed to this branch before the PR is opened.
- The Totoro runner was vendored in a separate DRM.jl PR (#638).

## Conditions the sweep ran under
Machine load 26–30 on 20 cores (six concurrent TMB compiles from the wave-1 fan-out). The sweep
slowed but completed; nothing was skipped.

# After Task: Version 0.1.0 Release Preparation

## Goal

Move `drmTMB` from a development-version preview target to a tagged `0.1.0`
preview candidate that users can install from GitHub.

## Implemented

`DESCRIPTION` now reports version `0.1.0`, `NEWS.md` has the dated heading
`drmTMB 0.1.0 (2026-05-10)`, the landing page describes this as the first
public preview, the pkgdown badge says `0.1.0 preview release`, and the roadmap
now describes the `0.1.0` preview boundary rather than a future release gate.

## Mathematical Contract

No model, likelihood, extractor, or formula grammar changed. The release keeps
the same public scope: implemented one-response and two-response workflows are
available, public `sigma` remains the modelled scale parameter, `rho12` remains
the residual bivariate correlation parameter, and full double-hierarchical
covariance, bivariate random-effect covariance, ordinal scale formulae,
denominator aliases, spatial effects, phylogenetic scale effects, skew-normal,
and skew-t remain post-preview work.

## Files Changed

- `DESCRIPTION`
- `NEWS.md`
- `README.md`
- `_pkgdown.yml`
- `ROADMAP.md`
- `docs/dev-log/check-log.md`
- `docs/dev-log/release-checklists/2026-05-10-0.1.0-preview-release.md`
- `docs/dev-log/after-task/2026-05-10-version-0.1.0-release-prep.md`

## Checks Run

- `Rscript -e "devtools::document()"`: completed.
- `Rscript -e "devtools::test()"`: 1400 passed, 0 failed, 0 warnings, 0 skips.
- `Rscript -e "pkgdown::check_pkgdown()"`: no problems found.
- `Rscript -e "pkgdown::build_site()"`: passed.
- `Rscript tools/fix-pkgdown-favicon-mime.R pkgdown-site`: passed.
- `Rscript -e "devtools::check(error_on = 'never', env_vars = c('_R_CHECK_SYSTEM_CLOCK_' = 'FALSE'))"`:
  0 errors, 0 warnings, 0 notes for `drmTMB 0.1.0`.
- `rg -n "0\\.0\\.0\\.9000|version will be bumped|development build; 0\\.1\\.0 preview planned|first public preview target|current development version|Development status" README.md NEWS.md DESCRIPTION _pkgdown.yml ROADMAP.md pkgdown-site/index.html pkgdown-site/news/index.html pkgdown-site/ROADMAP.html`:
  no matches in active source and rendered landing, news, or roadmap pages.
- `rg -n "0\\.1\\.0|Preview status|preview version|0.1.0 preview release|drmTMB 0.1.0|Released version" README.md NEWS.md DESCRIPTION _pkgdown.yml ROADMAP.md docs/dev-log/release-checklists/2026-05-10-0.1.0-preview-release.md pkgdown-site/index.html pkgdown-site/news/index.html pkgdown-site/ROADMAP.html`:
  confirmed source and rendered release wording.
- Chrome/Playwright layout sanity check over `pkgdown-site/index.html`: desktop
  `1280 x 900` and mobile `390 x 844` had no horizontal overflow, showed
  version `0.1.0`, and kept the first headings as `Start here`,
  `Preview status`, `Install`, and `Tiny example`.
- `git diff --check`: clean.

## Tests Of The Tests

This task did not add tests. It reran the full test suite and full package
check after the version bump, so the release candidate was checked in the same
state that will be pushed and tagged.

## Consistency Audit

The active README, NEWS, DESCRIPTION, roadmap, pkgdown config, rendered landing
page, rendered NEWS page, and rendered roadmap all agree that the current
preview version is `0.1.0`. Historical dev-log entries still mention
`0.0.0.9000` where they record earlier development-version checks; those notes
remain historical evidence rather than current release prose.

## What Did Not Go Smoothly

One first-pass grep command accidentally included backticks around
`DESCRIPTION` in the shell pattern and produced a harmless shell message. The
scan was rerun with a simpler pattern and recorded correctly. Rose's lesson is
to keep shell search patterns boring when checking release strings.

## Team Learning

Ada kept this task release-only rather than adding model features. Pat and Rose
focused on whether a first user sees `0.1.0`, installation, and preview scope
without contradiction. Grace required a clean full package check before the
release candidate can be pushed. Curie and Fisher rely on the already-green
family coverage audit rather than reopening model scope during the version bump.

## Known Limitations

The release remains a preview. It is pre-CRAN, and the roadmap items outside the
implemented one-response and two-response workflows remain planned work.

## Next Actions

1. Commit and push the `0.1.0` release-preparation changes.
2. Monitor GitHub Actions for pkgdown and R-CMD-check on macOS, Ubuntu, and
   Windows.
3. If CI is green, tag `v0.1.0` and push the tag.
4. Sync GitHub issue #1 with the final tag outcome.

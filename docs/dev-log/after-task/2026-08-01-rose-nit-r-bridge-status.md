# After-task — Rose nit: uni `r_bridge_status` honesty (2026-08-01)

Align `base_gaussian_location_scale` and `gaussian_response_mask` from
`r_bridge_status = "supported"` to `"experimental"` so they match
`claim_status = "partial"` (Phase 1.5 experimental bar). Regenerated
`docs/dev-log/dashboard/julia-capabilities.tsv` and
`inst/extdata/julia-capabilities.tsv`. Paired DRM.jl tip-docs note. No vignette
flip; no CRAN Depends change.

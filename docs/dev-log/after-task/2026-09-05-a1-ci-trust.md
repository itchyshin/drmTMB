# A1 (parity programme): CI trust -- #1083 errors-to-skips, #1081 zero bridge coverage, #1150 receipt checks

**Reader**: whoever reads a green `R-CMD-check` run on this repository and
wants to know what it proves about the `engine = "julia"` bridge and the
committed parity receipts; the integrator merging the parity-programme
leaves; the owner of `tools/check-julia-phylo-labels-receipt.R`.

**Leaf**: A1 of the drmTMB <-> DRM.jl parity programme. Branch
`claude/parity-a1` off `origin/main` ad8fc6524, worktree
`/Users/z3437171/local-scratch/parity-joint/wt-a1`. DRM.jl pin 430ef64cc at
`/Users/z3437171/local-scratch/parity-joint/drmjl-430ef64cc`.

**Ledger**: `.unlazy/parity/gates/leaf-a1.md` (G1-G5).

## Purpose

A green run must carry evidence. Three ways it did not:

1. **#1083** -- the Route-C parity test converted a fit ERROR into a skip.
2. **#1081** -- a green run said nothing countable about whether the bridge
   glue executed; its teardown line reads "bridge glue is UNTESTED".
3. **#1150** -- neither whole-file receipt (C17 model-15, lss-tip-identity)
   was checked at PR time, so a change to `R/` merged green with the receipt
   stale.

## What changed (five files, one new)

- `tools/ci-receipt-staleness.sh` (new). Detects, never regenerates. For
  `docs/dev-log/evidence/julia-r-parity/lss-tip-identity/public-001.json`
  (the one receipt a current claim cites) it compares the SHA-256 of every
  recorded `R/*.R` path and of the runner
  `tools/run-julia-phylo-labels-public.R` with the receipt's `source_before`
  -- the R-side half of the checker's `--current` mode, byte-identical
  semantics -- checks `status == PASS` and `source_unchanged`, names any
  `R/*.R` file present now but absent from the receipt (the runner globs
  `R/*.R`, so an unrecorded file means the receipt predates it), and
  verifies the DRM.jl-side entries only when `DRM_JL_PATH` names a checkout
  to map them onto (it says which it did). It then runs the R checker
  WITHOUT `--current` (structural validation) and
  `capability_ledger.check_c14_receipt_equivalence()` (the C17 whole-file
  blob check). Exit 0 only when every gated receipt matches the checkout.
- `.github/workflows/R-CMD-check.yaml`. Two Linux-only steps between
  "Evidence citations resolve" and `check-r-package`, in this order:
  **Julia bridge glue executes (mock-driven, no Julia)** runs eleven
  mock-driven bridge test files under `pkgload::load_all()`, prints a
  per-file and total tally, and fails unless `> 0` passed with `0` failures
  and `0` errors; then **Receipt staleness (detect, never regenerate)** runs
  the script. The glue step goes first so the receipt step -- red by design
  until the integrator's final regeneration -- cannot hide whether the glue
  step ran (on PR #1163's first CI run it did exactly that: the receipt step
  failed in 0.6 s and the glue step was never reached). `concurrency`
  untouched.
- `tests/testthat/helper-julia-bridge-path.R` (the summary-line tally only;
  OWNS extended 2026-09-05). The zero-live-tests line no longer says
  "bridge glue is UNTESTED": that over-claimed, because the mock-driven
  marshalling tests DO run in that configuration. It now reports the
  ENGINE boundary -- "the LIVE ENGINE was not exercised" -- and points at
  the CI step that counts the mock-driven tests. The any-live-tests line is
  unchanged ("bridge glue was exercised").
- `tests/testthat/test-julia-bridge-summary.R` (updated, never deleted).
  Pins the contract in both directions: zero live => "LIVE ENGINE was not
  exercised" and neither "UNTESTED" nor "bridge glue was exercised"; any
  live => "bridge glue was exercised" and never "not exercised".
- `docs/dev-log/after-task/2026-09-05-a1-ci-trust.md` (this file).

`tests/testthat/test-julia-tmb-parity.R` is in OWNS but is **unchanged**:
the #1083 fix is already on `origin/main` (450ab650e "stop converting
live-engine errors into skips (#1127)", 1c7ec1efd), as `drm_parity_run()` +
`drm_parity_run_error_handler()`, which skips only on the eight
environment-absent message patterns and calls `testthat::fail()` otherwise.
G1 is therefore a measured verification with a red control, not a code
change.

## Evidence (every number below was re-measured 2026-09-05 in the resumed run unless marked G1, which was measured 2026-09-04 by the first attempt and is carried as that attempt's record; macOS arm64, R 4.6, Julia 1.10.0, DRM.jl 430ef64cc)

The first attempt of this leaf died at a rate limit before reporting. Two
instances of it had run at once and left two numbers for the same thing
(1212 expectations across 11 files; 1958 across 40 files). Both were
discarded and the workflow step was re-measured: **1212 expectations across
11 files, 11.41 s and 12.12 s wall in two runs**. The 1958/40 figure was
never reproduced and appears nowhere in the branch.

### G1 (#1083): a planted engine error FAILS, and would have skipped under the old handler

Green baseline first, no plant:

```
ROUTE_C_BASELINE passed=5 failed=0 error=FALSE skipped=FALSE 27.8s
```

Plant 1 -- in `drm_parity_fit_route_c()`'s callr body, after
`pkgload::load_all()`, redefine the Julia entry point the bridge calls
(`drmTMB_drm_bridge(formula, family, data, tree, options)`, registered once
per session by `drm_julia_setup()`, so the redefinition sticks) to
`error("PLANTED engine error (A1 G1 control)")`. That is what a DRM.jl-side
signature/keyword/marshalling regression looks like from R: a JuliaCall
"Error happens in Julia." wrapped by callr.

```
 6.           └─drmTMB (local) drm_parity_run_error_handler(e, label) at test-julia-tmb-parity.R:75:30
── 2. Failure ('test-julia-tmb-parity.R:156:3') ... Expected `isTRUE(res$conv_tmb) && isTRUE(res$conv_jl)` to be TRUE.
── 3. Failure ('test-julia-tmb-parity.R:157:3') ...
── 4. Error ('test-julia-tmb-parity.R:159:3') ... Result of comparison must be `TRUE`, `FALSE`, or `NA`
ROUTE_C_RESULT passed=0 failed=3 error=TRUE skipped=FALSE 16.2s
```

Red control -- plant 1 still in place, plus the pre-#1083 handler restored
(an unconditional `testthat::skip(paste0(label, " unavailable: ", ...))` as
the first line of `drm_parity_run_error_handler()`):

```
1. engine='julia' == engine='tmb' to <=1e-6 on Gaussian location-scale (Route C) ('test-julia-tmb-parity.R:152:3') - Reason: tmb-vs-julia parity round-trip (Route C) unavailable: ! in callr subprocess.
Caused by error:
! Error happens in Julia.
PLANTED engine error (A1 G1 control)
ROUTE_C_RESULT passed=0 failed=0 error=FALSE skipped=TRUE 16.8s
```

Restore: `cp` from the pre-plant backup; `diff -q` empty ("restored
byte-identical"). The file in the PR is byte-identical to `origin/main`.

Observation, not fixed (outside the Route-C site): after `fail()` the
handler returns and the test body continues, so one planted engine error
reports as three failures and one error rather than one failure. Loud, not
wrong; a `return(invisible(NULL))` after `fail()` would not change the
verdict.

### G2 (#1081): the mock-driven bridge assertions run and are counted

Measured without Julia (`DRM_JL_PATH`, `DRM_JL_PHYLO_PATH`,
`DRMTMB_JULIA_TESTS` unset; `NOT_CRAN=true`), the exact step text from the
workflow, extracted by parsing the YAML (`suppressMessages()` around
`test_file()` drops the per-file teardown line; the `print()` output from
one test in `test-julia-inference.R` still reaches stdout -- pre-existing,
not A1's):

```
test-julia-bridge.R                        passed= 140 failed=0 error=0 skipped=2
test-bridge-payload-serialization.R        passed=   4 failed=0 error=0 skipped=0
test-julia-joint-call.R                    passed=   4 failed=0 error=0 skipped=0
test-julia-joint-dispatch.R                passed=   8 failed=0 error=0 skipped=0
test-julia-bridge-coef-labels.R            passed=  36 failed=0 error=0 skipped=0
test-julia-inference.R                     passed= 133 failed=0 error=0 skipped=4
test-julia-structured-inference.R          passed=  48 failed=0 error=0 skipped=0
test-julia-joint-finite-prepare.R          passed=  27 failed=0 error=0 skipped=0
test-structured-re-bridge-fixtures.R       passed= 755 failed=0 error=0 skipped=0
test-julia-objective-at-bridge.R           passed=   3 failed=0 error=0 skipped=2
test-xfam-bridge.R                         passed=  54 failed=0 error=0 skipped=4
Julia bridge glue (mock-driven, no Julia): 1212 expectations passed, 0 failed, 0 errors, 12 live skipped across 11 files
real 12.12   (first run of the day: real 11.41)
GLUE_EXIT=0
```

Red control -- append `test_that("A1 G2 red control", { expect_true(FALSE) })`
to `test-bridge-payload-serialization.R`:

```
test-bridge-payload-serialization.R        passed=   4 failed=1 error=0 skipped=0
Julia bridge glue (mock-driven, no Julia): 1212 expectations passed, 1 failed, 0 errors, 12 live skipped across 11 files
Error: mock-driven bridge glue did not execute cleanly (see tally above)
GLUE_RED_EXIT=1
```

Restored byte-identical (`git diff --quiet` on the file).

**The teardown line (the other half of G2).** Measured at
`cd tests && Rscript testthat.R` with `NOT_CRAN`, `DRMTMB_JULIA_TESTS`,
`DRM_JL_PATH`, `DRM_JL_PHYLO_PATH` all unset (the CRAN lane; package
installed from this worktree into a scratch library; helper and tests read
from the worktree):

```
[ FAIL 0 | WARN 18 | SKIP 25 | PASS 3570 ]
Julia bridge: 0 live tests ran, 2 skipped; the LIVE ENGINE was not exercised in this configuration (mock-driven marshalling tests are counted by CI's 'Julia bridge glue executes (mock-driven, no Julia)' step).
real 102.20
TESTTHAT_EXIT=0
```

The words "bridge glue is UNTESTED" no longer print in any lane. The
contract runs both ways (`tests/testthat/test-julia-bridge-summary.R`):

- without Julia (`NOT_CRAN` etc. unset): `SUMMARY_TEST passed=13 failed=0
  error=0 skipped=1` (the skip is the pre-existing "records a live run"
  test, which needs a DRM.jl directory);
- with `DRM_JL_PATH` = the 430ef64cc pin and `NOT_CRAN=true`:
  `SUMMARY_TEST_PIN passed=16 failed=0 error=0 skipped=0`;
- red control -- the old `"Julia bridge: %d live tests skipped; bridge glue
  is UNTESTED in this configuration."` planted back into the helper:
  `SUMMARY_TEST_RED passed=10 failed=3 error=0 skipped=1`; helper restored
  from backup, `diff -q` empty.

What the step's tally is NOT: live-engine evidence. A mock that replaces the
Julia call cannot see a signature mismatch with DRM.jl; that stays with the
local `DRM_JL_PATH` lane, and the teardown line now says so instead of
saying the glue was untested.

### G3 (#1150): the staleness script

`exit` codes captured directly (`$?` after the script, no pipe):

| checkout | result | exit |
|---|---|---|
| `origin/main` ea3156d73 (today's main, temporary detached worktree) | `STALE .../lss-tip-identity/public-001.json: current source hash differs from receipt: R/julia-bridge.R`, `...: R/phylo-utils.R`, `...: R/ file not recorded in receipt (added since): R/julia-family-registry.R`, `46 of 48 recorded R/ files match, 1 unrecorded R/ file(s), runner matches; 94 DRM.jl-side entries NOT verified (DRM_JL_PATH unset)`; `PHYLO_LABEL_RECEIPT_PASS`; C17 `OK` | **1** |
| a3ddf2e2d (last regeneration of that receipt; `wt-a1-receipt-fresh`) | `FRESH .../public-001.json: 48 of 48 recorded R/ files match, 0 unrecorded R/ file(s), runner matches; 94 DRM.jl-side entries NOT verified (DRM_JL_PATH unset)`; `PHYLO_LABEL_RECEIPT_PASS labels=12 rows=72 checks=8 tolerance=4e-06`; `C14 receipt equivalence: OK (3 eligible, 7 source-different retained receipts; C17 current-source compatibility PASS)` | **0** |
| a3ddf2e2d + one byte appended to `R/julia-bridge.R` | `current source hash differs from receipt: R/julia-bridge.R`; `47 of 48 recorded R/ files match` | **1** |
| a3ddf2e2d + an unrecorded `R/zzz-planted-a1.R` | `R/ file not recorded in receipt (added since): R/zzz-planted-a1.R`; `48 of 48 recorded R/ files match, 1 unrecorded R/ file(s)` | **1** |
| a3ddf2e2d with `DRM_JL_PATH` = the 430ef64cc pin | `DRM.jl source differs from receipt: .../drmjl-430ef64cc/src/bridge.jl`, `.../gaussian_sparse_lss.jl`, `.../inference.jl`; `91 of 94 DRM.jl-side entries match` | **1** |

The last row is a finding, not a defect of the script: the receipt at
a3ddf2e2d was produced against a DRM.jl clone (`.../scratchpad/drmjl-objat`)
whose `bridge.jl`, `gaussian_sparse_lss.jl` and `inference.jl` differ from
the programme's 430ef64cc pin. The integrator's final regeneration must be
made against the pin, and CI never sees this side (`DRM_JL_PATH` unset).

Restores after the red controls: `cp` from backup, `git diff --quiet` on
the file (`BYTE_RESTORED`); planted file removed, `git status` clean apart
from the script copy, which was then deleted; both temporary worktrees
(`wt-a1-receipt-fresh`, `wt-a1-main-tmp`) removed with `git worktree
remove`.

PR #1163's first CI run (job 101234044770, shard 1/4, 2026-09-05T02:40:42Z)
executed the committed script on the runner and reproduced the main
finding: the two `current source hash differs` lines, `47 of 49 R/ files +
runner match this checkout`, `PHYLO_LABEL_RECEIPT_PASS`, C17 `OK`, then
`##[error]Process completed with exit code 1.` -- so `jsonlite` and `digest`
are present on the runner (via `setup-r-dependencies`) and the R checker's
structural validation does run there.

C17 red control on main (byte appended to `R/methods.R`):

```
mc-0568: current source blob differs for R/methods.R
    receipt 77a050e5b1c558eb68f91b198b282be9269e5bc0
    current 35406fa511c16337037235e708ff1417a2b69e9e
   C17 receipt STALE or ledger invalid (see message above)
```

Restored byte-identical.

**Why main is red today, and why that is the finding.** The
lss-tip-identity receipt was last regenerated at a3ddf2e2d; merged commits
then touched `R/julia-bridge.R` -- 037cc1991 (#1152, engine-decided
estimator) and 94425965f (threads hint) -- `R/phylo-utils.R` moved as
well, and #1162 (c062a2285) added `R/julia-family-registry.R`, which the
receipt cannot know about. That is exactly the #1150 failure mode ("somebody forgot"), now
detected. G3's "exits 0 on main" cannot be met by A1: regeneration needs a
live Julia and the pinned DRM.jl clone, the receipt file is outside OWNS,
and the receipt must be regenerated LAST, after every other `R/` change in
this programme merges. Reported to the integrator as a blocker with the
exact command; the CI step will be red on main until that regeneration
lands, by design.

### G4: concurrency untouched

```
20:  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
```

`yaml.safe_load` of the edited workflow: `concurrency: {'group':
'${{ github.workflow }}-${{ github.ref }}', 'cancel-in-progress': "${{
github.event_name == 'pull_request' }}"}`; step order `[..., 'Evidence
citations resolve', 'Julia bridge glue executes (mock-driven, no Julia)',
'Receipt staleness (detect, never regenerate)',
'r-lib/actions/check-r-package@v2']`.

### G5: merge

Pending by rule -- the integrator merges in dependency order. Not A1's to
claim.

## Findings that changed the design while building it

1. **`capability_ledger.py --check` does NOT check the C17 receipt.** A
   byte appended to `R/methods.R` passed `--check`; the whole-file blob
   comparison lives behind `--check-c14-receipt-equivalence`, which CI
   reaches only via `tools/tests/test_capability_ledger.py`. The script
   calls the flag by name. Caught by the red control, not by reading.
2. **`check-julia-phylo-labels-receipt.R --current` cannot run on a CI
   runner.** The receipt manifest pins every DRM.jl `src/*.jl` file by the
   ABSOLUTE path of the clone that produced it (public-001.json:
   `/private/tmp/.../scratchpad/drmjl-objat/src/*.jl`, 94 of its 142 keys),
   and the checker fails closed on the first absent path. #1150's premise
   that the hash comparison "needs no Julia" is true only of the R side.
   The script does that half itself. A `--current-r-only` flag on the
   checker (owner: whoever owns `tools/check-julia-phylo-labels-receipt.R`)
   would let the script call the real checker instead of re-implementing
   48 hash comparisons; the semantics are identical today.
3. **The two `phylo-labels/public-00{1,2}.json` receipts are staler still**
   (`R/check.R` differs; same absolute-path pinning). They are not the
   receipt #1150 names and the script does not grade them; recorded so
   nobody reads their `PASS` status as current.
4. **The bridge glue was never 0% executed in CI.** #1081's "0%" is about
   the LIVE call; 1212 mock-driven expectations across eleven files already
   ran in every sharded job. What was missing was a counted, failing claim
   about it -- and the teardown line saying "UNTESTED" over the top of it.
   Both are now fixed (the line was in OWNS from 2026-09-05).
5. **A file ADDED under `R/` was invisible to the first script.** It
   iterated the receipt's recorded paths only, so `R/julia-family-registry.R`
   (#1162) produced no STALE line although the runner globs `R/*.R` and the
   receipt therefore predates it. Found by running the script on today's
   main and counting 49 files against 48 entries; fixed and red-controlled
   with a planted `R/zzz-planted-a1.R`.
6. **The last receipt regeneration was not against the programme's DRM.jl
   pin.** With `DRM_JL_PATH` pointing at 430ef64cc, three DRM.jl-side
   entries (`bridge.jl`, `gaussian_sparse_lss.jl`, `inference.jl`) differ
   from the receipt. The integrator's final regeneration must use the pin.
7. **A hard-fail step before the glue step hid the glue step.** On the
   PR's first CI run the receipt step failed (by design) and the glue step
   never executed, so CI had not yet exercised #1081's step. Reordered:
   glue first, receipt second.

## Red controls (each: planted, failed as required, restored byte-identical)

| gate | plant | observed | restore |
|---|---|---|---|
| G1 | Julia `drmTMB_drm_bridge` redefined to `error(...)` inside the Route-C fixture | `failed=3 error=TRUE skipped=FALSE` (fail, not skip) | `diff -q` empty |
| G1 (old handler) | same plant + unconditional `skip()` first in the handler | `skipped=TRUE`, reason quotes the planted Julia error | `diff -q` empty |
| G2 | `expect_true(FALSE)` appended to `test-bridge-payload-serialization.R` | tally `1 failed`, step `exit=1` | `diff -q` empty |
| G3 | one byte appended to `R/julia-bridge.R` at a3ddf2e2d | `current source hash differs from receipt: R/julia-bridge.R`, `exit=1` | `diff -q` empty |
| G3 (C17) | one byte appended to `R/methods.R` on main | `mc-0568: current source blob differs for R/methods.R` | `diff -q` empty |
| G3 (added file) | `R/zzz-planted-a1.R` created at a3ddf2e2d | `R/ file not recorded in receipt (added since): R/zzz-planted-a1.R`, exit 1 | file removed, `git status` clean |
| G2 (teardown) | old "bridge glue is UNTESTED" sprintf planted back into the helper | `SUMMARY_TEST_RED passed=10 failed=3` | `diff -q` empty |

## Errors made along the way

- Assumed `capability_ledger.py --check` covered the C17 receipt and wrote
  the script that way; the red control showed it did not. Fixed to
  `--check-c14-receipt-equivalence`.
- First package compile in the worktree failed with a spurious rename error
  on `src/drmTMB.o`; a plain retry succeeded. No code change involved.
- A plant not written by A1 appeared in `test-julia-tmb-parity.R` mid-run
  (a second agent in the same worktree). Backed up, then restored from
  `git show HEAD:` so the PR carries no plant; reported to the integrator.
- (First attempt) two instances of this leaf ran at once and left two
  unreconciled numbers (1212/11 files vs 1958/40 files) and a helper edit
  whose step name did not match the workflow. The resumed run re-measured
  and kept one truth.
- (First attempt) this after-task described the script as doing things it
  did not do: a `jsonlite`/`digest` guard, "every public-NNN.json", and the
  unrecorded-R/-file check. Rewritten to match the script as committed;
  the unrecorded-file check was then actually built.
- (Resumed run) the first CRAN-lane measurement of the teardown line began
  before the helper edit landed and printed the intermediate wording; the
  run was repeated (102.20 s) against the final helper.
- (Resumed run) placed the receipt step before the glue step, so the PR's
  first CI run never reached the glue step; reordered.

## Not covered

- The lss-tip-identity receipt is not regenerated; main will be red on the
  new step until it is, and the regeneration must be against DRM.jl
  430ef64cc (finding 6).
- (Closed after the reordered push.) The glue step has now executed on a
  GitHub runner: run 33961252504, job 101293413705 (shard 1/4,
  2026-09-05T10:44:43Z) printed the same eleven per-file rows and
  `Julia bridge glue (mock-driven, no Julia): 1212 expectations passed, 0
  failed, 0 errors, 12 live skipped across 11 files`; the receipt step then
  exited 1 by design (three STALE lines, the third naming
  `R/julia-family-registry.R` because the PR is checked as a merge with
  today's main).
- G1 was not re-measured in the resumed run; its numbers are the first
  attempt's record (Route-C test file is byte-identical to `origin/main`,
  verified again: `git diff origin/main -- tests/testthat/test-julia-tmb-parity.R` empty).
- `phylo-labels/public-00{1,2}.json` are not graded by the script.
- No DRM.jl change, no parity TSV rows produced (this leaf fits nothing new;
  the Route-C live run was a test-suite execution, not a parity-tool run).

## Contract sections (protocols/after-task.md; each points at the prose above)

## 1. Goal
A green `R-CMD-check` run must carry evidence about the Julia bridge and the parity receipts: #1083, #1081, #1150 (see "Purpose").

## 2. Implemented
Five files, one new: the staleness script, the two workflow steps (glue first, receipt second), the teardown line, its pinning test, this report (see "What changed").

## 3a. Decisions and Rejected Alternatives
- Re-implement the R-side hash comparison in Python instead of calling `check-julia-phylo-labels-receipt.R --current` (rejected: it cannot pass on a runner; finding 2).
- Gate only `lss-tip-identity/public-001.json`, not the dated `phylo-labels` receipts (rejected gating them: no current claim cites them; script header).
- Detect, never regenerate; A1 merges last (integration rule in the ledger).
- Reorder the steps so the designed-red receipt step cannot hide the glue step (finding 7).
- Teardown line reports the ENGINE boundary, not "UNTESTED" (finding 4).

## 4. Files Touched
`.github/workflows/R-CMD-check.yaml`, `tools/ci-receipt-staleness.sh` (new), `tests/testthat/helper-julia-bridge-path.R`, `tests/testthat/test-julia-bridge-summary.R`, `docs/dev-log/after-task/2026-09-05-a1-ci-trust.md`. `tests/testthat/test-julia-tmb-parity.R` in OWNS but byte-identical to `origin/main`.

## 5. Checks Run
Glue step (exact YAML text) twice: 1212/0/0, exit 0. `test-julia-bridge-summary.R` without Julia 13/0/0/1 and with the pin 16/0/0/0. CRAN-lane `cd tests && Rscript testthat.R` (Julia env unset): `FAIL 0 | WARN 18 | SKIP 25 | PASS 3570`, 102.20 s. Staleness script on main ea3156d73 (exit 1), a3ddf2e2d (exit 0), wt-a1 (exit 1). `yaml.safe_load` of the workflow. PR #1163 first CI run read for the receipt step's runner output.

## 6. Tests of the Tests
Seven red controls, each planted, failed as required, restored byte-identically (table "Red controls").

## 7a. Issue Ledger
#1083 verified (already fixed on main, G1). #1081 counted step + honest teardown line (G2). #1150 detect-only staleness step (G3), red on main by design until the integrator regenerates against DRM.jl 430ef64cc. Ledger: `.unlazy/parity/gates/leaf-a1.md`.

## 8. Consistency Audit
One measured number for the glue step everywhere it appears (workflow comment, ledger, this report, PR body): 1212 expectations, 11 files, 11.4-12.1 s. Step name identical in the workflow, the helper, and the test. The script's header, the ledger, and this report describe the same script.

## 9. What Did Not Go Smoothly
See "Errors made along the way": two concurrent first-attempt instances, an after-task that over-described the script, a CRAN-lane measurement that raced the helper edit, and a step order that hid the glue step on CI.

## 10. Known Residuals
See "Not covered": receipt regeneration (integrator, against the pin), the glue step's first execution on a runner, G1 not re-measured in the resumed run, the dated `phylo-labels` receipts ungraded.

## 11. Team Learning
A hash manifest that iterates only recorded entries cannot see an added file; check the glob against the manifest as well. A hard-fail step placed before a step you want evidence from hides that step -- order designed-red steps last. Re-measure every number a dead attempt left behind; two instances left two.

## 12. Cross-Product Coverage
Engine flag x surface. Covers: CI runner x receipt step (executed on the runner in PR #1163's first run, exit 1 as designed); CI runner x glue step (executed on the runner in run 33961252504: 1212/0/0); local x {without Julia, with the 430ef64cc pin} for the glue step, the summary test, and the CRAN-lane teardown line; the R-side and runner entries of the lss-tip-identity receipt on any checkout; the DRM.jl-side entries only locally with `DRM_JL_PATH` (3 of 94 differ from the pin). It does NOT cover: the DRM.jl-side receipt entries on CI (`DRM_JL_PATH` unset there, stated in the script's own output); a live `engine = "julia"` fit on any runner (mocks cannot see a DRM.jl signature mismatch); the dated `phylo-labels/public-00{1,2}.json` receipts; macOS and Windows runners (both steps are `runner.os == 'Linux'`); the release-tag shard-0 matrix, which runs the same steps but was not exercised.

## Addendum 2026-09-05: the final regeneration (closes "Not covered" item 1, finding 6)

**Confirmed red is the designed red, not a regression.** All four shards of
run 33986931674 (head 046f4728c, this branch merged with `origin/main`)
failed at the identical step, `Run bash tools/ci-receipt-staleness.sh`,
with the identical three `STALE` lines quoted above under "Why main is red
today" -- `current source hash differs from receipt` for
`R/julia-bridge.R`, `R/julia-family-registry.R`, `R/profile.R`, plus four
`R/ file not recorded in receipt (added since)` lines for
`R/coevolution-accessors.R`, `R/julia-diagnostics.R`, `R/lrt-boundary.R`,
`R/model-comparison.R`. The glue step immediately above it printed the same
`1212 expectations passed, 0 failed, 0 errors, 12 live skipped across 11
files` in every shard, confirming the reordering from the base report still
holds and the receipt step is reached and evaluated, not skipped. This is
exactly the mechanism the script was built to demonstrate (`ci-receipt-
staleness.sh`'s own header: "detect, never regenerate"), not a test the
branch broke or a script exit-contract bug -- no code in `tools/ci-receipt-
staleness.sh` changed.

**Why it recurred after 858b67256.** That commit (on `origin/main`,
2026-09-05T04:26) regenerated the receipt against the 430ef64cc pin over
main-plus-registry. Main then moved again -- more `R/`-touching parity
leaves merged (the four new files above, plus edits to the same three
hashed files) -- and this branch's merge commit `046f4728c` picked up that
later main state, so the receipt inherited from main was stale again
relative to the merge. This is the documented integration rule working as
designed: "the integrator's final regeneration must be made against the
pin" (finding 6) and it happens LAST, immediately before this leaf merges,
not once earlier in the programme.

**What was done.** Regenerated
`docs/dev-log/evidence/julia-r-parity/lss-tip-identity/public-001.json` with
`Rscript --no-init-file tools/run-julia-phylo-labels-public.R
/Users/z3437171/local-scratch/parity-joint/drmjl-430ef64cc
docs/dev-log/evidence/julia-r-parity/lss-tip-identity/public-001.json`
against the same 430ef64cc clone (confirmed clean: `git -C
.../drmjl-430ef64cc status -s -- src/` empty, `rev-parse HEAD` =
430ef64ccca5642c5abebd72194e00895314dfc2). `PHYLO_LABEL_PUBLIC_PASS
elapsed=33.833`; `result.status "PASS"`; all 8 checks (`names`,
`covariance`, `likelihood`, `native_parity`, `bridge_parity`, `rows`,
`converged`, `source`) true; `source_unchanged` true; `drmjl_ref` =
430ef64ccca5642c5abebd72194e00895314dfc2. The diff is exactly the four new
`R/*.R` manifest entries plus the three updated hashes named above, twice
(the manifest is recorded under both `source_before` and `source_after`),
plus the `native_dll` tmp-path/sha256 pair, which always changes between
runs (a fresh `pkgload` temp install). No other field changed.

**Verification, in order, matching the CI step order.**

| check | command | result |
|---|---|---|
| checker, structural + current | `Rscript tools/check-julia-phylo-labels-receipt.R public-001.json --current --self-test` | `PHYLO_LABEL_RECEIPT_PASS labels=12 rows=72 checks=8 tolerance=4e-06`, exit 0 (12 `SELFTEST_REJECTED` lines are the self-test's own negative-control assertions, not failures) |
| the CI script itself | `bash tools/ci-receipt-staleness.sh` | `FRESH .../public-001.json: 53 of 53 recorded R/ files match, 0 unrecorded R/ file(s), runner matches; 94 DRM.jl-side entries NOT verified (DRM_JL_PATH unset)`; `PHYLO_LABEL_RECEIPT_PASS`; `C14 receipt equivalence: OK (3 eligible, 7 source-different retained receipts; C17 current-source compatibility PASS)`; exit 0 |
| glue step, re-run locally | exact YAML block, 11 files | `1212 expectations passed, 0 failed, 0 errors, 12 live skipped across 11 files` (identical to the CI run) |
| evidence citations | `Rscript tools/check-evidence-citations.R` | `37 rows, 7 cited rejections checked, 0 violation(s)`, exit 0 |
| capability ledger | `python3 tools/capability_ledger.py --check` | `capability-ledger: OK (31 generated outputs)`, exit 0 |
| capability ledger unit tests | `python3 -m unittest tools/tests/test_capability_ledger.py` | `Ran 80 tests ... OK` (includes `C14 receipt equivalence: OK`) |
| profile-truth manifest | `Rscript tools/emit-profile-truth-manifest.R --check` | `profile-truth-manifest: OK (30 rows)`, exit 0 |
| capability runtime | `Rscript tools/check-capability-runtime.R` | `capability-runtime: OK (18 routes; G0=0 G1=0 G2=0; verified=18)`, exit 0 |
| `test-julia-bridge-summary.R` | `testthat::test_file`, `NOT_CRAN=true` | 13 passed, 0 failed, 1 skipped (live-engine skip, expected without `DRM_JL_PATH`) |
| C17-pinned files touched? | `git diff origin/main...HEAD -- R/drmTMB.R R/methods.R src/drmTMB.cpp tests/testthat/test-zero-one-beta.R tools/run-lane-c-c17c1-c14-model15-compatibility.R` | empty -- this leaf does not touch a C17-pinned file, so `tools/recertify-c17.py` was not run (and the staleness script's own C17 block already reports `PASS` above) |

Files changed by this addendum: `docs/dev-log/evidence/julia-r-parity/
lss-tip-identity/public-001.json`, `.../public-001.log` (the runner's
stdout/stderr, replacing the record of the previous regeneration),
this file. No R/, tests/, or workflow code changed.

## Not covered (addendum)
- Full `testthat::test_dir()` / CRAN-lane `tests/testthat.R` re-run after
  the regeneration: attempted locally on a machine running ~15-20 other
  agent sessions' worktrees concurrently (many simultaneous R/TMB fits);
  both runs surfaced testthat 3.3.2's `_problems/` reprex-saving for
  contexts this leaf never touches (e.g. `test-missing-predictor-
  transformed-covariance.R`, `test-binomial-correlated-re-mspl-prereq.R`).
  No R/, tests/, or workflow code changed by this leaf, and neither GitHub
  CI run (33986931674, 33998639976) reached `check-r-package` -- both
  failed earlier, at the receipt step -- so there is no clean-runner
  evidence either way for these contexts. Read as probable resource-
  contention artifacts of the shared machine, not a regression from this
  change; not chased further (out of this leaf's scope, and the local
  runs were killed rather than left competing for CPU with other
  sessions' legitimate work).
- The `phylo-labels/public-00{1,2}.json` receipts remain ungraded by
  design (unchanged from the base report).

## Addendum 2026-09-05 (second): the staleness step is racy against a
## fast-moving `main`, not fixable from inside this branch

Pushed 3f3973c8b (the regeneration above); the next CI run
(33998639976, same head SHA) failed all four shards again at the same
step, this time on a single new line: `STALE ... R/ file not recorded in
receipt (added since): R/julia-family-cumulative_logit.R`.

Cause, confirmed by evidence, not guessed: `.github/workflows/
R-CMD-check.yaml`'s `pull_request` trigger uses plain `actions/
checkout@v6.0.2` with no `ref:` override, so GitHub's default behavior
checks out the PR **merge-preview** commit (`refs/pull/1163/merge` --
head merged onto whatever `main` is at trigger time), not the branch tip.
Between the push and that run's checkout, `git log --merges
<merge-base>..origin/main` shows PRs #1192, #1194, #1195 (among many
others across the wider window) had landed on `main`; #1195 added `R/
julia-family-cumulative_logit.R`. The receipt, correct against the
branch content as regenerated, was immediately stale again against the
new merge preview -- reproduced identically on all four shards.

This is the same "detect, never regenerate" mechanism firing correctly,
not a second defect. It could not be corrected from inside this session:
doing so needs `git merge origin/main` into `claude/parity-a1`, and
"never merge" is a standing constraint on this task (`git merge` was
also refused by the local command guard when attempted). Filed as a
structural finding for the integrator rather than chased with further
regenerate/push cycles against a target moving roughly one `R/`-touching
merge per 30 minutes right now (`git log --oneline --since="2 hours ago"
origin/main -- 'R/*.R'` = 4 merges in the two hours checked): as long as
other parity leaves keep landing `R/`-touching PRs, this step will keep
going stale between any push and the next PR-triggered run, through no
fault of this branch's content. **The final regeneration needs to happen
at the actual moment no more such PRs are pending, immediately before
this PR is merged** -- not at any earlier snapshot, and not repeatable
to a stable green from this branch alone while the wider programme is
still landing concurrently.

Reported in the PR comment (this same finding, condensed) rather than
re-attempted here.

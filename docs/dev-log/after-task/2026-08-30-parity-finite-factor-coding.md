# Finite-state factor coding and native stopping audit

## 1. Goal
Continue the approved Julia–R parity programme (#563). This checkpoint covers finite predictor formula construction, an independent diagnostic of frozen stopping differences, and a native R regression covariance repair. Programme G0–G8 remain OPEN.

## 2. Implemented
Direct Julia expands the complete row-by-state mean formula with declared marker levels. It preserves first-factor coding without an intercept, ordinal marker polynomial contrasts, interaction column order and native coefficient names. Boolean columns keep FALSE/TRUE factor coordinates, including singleton columns. Native R regression covariance slot resolution was separately repaired in 05cf5ee64. The numerical likelihood and optimizer defaults are unchanged.

## 3a. Decisions and Rejected Alternatives
Keep the frozen native comparator and 4e-6 threshold. The independent Newton diagnostic explains the retained local displacement; it is not a replacement fit or global optimum certificate. Package-specific categorical/ordered values used in direct means are refused until a typed contrast contract is implemented. They remain required parity work. Do not infer warm speed or universal thread safety from a correctness pilot.

## 4. Files Touched
Julia frontend, factor tests/test wiring, generated native designs, stopping checker/receipt, bounded concurrent-fit pilot, developer page, progress/checkpoint and reports. R factor exporter and generated evidence/public005 refresh; the native covariance repair has its own report. Foreign S5 test/include, R ZOB changes, denied sparse source files and unrelated vault work were preserved.

## 5. Checks Run
Final local factor tests pass 109 assertions; existing finite frontend tests pass 86. Eight concurrent fits match independent frozen serial outputs in 83 assertions on four Julia threads and one BLAS thread: 278 total, 32.03 seconds including startup. Native R covariance tests pass 101 assertions and six ordinary profile/Wald tests pass 77. Public 005 passes both bridge adapters with unchanged source in 27.5 seconds including startup; independent replay rejects 17 corruptions normally and under Python -O. Both strict native comparisons still FAIL. Both edited documentation pages execute; this is not whole-site visual/deployed proof. The corrected Totoro pilot passes the same278 assertions in46seconds onJulia1.10.10 atfourJulia/oneBLASthreads; sourcebundlechecksum is retained. These elapsed times are not comparable warm benchmarks.

## 6. Tests of the Tests
Retained failures exposed an absent helper, lexical marker baseline, duplicated prefix columns, native name differences and singleton contrast construction. Rose added Boolean cases to the original 14 designs; 20 generated designs now compare complete arrays and names. Direct R probing corrected the mistaken singleton-logical refusal assumption. The concurrency checker now deep-copies serial baselines before threading; a deliberately changed coefficient fails exactly one assertion with no success token. The stopping checker rejects six damaged receipts normally and with Python -O. R covariance tests failed 16 assertions before repair.

## 7a. Issue Ledger
DRM.jl#563 stays OPEN. No whole missing-predictor capability row closes. All 24 initial native obligations, the complete valid-case/output manifest, and original LSS stamped SE/REML/masks/inference/large-tree/final-head requirements remain required. The approved programme plan is unchanged.

## 8. Consistency Audit
Rose independently approved final factor source and the repaired local concurrency oracle. Melissa required explicit direct-Julia newdata, raw/public ordinal coordinate and transformed covariance gaps. They are now in the progress ledger and checkpoint. Golden Set: immutable native finite 180-row/all-four-mask fixtures, generated first-factor/interaction/Boolean designs and existing one/two-predictor regressions. Native R and direct Julia remain separate admission/output surfaces.

## 9. What Did Not Go Smoothly
The native exporter initially passed an unevaluated formula expression to bf(). Independent review found language-specific Boolean coding after the first 78 assertions passed. The first Totoro bundle omitted the historical 001 fixture still read by the test, stopping after five seconds; that failed attempt is retained and the corrected bundle passed in a new directory. The current after-task validator requires an explicit twelfth cross-product section.

## 10. Known Residuals
Strict 4e-6 finite and earlier single-predictor losses remain open. Required: direct finite newdata/schema retention, typed categorical/ordered inputs, raw/public cutpoint and covariance accessors, positive-scale/mixture covariance Jacobians and interval conventions; profile/bootstrap validation; every registered warm win and automatic 1/2/4/8 policy; full manifest; unfinished Claude/Cursor work; recoverable cleanup; whole-site visual/deployed proof. Evidence stamps development working-tree bytes, not a clean final-head integration. No release, registration, public deployment or collaborator message. Actual agent-hours were not instrumented.

## 11. Team Learning
Equal matrix widths do not prove equal formula coordinates. Preserve factor order, level order, names and covariance axes. Concurrent tests need independently frozen reference outputs, so a shared-buffer defect cannot move both sides of a comparison.

## 12. Cross-Product Coverage
This slice covers Gaussian responses, one ordinal/categorical missing predictor, fixed designs and tested plain-string/Boolean mean factors. It does NOT cover typed ordered exogenous factors, direct Julia newdata prediction, non-Gaussian responses, random/structured predictor providers, REML, penalty, aggregation, executed profile/bootstrap workflows, all 1/2/4/8 settings or every public operation. The R bridge's prepared matrices and direct Julia's schema admission are separate contracts. Whole-programme G0–G8 remain OPEN.

# D-269 bridge reader wording — after-task report

## 1. Goal

Align the remaining reader-facing bridge wording with the selected-checkout
loader implemented by drmTMB PR #1381.

## 2. Implemented

Replaced the inaccurate canonical-module-first fallback description in NEWS and
the Julia-engine vignette with the declared-package-name and source-path
verification contract.

## 3a. Decisions and Rejected Alternatives

Corrected prose only. No loader, API, option precedence, or legacy compatibility
behavior changed because the implementation already had the intended contract.

## 4. Files Touched

- `NEWS.md`
- `vignettes/julia-engine.Rmd`
- `docs/dev-log/check-log.d/2026-09-18-d269-bridge-reader-wording.md`
- `docs/dev-log/after-task/2026-09-18-d269-bridge-reader-wording.md`

## 5. Checks Run

- `git diff --check` PASSed.
- `knitr::knit("vignettes/julia-engine.Rmd", output = tempfile(...))` PASSed.

## 6. Tests of the Tests

The bridge PR's existing focused tests exercise selected canonical and legacy
checkouts and path verification. This addendum changes only the wording that
describes those tested behaviors.

## 7a. Issue Ledger

This is a scoped addendum to open draft PR #1381. It does not close an issue or
change the separate merge hold on #1111.

## 8. Consistency Audit

Checked #1110 and #1111 file lists: neither owns these two prose files. The
selection precedence stated immediately before the corrected vignette text was
retained unchanged.

## 9. What Did Not Go Smoothly

The original bridge report correctly held these passages out of scope while
their ownership was uncertain. File-list inspection later showed the boundary
was clear.

## 10. Known Residuals

PR #1381 is green but remains draft and unmerged pending #1111 reconciliation
and Shinichi's merge gate. No full release check or Pages deployment was run.

## 11. Team Learning

When package identity is selected from a checkout, documentation must describe
the declared-name and resolved-source contract, not an import-order heuristic.

## 12. Cross-Product Coverage

This covers ✓ reader-facing NEWS and the Julia-engine vignette. It does NOT cover loader implementation, help generated by #1110, all package checks,
release readiness, or public Pages deployment.

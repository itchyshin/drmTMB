# After-task: Non-Gaussian fixed-effect recovery 500-rep promotion

**Date:** 2026-06-20
**Author:** Ada (autonomous, pushes held)
**Branch:** `shannon/overnight-audit-gaps-20260619`

## Task goal

Scale the in-flight non-Gaussian fixed-effect recovery pilot (50 reps) to
promotion grade (500 reps) and, only if the evidence holds under Rose+Fisher
adversarial verification, promote the scoped "Non-Gaussian models" matrix cell.
Discipline: partial/planned falls only to new measuring evidence, scoped; never a
status-flip.

## Files created or changed

- `docs/dev-log/simulation-artifacts/2026-06-20-nongaussian-fe-recovery-calibration/tables/nongaussian-fe-coverage-summary.csv`
  and `.../nongaussian-fe-fits.csv` — regenerated by the 500-rep run (the banked
  evidence).
- `docs/dev-log/simulation-artifacts/2026-06-20-nongaussian-fe-recovery-calibration/README.md`
  — rewritten from "PILOT (50 reps)" to the 500-rep result + verified promotion
  outcome (clears the staleness blocker both verifiers raised).
- `docs/design/168-r-julia-finish-capability-matrix.md` — "Non-Gaussian models"
  row: point `partial -> covered`; next-gate text updated.
- `docs/dev-log/dashboard/status.json` — same cell promoted (point `covered`),
  `next`/`evidence_url` updated, `updated` timestamp refreshed, one activity
  entry prepended.
- `docs/dev-log/check-log.md` — new top entry.
- This after-task report.

No code, family, formula-grammar, or likelihood change. Doc/status surfaces only.

## Checks run and exact outcomes

- `Rscript --vanilla run.R 500`: 12,000 fits, 0 fit/confint errors, n_ok=500 every
  cell, elapsed 289.4 s. Largest absolute bias 0.0052 (nbinom2 n=300 intercept);
  pdHess 1.000 for five families, 0.996 for student n=300. Wald coverage by family:
  beta 0.950-0.960, Gamma 0.944-0.956, lognormal 0.940-0.962, nbinom2 0.932-0.954,
  poisson 0.938-0.970, student 0.926-0.952.
- `python3 -c "import json; json.load(...)"`: status.json valid JSON.
- `python3 tools/validate-mission-control.py`: `mission_control_ok: 25/68
  banked_or_verified, 1 active, 17 matrix rows, 11 finish rows, 15 Julia gate
  rows, 9 Julia capability rows` (metrics and counts unchanged — a cell-status
  change does not move slice metrics).
- `git diff --check`: clean.

## Consistency audit

- Forbidden-term scan
  `grep -nE "meta_gaussian|tau ~|rho ~|meta_known_V\([^V]"` over the four changed
  doc surfaces: the only hits are historical `rg`-pattern records inside
  `check-log.md`; the promoted surfaces (design 168, status.json, README, the new
  check-log entry) introduced none. Terms stay stable (`mu`, `sigma`, `rho12`,
  family names unchanged).
- Status-surface sync: the only status surfaces for "Non-Gaussian models" are the
  matrix row and the dashboard cell — both promoted together. The `finish_board`
  has no non-Gaussian row (unlike binomial / rho12, which have their own), so
  there was no finish row to sync.

## Tests of the tests

- The run is deterministic (`master_seed = 20260620` + per-cell/per-rep offsets),
  so the evidence is reproducible bit-for-bit.
- Rose and Fisher each independently recomputed the per-cell coverage/bias/n_ok
  from the 12,000-row per-fit CSV and reproduced the summary CSV exactly — the
  aggregate is faithful, not taken on trust.
- The contract's escape hatch was actually exercised: the sim was sensitive enough
  to flag student n=300 mu:x = 0.926 below the 0.93 floor, so wald was correctly
  held. The test is discriminating, not rubber-stamping.

## What did not go smoothly

- Verifiers split on the `simulation` cell (Fisher promote, Rose hold). Resolved
  by the "both must agree" rule with the guardrail hold winning; Rose's argument
  (no explicit simulation-promotion clause here, and the binomial precedent keeps
  simulation partial despite covered intervals) is the disciplined call.
- The Edit tool required a fresh Read of `check-log.md` (it had only been viewed
  via shell); minor.
- The cwd-reset hazard (shells default to the stale Dropbox checkout) held
  throughout; mitigated by pinning absolute 540b paths on every call.

## Team learning and process improvements

- "Covered intervals do not auto-carry a simulation promotion" is now a recorded
  precedent: the binomial row (covered wald+profile, simulation partial) is the
  governing analogue, not rho12 (which carried an explicit clause). When an
  artifact intends to promote the simulation cell, it should state so explicitly
  in its README, mirroring the rho12 pattern.

## Design-doc updates

- `docs/design/168-r-julia-finish-capability-matrix.md` updated (the canonical
  claim matrix / widget source of truth). No other design doc needed changes (no
  grammar/likelihood/family change).

## pkgdown/documentation updates

- None required. This is an internal status/evidence promotion; no exported
  function, vignette, or `_pkgdown.yml` entry changed. README/ROADMAP/NEWS were
  deliberately left unchanged, consistent with the binomial and rho12 scoped-cell
  promotions (which also did not touch them).

## GitHub issue maintenance

- Deliberately left unchanged: pushes are held this session, and no single open
  issue maps to this scoped point-cell promotion. The dashboard/matrix is the
  work ledger; the evidence trail is in the artifact README + check-log.

## Known limitations and next actions

- Wald and simulation cells for "Non-Gaussian models" remain partial. Wald's
  blocker is the student small-n cell (0.926); a future slice could add reps or
  larger n for student specifically, or flag it permanently as a heavy-tail
  small-n caveat.
- The point promotion is strictly fixed-effect mu, native R/TMB, complete data,
  six implemented one-response families. Random/structured effects, scale/shape
  intervals, profile/bootstrap, bivariate/mixed, and the Julia bridge stay
  planned and each need their own recovery row.
- Next evidence-path items (from the reconciliation): a binomial coverage visual
  (Florence-gated); high-2 atomic::logdet re-land after q4-convergence
  adjudication (owner-gated); Route C interval parity for the bridge.

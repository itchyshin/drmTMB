# After Task: NB2 PR Split Slices 556-560

## Goal

Move from the mixed local checkout to clean PR lanes: merge the completed
Poisson q1 PR, separate the remaining Poisson formal-admission tail, and open a
reviewable NB2 branch for Slices 496-555 plus the split evidence from
Slices 556-560.

## Implemented

PR #316 was merged after its R-CMD-check jobs were green. PR #318 had already
merged with green checks. The remaining Poisson q1 formal-admission commit was
split into PR #319 so the NB2 work does not quietly carry Poisson formal
machinery as an unrelated change.

The new NB2 branch, `codex/nb2-smoke-grid-511-560`, is stacked on the PR #319
head. It replays the fitted NB2 gates from the local branch, excludes the old
pkgdown-logo CSS slice, and ports only the NB2 simulation, documentation, and
audit files for Slices 511-555.

## Mathematical Contract

The branch makes no new likelihood claim beyond the existing fitted NB2 gates:
ordinary NB2 log-`sigma` random intercepts and ordinary NB2 q=1 phylogenetic
`mu` intercepts. Slices 511-555 add simulation/evidence artifacts and a formal
audit lane, not broader NB2 structured-count support. The NB2 q1 formal-audit
decision remains `hold_smoke_only` until the 500-replicate formal grid is run
and reviewed.

## Files Changed

- PR split evidence: `docs/dev-log/check-log.md` and this after-task report.
  The recovery checkpoint was written locally under the ignored
  `docs/dev-log/recovery-checkpoints/` handoff directory.
- NB2 smoke/formal evidence files under `inst/sim/dgp`, `inst/sim/fit`, and
  `inst/sim/run`.
- Focused NB2 Phase 18 tests under `tests/testthat/`.
- NB2 ADEMP/audit docs under `docs/design/73-*`, `docs/design/74-*`, and
  `docs/design/75-*`.
- Shared sync files: `NEWS.md`, `ROADMAP.md`, `inst/sim/README.md`,
  `vignettes/source-map.Rmd`, Phase 18 readiness/programme docs, validation
  debt, the Actions workflow, and `sim_run_actions_cell.R`.

## Checks Run

```sh
Rscript -e "files <- c('inst/sim/dgp/sim_dgp_nbinom2_sigma_random_effect.R','inst/sim/fit/sim_summarise_nbinom2_sigma_random_effect.R','inst/sim/run/sim_run_nbinom2_sigma_random_effect_smoke.R','inst/sim/run/sim_summary_nbinom2_sigma_random_effect_smoke.R','inst/sim/run/sim_write_nbinom2_sigma_random_effect_grid.R','inst/sim/dgp/sim_dgp_nbinom2_phylo_q1.R','inst/sim/fit/sim_summarise_nbinom2_phylo_q1.R','inst/sim/run/sim_run_nbinom2_phylo_q1_smoke.R','inst/sim/run/sim_summary_nbinom2_phylo_q1_smoke.R','inst/sim/run/sim_write_nbinom2_phylo_q1_grid.R','inst/sim/run/sim_run_actions_cell.R','tests/testthat/test-phase18-nbinom2-sigma-random-effect.R','tests/testthat/test-phase18-nbinom2-phylo-q1.R'); invisible(lapply(files, parse)); cat('parse ok\n')"
Rscript -e "devtools::test(filter = 'phase18-nbinom2-sigma-random-effect|phase18-nbinom2-phylo-q1|phase18-actions-runner|nbinom2-location-scale|nongaussian-structured-boundary', reporter = 'summary')"
Rscript -e "pkgdown::check_pkgdown()"
rg -n 'NB2.*q1.*formal recovery.*(now|passed|complete|closed)|NB2.*q1.*coverage.*(now|passed|complete|closed)|nbinom2_phylo_q1.*promote_narrowly|broad NB2 structured.*(ready|now)|NB2 sigma phylogeny.*now|zero-inflated NB2 phylogeny.*now|NB2 phylogenetic slopes.*now' NEWS.md ROADMAP.md README.md inst/sim/README.md docs/design vignettes tests -g '!*.html'
git diff --check
```

Results:

- Parse smoke printed `parse ok`.
- Focused tests passed for the NB2 location-scale, NB2 sigma smoke, NB2 q1
  phylogenetic smoke/formal, Actions-runner, non-Gaussian structured-boundary,
  and filter-adjacent truncated NB2 test sets.
- `pkgdown::check_pkgdown()` reported no problems.
- Broad-promotion scan returned no hits.
- `git diff --check` was clean.

## Tests Of The Tests

The focused test set includes DGP reproducibility, malformed-input rejection,
artifact row-count checks, direct profile-target status checks, overwrite
protection, formal-grid read-back QA, Actions dry-run planning, fitted NB2
extractor checks, and neighbouring unsupported-route guards.

## Consistency Audit

Rose checked that the branch does not carry the active direct-SD phylogenetic
syntax work from the other conversation. The only direct-SD scan hits left in
the checked files are pre-existing historical NEWS/ROADMAP baseline wording,
not new NB2 hunks.

## GitHub Issue Maintenance

Direct searches for `NB2 phylo q1` and `nbinom2 sigma` returned no open direct
issue. The broader `Phase 18 NB2` search returned #59, #60, and #128, but those
are broader simulation, comparator, and slope-capacity ledgers, so no issue was
mutated for this PR split.

## What Did Not Go Smoothly

The checkout had three active lanes braided together: NB2, the Poisson formal
tail, and hex/pkgdown polish, plus another active phylo/meta-analysis
conversation touching core files. The safe path was a secondary worktree and a
stacked PR, not editing the dirty checkout in place.

## Team Learning

Ada should split follow-up PRs before additional simulation evidence lands on a
dirty branch. Grace should keep PR base state visible whenever an Actions task
depends on a preceding Actions-task PR. Rose should scan copied shared docs for
cross-thread wording before tests, not after pushing.

## Known Limitations

PR #319 was still running R-CMD-check when this branch was prepared, so the NB2
PR is intentionally stacked on #319 rather than pretending `main` already has
the Poisson formal Actions machinery. The NB2 q1 route remains
`hold_smoke_only`; formal recovery and coverage claims still require the full
500-replicate grid and artifact audit.

## Next Actions

Open the NB2 PR against `codex/poisson-phylo-q1-formal-481-495`, monitor CI,
and retarget to `main` after PR #319 merges.

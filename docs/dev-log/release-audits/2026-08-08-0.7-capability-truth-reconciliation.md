# drmTMB 0.7 capability-truth reconciliation and finite candidate set

## Verdict

The capability-truth discrepancy exposed after the 29-issue sweep is repaired
on the current lane. It is a prerequisite to freezing an honest candidate, but
it does not itself create or validate a 0.7 artifact and does not advance the
release rung.

The finite decision set is:

1. **Issue-derived candidate blocker — #61.** Retain open. A real exact 0.7
   candidate still needs separate authorization, frozen source bytes, and its
   own release-gate evidence.
2. **Capability-truth prerequisite — closed by PR #953, merged green at
   `08c9f2330`.** Runtime, ledger, generated surface, and reader docs now agree on
   the exact public O2 and internal-only O3 boundaries. This is not a second
   open issue-derived blocker.
3. **Owner policy decision — #870.** Decide separately whether `offset()` is an
   additive `mu` term for all families or count-family-only. Current evidence
   does not demonstrate a release-blocking defect.
4. **Post-0.7 methods programme.** Broader non-Gaussian REML, public AGHQ/O3,
   correlated/labelled/structured slopes, Q-series interval calibration, GVA,
   EVA, and comparator/compute campaigns remain deferred research rather than
   candidate blockers.

## Exact capability disposition

| Cell | Public disposition | Evidence boundary |
| --- | --- | --- |
| `mc-0058` | fixed-only binomial REML rejected by design | early gate; use `REML = FALSE` or add an admitted ordinary `mu` random effect |
| `mc-0060` | ordinary unlabelled binomial `mu` random intercept implemented | `diagnostic_only`; deterministic same-construction comparator, convergence, `pdHess`, finite `vcov()` |
| `mc-0062` | one independent ordinary binomial `mu` random slope implemented | `diagnostic_only`; no correlated/labelled slope, recovery, interval, or coverage claim |
| `mc-0068` | structured binomial REML rejected by design | no `phylo()`, `spatial()`, `animal()`, `relmat()`, or `phylo_interaction()` REML route |
| `mc-0227` | public cumulative-logit ML slope retained | `point_fit_recovery`; O3 is internal technical evidence and grants no public interval/report permission |

## Release boundary

This lane does not alter DESCRIPTION, freeze an artifact, earn
`platform-clean`, discharge D-43, finalize `cran-comments.md`, create a tag, or
authorize CRAN upload. Historical PR #947 and the separate #858 and #937 lanes
remain untouched. The dirty primary and MSPL checkouts and all existing stashes
are preserved.

## Recommendation

After the exact merge-commit main check is green, treat #61 as the only
issue-derived candidate blocker. Shinichi may then authorize a fresh
exact-candidate preparation arc. That later arc must establish its own source
identity and evidence; it cannot inherit `platform-clean` or upload authority
from historical artifacts or from this capability repair.

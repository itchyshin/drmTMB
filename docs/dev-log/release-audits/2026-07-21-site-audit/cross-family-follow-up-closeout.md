# Cross-family wording follow-up

- **Source:** `vignettes/cross-family.Rmd`
- **Audit date:** 2026-07-21

The repeated public-surface scan found two remaining phrases that described the
halted Julia bridge as experimental or optional. They now say explicitly that
the bridge is halted/deferred and point readers to future bridge support. The
article still keeps cross-family fitting, `rho_latent` estimation, and interval
claims outside the 0.6.0 release surface.

The same scan confirmed the owner-held P1 `rho12` certification defect in
`bivariate-coscale`; it is recorded in `owner-held-findings.md` without editing
Shinichi's source.

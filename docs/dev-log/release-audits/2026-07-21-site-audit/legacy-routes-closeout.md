# Legacy article-route audit closeout

## Function map and cheat sheet

The route had a current reader workflow but retained two stale claims that
Julia was optional post-0.6. Its figure alt text and prose now say the bridge
is halted/deferred future work and that the map covers native R/TMB only.
`pkgdown::build_article("function-map-cheatsheet", ...)` and `git diff --check`
passed; fresh mobile evidence is
`renders/function-map-cheatsheet-mobile-390x844.png`.

## Location-scale-scale

The source, navigation placement, and the 2026-07-21 after-task evidence were
reviewed. It keeps residual `sigma` distinct from latent `sd()` surfaces and
does not imply interval calibration or broad non-Gaussian support. No repair
was warranted.

Neither route changes the owner-held `bivariate-coscale` source.

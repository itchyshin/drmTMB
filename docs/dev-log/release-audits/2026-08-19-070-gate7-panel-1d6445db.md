# Gate 7 panel: drmTMB 0.7.0 candidate `1d6445db…`

## Candidate

- Source: `6170fbeeea65f22444d7b0934f4e808c40744d22`
- SHA-256: `1d6445db583d4e4586d177ce9a6ada78b27373e104a2f6754926b61a188ed9f3`
- Size: 4,368,396 bytes
- Entry count: 946

No review vote authorizes `submit_cran()` or a CRAN submission.

## Round 1

### Grace — READY

Grace independently rehashed the artifact, matched the archived inventory, and
verified all 69 entries in `EVIDENCE-SHA256SUMS`. Exact-byte local and
win-builder checks are clean apart from the disclosed first-submission and
spelling NOTEs; Windows tests take 110–152 seconds. Exact-source 3-OS CI and
three sanitizer jobs pass. The red `rchk` findings remain confined to installed
TMB headers. Grace judged the stale PNG finding to be non-candidate residue and
the twelve unused `sigma_i` compiler warnings to be post-release cleanup debt.

### Pat — READY

Pat installed the exact tarball into a disposable library, confirmed version
0.7.0, and ran the README Gaussian location-scale first workflow in 1.4 seconds.
The fit converged, all 12 `check_drm()` checks were `ok`, and the reported scale
ratios were interpretable. An unsupported Poisson `REML = TRUE` request returned
an actionable error. Pat found the reader surfaces coherent and the remaining
NEWS/navbar/README navigation friction non-blocking.

### Rose — NOT READY for the evidence closeout; candidate bytes acceptable

Rose independently matched the artifact hash, inventory, packet hashes, local
and Windows results, rights record, rendered pages, and exact source delta. Rose
found no shipped-byte blocker. She withheld `submission-ready` because the
current governance pointers still described the completed ladder as future work
and the final packet, comments, and ledger had not yet been landed durably.

## Closeout response

The evidence branch refreshes `AGENTS.md`, `docs/dev-log/coordination-board.md`,
`docs/dev-log/active-lane-split.md`, and the historical Ligges handover with the
current source commit, artifact hash, and proven rung. The checksummed packet is
filed both under
`docs/dev-log/release/0.7.0-cran-gate/candidate-6170fbeee/` and beside the
immutable tarball under
`/Users/z3437171/drmTMB-release-artifacts/0.7.0-6170fbeee/evidence/`.

The ledger remains at `platform-clean` with Rose recorded `NOT_READY` until she
re-audits the pushed build-excluded closeout. A later section records that fresh
verdict; promotion is forbidden unless all three votes are READY and the
executable gate passes again.

## Round 2

Pending Rose re-audit of the pushed closeout branch.

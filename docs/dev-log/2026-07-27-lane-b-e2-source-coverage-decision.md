# Lane B E2 — source-coverage decision

## Decision

**No tranche selected.**  The E2 census finds no unresolved E0 cell with all
required source fields present and exactly one documented direct target.
Therefore the deterministic selection rule returns `no_tranche_selected`.

This is a decision about review order, not an inference verdict.  It leaves
all 97 rows unresolved and every canonical binding input unchanged.

## Census result

The canonical 97-row union is documented in
`interval-campaign-bindings/2026-07-27-e2-unresolved-source-census.md` and its
three referenced source-card TSVs.

| Result | Cells |
| --- | ---: |
| Source found but at least one required field missing | 85 |
| Source found but not a direct row-level profile route | 12 |
| Complete pending exact-binding review | 0 |

The fixed/ordinary group contributes 15 rows; phylo/spatial contributes 46;
animal/relmat/phylo-interaction contributes 36.  Those counts exhaust the
frozen E0 unresolved inventory.

## Why E1 is not selected here

E1's eight count-q1 strings remain proposed contracts with archived source
paths.  They are not members of the 97-cell unresolved census and have not
been reclassified by E2.  Their later review remains governed by the E1
handover: inspect their archived functions alongside the complete non-foreign
cohort before any canonical binding edit.

## Preserved boundaries

No canonical binding TSV, readiness module, package code, test, schedule,
local smoke receipt, pregrid request, compute job, ledger/capability surface,
association route, bootstrap route, missing-response route, or public/default
behaviour changed.

`clamp_limited`, `trace_incomplete`, nonfinite, failed, and missing outcomes
remain unavailable and non-covering.  A finite `mc-0260m` K=12 interval remains
an error.  Clamp classification remains limited to the full `tmbprofile` route,
not endpoint profiles.

## Next approval gate

No execution authority carries over.  A later owner decision must choose how
to recover the missing primary-source fields; it may not substitute a nearby
cell, formula hint, provider/q label, or finite interval.  Only after reviewed
exact bindings for the complete non-foreign cohort may a separate pregrid
packet be drafted, and Totoro/DRAC still require Shinichi's explicit approval.

# Handover → Claude (2026-07-21): 0.6 DEV ARC — submission parked, Track A ready

You are **Claude**, picking up drmTMB from a Claude session. This doc stands alone. Read
`AGENTS.md`, then **the approved ultra-plan** — it is the real artifact:
[`docs/dev-log/2026-07-21-0.6-dev-arc-ultra-plan.md`](../2026-07-21-0.6-dev-arc-ultra-plan.md)
(GOAL block · Landing State · slice tables · verification · budgets).

## Critical context

**Shinichi decided 2026-07-21: drmTMB 0.6 will NOT be submitted to CRAN.** It needs substantially
more work first. The CRAN release gate is **PARKED, not failed** — re-freeze, platform matrix,
cran-comments, and submission are all out of scope. Do not restart them.

## Goals / mission

Make 0.6 materially stronger on three fronts: **(A)** a correct first-impression API surface,
**(B)** a broader *honestly-qualified* capability surface, **(C)** a deeper reader path.
Honest ceiling, state it this way: coverage certification **cannot** produce a `supported` cell —
the deliverable is "+N `inference_ready_with_caveats`".

## What was accomplished (this session)

- **PR #810 MERGED** (squash `e7ac5896`, 18:34Z) and pkgdown auto-deployed (19:03Z). Verified by
  content in `origin/main`: the rho12 revert, manifest §3/§5 correction, Gamma §1b-2, README guards.
- **Reverted four false-positive "blockers."** `conf.status = "newdata_required"` means *supply
  newdata*, not "no interval" — proven by fitting a `rho12 ~ x` model (profile and Wald agreed to
  3 dp). Manifest corrected; **#802 retitled** to a coverage-evidence gap.
- **Restored the README capability guards** the landing-page rewrite erased (6 of 8 failing; CI
  showed only 2 because `unittest` short-circuits per test). Guard tests untouched.
- **Newcomer-syntax sweep** (`tools/newcomer-syntax-sweep.R`): 14/17 fit or error cleanly; found
  `(1 + x || g)` falls through to the *fixed-effect* design matrix.
- **Ultra-plan written and plan-reviewed by three lenses** (Fisher/Rose/Ada) — their corrections
  are folded into v2. See the plan's sweep receipt.

## Current working state

- **Branch `claude/0.6-dev-arc`**, cut fresh from merged `origin/main`. Carries only: the 3 files
  stranded by the squash merge + the plan copy + this handover.
- **`codex/precran-review-20260721` is a squash-merge ORPHAN** — its 10 commits duplicate content
  already in `main`. **Do not open a PR from it.**
- Worktree `drmtmb-pr794.wNJd62` (PR #794 draft) **PARKED** — revisit before any capability-surface
  tooling work, or 2026-08-21.

## Next immediate steps

1. **A0 — RECON** (Haiku): enumerate `Phase \d` jargon leaks in user-facing `cli::` strings; map the
   random-term parse path; list ledger/vignette rows touching missing-data.
2. **A1 — `||` desugaring** (the headline of Track A): `(1 + x || g)` → `(1|g) + (0+x|g)` for numeric
   slopes; explicit `cli_abort` for factor slopes. Closes **#776**. No evidence burden — it desugars
   onto an already-certified route. *Verify:* `||` returns the **same SDs** as the explicit two-term
   form; that equivalence IS the test.
3. **A2** (serialize AFTER A1 — same file region `R/drmTMB.R:9600-9618`): strip `Phase \d` jargon,
   clean `s(x)` error, and **add the test that locks the simple-grouping rule** (currently untested).
4. **A3 / A4** in parallel: surface the wired-but-unclaimed missing-data slice at its evidenced tier;
   `coef()` Rd example (do NOT change the return type).

## Blockers / open questions

- **Track B is compute-gated:** no campaign fit until its pre-registered spec is plan-reviewed AND
  Shinichi approves (B3). Totoro for the rho12 study (≤100 cores); DRAC array for the multi-cell
  batch; **never GitHub Actions** (D-50).
- **B0 first:** a live rho12 ADEMP already exists (`docs/design/52-phase-18-bivariate-rho12-ademp.md`,
  N=500 vs precedent 1200). Reconcile or formally supersede it before writing any new spec.

## Gotchas

- **Squash merges break `--is-ancestor`.** Check landed work by **content**, not ancestry — that is
  how this session confirmed nothing was lost.
- **Don't infer a capability's absence from a status token or a grep — USE it** (a toy fit). That rule
  found both the rho12 reversal and the `||` defect.
- **Never hand-edit `vignettes/includes/*`** — they are generated; regenerate them.
- **Pre-push habit** (0.3 s; would have caught this session's CI break):
  `python3 -m unittest tools/tests/test_capability_ledger.py && python3 tools/capability_ledger.py --check`

## How to resume

```
Rehydrate from docs/dev-log/2026-07-21-0.6-dev-arc-ultra-plan.md + the AGENTS.md snapshot, then start
Track A at A0. 0.6 is NOT being submitted to CRAN — the release gate is parked. Branch
claude/0.6-dev-arc is already cut from merged main. Spawn Rose before any capability claim.
```

## Mission control

| Lane | Branch / PR | CI | What shipped | Next by leverage |
|---|---|---|---|---|
| 0.6 dev arc | `claude/0.6-dev-arc` (PR open, **do not auto-merge**) | ledger 37/37 local | plan + 3 stranded files + this handover | **Track A: A0 → A1 (`\|\|`)** |
| Pre-CRAN review | #810 **MERGED** `e7ac5896` | green; Pages deployed | rho12 revert · manifest fix · README guards | closed |
| Track B (breadth) | — | — | plan-reviewed by Fisher | B0 → B1 → B2 → **✋ B3 approval** |
| CRAN submission | — | — | — | **PARKED** by maintainer decision |

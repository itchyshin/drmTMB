# Lane B2 q2 phylogenetic REML — later execution approval

This file is an approval template only.  It does not authorise a profile run.

```text
Approve exactly one serial local high-rung profile attempt for every tuple in
2026-07-30-b2-e0-q2-reml-admission-registry.tsv on frozen SHA
8798d6f7762642e72823b6f930e254d3c6c3b5a5. Retain every outcome; no retry or
substitution; require convergence, a real sdreport with pdHess, complete trace,
valid hashes, and finite ordered unclamped endpoints containing the estimate.
Do not promote unless both receipts pass independent review.
```

The eventual runner must use a new result root keyed by this SHA and may not
reuse any existing retained receipt.

# B2 E0 q2 REML original authorization — unused and superseded

The approved authorization in
`2026-07-31-b2-e0-q2-reml-execution-authorization.tsv` remains retained
evidence of the original approval only. It names source SHA
`481cf61bc0886ff731ca6adbeb1f98add98374ae`.

Before any attempt began, static inspection found that the runner passed its
private `root` argument to the fixture, whose source-addressed interface has no
such formal. The runner repair is frozen at
`8798d6f7762642e72823b6f930e254d3c6c3b5a5`; the source assertion therefore
rejects the old authorization. No result root or attempt marker exists for the
old SHA. The old authorization cannot be reused, retried, or treated as a
receipt.

A separate replacement authorization names the corrected SHA and still requires
renewed, exact local-profile approval before either tuple can run.

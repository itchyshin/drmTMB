# Homogeneous Toeplitz P2 closeout receipt

Source commit: `cbd8181616fbdad686a3a63c68dc8f695ae7aa14e`.

## Independent review

Noether rechecked the marginal `sigma^2 R` likelihood, PACF map, per-series
normalization, native dense-oracle agreement, and the public profile/Wald
boundary. Pat rechecked a live complete-panel workflow, finite mean-effect
profile endpoints, rendered teaching material, and error recovery for an
incomplete factor-labelled series. Both reviewers cleared the repaired source.

The repair arose from their first review: a stale native expectation refused a
now-qualified fixed-mean profile route; Wald diagnostics implied that profile
calibration was pending; and an incomplete-panel error exposed an internal
series index. The retained regression tests exercise all three corrections.

## Package check

Commands run from this source:

```sh
R CMD build .
R CMD check --no-manual /private/tmp/drmTMB_0.7.1.tar.gz
```

The final check completed installation, documentation, examples, spelling,
the full test suite, and vignette rebuilding. It ended with two package-wide
warnings: the source tar deliberately omits generated `inst/doc` outputs while
retaining the vignette sources. The check rebuilt those vignette outputs
successfully. These warnings are not introduced by the Toeplitz phase and are
not presented as a clean warning-free check.

## Gate boundary

P2 qualifies only the marginal homogeneous Toeplitz mean-effect profile route
in its frozen primary cells. It does not qualify Wald covariance or intervals,
total-scale or lag-correlation intervals, forecast or `newdata` prediction, or
temporal structure on `sigma`.

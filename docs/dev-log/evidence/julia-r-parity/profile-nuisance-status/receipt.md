# Public R profile status verification

Approved programme#563, scoped to generic nuisance acceptance and status
transport. Final public004 ordinary Rscript completed20.899seconds with both
DRMTMB_JULIA_TESTS and NOT_CRAN unset. Actual GaussianML80-row90% profile mu:x
agrees with an independent least-squares LR oracle within1e-5. Fourteen injected
fixed-effect cases and one SD transformation case pass through public confint
and the actual generated wrapper. Preparation/profile results in these injected
cases are stubs; they are transport evidence, not optimizer/admission proof.

checker003.log verifies all141 exact before/after/current source hashes, loaded
Julia path, actual generated wrapper hash, coefficient/loglik/oracle values and
status/bound shapes. Twelve deliberately damaged receipts are rejected. The
checker was independently reviewed by Rose.

Keep public001 depot-lock failure (beforefit), public002 successful old-checker
run, public003 successful run/checker002 AST failure, and final public004. Old
receipt manifests are historical after checker changes. No saved receipt was
rewritten. No native-R interval campaign or coverage result is claimed.

R/julia-bridge.R runtime source contains foreign zero-one-beta changes outside
this slice. Scoped staging must exclude them. Whole-programme integration must
revalidate the final combined source: this is not a clean-head claim.

Julia docs/source implementation and independent212 checks are retained in the
DRM.jl twin. ProgrammeG0-G8, specialized profilers, score/global-optimum proof,
full-tree speed and whole-site publication remain open. No remote compute or
collaborator communication was performed. Root actualSol/medium, builder
Terra/high, independentRoseSol/high; agent-hours uninstrumented.

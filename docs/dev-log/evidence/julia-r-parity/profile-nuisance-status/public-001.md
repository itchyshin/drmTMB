# Public001 — startup infrastructure failure

Ordinary Rscript started JuliaCall, then Julia package activation failed writing
~/.julia/logs/manifest_usage.toml.pid with EPERM. It stopped after8.742seconds,
before fitting or a numerical receipt. This is not a profile solver failure.
The next bounded run requires scoped permission for existing Julia depot
metadata. No new package installation or remote compute is intended.

Host NOT_CRAN was read and empty; runner now explicitly clears both NOT_CRAN
and DRMTMB_JULIA_TESTS to make ordinary-batch provenance portable.

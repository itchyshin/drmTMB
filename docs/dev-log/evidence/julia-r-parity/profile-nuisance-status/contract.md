# Public profile status transport — pre-run contract

Approved programme#563; root owns the generated profile wrapper hunk, two new
runner/checker tools and this evidence. Foreign ZOB96insertions/10deletions in
R/julia-bridge.R are outside this slice and must not be staged.

Estimated ordinary Rscript run: under3minutes,180second watchdog. No remote
compute, installations or campaign. DRMTMB_JULIA_TESTS stays unset; DRM_JL_PATH
points explicitly to the execution Julia tree.

CHECK: run tools/run-julia-profile-status-public.R with a fresh prefix; preserve
JSON/RDS/log and source hashes. Then tools/check-julia-profile-status-receipt.R
RECEIPT.rds --self-test.
EXPECT: actual small GaussianML public profile agrees with the independently
recomputed least-squares LR oracle within1e-5; fourteen injected fixed-effect
cases and one injected SD case retain exact bounds/status/message through public
R confint. Twelve damaged receipts must be rejected (expanded during independent review). Before/after/current source
hashes must match before any exact-source qualification.

Injected cases run the actual generated Julia wrapper unchanged inside isolated
modules. Test-local DRM facades replace preparation/fitting/profile results;
real outcome/flatten helpers are reused. These are transport checks, not
numerical optimizer failure, model admission or missing-row reconstruction
proof. The older facade tests absence of the new helper, not all older releases.
An SD limit exp(-Inf)=0 must retain profile_failed. Successful inference remains
separate from searched-range non-crossing and from failed endpoints.

The whole parity programme, coverage, large-tree performance, source cleanup,
shared-workspace safety, specialized profilers and deployed docs remain open.

Review refinement: require the exact expected source-file set, numeric field shapes,
loaded Julia path and extracted generated-wrapper hash. public003 completed but
checker002 failed during AST extraction; retain it as a checker failure. The
fixed extractor reads only the setup function body. Final qualification requires
a fresh public004 receipt binding the final checker bytes.

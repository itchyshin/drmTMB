# Native ordinal/categorical missing-predictor reference

Generated by tools/export-finite-joint-reference.R with unchanged native defaults,
Gaussian response, 180 rows per family, three states and all four response/predictor
missingness combinations. Each case retains three parameter points, state-expanded
mean design, raw native parameter names/order, covariance, posterior probabilities,
imputation summaries, predictions, controls, source and loaded native DLL hashes.
The 003 export took1.843seconds; this is not a warm performance comparison.

The RDS belongs only in this GPL repository. Julia receives generated numerical
JSON/TOML data, never GPL implementation source. Earlier exports001/002 are retained
as failed metadata attempts in the Julia programme evidence (wrong control field,
then a one-row gradient matrix). Only003 anchors current comparisons.

This reference supplies a prepared-Julia development slice for DRM.jl#563.
Native default fits are retained unchanged; ordinal prediction/imputation and
categorical coefficient/prediction discrepancies above4e-6 remain unresolved.
No Julia bridge admission, release, full parity or performance claim is made.

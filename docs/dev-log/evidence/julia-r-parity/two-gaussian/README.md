# Two Gaussian native reference — #563

Frozen native default fit, original160rows, all8response/predictor masks,
SEenabled. No optimizer/start/tolerance adjustment. The paired Julia repository
retains the independently checked numerical export and prepared-kernel receipt.
RDScontains the nativeRfit and stays in thisGPLrepository; it is not vendored
into MITJulia. This is not recovery, coverage, publicbridge or performance proof.

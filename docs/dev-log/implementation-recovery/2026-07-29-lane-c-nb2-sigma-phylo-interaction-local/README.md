# C7 NB2 sigma phylo-interaction initial runner receipt

This retained initial run used the pre-fix C7 local runner. All four planned
attempts stopped at DGP construction because `ape::stree()` did not supply
finite edge lengths. No model was fitted and this is not evidence about the
NB2 route. The separate `-local-repaired` directory records the corrected,
all-attempt local point-recovery fixture; this directory is intentionally not
overwritten.

# C17-C2 Totoro smoke receipt

The authenticated Totoro checkout at
`ac86a6429f67b738d9b2e21072b109c9c7681b79` reproduced the local one-fit
smoke before the retained ladder launched. The fit had convergence code 0,
`pdHess = TRUE`, maximum gradient `0.000875`, a non-boundary random-slope SD,
mode correlation 0.629, and all sample-information diagnostics satisfied. The
largest difference from the boundary-only `lme4::glmer()` comparator was
`8.56e-5`.

This receipt verifies deployment and non-empty output only; it is not recovery
evidence or a capability promotion.

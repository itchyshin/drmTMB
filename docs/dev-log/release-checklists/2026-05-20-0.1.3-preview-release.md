# Release Checklist: drmTMB 0.1.3 Preview

This checklist records the `0.1.3` GitHub preview release gate after the first
known-relatedness Gaussian `mu` slice and the first coordinate-spatial bivariate
q=2 location-covariance slice.

Labels: `release`, `0.1.3`, `pkgdown`, `install`

## Current Evidence Snapshot

- `v0.1.2` remains immutable and installable as the previous profile-inference
  and tutorial-hardening preview.
- `v0.1.3` is intended as the next tagged preview for users who need the first
  fitted `animal()` / `relmat()` known-matrix Gaussian `mu` slice and the first
  fitted coordinate-spatial bivariate `mu1`/`mu2` q=2 covariance slice.
- The active install instructions now point to
  `pak::pak("itchyshin/drmTMB@v0.1.3")`.
- The release is a GitHub preview tag, not a CRAN submission.
- Local post-bump package check for `drmTMB 0.1.3` passed with 0 errors and 0
  warnings. The only NOTE was the environment-time check:
  `unable to verify current time`.
- Local `pkgdown::check_pkgdown()` reported no problems after the version bump.
- Local targeted post-bump tests for `gaussian-location-scale` and
  `spatial-gaussian` passed.
- Final compact release tests for `animal-relmat-gaussian`,
  `gaussian-location-scale`, and `spatial-gaussian` passed before staging.
- Local full `devtools::test(reporter = "summary")` passed before the
  version-only metadata bump.

## Slice A: Candidate Gate

- [x] Bump `DESCRIPTION` to `0.1.3`.
- [x] Add a dated `NEWS.md` heading for `drmTMB 0.1.3`.
- [x] Update README, getting-started article, roadmap, and pkgdown version note
      to the `0.1.3` tagged preview.
- [x] Document the first bivariate coordinate-spatial q=2 covariance slice in
      NEWS, README, roadmap, model-map, formula-grammar, structural-dependence
      article, family registry, readiness matrix, and validation-debt register.
- [x] Regenerate roxygen documentation for `corpairs()`.
- [x] Run local targeted tests.
- [x] Run full local tests.
- [x] Check pkgdown structure.
- [x] Run local R CMD check for `drmTMB 0.1.3`.
- [x] Comment on issue #5 with the candidate evidence and remaining covariance
      boundaries.

## Slice B: PR And CI Gate

- [ ] Open a focused PR for the `0.1.3` candidate.
- [ ] Pass branch CI on macOS, Ubuntu, and Windows.
- [ ] Pass pkgdown build/deploy on the PR or merge target.
- [ ] Review the rendered pkgdown site for visible `0.1.3` status and
      spatial/known-relatedness boundaries.

## Slice C: Tag Gate

- [ ] Merge the release-gate PR.
- [ ] Push annotated tag `v0.1.3` with message `drmTMB 0.1.3 preview`.
- [ ] Pass tag-triggered R-CMD-check on macOS, Ubuntu, and Windows.
- [ ] Run a clean temporary-library install smoke test:

```sh
Rscript tools/install-smoke.R v0.1.3 0.1.3
```

- [ ] Record the tag CI run, pkgdown deploy, and install-smoke output in the
      check log and after-task evidence.

## Future Release Rule

Every tagged preview should run the install smoke after the tag is pushed:

```sh
Rscript tools/install-smoke.R vX.Y.Z X.Y.Z
```

This catches mismatches between the tag users install and the documentation they
read on the current pkgdown site.

Status (2026-09-14): design note only, no code — `docs/design/274-bootstrap-interval-bias.md` (`37b5d7ce6`), independently reviewed.

The note derives the mechanism (a percentile interval built from refit draws centred on an already-biased estimate re-applies that bias; the basic/reflected interval `2θ̂ − q*` centres near the truth), records where the code takes its percentiles (link scale for positive targets; `bias_correct` reaches only the Wald path; `method` defaults to `"wald"`, so the bootstrap is opt-in), and lays out five options with their user-visible cost. Recommendation: document the bias-doubling on `?confint.drmTMB` and recommend Wald/profile for scale intercepts at small n now; add basic and bias-corrected percentile intervals as opt-in choices; leave the default unchanged until the specified simulation runs (about 199,000 refits, roughly 24–27 h serial from a measured 0.19 s per refit, so a Totoro job with a pre-run first). Your three numbers are carried as transcriptions and marked as not re-derived in this repository.

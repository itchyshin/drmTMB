Scope note (2026-09-13, from the fix branch and the independent Fisher review).

Commit `bb7b0de8b` on `claude/audit-dinnage-wave1-20260913` moves `weights(i)` outside the `mi()` two-point mixture at the site Russell traced (`beta_binomial`, was `src/drmTMB.cpp:3510-3519`) and at the nine identical copy-pasted `mi_family == 1` (Bernoulli-imputed covariate) sites, weights the imputation prior, and adds a weight-invariance test that fails on the old kernel. The reviewer confirmed the algebra (`w · log(p1·f1 + p0·f0)`, prior weighted, posterior untempered).

**Not yet fixed:** the same weight-inside-the-mixture structure survives at eleven further sites, one per non-Bernoulli `mi()` imputation family (the `mi_family` 2–12 quadrature blocks at `src/drmTMB.cpp:1384, 1464, 1548, 1686, 1817, 1879, 1961, 2065, 2146, 2230, 2323` on the fix branch), with no test. Any fit that combines `weights` with `mi()` on an ordinal, categorical, beta, Poisson, lognormal, gamma, NB2, Tweedie, zero-one-beta or truncated-NB2 covariate still carries the bias Russell measured. `weights = NULL` or all-ones weights are unaffected. This issue stays open until those eleven sites are fixed with a per-family invariance test.

Status (2026-09-15). `AIC.drmTMB()`/`BIC.drmTMB()` (`R/methods.R`, `7c1f248fa`) no longer filter `...` down to drmTMB fits before comparing; each non-drmTMB object's log-likelihood and df are now read through `stats::logLik()`/`stats::nobs()`, so `AIC(drm_fit, foreign_fit)` returns the standard one-row-per-model data frame instead of dropping the foreign model and returning a bare scalar. `tests/testthat/test-dinnage-audit-wave2b.R` adds the comparison-table assertion. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 2)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding S5. Closing.

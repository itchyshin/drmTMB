Status (2026-09-15). `drm_logsigma_clamp_active()` (`R/drmTMB.R`) now uses a two-sided predicate that detects the lower `log(sigma)` clamp arm, not only the upper one, and `check_drm()`'s `logsigma_clamp_active` row (`R/check.R`) reports a lower-arm hit as a note with lower-bound wording instead of the upper-arm warning text (`328507d59`, then `9b4fec4fa` on reviewer feedback). `tests/testthat/test-clamp-active-guard.R` locks both: a lower-arm fixture that used to assert the predicate stayed null now asserts the two-sided arm/value, plus a new upper-arm assertion, and a further test checks the note-vs-warning branch and its lower-bound text. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". The fit-time `cli_warn()` still fires only for the upper arm, since the lower arm is often a legitimate variance-zero boundary (meta-analysis `tau = 0`). Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding C1. Closing.

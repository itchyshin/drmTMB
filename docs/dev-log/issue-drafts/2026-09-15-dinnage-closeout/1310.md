Status (2026-09-15). `simulate.drmTMB()` (`R/methods.R`) now routes masked missing-response rows through the same `drm_mask_missing_response_values()` helper `residuals()` already used, so it returns `NA` there instead of leaking the internal sentinel, for every family (`90afa4b6a`, then `eb122ea31` after review masked the four remaining count-mixture families: `zi_poisson`, `zi_nbinom2`, `hurdle_nbinom2`, `truncated_nbinom2`). `tests/testthat/test-dinnage-audit-wave2b.R` adds the masking assertions, and the count-mixture and truncated-NB2 test files were updated to assert `NA` at masked rows instead of a finite draw. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 2)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding M4. Closing.

Status (2026-09-15). `mi()` is now rejected with an error on every formula parameter except `mu` (`R/drmTMB.R`, `7c061c8a9`), via a dpar-conditional check in `drm_reject_phase1_terms()`; previously `sigma ~ mi(z)` and similar silently parsed `mi(z)` as an ordinary covariate, giving a bit-identical logLik to the same formula written with plain `z`. `tests/testthat/test-dinnage-audit-wave1.R` adds a Md-D block asserting `sigma ~ mi(z)` errors while `mi()` in `mu` still fits normally. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding Md-D. Closing.

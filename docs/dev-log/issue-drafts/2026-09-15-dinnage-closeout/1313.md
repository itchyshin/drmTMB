Status (2026-09-15). `vcov(fit, type = "robust")` and `vcov(fit, robust = TRUE)` (`R/methods.R`, `797c0c067`) now abort with class `drmTMB_vcov_robust_unsupported` and name `confint(method = "boot")` or refitting with a heavier-tailed family as what to try instead; they used to return the model-based matrix silently. `tests/testthat/test-dinnage-audit-wave2b.R` adds the abort assertion. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 2)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding S4. Closing.

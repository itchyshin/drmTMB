Status (2026-09-15). The S2 part of this issue is landed: `3192db3f6`, `1c44d2f12` and `19849f0dc`, merged into `main` via PR #1361 (`8195b1215`). The 2026-09-14 status comment above already describes the fix in full: `drm_constant_residual_sigma()` now returns the marginal `sqrt(E[sigma^2])` instead of the median scale when `sigma` carries random intercepts, and `summary()$derived` and the `heritability()`/`icc()`/`repeatability()` accessors use it; the two follow-up commits corrected the structured-sigma unit-diagonal check to read the modelled rows (tips, not internal nodes) rather than the whole precision matrix, and added a `clamp_limited` refusal when the `log(sigma)` soft clamp is active. NEWS.md, "Independent-evaluation fixes (wave 3)". This issue stays open: the wider D-252 question of repeatability vocabulary on non-Gaussian scales, per the de Villemereuil et al. 2016 three-scale framework, is a feature decision recorded in `docs/design/275-repeatability-scale-and-residual-variance.md`, not resolved by this fix.

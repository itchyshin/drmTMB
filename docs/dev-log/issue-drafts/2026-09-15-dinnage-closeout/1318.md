Status (2026-09-15). `drm_clamped_scale_families()` (`R/drmTMB.R`, `0acc4a477`) now names `biv_lognormal` and `biv_student` alongside `biv_gaussian`, so `check_drm()`'s `logsigma_clamp_active` row no longer prints "the clamp does not apply to this family" for those two families, which `src/drmTMB.cpp` clamps identically to `biv_gaussian`. `tests/testthat/test-clamp-extension.R` adds a unit test on the family list plus an end-to-end `check_drm()` test on a small `biv_lognormal` and a small `biv_student` fit. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding Md-A. Closing.

Status (2026-09-15). The 2026-09-14 status comment above covers the base fix (`0526baa2f`) and the first two follow-ups (`2a5b0665e`, `e86359fe2`): `predict_parameters()` now returns `NA` endpoints flagged `conf.status = "clamp_limited"` at clamp-bent rows, and in-band rows keep the ordinary Wald interval on the raw predictor. What is new here is a fourth commit, `d61f65183` (`R/methods.R`): `summary()$derived$residual_sd` now passes the sigma intercept through the same clamp helper `sigma()` uses, so the two no longer disagree on a clamp-active fit; a no-op when the clamp is inactive. Still on the raw predictor, as the existing comment lists: the response-scale `profile()` output for `sigma`, the Julia-bridge scale target, `summary_parameter_delta_derivative()`, and `check_drm()`'s clamp detector for a modelled `sd(group)` scale. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 3)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding M2. Closing.

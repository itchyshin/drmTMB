Status (2026-09-15). The 2026-09-14 status comment above already covers the fix in full: `bb7b0de8b` moved `weights(i)` outside the Bernoulli `mi()` mixture, and `fc461aae4` did the same at the eleven remaining `mi_family` quadrature blocks in `src/drmTMB.cpp`. The per-family test `tests/testthat/test-dinnage-audit-m1-families.R` asserts the `weights = 2` objective equals twice the `weights = 1` objective at converged optima, plus coefficient equality and row-duplication equivalence; it fails 43 times on the previous kernel and passes now. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 3)". As the 2026-09-14 comment notes, the Gaussian latent `mi()` route (`mi_family == 0`) is a separate, unfixed neighbour: its covariate-model density inside the Laplace-integrated latent still carries no weight. Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding M1. Closing.

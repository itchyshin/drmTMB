Status (2026-09-15). `fitted_distribution()`'s `$p()` and `$d()` closures for the zero-inflated, hurdle and truncated count families now recycle a scalar threshold across every row, via the new `drm_recycle_scalar_arg()` helper (`R/family-dpq.R`, `769a4e409`); previously an `ifelse()`-based closure returned only row 1's value. `tests/testthat/test-dinnage-audit-wave2a.R` asserts scalar and length-n calls agree elementwise for `zi_poisson`, `zi_nbinom2`, `hurdle_nbinom2` and `truncated_nbinom2`, against `poisson`/`nbinom2` as unaffected baselines. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 2)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding S1. Closing.

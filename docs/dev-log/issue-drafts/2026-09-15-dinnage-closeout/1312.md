Status (2026-09-15). This is a decision, not a code change. The 2026-09-14 status comment above already records decision D-266: keep the compiled penalty and document the estimator honestly. `d44a495be` rewrites `?drm_phylo_penalty` so the help page describes the log-scale MAP the package actually reports; the code path is unchanged by choice. Refer to that comment for the full reasoning (Fisher's review that overturned the earlier Gamma(2, rate) rewrite). Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 3)". Reopen is welcome if the documented estimator is not what users need. Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding S3. Closing.

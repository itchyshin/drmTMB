Status (2026-09-15). The `default:` branch of `drm_response_log_density()` (`src/drm_response_kernels.h`, `46eec76bd`) now calls `error()` instead of silently returning `Type(0.0)`, a likelihood contribution of 1, for an unhandled `model_type`. No test was added: all 27 current call sites are reachable-safe, so there is no pure-R path that reaches the default branch today, and this is a guard against the next family wired into an `mi()` two-point sum before its case is added here, not a fix to an observable behaviour. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding Mi-9. Closing.

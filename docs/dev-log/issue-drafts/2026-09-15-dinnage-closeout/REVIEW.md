# Closeout review: Dinnage independent-evaluation drafts (2026-09-15)

Method. For every sha cited in the seventeen drafts I ran `git branch -r --contains <sha>` (all 26 shas return `origin/main`), `git merge-base --is-ancestor <sha> 8195b1215` plus `git log --oneline 8195b1215 -1` (the PR #1361 merge; `743191f8b` is dated 2026-08-30 and predates the audit arc, which is why draft 1352 correctly omits the PR line), `git log -1 --format='%h %ad %s%n%b'` for every commit message, and `git show --stat` and `git show <sha> -- <path>` to confirm the files, helper names (`drm_mask_missing_response_values()`, `drm_recycle_scalar_arg()`, `drm_reject_phase1_terms()`, `drm_clamped_sigma_eta()`, `drm_droplevels_fixed_predictors()`) and test files each draft names; I read `NEWS.md` lines 1 to 240 for the wave 1, 2 and 3 sections and checked every number verbatim (43 failures, 376 assertions, 27 call sites, n = 2000, 4 of 4 SEs, `dropped = 1`); I read the existing issue comments with `gh issue view <N> --repo itchyshin/drmTMB --json title,state,body,comments`; and I scanned all drafts for the em-dash character with a grep for it across `*.md` (no hits).

| issue | id | verdict | note |
| --- | --- | --- | --- |
| 1301 | S2 | ACCEPT | Does not say "Closing"; states the issue stays open for the D-252 vocabulary question. All three shas, the design doc path and the follow-up descriptions match NEWS wave 3 and the commit bodies of `1c44d2f12` and `19849f0dc`. |
| 1306 | C1 | ACCEPT | Two-sided predicate, note-vs-warning branch and the three `test-clamp-active-guard.R` assertions all match the diffs of `328507d59` and `9b4fec4fa`; the upper-arm-only `cli_warn()` caveat matches NEWS wave 1. |
| 1307 | M1 | ACCEPT | "Eleven remaining", "fails 43 times" and the objective/coefficient/duplication assertions are verbatim in NEWS wave 3 and `fc461aae4`. Note for the poster: the 2026-09-14 comment says the issue is left open for the `mi_family == 0` neighbour, and no separate issue exists for it yet. |
| 1308 | M2 | ACCEPT | The base fix, the two follow-ups and the four still-raw consumers match NEWS wave 3 and the existing comment; the `d61f65183` claim (`residual_sd` through `drm_clamped_sigma_eta()`, no-op when inactive) matches that commit's diff. |
| 1309 | M3 | CHANGE-APPLIED | Deleted "at the sample sizes the report used": `61db027b0` scanned n = 2000, weighted n = 300 and skew-normal n = 4000, not the report's n = 1000, so the claim overstated coverage; NEWS wave 2 makes the unqualified claim instead. Also made the sign-off the uniform "Closing." (the "not reproducible on main, with a lock" wording is already stated in the first two sentences). |
| 1310 | M4 | ACCEPT | The helper name, the four count-mixture families and the two updated test files are verbatim in `90afa4b6a`/`eb122ea31` and NEWS wave 2. |
| 1311 | S1 | ACCEPT | `drm_recycle_scalar_arg()`, `R/family-dpq.R`, the four affected families and the `poisson`/`nbinom2` baselines are all in `769a4e409`. |
| 1312 | S3 | CHANGE-APPLIED | No code-fix claim, D-266, `d44a495be` and "code path unchanged" are correct (the `R/penalty.R` diff is roxygen only). Moved the reopen invitation ahead of the attribution line so the comment ends with the uniform "Closing."; the invitation itself is unchanged. |
| 1313 | S4 | ACCEPT | Condition class, the `confint(method = "boot")` and heavier-tailed-family advice, and the wave2b test all match the `797c0c067` diff. |
| 1314 | S5 | ACCEPT | "No longer filter `...`" and the `stats::logLik()`/`stats::nobs()` route match the `7c1f248fa` diff and commit body. |
| 1318 | Md-A | ACCEPT | Family list plus the end-to-end `check_drm()` tests on small `biv_lognormal` and `biv_student` fits are in `0acc4a477`; the shared clamp branch is in NEWS wave 1. |
| 1321 | Md-D | ACCEPT | The dpar-conditional check in `drm_reject_phase1_terms()` and the bit-identical-logLik description match `7c061c8a9` and NEWS wave 1. |
| 1322 | Md-E | ACCEPT | The narrowing in `743024b8b`, the excluded marker arguments, "all four fixed-effect SEs finite" (4 of 4 in the commit) and the Julia test update are all supported. |
| 1330 | Md-M | ACCEPT | `drm_log_pnorm()`, the removed 1e-300 floor, "376 assertions pass" (PASS 376) and the out-of-scope R-side `d()` closure are all in `813294ac0` and `743024b8b`. |
| 1331 | Md-N | ACCEPT | `mspl_frequency_rows$kept`, the reused MSPL fixture and `dropped = 1` instead of `dropped = 0` are verbatim in `fad47e21a`. |
| 1347 | Mi-9 | ACCEPT | "27 current call sites", the no-test rationale and the future-family framing are verbatim in `46eec76bd` and NEWS wave 1. |
| 1352 | Mi-14 | CHANGE-APPLIED | Replaced "This was fixed before the audit was filed" with "This was already fixed on `main` when the issue was filed": the fix (2026-08-30) landed AFTER the report pin `945da24f` (2026-08-29), so the original wording contradicted both the issue body ("FIXED since audit") and the draft's own next clause. No code-fix or PR #1361 claim, as required. |

Status (2026-09-15). `skew_normal()`'s log-density (`src/drmTMB.cpp`, `813294ac0`) now calls the package's own tail-safe `drm_log_pnorm()` for its skew-CDF factor, instead of flooring `pnorm(alpha * z, 0, 1) + 1e-300`; the floor saturated the far-tail log-density to a constant once `pnorm()` underflowed to exactly 0. A follow-up (`743024b8b`) repointed `tests/testthat/test-numeric-kernel-oracle.R`'s numeric-kernel oracle, which had pinned the removed 1e-300 floor, to compare against the exact Azzalini reference instead (376 assertions pass). `tests/testthat/test-dinnage-audit-wave1.R` adds the Md-M block. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". The R-side `d()` density closure Russell also flagged is out of scope for this fix. Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding Md-M. Closing.

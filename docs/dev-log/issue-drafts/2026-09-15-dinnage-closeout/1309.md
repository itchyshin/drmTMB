Status (2026-09-15). This is not a tolerance fix. The finding no longer reproduces on `main`: the Newton polish added for #1130 drives the gradient of every correct fit far below `gradient_tolerance`. A regression test (`61db027b0`) locks that behaviour at n = 2000. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 2)", the bullet starting "Finding M3". `gradient_tolerance` is still an absolute threshold and the sqrt(n) scaling question Russell raised is not changed; if a correct fit at large n still trips `fixed_gradient`, reopen with the reproduction. Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding M3. Closing.

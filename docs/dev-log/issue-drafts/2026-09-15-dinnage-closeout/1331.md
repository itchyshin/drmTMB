Status (2026-09-15). `check_drm()`'s `dropped_rows` row (`R/drmTMB.R`, `fad47e21a`) now re-expresses `model$keep` relative to the original input data using `mspl_frequency_rows$kept`, so rows the MSPL estimator discarded for a zero frequency weight are counted, not just rows dropped by complete-case or known-covariance filtering. `tests/testthat/test-dinnage-audit-wave1.R` adds the Md-N block, reusing the package's own MSPL fixture with one extra zero-weight row and asserting `dropped_rows` reports `dropped = 1` instead of `dropped = 0`. Merged into `main` via PR #1361 (`8195b1215`); NEWS.md, "Independent-evaluation fixes (wave 1)". Credit: Russell Dinnage (rdinnager/drmTMB_eval), finding Md-N. Closing.

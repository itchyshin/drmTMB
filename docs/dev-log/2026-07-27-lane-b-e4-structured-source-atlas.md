# Lane B E4 — structured-source atlas

**PLATFORM: Codex | LANE: B — `sd()` scale and intervals.**

## Purpose and authority fence

E4 is a documentation-only atlas for the frozen 76 structured E2 cells marked
`source_found_field_missing`. It traces primary local sources and preserves the
exact missing field or non-recovery disposition. It creates no canonical
binding, profile result, recovery/interval claim, smoke, schedule, pregrid, or
compute authority.

The E1 adjacent population remains exactly eight `pending_exact_binding_review`
proposals. It is disjoint from the E2 census and from this 76-cell atlas.

## Atlas inputs and partitions

The immutable manifest is
`interval-campaign-bindings/2026-07-27-e4-structured-atlas-manifest.tsv`.
The three source-card outputs are `e4-atlas-q1.tsv` (26 rows),
`e4-atlas-q2-q4.tsv` (26 rows), and `e4-atlas-highq.tsv` (24 rows).

| Partition | Cells | Source-only outcome |
|---|---:|---|
| P1 — q1 source-complete, target unrecorded | 11 | Components/provenance retained; direct target or cardinality remains unrecorded. |
| P2 — q1 matched one-slope, truth absent | 4 | Truth scale plus target/cardinality remains absent. |
| P3 — q2/q4 fixture/directness archaeology | 26 | Block/fixture sources retain multicomponent or uncommitted-source limits. |
| P4 — q1 generic/recovery sources | 11 | Generic or recovery evidence has no replayable row contract. |
| P5 — q6 provider-wide recovery | 8 | Provider-wide recovery cannot identify one cell target. |
| P6 — q12 weak-identification fence | 16 | Multi-component weak identification remains unavailable; q12 is not K=12. |
| **Total** | **76** | **All remain `source_receipt_only_not_recovered`.** |

Every atlas row records source locator and receipt, plus field states for
DGP/version, formula, truth scale, direct target, cardinality, and information
rung. No field was selected from provider/q metadata, a sibling component,
formula shape, generic `profile_targets()` support, or recovery output.

## Retained controls

The following six cells are outside the 76-cell atlas cohort and remain
`source_found_not_direct`: `mc-0113`, `mc-0114`, `mc-0214`, `mc-0215`,
`mc-0321`, and `mc-0409`. Their rejected fixture, unknown q4 target, generic
provider, or recovery-without-profile source is negative evidence, not a route
to completion.

## Non-recovery verdict

No row found all required immutable fields with a singular namespaced direct
target. Therefore E4 writes **zero** `pending_exact_binding_review` cards and
does not change E2 status. This is an evidence result, not a global claim that
no future source could exist: each row says only what the inspected sources do
not provide.

## Safety and inference fences

- `clamp_limited`, `trace_incomplete`, nonfinite, failed, and missing outcomes
  remain unavailable/non-covering; a finite K=12 endpoint remains an error.
- q12 denotes a structured-block dimension, not the separate K=12 control.
- Design-2 semantics remain restricted to full `tmbprofile`; E4 runs no trace
  or profile operation.
- E0 remains unchanged: 158 targets, 62 recovered, two retained K=12 negatives,
  97 unresolved, and `pregrid_authorized=FALSE`.

## Next gate

**CARRIED-OVER: no execution authority.** A later owner may decide whether to
design new exact sources for a selected non-recovered cluster. Any canonical
binding decision, smoke/pregrid packet, and compute campaign remains a separate
approval sequence.

# drmTMB v1.0 — ultra-plan brief (2026-07-09)

Turnkey brief for a fresh session to open and run `ultra-plan` toward the CRAN v1.0
release. Written after shipping **0.3.0** (`main` `5b70ab39`, tag `v0.3.0`).

## The reframe (read first)

The **practical v1.0 method surface is 104/104 (100%) complete** — 8 `inference_ready`
anchors + 59 Gaussian basic-working + 37 recovery rows, **zero rows outstanding**
(`docs/dev-log/release-audits/q-series-v1-release-status.md`). Full
`inference_ready` / `supported` / coverage / REML / q4–q8 are **explicitly post-v1.0**.

**⇒ v1.0 is release engineering + docs + paper, not new method capability.** The interval /
coverage / REML arc (incl. the C1 work just shipped in 0.3.0) is post-v1.0 enrichment; it
does not block the release.

## The scope decision (open the ultra-plan here)

How much inference goes in the v1.0 *story*?
- **Lean** — audited basic-working surface + 8 `inference_ready` anchors (ledger wording).
- **Lean + profile CIs (recommended)** — also feature the profile-likelihood CI method
  (shipped; issue #682 wants it as the headline).
- **+ shipped REML** — fold in the REML/C1 story (richer; widens the audit surface).

Also decide: is there a **0.4.0** step first, or straight to **1.0**? (We just cut 0.3.0.)

## The arcs (grounding pointers in parentheses)

| # | Arc | Closes | Size |
|---|---|---|---|
| 1 | **Scope lock** | the decision above + version target | S |
| 2 | **Four-ledgers alignment** | README · NEWS · ROADMAP · `known-limitations.md` agree, honest wording (release ledger "Next Gates") | M |
| 3 | **Family/route test-completeness** | recovery/likelihood/comparator + malformed-input tests per implemented route (Phase 18 narrow, #59) | L |
| 4 | **pkgdown & learning path** | `check_pkgdown()` clean; landing + family reference + 31 vignettes curated (Phase 17, #58, #31) | M–L |
| 5 | **CRAN readiness** | `cran-extrachecks`: examples, DESCRIPTION polish, 3-OS check on a tag, first-submission notes (Phase 20, #61) | M |
| 6 | **Comparator demos** | vs brms/glmmTMB/metafor — paper-coupled, likely not release-blocking (Phase 19, #60) | M |
| 7 | **Method paper / honest examples** | replication-roadmap unless implemented+tested; CRAN + paper = Phase 20 | L |

**Critical path to a shippable tarball:** 1 → 2 → 3 → 4 → 5. Arcs 6–7 run alongside.

## How to run it (ultra-plan phases)

- **Arc 1** = a decision phase (bring the scope options as a structured question first).
- **Arcs 2 & 3** fan out cleanly — doc-audit agents per ledger, test-audit agents per
  family. High parallelism.
- **Arcs 4 & 5** are sequential (pkgdown build → CRAN check loop) — **local checks first**
  per the repo discipline; expand to 3-OS only on the release tag.
- **Arc 7** is the writing arc (separate voice/agents).

## State at handoff

- `main` `5b70ab39`, tag `v0.3.0` released; `--as-cran` 0E/0W/1N (benign feasibility note);
  `pkgdown::check_pkgdown()` clean; full suite green.
- Board honest & unchanged: **8 `inference_ready`, 0 `supported`** — do NOT demote (tier
  confusion; `~/shinichi-brain/memory/LEARNINGS.md` top entry).
- Ayumi issues #3/#4 answered; 0.3.0 carries her ceiling fix + the weak-ID diagnostics.
- **Post-v1.0 backlog (not v1.0):** bivariate C1, the inference/coverage arc, q4/q8
  promotion, C2 location-scale-scale (design 222), DRM.jl (parked until drmTMB v1.0).

## Open issues → arc map

#61 Phase 20 CRAN → Arc 5/7 · #60 Phase 19 comparators → Arc 6 · #59 Phase 18 sims → Arc 3 ·
#58 Phase 17 viz → Arc 4 · #31 tutorials → Arc 4 · #682 profile-CI headline → Arc 1 scope ·
#342 release → subsumed (0.3.0 shipped).

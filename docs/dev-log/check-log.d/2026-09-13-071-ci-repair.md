# 0.7.1 ordinary-Laplace CI repair (PR #1304)

Four CI failures on draft PR #1304 were repaired without touching any retained
evidence byte, source pin, fixture, manifest, or denominator.  `git diff --stat
afa88a5b3 -- docs/dev-log/evidence/julia-r-parity/071-ordinary-laplace
docs/dev-log/implementation-recovery` is empty.

The blind-spot job and release shard 1 failed for one cause: the new
`tests/testthat/test-071-four-fixture-summary.R` reads `.Rbuildignore`d campaign
scripts, so it belongs in `tools/source-tree-tests.txt`.  `Rscript
--no-init-file tools/run-source-tree-tests.R --update` added that one line;
`--check` now reports the manifest agrees with `.Rbuildignore` (37 files).
Release shard 4 failed because `inst/extdata/env-skip-census.tsv` lacked the
test's two skip rows; `Rscript tools/write-env-skip-census.R` added exactly the
rows CI named (`build-premise 1`, `engine-drm-jl 2`).

Release shard 2 failed at `test-julia-gate-vs-engine.R:176` because the two new
registry rows in `R/julia-bridge.R` carried a bare repository path as
`evidence_url`.  Both now point at the retained receipt by GitHub blob URL
pinned to `0285af445`, and `tools/write-julia-capability-comparison.R`
regenerated the dashboard and installed TSVs (only those two cells changed).

Release shard 4 also failed at `test-coefficient-labels.R:869`: the branch's new
`drm_julia_ordinary_mu_intercept_group()` labelled a mu-only ordinary random
intercept as `resd` for every family, while `drm_julia_validate_marginal()`
admits the scalar Laplace route only for binomial, poisson, and nbinom2.  The
label is now gated on those three families; a Gaussian mu-only `(1 | g)`
keeps its conditional-Gaussian-components route unlabelled.  A new two-arm
test in `test-julia-bridge-coef-labels.R` pins both behaviours.

The first pushed repair (`e6b4981bc`) let the blind-spot job run test-071 for
the first time, which surfaced two more stale expectations: the test itself
pinned the bare-path `evidence_url`, and `docs/design/261-reml-by-route.md`
(generated from the registry by `tools/write-reml-route-table.R`) still said
37 registry rows where the branch has 39.  The test now expects the pinned
GitHub URL and the table was regenerated; only its row count changed.
`Rscript --no-init-file tools/run-source-tree-tests.R` then passed locally
with `[ FAIL 0 | WARN 0 | SKIP 6 | PASS 24710 ]`.

The release shards then failed on a code/documentation mismatch rather than
a test: the branch added the `marginal` argument to `drmTMB()` with a roxygen
`@param`, but `man/drmTMB.Rd` was never regenerated.  `devtools::document()`
changed only that file (eight lines).

Release shard 1 then showed the manifest entry alone does not protect
`R CMD check`: the installed package still runs test-071 and fails on its
48 missing source-tree inputs.  Every block now opens with a premise skip
(`skip_if(!file.exists(tools/write-parity-scoreboard.R) || !dir.exists(the
071 evidence directory))`), the repo's convention for the blind spot.  From
the source tree the file reports `[ FAIL 0 | WARN 0 | SKIP 2 | PASS 201 ]`;
from a copy of `tests/testthat` with no `tools/` or `docs/` beside it (the
`R CMD check` condition) it reports `[ FAIL 0 | WARN 0 | SKIP 38 | PASS 0 ]`.
The census gained the second build-premise row that helper adds.

Local runs after the repair, each under `pkgload::load_all()`:
`test-env-skip-census.R`, `test-julia-gate-vs-engine.R`,
`test-coefficient-labels.R` (5 live-Julia skips, no failures), and
`test-julia-bridge-coef-labels.R` all pass.  The acceptance ledger for this
leaf lives in the untracked `.unlazy/dinnage-wave1/gates/leaf-A1.md`.

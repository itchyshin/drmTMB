# Arcs — true-parity overnight lane (2026-09-02/03). Status: [ ] todo · [~] in progress · [x] verified · [G] open gate

- [x] N0 — main CI green (0ceb77eb0: ubuntu release, os-matrix, pkgdown, deploy all success, 2026-09-02 23:4x UTC). Watch R-CMD-check + pkgdown on main 0ceb77eb0; if red, read the job's own remediation text and fix via a docs/receipt PR. Verify: both check-runs success.
- [x] N1 (merged d3d205486 via PR #1124; Rose-repaired; S3-G4 flipped to MET) — HEADLINE. Label contract for the remaining bridge routes: extend `coef_labels` (design 258 §7) to `drm_julia_structured_payload`, `drm_julia_biv_known_structured_payload`, the joint (`R/julia-joint-call.R`) and cross-family (xfam) bridge payloads, using the ONE producer `drm_julia_bridge_payload_coef_labels()`; then delete the legacy `gsub()` rewrite in `drm_julia_predict_fixed_eta()` and flip `.unlazy/true-parity/gates/leaf-s3.md` S3-G4 from ABANDONED to MET. Tests: unit (no Julia) per builder; live Julia at 77513aa0 for one structured route (relmat/animal), the xfam route, and a joint fit, each through DRM.jl's echo; predict() with factor/interaction terms on a structured fit (Rose A10 case). Docs: design 258 §7.4 rewritten (now covered). Gate file: `.unlazy/night/gates/leaf-n1.md`. Lands as a PR; merge when green (D-208).
- [x] N2 (merged 9bc9db99f via PR #1125 after a tarball-safe test fix; closes #1115) — Reverse gap #1115: `heritability()`, `icc()`, `repeatability()`. Symbolic alignment table FIRST against DRM.jl's definitions (read `src/` in the pinned clone; do not port code); R implementation on the Gaussian random-intercept, `phylo()`, and animal/relmat routes; delta-method SE where `vcov()` supports it; known-DGP recovery tests; roxygen; `check_pkgdown()`; board row. Gate file `leaf-n2.md`. PR; merge when green.
- [x] N3 (merged fa1ebf95b via PR #1128; Rose-repaired: phylo_cor NaN band, honest fill-order comment) — `objective_at()` / `start=` label vocabulary reaches `rho12` and the q4 phylo covariance block (A5 finding). RED tests from the A5 receipt script's workaround; gate `leaf-n3.md`. PR; merge when green.
- [x] N4 (merged ef796e7f9 via PR #1126) — Board + B2: project `Non-Gaussian phylogenetic location-scale (mu + log sigma)` onto `docs/design/capability-status.md` as scope-limited (nbinom2, zero_one_beta implemented; ten families rejected by design — measured in the decision map); deterministic indefinite-Hessian test for `check_drm()` (exactly rank-deficient design or injected indefinite covariance) replacing the platform-boundary premise. Gate `leaf-n4.md`. PR; merge when green.
- [x] N5 (pre-run on Totoro) — ABANDONED HONESTLY: Totoro's R cannot embed Julia (JuliaCall::julia_setup() segfaults, R 4.5.3 / JuliaCall 0.17.6, Julia 1.12.6 and 1.10.10); TMB half ran; receipt + tools/parity-p2-pilot.R = draft PR #1122; bug found and filed: bootstrap CIs fail on cbind() binomial responses (#1123). Recorded in the vault runbook.
- [x] N5b — P2 pilot LOCAL: pre-run 62 s; full pilot 69 s on 4 PSOCK workers (mclapply forks segfault with libjulia on macOS); 4 rows x {wald, profile, bootstrap where single-column} x 2 engines x 5 seeds, all converged; wald/profile endpoint deltas ≤ 1.6e-5; bootstrap deltas up to 0.18 = independent RNG streams at R=20 (same-seed resampling or larger R is the P2 design point); biv_gaussian_residual profile/bootstrap not Julia-ready (documented). Receipts on PR #1122. Original text: pre-run on this Mac (profile CIs, both engines, one row) → estimate → RUN if ≤ 30 min on ≤ 6 cores, else STOP for the morning. Gates leaf-n5b.md. Original plan for reference: P2 pilots on Totoro: pre-run FIRST (one bridge route, profile + parametric bootstrap on both engines, small n, single core pinned, <10 min, shown in the log); then the pilot over the four `partial` rows; receipts only, no promotion, no coverage claim. Gate `leaf-n5.md`; D-139 estimate stated before each run; ≤150 cores.
- [x] N6 (closed 03:35 UTC; docs merged via #1131 dedb105a5; pipeline ALL MET; lease released) — Close: Melissa reconcile → `docs/dev-log/plan-actual/2026-09-03-true-parity-night.md`; after-task → `docs/dev-log/after-task/2026-09-03-true-parity-night.md`; handover refreshed; vault log + any finding of record (D-175); lease released; LOOP/checkpoint.md final.

Gates that would STOP the loop: any DRM.jl edit; CRAN/release/registration; a claim without a receipt; reopening D-179/D-181/D-202/D-204; compute beyond the cap or an estimate without a pre-run; a surprise that invalidates the plan.

## Follow-on arcs added 2026-09-03 03:50 UTC (21:50 MDT; window to 05:00 MDT). Both are drmTMB-side bugs found tonight inside the mission's scope; each lands as a green PR under D-208. Owner-decision items (#1129, #1130, promotion, q4_vcov) stay in the morning queue.
- [x] N7 (merged 20b107bf3 via PR #1133; Rose 4/5 survived + 1 repaired; closes #1127; 4 measured-broken constructs now visible) — #1127: the live Julia tests fail on engine errors instead of skipping (tryCatch->skip removed at all 16 sites); one env-var gate (DRM_JL_PATH, DRM_JL_PHYLO_PATH only as a fallback in the shared helper). Gate `leaf-n7.md`. Sonnet builder, Sonnet Rose. PR; merge when green.
- [x] N8 (merged 208e0c903 via PR #1132; Rose 5/5 survived; closes #1123) — #1123: parametric bootstrap for cbind(successes, failures) binomial responses (R/profile.R bootstrap_response_data). RED test first; trial sizes preserved per row. Gate `leaf-n8.md`. Sonnet builder, Sonnet Rose. PR; merge when green.
- [ ] N9 (added 07:15 UTC = 01:15 MDT; ~3.5 h left in the window) — morning question 11 made an arc: the three remaining coef_labels gaps under DRM.jl's echo (resd_sigma for sigma-side phylo, resd for a random-slope phylo block, phylocov at test-julia-tmb-parity.R:348), same producer pattern as N1's repair; the four N7 visible skips removed and run live. Gate `leaf-n9.md`. If not merged-green by 04:30 MDT, checkpoint it with its open gate named (definition of done allows that) rather than merging in a hurry.
- [ ] N10 (added 08:32 UTC = 02:32 MDT; based on N9's head, PR after #1135 merges) — morning question 12 made an arc: non-phylo random intercept on sigma, `sigma ~ (1 | g)`, labelled under the echo (Julia names resd_g_logsigma). Gate `leaf-n10.md`. Same stop rule: not merged-green by 10:30 UTC -> checkpointed with its gate named.

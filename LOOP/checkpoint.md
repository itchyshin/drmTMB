# Checkpoint — OVERWRITTEN every arc (a pointer to truth, not a log)

GOAL: see LOOP/GOAL.md.  STATE: lane scaffolded; Wave 1 (classify) starting.

- **DONE (verified):** lane pre-flight run (verdict: FOREIGN LANE ACTIVE, codex direct-to-main;
  9+ lanes live — plus 2 cursor lanes the census did not name, found via the LOOP-path hook).
  Worktree `~/local-scratch/lanes/drmTMB-interval-truth-audit` on `claude/lane-interval-truth-audit`
  off `origin/main` `82cd00560`. Kit filled. Headline numbers re-verified against `origin/main`:
  192/41/4 by tier = 237 claiming, 27 in manifest, **209 to classify**.
- **IN PROGRESS:** Wave 1 — T1 (gate extension points), T2a–c (classify 209), T3 (adjudicate split).
- **NEXT:** T1 + T2a–c in parallel, then T3.
- **OPEN GATE:** `T1-classify` — STOP after T3 and report the re-check vs re-run split, the 1-seed
  instrument problem, the 52 receiptless + 6 association scoping decision, and the two known blockers.
  **No compute before that returns.**
- **WHERE TRUTH LIVES:** this worktree's branch `claude/lane-interval-truth-audit`; artefacts under
  `LOOP/`, `scratchpad/`, `docs/dev-log/`. Cross-arc reads for `mc-0595/0596/0321/0653` come from
  `codex/response-missing-formula-surface` (`a075ff2d0`), NOT `origin/main`.
- **RESUME:** read LOOP/GOAL.md → LOOP/checkpoint.md → LOOP/arcs.md → LOOP/ultra-plan.md, then
  continue from NEXT. Pause at gate `T1-classify`.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data.R
\name{drm_grid_posterior_sd}
\alias{drm_grid_posterior_sd}
\title{Posterior standard deviation over a shared or row-specific grid}
\usage{
drm_grid_posterior_sd(grid, probability)
}
\arguments{
\item{grid}{A numeric vector (one grid shared by every row) or a numeric
matrix the same shape as \code{probability} (a row-specific grid).}

\item{probability}{A numeric matrix, one row per missing-predictor value,
one column per grid point; rows sum to 1 over the finite (non-\code{NA})
entries.}
}
\value{
A numeric vector the same length as \code{nrow(probability)}, or \code{NULL}
if \code{grid} or \code{probability} is missing or shaped inconsistently.
}
\description{
Posterior standard deviation over a shared or row-specific grid
}
\keyword{internal}

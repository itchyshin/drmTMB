% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty.R
\name{drm_phylo_penalty_sweep}
\alias{drm_phylo_penalty_sweep}
\title{Prior-sensitivity sweep for the phylogenetic correlation penalty}
\usage{
drm_phylo_penalty_sweep(
  formula,
  data,
  family = gaussian(),
  cor_sd = c(0.25, 0.5, 1),
  sd_u = 1,
  sd_alpha = 0.05,
  control = drm_control(),
  ...
)
}
\arguments{
\item{formula, data, family, control}{Passed to \code{\link[=drmTMB]{drmTMB()}}.}

\item{cor_sd}{Numeric vector of positive correlation-penalty SDs to sweep.}

\item{sd_u, sd_alpha}{Penalty SD-prior parameters; see \code{\link[=drm_phylo_penalty]{drm_phylo_penalty()}}.}

\item{...}{Further arguments passed to \code{\link[=drmTMB]{drmTMB()}}.}
}
\value{
A list with \verb{$summary} -- a data frame with one row per \code{cor_sd}
giving \code{convergence}, \code{pdHess}, \code{logLik}, and any fit \code{error} -- and \verb{$fits}
-- the fitted objects, named by \code{cor_sd}, for extracting \code{corpars()},
\code{coef()}, and other couplings.
}
\description{
Refits a penalized (MAP) phylogenetic model across a range of \code{cor_sd} values
so you can see whether a weakly identified coupling is data-informed or
prior-shaped. There is no universal \code{cor_sd}: a coupling that is stable across
the sweep is data-informed, while one that tracks \code{cor_sd} is prior-shaped.
This is the sweep the penalized/MAP workflow asks you to run; it is most
informative for coupled location-scale or bivariate phylogenetic models that
actually estimate a phylogenetic correlation.
}

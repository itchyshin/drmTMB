% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-markers.R
\name{relmat}
\alias{relmat}
\title{User-supplied relatedness structured-effect marker}
\usage{
relmat(term, K = NULL, Q = NULL)
}
\arguments{
\item{term}{Structured random-effect term, such as \code{1 | id} or
\code{1 + x | id}.}

\item{K}{Known relatedness or covariance matrix for the first fitted
univariate Gaussian \code{mu} path.}

\item{Q}{Known precision or inverse covariance matrix for the first fitted
univariate Gaussian \code{mu} path.}
}
\value{
A formula marker; never evaluated by users.
}
\description{
\code{relmat()} marks syntax for a validated user-supplied relatedness matrix. It
is the lower-level route for latent group-level dependence structures that
are not best named as \code{animal()}, \code{phylo()}, or \code{spatial()}: for example a
genomic relationship matrix, a laboratory relatedness kernel, or a graph,
river-network, areal, or Gaussian Markov random-field precision matrix built
outside \code{drmTMB} and checked by the analyst. If the matrix is known sampling
covariance among observed estimates, use \code{\link[=meta_V]{meta_V()}} instead.
}
\details{
Use \code{K} for a covariance or relatedness matrix and \code{Q} for an inverse
covariance or precision matrix. A correlation matrix with diagonal 1 is a
natural \code{K} input because the fitted relatedness SD supplies the latent
variance scale. The fitted known-matrix routes are a univariate Gaussian
\code{mu} random intercept, for example
\code{relmat(1 | line, Q = Q)}, the first bivariate Gaussian q=2 location
covariance from matching labelled terms in \code{mu1} and \code{mu2}, matching
univariate Gaussian \code{mu} and \code{sigma} intercept terms estimate one
relatedness mean-scale correlation, and the constant all-four q=4
location-scale block comes from matching labelled terms in \code{mu1}, \code{mu2},
\code{sigma1}, and \code{sigma2}, for example \code{relmat(1 | p | line, Q = Q)}. The
univariate Gaussian \code{mu} path also
supports one numeric slope, for example \code{relmat(1 + x | line, Q = Q)}, as
independent intercept and slope fields with separate SDs and no
intercept-slope correlation. Multiple structured slopes, slope correlations,
residual-scale structured slopes, predictor-dependent \code{corpair()}
regression, and relatedness \verb{sd*()} direct-SD grammar remain planned.
\code{relmat()} is
intentionally separate from \code{\link[=meta_V]{meta_V()}}, which adds known sampling covariance
among observations, and from residual \code{rho12}, which models
within-observation bivariate residual correlation.
}
\examples{
# Fitted: a genomic relatedness matrix for among-line genetic variance.
bf(seed_mass ~ temperature + relmat(1 | line, K = G),
  sigma ~ temperature
)

# Fitted: a user-built sparse precision for another dependence structure.
bf(growth ~ treatment + relmat(1 | plot, Q = Q_plot),
  sigma ~ treatment
)
}

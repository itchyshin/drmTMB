% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{structured_effects}
\alias{structured_effects}
\alias{structured_effects.default}
\alias{structured_effects.drmTMB}
\title{Extract structured-effect metadata}
\usage{
structured_effects(object, ...)

\method{structured_effects}{default}(object, ...)

\method{structured_effects}{drmTMB}(object, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future extractor options.}
}
\value{
A data frame with one row per fitted structured-effect marker. The
\code{args}, \code{dpars}, and \code{coef_names} columns are list columns. Empty fits
return the same columns with zero rows.
}
\description{
\code{structured_effects()} returns the structured random-effect markers that
\code{drmTMB} parsed and fitted. It gives downstream tools a stable post-fit
metadata table so they do not need to grep or re-parse formula text.
}
\details{
The current fitted marker grammar is:
\itemize{
\item \code{phylo(1 | species, tree = tree)}
\item \code{spatial(1 | site, coords = coords)}
\item \code{animal(1 | id, pedigree = pedigree)},
\code{animal(1 | id, A = A)}, or \code{animal(1 | id, Ainv = Ainv)}
\item \code{relmat(1 | id, K = K)} or \code{relmat(1 | id, Q = Q)}
}

Some Gaussian routes also fit matching location-scale, bivariate, or
one-slope structured blocks. Those routes still return one row per parsed
structured marker, with \code{dpars} and \code{coef_names} stored as list columns.
}
\examples{
dat <- data.frame(
  y = c(0.1, 0.4, 0.8, 1.1),
  x = c(-1, 0, 1, 2)
)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
structured_effects(fit)
}

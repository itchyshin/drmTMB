% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction-grid.R
\name{prediction_grid}
\alias{prediction_grid}
\alias{prediction_grid.drmTMB}
\title{Build prediction grids for distributional-parameter summaries}
\usage{
prediction_grid(object, ...)

\method{prediction_grid}{drmTMB}(
  object,
  focal = NULL,
  at = list(),
  condition = list(),
  margin = c("mean_reference", "empirical"),
  n = 50L,
  weights = c("equal", "proportional"),
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit that retained its fitted model data.}

\item{...}{Reserved for future options.}

\item{focal}{Optional character vector of predictor names to vary.}

\item{at}{Optional named list of values for focal predictors. Focal numeric
predictors without an \code{at} entry use an evenly spaced sequence over the
fitted range. Focal factors without an \code{at} entry use all fitted levels.}

\item{condition}{Optional named list of non-focal predictors to hold at
supplied values.}

\item{margin}{Grid rule. \code{"mean_reference"} returns one row for each focal
combination with nuisance predictors set to reference values.
\code{"empirical"} crosses focal combinations with the fitted model rows.}

\item{n}{Number of points for automatically generated numeric focal grids.}

\item{weights}{Metadata label for later marginalisation helpers. The current
function records the choice but does not compute weighted summaries.}
}
\value{
A data frame with class \code{drm_prediction_grid}. The ordinary columns
are valid \code{newdata} columns for the fitted model. Attribute
\code{"prediction_grid"} stores \code{focal_terms}, \code{conditioned_terms}, \code{margin},
\code{weights}, \code{grid_source}, \code{reference_terms}, \code{predictor_terms},
\code{n_source_rows}, and \code{n_grid_rows}.
}
\description{
\code{prediction_grid()} creates explicit \code{newdata} grids for
\code{\link[=predict_parameters]{predict_parameters()}} and \code{\link[=marginal_parameters]{marginal_parameters()}}. It does not fit, predict,
average, or plot. The helper records which terms were varied, which terms
were fixed, and which grid rule was used so later interpretation and plotting
helpers do not hide those choices.
}
\details{
Use \code{margin = "mean_reference"} for adjusted predictions at named covariate
values. Use \code{margin = "empirical"} when the target is an average over the
fitted covariate distribution after replacing one or more focal predictors.

With \code{margin = "mean_reference"}, focal terms vary across the requested grid
and all other predictors are set to reference values: numeric predictors use
their fitted-row mean, factors use their first fitted level, character
predictors use their first fitted value with fitted levels preserved, and
logical predictors use their first fitted value unless supplied through
\code{condition}.

With \code{margin = "empirical"}, focal terms are crossed with the fitted model
rows. Non-focal predictors keep their observed fitted-row values unless
supplied through \code{condition}. This produces a counterfactual-style grid that
can be passed to \code{marginal_parameters(..., by = focal_terms)} for simple
empirical averaging.
}
\examples{
set.seed(20260523)
n <- 48
x <- seq(-1.5, 1.5, length.out = n)
habitat <- factor(rep(c("reef", "sand"), length.out = n))
eta <- 0.4 + 0.7 * x + ifelse(habitat == "reef", 0.25, -0.15)
sigma <- exp(-0.35 + 0.15 * x)
dat <- data.frame(y = eta + rnorm(n, sd = sigma), x = x, habitat = habitat)
fit <- drmTMB(bf(y ~ x + habitat, sigma ~ x), data = dat)

grid <- prediction_grid(
  fit,
  focal = "x",
  at = list(x = c(-1, 0, 1)),
  condition = list(habitat = "reef")
)
predict_parameters(fit, newdata = grid, dpar = c("mu", "sigma"), conf.int = TRUE)

empirical_grid <- prediction_grid(
  fit,
  focal = "habitat",
  at = list(habitat = levels(dat$habitat)),
  margin = "empirical"
)
marginal_parameters(fit, newdata = empirical_grid, dpar = "mu", by = "habitat")
}

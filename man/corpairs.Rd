% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{corpairs}
\alias{corpairs}
\alias{corpairs.drmTMB}
\title{Extract fitted correlation pairs}
\usage{
corpairs(object, ...)

\method{corpairs}{drmTMB}(
  object,
  level = NULL,
  group = NULL,
  block = NULL,
  class = NULL,
  conf.int = FALSE,
  conf.level = 0.95,
  method = "profile",
  trace = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Additional arguments passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} when
\code{conf.int = TRUE}.}

\item{level}{Optional character vector of correlation levels to keep, such
as \code{"residual"}, \code{"group"}, \code{"phylogenetic"}, or \code{"spatial"}.}

\item{group}{Optional character vector of grouping factors to keep, such as
\code{"id"}. Residual rows have no grouping factor and are removed by this
filter.}

\item{block}{Optional character vector of covariance-block labels to keep,
such as \code{"p"}. Residual rows have no block label and are removed by this
filter.}

\item{class}{Optional character vector of pair classes to keep, such as
\code{"residual"} or \code{"mean-slope"}. Location aliases such as
\code{"location-location"} and \code{"location-scale"} are accepted as filters for
the current \code{"mean-mean"} and \code{"mean-scale"} rows.}

\item{conf.int}{Logical; include profile-likelihood confidence intervals
where the correlation target is currently profile-ready. Unsupported
derived targets receive an explicit interval status instead of silent
missing bounds.}

\item{conf.level}{Confidence level used when \code{conf.int = TRUE}. This is
named separately from the \code{level} filter to avoid ambiguity with
correlation levels such as \code{"phylogenetic"}.}

\item{method}{Interval method used when \code{conf.int = TRUE}. Only
\code{"profile"} is currently supported for correlation-pair intervals.}

\item{trace}{Logical; passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} when profile intervals
are requested.}
}
\value{
A data frame with one row per fitted correlation pair or pair
summary. Predictor-dependent \code{rho12} is summarized by its mean, minimum,
and maximum over the fitted rows. Rows include \code{conf.status} and
\code{interval_source} so point-only and interval-aware pair tables use the same
provenance vocabulary as prediction tables.
}
\description{
\code{corpairs()} returns a long table of fitted correlation pairs from a
\code{drmTMB} model. The first implementation reports correlations that are
already fitted elsewhere: residual bivariate \code{rho12}, ordinary univariate
group-level \code{mu} random-effect correlations, matched univariate and
same-response bivariate \code{mu}/\code{sigma} random-intercept covariance blocks, and
matched bivariate \code{mu1}/\code{mu2} and \code{sigma1}/\code{sigma2} random-intercept
covariance blocks from \code{corpars}, plus fitted bivariate phylogenetic and
coordinate-spatial correlation rows. Full q4 phylogenetic blocks report six
derived endpoint correlations; block-diagonal q4 fallback fits report the
direct \code{mu1}/\code{mu2} and \code{sigma1}/\code{sigma2} block correlations.
}
\details{
The table is intentionally more explicit than \code{rho12()} or \code{corpars}
because future double-hierarchical, phylogenetic, spatial, and study-level
models will contain several scientifically different correlations.
}
\examples{
set.seed(1)
n <- 40
x <- rnorm(n)
z1 <- rnorm(n)
z2 <- rnorm(n)
mu1 <- 0.2 + 0.5 * x
mu2 <- -0.1 + 0.4 * x
sigma1 <- exp(-0.2 + 0.15 * z1)
sigma2 <- exp(0.1 - 0.1 * z2)
rho <- 0.35
e1 <- rnorm(n)
e2 <- rho * e1 + sqrt(1 - rho^2) * rnorm(n)
dat <- data.frame(
  y1 = mu1 + sigma1 * e1,
  y2 = mu2 + sigma2 * e2,
  x = x,
  z1 = z1,
  z2 = z2
)
fit <- drmTMB(
  bf(
    mu1 = y1 ~ x,
    mu2 = y2 ~ x,
    sigma1 = ~ z1,
    sigma2 = ~ z2,
    rho12 = ~ 1
  ),
  family = c(gaussian(), gaussian()),
  data = dat
)
corpairs(fit)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.drmTMB}
\alias{summary.drmTMB}
\title{Summarize a fitted model}
\usage{
\method{summary}{drmTMB}(
  object,
  conf.int = FALSE,
  level = 0.95,
  method = c("wald", "profile"),
  ci_parm = NULL,
  trace = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{conf.int}{Logical; include confidence intervals when \code{TRUE}.}

\item{level}{Confidence level for intervals.}

\item{method}{Interval method used when \code{conf.int = TRUE}: \code{"wald"} for
fixed-effect intervals or \code{"profile"} for profile-likelihood intervals on
selected direct targets.}

\item{ci_parm}{Optional character or integer vector selecting confidence
interval targets. For \code{method = "wald"}, targets must be fixed effects. For
\code{method = "profile"}, targets use the \code{\link[=profile_targets]{profile_targets()}} namespace, such
as \code{"sigma"}, \code{"rho12"}, \code{"sd:mu:(1 | id)"}, or
\code{"cor:mu:cor((Intercept),x | id)"}. \code{NULL} selects all fixed effects for
Wald intervals and all currently ready direct targets for profile
intervals.}

\item{trace}{Logical; passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} for profile intervals.}

\item{...}{Additional arguments passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} when
\code{conf.int = TRUE} and \code{method = "profile"}.}
}
\value{
An object of class \code{summary.drmTMB}.
}
\description{
\code{summary()} returns a compact summary of fixed-effect estimates,
response-scale distributional, scale, shape, random-effect SD, correlation,
and fitted random-effect covariance quantities when they are present.
The covariance component reports currently fitted registry-backed rows and
the first fitted bivariate phylogenetic \code{mu1}/\code{mu2} mean-mean row.
The derived component reports simple point-estimate variance ratios, such as
Gaussian random-intercept repeatability and phylogenetic signal, when the
ingredients are unambiguous. Derived confidence intervals are marked as
unavailable until a nonlinear interval method is implemented.
When \code{TMB::sdreport()} succeeds, direct response-scale parameter rows also
include delta-method standard errors; descriptive fitted ranges and derived
variance ratios do not.
Confidence intervals are opt-in: fast Wald intervals are available for fixed
effects, and slower profile-likelihood intervals are available for selected
direct profile targets. Interval-aware tables include \code{conf.status} so rows
without intervals can say whether an interval was not requested, needs
\code{newdata}, is ready but unselected, or is currently unavailable.
}
\examples{
dat <- data.frame(y = c(0.2, 0.5, 1.1, 1.4), x = c(-1, 0, 1, 2))
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
summary(fit)
}

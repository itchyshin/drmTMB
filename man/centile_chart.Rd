% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributional-outputs.R
\name{centile_chart}
\alias{centile_chart}
\alias{centile_chart.drmTMB}
\title{Model-conditional centile chart}
\usage{
centile_chart(object, covariate, prob = c(0.03, 0.15, 0.5, 0.85, 0.97), ...)

\method{centile_chart}{drmTMB}(
  object,
  covariate,
  prob = c(0.03, 0.15, 0.5, 0.85, 0.97),
  dpar = NULL,
  n = 100L,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{covariate}{Character scalar naming the predictor to vary.}

\item{prob}{Numeric vector of probabilities in (0, 1) giving the centiles
to draw.}

\item{...}{Reserved for future options.}

\item{dpar}{Distributional parameter identifying the response; see
\code{\link[=predict.drmTMB]{predict.drmTMB()}}. If \code{NULL}, the first fitted distributional parameter
is used (for bivariate \code{biv_gaussian} fits, \code{dpar} selects the response
-- see \code{\link[=predict.drmTMB]{predict.drmTMB()}}'s Details for the marginal-only scope).}

\item{n}{Number of grid points spanning \code{covariate}'s fitted range; passed
to \code{\link[=prediction_grid]{prediction_grid()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{centile_chart()} draws fitted response centile curves against one
covariate, holding every other predictor at a reference value (numeric
predictors at their fitted mean, factors at their first fitted level; see
\code{\link[=prediction_grid]{prediction_grid()}}). Each curve is \code{predict(object, newdata = grid, type = "quantile", prob = p)} for one \code{p} in \code{prob}.
}
\details{
\code{centile_chart()} is a MODEL-CONDITIONAL summary at \code{\link[=predict_parameters]{predict_parameters()}}'s
fixed-effect, population-level parameter estimates \code{theta_hat} -- it is
\strong{not} a WHO-style or other population reference standard. The reference
values every non-focal covariate is held at are reported in the plot
subtitle so a reader is not left guessing what "conditional" means here.
}
\examples{
set.seed(20260712)
n <- 60
x <- stats::rnorm(n)
dat <- data.frame(y = 0.5 + 0.8 * x + stats::rnorm(n), x = x)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), family = gaussian(), data = dat)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  centile_chart(fit, covariate = "x")
}
}
\seealso{
\code{\link[=predict.drmTMB]{predict.drmTMB()}}, \code{\link[=prediction_grid]{prediction_grid()}}
}

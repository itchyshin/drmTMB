% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/associate-pairs.R
\name{confint.drm_pair_association}
\alias{confint.drm_pair_association}
\title{Alpha-scale confidence intervals for a frozen-margin association}
\usage{
\method{confint}{drm_pair_association}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A fitted \code{drm_pair_association} object.}

\item{parm}{Association coefficients to include. \code{NULL} (the default) or
\code{"alpha"} selects all association coefficients; a numeric or character
subset may also be supplied.}

\item{level}{Confidence level in \verb{(0, 1)}.}

\item{...}{Reserved for future options.}
}
\value{
A matrix with alpha-scale Wald confidence limits.
}
\description{
For admitted fixed-effect complete-pair association routes, \code{\link[=vcov]{vcov()}} returns
the association-coefficient block of the two-stage Godambe sandwich and this
method returns the corresponding Wald intervals. These intervals are on the
association-link (\code{alpha}) scale, not the bounded latent-association (\code{eta})
scale. Every route is interval-feasible when its fit-specific covariance
diagnostics pass. Coverage evidence currently promotes only the retained
Bernoulli x ordinary-NB2 intercept domain to inference-ready with caveats;
other routes receive an explicit experimental-coverage warning.
}
\seealso{
\code{\link[=vcov]{vcov()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/associate-pairs.R
\name{confint.drm_pair_association}
\alias{confint.drm_pair_association}
\title{Confidence intervals for a frozen-margin association}
\usage{
\method{confint}{drm_pair_association}(object, parm = NULL, level = 0.95, type = c("alpha", "eta"), ...)
}
\arguments{
\item{object}{A fitted \code{drm_pair_association} object.}

\item{parm}{Association coefficients to include. \code{NULL} (the default) or
\code{"alpha"} selects all association coefficients; a numeric or character
subset may also be supplied.}

\item{level}{Confidence level in \verb{(0, 1)}.}

\item{type}{\code{"alpha"} for coefficient-scale intervals or \code{"eta"} for the
bounded latent association of an intercept-only model.}

\item{...}{Reserved for future options.}
}
\value{
A matrix with Wald confidence limits on the requested scale.
}
\description{
For admitted fixed-effect complete-pair association routes, \code{\link[=vcov]{vcov()}} returns
the association-coefficient block of the two-stage Godambe sandwich and this
method returns corresponding Wald intervals. \code{type = "alpha"} returns the
coefficient-scale intervals. For an intercept-only association,
\code{type = "eta"} monotonically transforms its link-scale limits to the bounded
latent-association scale. A covariate-varying association has no single eta;
use \code{\link[=predict.drm_pair_association]{predict.drm_pair_association()}} with \code{newdata} for row-specific eta
uncertainty. Every route is interval-feasible when its fit-specific
covariance diagnostics pass. Coverage evidence currently promotes only the
retained Bernoulli x ordinary-NB2 intercept domain to inference-ready with
caveats; other routes receive an experimental-coverage warning.
}
\examples{
\dontrun{
set.seed(20260801)
dat <- data.frame(x = rnorm(100))
z_1 <- rnorm(100)
z_2 <- 0.35 * z_1 + sqrt(1 - 0.35^2) * rnorm(100)
dat$continuous <- 0.2 + 0.4 * dat$x + z_1
dat$binary <- as.integer(z_2 > qnorm(0.55))
assoc <- biv_associate(
  bf(mu = continuous ~ x, sigma = ~ 1),
  bf(mu = binary ~ x),
  family = list(gaussian(), binomial()), data = dat
)
confint(assoc, type = "eta")
}
}
\seealso{
\code{\link[=vcov]{vcov()}}, \code{\link[=predict.drm_pair_association]{predict.drm_pair_association()}}
}

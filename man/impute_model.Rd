% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data.R
\name{impute_model}
\alias{impute_model}
\title{Define a missing-predictor model}
\usage{
impute_model(formula, family = stats::gaussian(), trials = NULL)
}
\arguments{
\item{formula}{Two-sided predictor-model formula. For most families, the
left-hand side must be the same variable used inside \code{\link[=mi]{mi()}}. For
\code{family = beta_binomial()}, the left-hand side is the success-count
column, while the \code{\link[=mi]{mi()}} variable is the success proportion used in the
response model.}

\item{family}{Predictor-model family. \code{gaussian()} keeps the existing
continuous predictor route. \code{binomial(link = "logit")} fits the binary
missing-predictor route. \code{\link[=cumulative_logit]{cumulative_logit()}} fits the ordered categorical
missing-predictor route. \code{\link[=categorical]{categorical()}} fits the unordered categorical
missing-predictor route. \code{\link[=beta]{beta()}} fits the strict beta/proportion
missing-predictor route. \code{\link[=zero_one_beta]{zero_one_beta()}} fits the boundary-proportion
missing-predictor route. \code{\link[=beta_binomial]{beta_binomial()}} fits a denominator-aware
success/trial proportion route and requires \code{trials}. \code{poisson(link = "log")}, \code{\link[=nbinom2]{nbinom2()}}, and \code{\link[=truncated_nbinom2]{truncated_nbinom2()}} fit count
missing-predictor routes. \code{\link[=lognormal]{lognormal()}} and \code{Gamma(link = "log")} fit
positive continuous missing-predictor routes. \code{\link[=tweedie]{tweedie()}} fits a
semi-continuous non-negative missing-predictor route with exact zeros.}

\item{trials}{Optional trial-count column for \code{family = beta_binomial()}.
The formula left-hand side is the success count and \code{trials} is the known
denominator for each row.}
}
\value{
A \code{drm_impute_model} object for the \code{impute} argument of \code{\link[=drmTMB]{drmTMB()}}.
}
\description{
\code{impute_model()} wraps the model for a predictor used inside \code{\link[=mi]{mi()}}. A bare
formula in \code{impute}, such as \code{impute = list(x = x ~ z)}, is still treated as
a Gaussian model for a numeric missing predictor. Use \code{impute_model()} when
the missing predictor needs an explicit non-Gaussian predictor family. The
first non-Gaussian fitted routes are fixed-effect Bernoulli/logit models for
one binary predictor, fixed-effect cumulative-logit models for one ordered
categorical predictor, fixed-effect baseline-category softmax models for one
unordered categorical predictor, fixed-effect beta models for one strict
proportion predictor in \verb{(0, 1)}, fixed-effect zero-one beta models for one
boundary proportion predictor in \verb{[0, 1]}, fixed-effect beta-binomial
models for one denominator-aware success/trial proportion predictor,
fixed-effect Poisson, negative-binomial, or zero-truncated
negative-binomial models for one count predictor, fixed-effect lognormal or
Gamma models for one positive continuous predictor, and fixed-effect
Tweedie models for one non-negative semi-continuous predictor with exact
zeros. Most current non-Gaussian predictor families are fitted inside a
Gaussian response location model; Poisson, binomial, negative-binomial, and
beta responses are currently supported for one binary missing predictor.
}
\examples{
impute_model(x ~ z)
impute_model(treatment ~ z, family = binomial())
impute_model(score ~ z, family = cumulative_logit())
impute_model(habitat ~ z, family = categorical())
impute_model(cover ~ z, family = beta())
impute_model(cover ~ z, family = zero_one_beta())
impute_model(success ~ z, family = beta_binomial(), trials = trials)
impute_model(abundance ~ z, family = poisson())
impute_model(abundance ~ z, family = nbinom2())
impute_model(abundance ~ z, family = truncated_nbinom2())
impute_model(biomass ~ z, family = lognormal())
impute_model(biomass ~ z, family = Gamma(link = "log"))
impute_model(biomass ~ z, family = tweedie())
}

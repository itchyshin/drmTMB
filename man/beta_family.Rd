% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{beta_family}
\alias{beta_family}
\alias{beta}
\title{Beta response family}
\usage{
beta_family()

beta()
}
\value{
A \code{drm_family} object.
}
\description{
\code{beta_family()} defines a one-response distribution for continuous proportions
strictly inside \verb{(0, 1)}, with formulas for mean \code{mu} and scale \code{sigma}.
Write \code{family = beta_family()}. Unqualified \code{beta()} is \code{\link[base:beta]{base::beta()}},
the Euler beta function, and is not a drmTMB family.
}
\details{
The implemented contract is
\code{logit(mu) = eta_mu}, \code{log(sigma) = eta_sigma}, and internal precision
\code{phi = 1 / sigma^2}. Larger \code{sigma} therefore means more variation around
the mean, not more precision.
Ordinary unlabelled random intercepts and independent numeric slopes such as
\code{(1 | id)} and \code{(0 + x | id)} may enter the logit-\code{mu} predictor; \code{sigma}
remains fixed-effect in this first slice.
}
\section{Functions}{
\itemize{
\item \code{beta()}: Deprecated alias; use \code{\link[=beta_family]{beta_family()}} instead.
Not exported (it would mask \code{\link[base:beta]{base::beta()}}). Call \code{drmTMB:::beta()} only
if an old script still needs the alias.

}}
\examples{
beta_family()
}
\keyword{internal}

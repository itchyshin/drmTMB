% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/associate-pairs.R
\name{biv_associate}
\alias{biv_associate}
\title{Fit two margins and construct a frozen-margin association in one call}
\usage{
biv_associate(
  formula_1,
  formula_2,
  family,
  data,
  kernel = latent_normal(),
  association = ~1,
  control_1 = list(),
  control_2 = list()
)
}
\arguments{
\item{formula_1, formula_2}{Univariate \code{drm_formula} objects for the first
and second response margins.}

\item{family}{A two-element list of marginal family objects.}

\item{data}{A data frame containing both responses and every predictor used
by either margin.}

\item{kernel}{Association kernel. Arc 6 accepts only \code{\link[=latent_normal]{latent_normal()}}.}

\item{association}{Association formula. Most Arc 6 pair classes accept only
\code{~ 1}, which estimates one constant association parameter. The beta
Bernoulli x ordinary-NB2 route also accepts \code{~ x} for one finite numeric
covariate.}

\item{control_1, control_2}{Optional control lists passed to the corresponding
marginal \code{\link[=drmTMB]{drmTMB()}} fits.}
}
\value{
A \code{drm_pair_association} object that retains frozen snapshots of both
fitted margins. \code{\link[=association]{association()}} returns the point estimate unless the
numerical diagnostic is boundary-unresolved; a near-boundary status remains
flagged. No standard error, confidence interval,
or profile is available.
}
\description{
\code{biv_associate()} is the convenience front end for the reviewed Arc 6
frozen-margin route. It fits two univariate margins to the supplied data and
then calls \code{\link[=associate_pairs]{associate_pairs()}} without refitting either margin. It is one R
call, but it is not a jointly fitted bivariate model: stage 2 treats the fitted
marginal parameters as fixed and estimates only the latent-normal
association \code{eta}.
}
\details{
The two formulas must be univariate \code{\link[=bf]{bf()}} or \code{\link[=drm_formula]{drm_formula()}} objects, and
\code{family} must be a two-element list. The supplied \code{data} must already be the
same complete paired analysis data for both margins. If the two marginal fits
retain different rows, the constructor fails rather than silently comparing
different individuals.
}
\examples{
set.seed(20260725)
dat <- data.frame(x = rnorm(80))
z_continuous <- rnorm(80)
z_binary <- 0.35 * z_continuous + sqrt(1 - 0.35^2) * rnorm(80)
dat$trait_continuous <- 0.2 + 0.5 * dat$x + z_continuous
dat$trait_binary <- as.integer(z_binary > qnorm(0.4))

assoc <- biv_associate(
  bf(mu = trait_continuous ~ x, sigma = ~ 1),
  bf(mu = trait_binary ~ x),
  family = list(gaussian(), binomial()), data = dat
)
association(assoc)
}

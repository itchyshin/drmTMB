% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lrt-boundary.R
\name{lrt-boundary}
\alias{lrt-boundary}
\alias{chibar_pvalue}
\alias{lrt_boundary}
\title{Boundary-corrected likelihood-ratio test for variance components}
\usage{
chibar_pvalue(statistic, q = 1L)

lrt_boundary(full, reduced, q = 1L)
}
\arguments{
\item{statistic}{Likelihood-ratio statistic(s),
\code{2 * (logLik(full) - logLik(reduced))}. Vectorised; \code{NA} propagates.}

\item{q}{Number of variance components tested at zero (the mixture
order). Only \code{1} and \code{2} are supported; any other value aborts, as in
\code{DRM.jl}.}

\item{full}{A \code{drmTMB} fit carrying the \code{q} variance component(s).}

\item{reduced}{The same model with those component(s) removed.}
}
\value{
\code{chibar_pvalue()} returns a numeric vector of upper-tail p-values
the length of \code{statistic}.

\code{lrt_boundary()} returns a list of class \code{drm_lrt_boundary} with
elements \code{statistic} (\code{2 * (logLik(full) - logLik(reduced))}), \code{q},
\code{pvalue} (\code{chibar_pvalue(statistic, q)}), \code{pvalue_naive} (the
\eqn{\chi^2(q)} upper tail at \code{max(statistic, 0)}, for comparison; always
\code{pvalue <= pvalue_naive}), and \code{df} (\code{df(full) - df(reduced)}, the number
of extra parameters in \code{full}; \code{DRM.jl} does not report it). The two
fits must be on the same observations; when \code{df} differs from \code{q} a
\code{drmTMB_lrt_boundary_df_mismatch} warning says the mixture may not apply.
}
\description{
\code{chibar_pvalue()} and \code{lrt_boundary()} are the chi-bar-square (\eqn{\bar\chi^2})
boundary-corrected likelihood-ratio machinery for testing \code{q} \strong{variance
components = 0}, ported term-for-term from \code{DRM.jl}'s \code{src/chibar.jl}
(\code{chibar_pvalue}, lines 83-95; \code{lrt_boundary}, lines 134-140, at DRM.jl
pin \verb{430ef64cc}). Testing a variance at zero is a \emph{boundary} problem: the
null value sits on the edge of the parameter space, so the usual
\eqn{\chi^2(q)} reference for \code{2 * (logLik(full) - logLik(reduced))} is
wrong and \strong{conservative} (its p-values are too large, so the test loses
power). Under the regularity conditions of Self and Liang (1987) and Stram
and Lee (1994) the statistic follows a mixture of \eqn{\chi^2}
distributions instead:
}
\details{
\itemize{
\item \code{q = 1} (one boundary parameter, e.g. dropping one \code{(1 | g)}): the null
is \eqn{0.5\,\chi^2_0 + 0.5\,\chi^2_1}, so \code{p = 0.5 * P(chisq_1 > stat)}.
At \code{stat = 0} this is \code{0.5}; for \code{stat > 0} it is exactly half the naive
\eqn{\chi^2_1} p-value.
\item \code{q = 2} (two \strong{independent} boundary parameters): the null is
\eqn{0.25\,\chi^2_0 + 0.5\,\chi^2_1 + 0.25\,\chi^2_2}, so
\code{p = 0.5 * P(chisq_1 > stat) + 0.25 * P(chisq_2 > stat)}. At \code{stat = 0}
this is \code{0.75}.
}

\code{drmTMB}'s \code{anova()} deliberately implements no likelihood-ratio
comparison, and \code{confint(method = "wald")} only \emph{flags} a variance
component at its boundary (\code{conf.status = "wald_at_boundary"}); this pair
is the first drmTMB tool that computes a p-value that is correct there.
}
\section{Assumptions}{

\itemize{
\item The \code{q} parameters dropped between \code{full} and \code{reduced} are variances
tested at the boundary \code{0}, and \code{reduced} is \code{full} with exactly those
components removed (nested models, same data).
\item For \code{q = 2} the two components are independent (zero information
correlation). With correlated components the 0.25/0.5/0.25 weights
depend on the information matrix and this mixture is only approximate.
\item All other parameters are interior, so the standard asymptotic expansion
holds for them.
\item Maximum-likelihood fits. REML log-likelihoods are comparable only across
variance structures with the same fixed effects, so \code{lrt_boundary()}
aborts when either fit is REML and the two mean structures differ, or
when one fit is REML and the other ML. A penalized (\code{penalty = drm_phylo_penalty(...)}, MAP) fit aborts too: its variance components
are shrunk by the prior, so the likelihood ratio has no chi-square
reference. Experimental MSPL fits abort as for every likelihood method.
}

A negative statistic (the reduced model fit \emph{better}, which signals
non-nested models or a fit that did not converge) is returned as-is but is
clamped to \code{0} inside both p-values, which then take their boundary values
(\code{0.5} for \code{q = 1}, \code{0.75} for \code{q = 2}; \code{1} for the naive p-value). Inspect
\code{statistic} directly in that case.
}

\examples{
chibar_pvalue(3.5, q = 1)
chibar_pvalue(3.5, q = 1) == 0.5 * pchisq(3.5, 1, lower.tail = FALSE)
chibar_pvalue(0, q = 1)   # 0.5, the boundary point mass
chibar_pvalue(0, q = 2)   # 0.75

# Random intercept vs no random effect: dropping (1 | g) removes ONE
# variance, so q = 1.
set.seed(20260610)
G <- 30; m <- 12
g <- factor(rep(seq_len(G), each = m))
x <- rnorm(G * m)
b <- 0.8 * rnorm(G)
dat <- data.frame(y = 0.5 - 0.4 * x + b[g] + 0.7 * rnorm(G * m), x = x, g = g)
full <- drmTMB(bf(y ~ x + (1 | g), sigma ~ 1), family = gaussian(), data = dat)
reduced <- drmTMB(bf(y ~ x, sigma ~ 1), family = gaussian(), data = dat)
lrt_boundary(full, reduced, q = 1)
}
\references{
Self, S. G. and Liang, K.-Y. (1987). Asymptotic properties of maximum
likelihood estimators and likelihood ratio tests under nonstandard
conditions. \emph{Journal of the American Statistical Association}, 82,
605-610.

Stram, D. O. and Lee, J. W. (1994). Variance components testing in the
longitudinal mixed effects model. \emph{Biometrics}, 50, 1171-1177.
}

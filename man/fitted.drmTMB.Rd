% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{fitted.drmTMB}
\alias{fitted.drmTMB}
\title{Extract fitted response values}
\usage{
\method{fitted}{drmTMB}(object, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future fitted-value options.}
}
\value{
A numeric vector for univariate fits, or a two-column matrix for
bivariate Gaussian fits.
}
\description{
\code{fitted()} returns fitted response values from a \code{drmTMB} model. For
univariate Gaussian, Student-t, Gamma, beta, ordinary Poisson, and ordinary
negative-binomial fits this is the fitted \code{mu} vector. For zero-truncated
negative-binomial 2 fits this is the positive-count mean
\code{mu / (1 - Pr_NB2(0))}, where \code{mu} is the untruncated NB2 component mean.
For hurdle negative-binomial 2 fits this is the unconditional response mean
\code{(1 - hu) * mu / (1 - Pr_NB2(0))}.
For zero-inflated Poisson and zero-inflated negative-binomial 2 fits this is
the unconditional response mean \code{(1 - zi) * mu}, where \code{mu} is the
conditional count mean. For bivariate Gaussian fits this is a
two-column matrix with \code{mu1} and \code{mu2}. For lognormal fits this is the
arithmetic response mean, \code{exp(mu + sigma^2 / 2)}.
}
\details{
Fitted values are returned for the original fitted rows. Use \code{\link[=predict]{predict()}} for
new data or for non-location distributional parameters such as \code{sigma} or
\code{rho12}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{zero_one_beta}
\alias{zero_one_beta}
\title{Zero-one beta response family}
\usage{
zero_one_beta()
}
\value{
A \code{drm_family} object.
}
\description{
\code{zero_one_beta()} defines a one-response distribution for continuous
proportions on \verb{[0, 1]} when exact zeroes or ones are structural outcomes
rather than binomial denominator outcomes.
}
\details{
The implemented fixed-effect contract is \code{logit(mu) = eta_mu},
\code{log(sigma) = eta_sigma}, \code{logit(zoi) = eta_zoi}, and
\code{logit(coi) = eta_coi}. Here \code{zoi} is the probability that an observation
is exactly 0 or 1, and \code{coi} is the conditional probability of an exact 1
given that the observation is on the boundary. Interior observations follow
the same beta mean-scale contract as \code{\link[=beta]{beta()}}, with internal precision
\code{phi = 1 / sigma^2}.

\code{fitted()} returns the unconditional response mean
\code{(1 - zoi) * mu + zoi * coi}. Ordinary unlabelled random intercepts and
independent numeric slopes such as \code{(1 | id)} and \code{(0 + x | id)} may enter
\code{mu}. The point-fit-only q1 gates additionally admit one ordinary
intercept-only random effect in \code{sigma}, \code{zoi}, or \code{coi}, separately, one ordinary
slope-only \code{sigma} effect \code{(0 + x | id)}, and one ordinary slope-only \code{zoi}
effect when the fixed and random terms use the same raw symbol, for example
\code{zoi ~ x + (0 + x | id)}. The exact \code{coi} admission is
\code{coi ~ 1 + (1 | id)} with fixed \code{sigma ~ 1} and \code{zoi ~ 1}. These routes are
point-fit recovery only:
direct profiling, intervals, coverage, and broader recovery claims remain
unavailable. In the retained \code{coi} recovery rung (\code{M = 64}, 50 observations
per group), population-level point recovery remained stable when a few groups
had fewer than two observed zeroes or ones, but those groups can have weakly
identified conditional modes; inspect the within-group atom counts before
interpreting individual modes. Correlated or
labelled effects, intercept-plus-slope \code{zoi}
effects, transformed or mismatched \code{zoi} slope symbols, \code{coi} slopes or
structured effects,
covariance blocks, and denominator syntax remain unsupported. One further
point-fit-only structured route is
available: an unlabelled q1 \code{phylo(1 | group, tree = tree)} intercept in
\code{mu}. It does not license other providers, slopes, labels, covariance,
q2-plus structured effects, profiling, intervals, coverage, or inference
claims.
}
\examples{
zero_one_beta()
}

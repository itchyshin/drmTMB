% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_drm}
\alias{check_drm}
\alias{check_drm.drmTMB}
\title{Check convergence and diagnostic flags for a drmTMB fit}
\usage{
check_drm(object, ...)

\method{check_drm}{drmTMB}(
  object,
  gradient_tolerance = 0.001,
  rho_boundary = 0.98,
  sd_boundary = 1e-04,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future diagnostic options.}

\item{gradient_tolerance}{Maximum absolute fixed-parameter gradient treated
as acceptable.}

\item{rho_boundary}{Absolute residual or structured correlation value above
which a bivariate Gaussian fit receives a warning.}

\item{sd_boundary}{Random-effect standard deviation below which a fit
receives a warning that the variance component is near the lower
boundary.}
}
\value{
A data frame of checks with columns \code{check}, \code{status}, \code{value}, and
\code{message}. The returned object has class \code{drm_check}.
}
\description{
\code{check_drm()} runs a compact set of model-fit diagnostics. It is intended as
a first-pass guardrail before interpreting distributional models, especially
fits with random effects, known sampling covariance, phylogenetic location
effects, or bivariate residual correlation \code{rho12}.
}
\details{
The current checks cover optimizer convergence, finite objective values,
optimizer evaluation counts, fixed-parameter gradients including the largest
gradient component label, whether
\code{\link[TMB:sdreport]{TMB::sdreport()}} was computed, skipped, or failed, Hessian status from
\code{\link[TMB:sdreport]{TMB::sdreport()}}, finite fixed-effect standard errors, dropped rows,
positive scale parameters, random-effect standard deviations near the lower
boundary, bivariate residual-correlation \code{rho12} values near the boundary,
Student-t \code{nu} boundary behaviour, known sampling covariance summaries,
dense known-covariance storage scale, dense fixed-effect design size and
density, random-effect replication, and random-slope design variation. If a
univariate Gaussian fit includes one or more matched labelled
\code{mu}/\code{sigma} random-intercept covariance blocks, \code{check_drm()} also reports
group replication and whether either component is tiny relative to its
interpretation scale for each independent block. If a bivariate Gaussian fit
includes a matched
labelled \code{mu1}/\code{mu2} random-intercept covariance block, \code{check_drm()} reports
group replication and whether either group-level SD is tiny relative to the
matching residual scale. For a matched labelled \code{sigma1}/\code{sigma2} block, it
reports group replication and whether either log-\code{sigma} random-effect SD is
tiny. If a bivariate Gaussian fit includes an ordinary all-four q=4
\code{mu1}/\code{mu2}/\code{sigma1}/\code{sigma2} block, it reports group replication, location
SDs relative to residual scales, log-\code{sigma} SDs, and whether any latent
correlation is near the boundary. If a bivariate Gaussian fit includes
matching \code{mu1}/\code{mu2} phylogenetic location effects, \code{check_drm()} also reports
whether the fitted phylogenetic mean-mean correlation is near the boundary,
whether either phylogenetic SD is tiny relative to the matching residual
scale, and whether an ordinary group-level covariance block uses the same
grouping factor. If a bivariate Gaussian fit includes a phylogenetic q=4
\code{mu1}/\code{mu2}/\code{sigma1}/\code{sigma2} block, it reports species replication,
location SDs relative to residual scales, log-\code{sigma} SDs, and whether any
latent phylogenetic correlation is near the boundary. If a univariate
Gaussian fit includes \code{spatial(1 | site, coords = coords)} in \code{mu}, it
reports site replication, fitted coordinate range, the spatial SD, and
whether the spatial SD is tiny relative to the residual scale. If a Gaussian
fit includes \code{sd_phylo(species) ~ x_species},
\code{sd_phylo1(species) ~ x_species}, or
\code{sd_phylo2(species) ~ x_species}, it reports species replication and the
fitted direct-SD surface range. If a univariate Gaussian fit used
\code{drm_control(aggregate_gaussian = TRUE)}, it reports original rows,
aggregation cells, compression ratio, and largest cell size. If a fit was
stored with
\code{drm_control(keep_tmb_object = FALSE)}, the
fixed-gradient check is reported as a note because the TMB
automatic-differentiation object is not available. If a fit used
\code{drm_control(se = FALSE)}, the \code{sdreport_status}, Hessian, and
finite-standard-error checks are reported as notes. If \code{sdreport()} was
requested but failed, those rows are warnings.

Use \code{check_drm()} before interpreting coefficients, fitted values, or
response-scale quantities. A \code{note} records something to inspect, such as
dropped rows or a singly observed random-effect level. A \code{warning} means the
fitted model may still be useful but needs inspection before inference. An
\code{error} means at least one basic diagnostic failed. For programmatic checks,
the returned object has \code{attr(x, "ok") == TRUE} only when no rows have
\code{warning} or \code{error} status.
}
\examples{
set.seed(1)
dat <- data.frame(y = rnorm(40), x = rnorm(40))
fit <- drmTMB(drm_formula(y ~ x, sigma ~ x), data = dat)
check_drm(fit)
}

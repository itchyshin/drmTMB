% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-parameter-surface.R
\name{plot_parameter_surface}
\alias{plot_parameter_surface}
\title{Plot predicted distributional-parameter surfaces}
\usage{
plot_parameter_surface(
  data,
  x,
  colour = NULL,
  group = NULL,
  facet = "dpar",
  dpar = NULL,
  type = NULL,
  line = TRUE,
  point = TRUE,
  interval = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=predict_parameters]{predict_parameters()}}, or a compatible
long table with columns \code{dpar}, \code{type}, \code{estimate}, \code{conf.status}, and
\code{interval_source}. If \code{conf.low} and \code{conf.high} are present, both must be
numeric.}

\item{x}{Character scalar naming the column to draw on the x-axis.}

\item{colour}{Optional character scalar naming a column to map to colour.}

\item{group}{Optional character scalar naming a column to group lines. If
\code{NULL}, lines are grouped by \code{dpar}, \code{colour}, and \code{facet} columns when
present.}

\item{facet}{Optional character scalar naming a column to facet by. Use
\code{NULL} to suppress faceting. The default facets by \code{dpar}.}

\item{dpar}{Optional character vector of distributional parameters to keep.}

\item{type}{Optional character vector of prediction scales to keep, such as
\code{"response"} or \code{"link"}.}

\item{line}{Logical; draw lines through the estimates.}

\item{point}{Logical; draw points at the estimates.}

\item{interval}{Logical; draw finite \code{conf.low}/\code{conf.high} intervals when
those columns are present and \code{conf.status} plus \code{interval_source} indicate
that an interval was actually computed.}

\item{...}{Reserved for future options.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_parameter_surface()} is a small \code{ggplot2} consumer for long tables
returned by \code{\link[=predict_parameters]{predict_parameters()}}. It does not fit a model, build a grid,
compute predictions, compute confidence intervals, or choose an estimand.
Build an explicit grid with \code{\link[=prediction_grid]{prediction_grid()}} or another data-frame
workflow first, then pass the resulting prediction table to this helper.
}
\details{
The helper plots \code{estimate} against one supplied column. It expects the
interval provenance columns created by \code{\link[=predict_parameters]{predict_parameters()}}. When finite
\code{conf.low} and \code{conf.high} columns are present and the provenance columns
describe a real interval, it draws confidence bands for continuous x-values
and interval bars for discrete x-values. Rows without finite supported bounds
remain visible as point or line estimates only. When the filtered table
contains a single
distributional parameter, the y-axis label names that parameter and, when
unique, the prediction scale.
}
\examples{
dat <- data.frame(
  y = c(0.2, 0.5, 1.1, 1.4, 1.8, 2.2),
  x = c(-1, -0.5, 0, 0.5, 1, 1.5)
)
fit <- drmTMB(bf(y ~ x, sigma ~ x), data = dat)
grid <- data.frame(x = seq(-1, 1.5, length.out = 6))
pred <- predict_parameters(
  fit,
  newdata = grid,
  dpar = c("mu", "sigma"),
  conf.int = TRUE
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_parameter_surface(pred, x = "x")
}
}

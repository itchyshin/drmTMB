% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty.R
\name{drm_boundary_penalty}
\alias{drm_boundary_penalty}
\title{Scale-equivariant boundary soft-penalty for ordinary RE SDs (experimental)}
\usage{
drm_boundary_penalty(kappa_minus = 1, kappa_plus = 1)
}
\arguments{
\item{kappa_minus, kappa_plus}{Positive slope scales on the lower and upper
sides of the negative Huber. Defaults \code{1} match the shipped MSPL shape.}
}
\value{
An object of class \code{drm_boundary_penalty}.
}
\description{
Builds the Design-256 moving-anchor negative-Huber penalty for a univariate
Gaussian A1 cell: one ordinary iid \code{mu} random intercept \code{(1 | group)}. Pass
the result to \code{\link[=drmTMB]{drmTMB()}}'s \code{penalty} argument. The fit is labeled \code{MAP};
intervals (\code{confint} / \code{profile}) are withheld on this experimental route.
}
\details{
Softness rate \code{c_g = 2 * sqrt(q_v / g)} with \code{q_v = 1} for this cell. The
Huber argument is \code{log(sd_u) - mean(eta^sigma)}, so the penalty is exactly
scale-equivariant on the identity-link Gaussian location model (Theorem 1).
Defaults keep the shipped symmetric shape (\code{kappa_minus = kappa_plus = 1}).
}
\examples{
pen <- drm_boundary_penalty()
pen$c_g_formula
}
\references{
Sterzinger, W., Kosmidis, I., & Moustaki, I. (2026). Softly penalized
likelihood for factor models. Psychometrika.

Chung, Y., Rabe-Hesketh, S., Dorie, V., Gelman, A., & Liu, J. (2013). A
nondegenerate penalized likelihood estimator for variance parameters in
multilevel models. Psychometrika, 78(4), 685-709.
}
\seealso{
\code{\link[=drm_phylo_penalty]{drm_phylo_penalty()}}, design
\code{docs/design/256-mspl-boundary-penalty-derivation.md}
}

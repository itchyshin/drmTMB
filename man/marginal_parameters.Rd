% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal-parameters.R
\name{marginal_parameters}
\alias{marginal_parameters}
\alias{marginal_parameters.drmTMB}
\title{Marginal summaries of predicted distributional parameters}
\usage{
marginal_parameters(object, ...)

\method{marginal_parameters}{drmTMB}(
  object,
  newdata = NULL,
  dpar = NULL,
  by = NULL,
  type = c("response", "link"),
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future options.}

\item{newdata}{Optional data frame for prediction. If omitted, fitted rows
are used.}

\item{dpar}{Optional character vector of distributional parameters to
summarise, including fitted random-effect scale model names such as
\code{"sd(id)"}. \code{NULL} summarises all fitted distributional parameters.}

\item{by}{Optional character vector of columns in \code{newdata} used to define
marginal groups. \code{NULL} averages over all prediction rows for each
distributional parameter.}

\item{type}{Prediction scale: \code{"response"} or \code{"link"}.}
}
\value{
A data frame with one row per distributional parameter and grouping
combination. The returned columns are \code{dpar}, \code{component}, \code{type},
optional \code{by} columns, \code{estimate}, \code{n}, \code{conf.status}, and
\code{interval_source}.
}
\description{
\code{marginal_parameters()} averages predicted distributional parameters over
fitted rows or a supplied \code{newdata} grid. It is a simple plug-in summary
layer built on \code{\link[=predict_parameters]{predict_parameters()}}, intended for interpretation tables and
future emmeans-style marginalisation or plotting helpers.
}
\details{
This helper does not compute uncertainty, contrasts, or profile intervals.
It reports unweighted averages of already-predicted parameter values. For
population-level summaries, supply an explicit \code{newdata} grid; with
\code{newdata = NULL}, the fitted-row prediction contract is the same as
\code{\link[=predict.drmTMB]{predict.drmTMB()}}.

The returned table carries the same interval provenance columns as
\code{\link[=predict_parameters]{predict_parameters()}}. In this first contract, marginal summaries are point
estimates with \code{conf.status = "not_requested"} and
\code{interval_source = "not_available"}.
}
\examples{
dat <- data.frame(
  y = c(0.2, 0.5, 1.1, 1.4, 1.8, 2.2),
  x = c(-1, -0.5, 0, 0.5, 1, 1.5),
  habitat = factor(rep(c("reef", "sand"), each = 3))
)
fit <- drmTMB(bf(y ~ x + habitat, sigma ~ x), data = dat)
grid <- expand.grid(
  x = c(0, 1),
  habitat = levels(dat$habitat)
)
marginal_parameters(fit, newdata = grid, dpar = c("mu", "sigma"), by = "habitat")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data.R
\name{drm_imputed_uncertainty_status}
\alias{drm_imputed_uncertainty_status}
\title{Row-level uncertainty status for \code{imputed()}}
\usage{
drm_imputed_uncertainty_status(fit_status, observed, std_error, se)
}
\arguments{
\item{fit_status}{The fit-level status from \code{drm_standard_error_status()},
computed once by the caller.}

\item{observed}{Logical vector, one entry per retained model row; \code{TRUE}
where the missing-predictor value was observed.}

\item{std_error}{Numeric vector, one entry per retained model row, as
assembled by \code{imputed.drmTMB()} (\code{NA} for observed rows).}

\item{se}{Logical; the \code{se} argument \code{imputed()} was called with.}
}
\value{
A character vector the same length as \code{observed}.
}
\description{
\code{drm_standard_error_status()} (\code{R/methods.R}) reports only the FIT-level
\code{\link[TMB:sdreport]{TMB::sdreport()}} state (\code{"ok"}, \code{"sdreport_skipped"},
\code{"sdreport_failed"}, \code{"sdreport_non_pd_hessian"}, or
\code{"sdreport_unavailable"}). That fit-level state does not distinguish "the
fit has no usable covariance at all" from "the fit is fine, but this
particular missing-predictor route has no well-defined posterior standard
error". \code{imputed()} needs the second distinction because most non-Gaussian
routes now report a real conditional standard error
(see \code{\link[=drm_imputed_route_conditional_sd]{drm_imputed_route_conditional_sd()}}), while the unordered categorical
route never can.
}
\details{
This wrapper leaves \code{drm_standard_error_status()} itself untouched, so its
other call site (\code{drm_summary_coefficients()}, the fixed-effect
coefficient table) keeps its existing behaviour. It layers one more state,
\code{"route_conditional_se_unavailable"}, on top:
\enumerate{
\item If the fit-level status is not \code{"ok"}, it wins outright for every row
(\code{sdreport()} was skipped, failed, or returned a non-positive-definite
Hessian). This also means \code{std_error} is \code{NA} throughout in that case:
\code{drm_imputed_missing_predictor_se()} only attempts a route-conditional
standard error when the fit-level status is \code{"ok"}, even though that
computation does not itself call \code{sdreport()} — this keeps \code{std_error}
and \code{uncertainty_status} consistent instead of reporting a real number
next to a status that says standard errors were skipped.
\item If the caller passed \code{se = FALSE} to \code{imputed()}, the fit-level \code{"ok"}
status is reported unchanged; nothing was requested, so nothing is
"unavailable".
\item Otherwise, for each row where a missing-predictor value was actually
estimated (\code{!observed[row]}), \code{"route_conditional_se_unavailable"}
replaces \code{"ok"} when that row's \code{std_error} is not finite. Observed
rows never carry a \code{std_error} by design and keep the fit-level \code{"ok"}
status; they are not "unavailable", because no missing-predictor value
was estimated for them.
}
}
\keyword{internal}

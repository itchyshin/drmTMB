% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adequacy-plots.R
\name{qq_plot}
\alias{qq_plot}
\title{Normal QQ plot of randomized quantile residuals}
\usage{
qq_plot(object, seed = NULL, nsim = 1L, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{seed}{Optional single integer seed, passed to
\code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}.}

\item{nsim}{Number of Dunn-Smyth realizations to overplot as an envelope;
passed to \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}.}

\item{...}{Reserved for future options.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{qq_plot()} draws a normal QQ plot of \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}} against
their N(0,1) order-statistic theoretical quantiles, with the y = x
reference line dotted. Points on the reference line are no detectable
departure from N(0,1); systematic curvature away from it flags a
mis-specification of the fitted distributional form.
}
\details{
When \code{nsim > 1}, a pale grey envelope (with a darker outline) overplots the
per-rank range across the \code{nsim} Dunn-Smyth realizations, so a single
randomized draw is not over-read; the first realization's points are drawn
on top.

This is fixed-effect adequacy only -- see \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}. See
\code{\link[=worm_plot]{worm_plot()}} for the detrended variant that makes systematic bends easier
to read.
}
\examples{
set.seed(20260712)
n <- 60
x <- stats::rnorm(n)
dat <- data.frame(y = 0.5 + 0.8 * x + stats::rnorm(n), x = x)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), family = gaussian(), data = dat)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  qq_plot(fit)
}
}

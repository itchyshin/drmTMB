% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{spatial_coords}
\alias{spatial_coords}
\title{Transform longitude and latitude to planar spatial coordinates}
\usage{
spatial_coords(data, longitude, latitude, crs_out)
}
\arguments{
\item{data}{A non-empty data frame containing the longitude and latitude
columns.}

\item{longitude, latitude}{Unquoted or quoted names of the longitude and
latitude columns, respectively.}

\item{crs_out}{A projected output CRS understood by \code{\link[sf:st_crs]{sf::st_crs()}}, for
example \code{32610} or \code{"EPSG:32610"}.}
}
\value{
A two-column numeric matrix with class \code{drmTMB_coords}. Its \code{crs}
attribute records the projected CRS and it can be supplied to \code{\link[=make_mesh]{make_mesh()}}.
}
\description{
Converts WGS84 longitude/latitude columns to an explicitly selected projected
coordinate reference system. Spatial mesh models use metric planar distances;
this helper deliberately does not infer a UTM zone or treat decimal degrees as
planar coordinates.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  locations <- data.frame(lon = c(-123.2, -123.1), lat = c(49.2, 49.3))
  spatial_coords(locations, lon, lat, crs_out = 32610)
}
}

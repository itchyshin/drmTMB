% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{simulate.drmTMB}
\alias{simulate.drmTMB}
\title{Simulate from a fitted model}
\usage{
\method{simulate}{drmTMB}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{nsim}{Number of simulated data sets.}

\item{seed}{Optional random-number seed. The previous \code{.Random.seed} state
is restored after simulation.}

\item{...}{Reserved for future simulation options.}
}
\value{
A data frame. Univariate models return one column per simulation.
Bivariate models return paired columns named \verb{sim_<j>_y1} and
\verb{sim_<j>_y2}.
}
\description{
\code{simulate()} draws new response values from the fitted \code{drmTMB} model. For
univariate Gaussian models with known sampling covariance, simulation uses
the total observation covariance implied by the known sampling covariance
plus the fitted residual scale. For Student-t models, simulation uses fitted
\code{mu}, \code{sigma}, and \code{nu}. For lognormal models, simulation uses fitted
log-scale \code{mu} and \code{sigma}. For Gamma models, simulation uses fitted mean
\code{mu} and coefficient of variation \code{sigma}. For beta models, simulation uses
fitted mean \code{mu} and public scale \code{sigma} with internal
\code{phi = 1 / sigma^2}. For zero-one beta models, simulation draws exact
boundary values from the fitted \code{zoi}/\code{coi} probabilities and interior
values from the fitted beta component. For beta-binomial models, simulation
draws latent success probabilities from the fitted beta distribution and then
success counts from the stored trial denominators. For cumulative-logit ordinal
models, simulation draws ordered categories from the fitted cumulative-logit
probabilities. For Poisson models, simulation uses the fitted mean \code{mu}. For
zero-inflated Poisson models, simulation uses
fitted conditional mean \code{mu} and structural-zero probability \code{zi}. For
negative-binomial 2 models, simulation uses fitted \code{mu} and overdispersion
scale \code{sigma}, with \code{Var(y) = mu + sigma^2 * mu^2}; zero-truncated NB2
models draw from this NB2 component conditional on positive counts. The
zero-inflated NB2 path adds structural-zero probability \code{zi}; the hurdle NB2
path adds hurdle-zero probability \code{hu} and draws nonzero counts from the
zero-truncated NB2 component. For bivariate
Gaussian models without known
sampling covariance, simulation uses the fitted \code{mu1}, \code{mu2}, \code{sigma1},
\code{sigma2}, and residual \code{rho12}. If a dense bivariate known \code{V} was supplied,
simulation uses the full row-paired observation covariance \code{V + Omega}.
}
\examples{
dat <- data.frame(y = c(0.2, 0.5, 1.1, 1.4), x = c(-1, 0, 1, 2))
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
simulate(fit, nsim = 2, seed = 1)
}

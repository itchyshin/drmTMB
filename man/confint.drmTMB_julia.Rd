% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/julia-bridge.R
\name{confint.drmTMB_julia}
\alias{confint.drmTMB_julia}
\title{Confidence intervals for a Julia-engine \code{drmTMB} fit}
\usage{
\method{confint}{drmTMB_julia}(
  object,
  parm = NULL,
  level = 0.95,
  method = c("wald", "profile", "bootstrap"),
  R = 199L,
  seed = NULL,
  threads = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB_julia} fit.}

\item{parm}{Optional target selection. For \code{"wald"}, compact coefficient
labels (\code{"mu:x"}) or full names (\code{"fixef:mu:x"}); for \code{"profile"} /
\code{"bootstrap"}, the SD target name (\code{"sd:mu:phylo(1 | species)"}).}

\item{level}{Confidence level.}

\item{method}{\code{"wald"} (default), \code{"profile"}, or \code{"bootstrap"}.}

\item{R}{Bootstrap replicate count (used only when \code{method = "bootstrap"}).}

\item{seed}{Optional bootstrap seed.}

\item{threads}{Logical; request Julia-side threaded inference for the
profile / bootstrap path.}

\item{...}{Unused.}
}
\value{
A confidence-interval data frame with the shared \code{parm}, \code{level},
\code{lower}, \code{upper}, \code{scale}, \code{transformation}, \code{tmb_parameter}, \code{index},
\code{method}, and \code{conf.status} columns.
}
\description{
\code{confint()} on a \code{engine = "julia"} fit exposes two interval families:
}
\details{
\itemize{
\item \code{method = "wald"} (the default) builds symmetric Wald intervals for the
fixed-effect coefficients (mu, sigma, ...) on the linear-predictor (link)
scale, using the fixed-effect covariance DRM.jl marshals back through the
bridge (\code{vcov(object)}). This mirrors the native drmTMB Wald path, whose
fixed-effect rows are also reported on the link scale.
\item \code{method = "profile"} / \code{method = "bootstrap"} re-enter DRM.jl's inference
primitive for the Gaussian phylogenetic SD target only, transformed back to
the positive response scale.
}
}

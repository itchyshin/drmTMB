% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ou-sensitivity.R
\name{ou_sensitivity}
\alias{ou_sensitivity}
\title{Fixed-decay phylogenetic OU sensitivity fits}
\usage{
ou_sensitivity(
  formula,
  data,
  alpha,
  alpha_scale = c("root_depth", "raw"),
  control = list()
)
}
\arguments{
\item{formula}{A \code{\link[=bf]{bf()}} formula containing exactly one
\code{phylo(1 | group, tree = tree, model = "ou")} term in \code{mu}.}

\item{data}{A data frame with complete model variables.}

\item{alpha}{Positive, prespecified OU decay values.}

\item{alpha_scale}{Whether \code{alpha} is scaled by the ultrametric tree height
(\code{"root_depth"}, the default) or supplied in native branch-length units
(\code{"raw"}).}

\item{control}{A \code{\link[=drm_control]{drm_control()}} list.}
}
\value{
An object of class \code{drmTMB_ou_sensitivity} containing \verb{$fits} (the
BM fit followed by fixed-OU fits), \verb{$summary} (likelihood and diagnostic
information), and \verb{$coefficients} (fixed effects for every distributional
parameter and grid value).
}
\description{
Fit a Brownian-motion (BM) phylogenetic baseline and a user-specified grid
of \emph{fixed} location-side Ornstein--Uhlenbeck (OU) decay rates.  This is a
robustness analysis for assumptions about phylogenetic decay; it does not
estimate a decay rate, select a preferred process, or provide a confidence
interval for decay.
}
\details{
The first public slice is deliberately narrow: one complete-response
univariate Gaussian model, one unlabelled phylogenetic intercept in the
location formula, and deterministic fixed-effect predictors in \code{mu} and
\code{sigma}.  \code{alpha} is dimensionless when \code{alpha_scale = "root_depth"}: its
native rate is \code{alpha / tree_height}.  The tree must then be ultrametric.
Use \code{alpha_scale = "raw"} when rates are already in the tree's branch-length
units.  The package's phylogenetic routes still require an ultrametric tree.
}
\examples{
if (requireNamespace("ape", quietly = TRUE)) {
  set.seed(1)
  tree <- ape::rcoal(5)
  dat <- data.frame(
    y = rnorm(20), x = rnorm(20),
    species = rep(tree$tip.label, each = 4)
  )
  ou_sensitivity(
    bf(y ~ x + phylo(1 | species, tree = tree, model = "ou"), sigma ~ x),
    data = dat, alpha = c(0.3, 0.7, 1.3)
  )
}
}

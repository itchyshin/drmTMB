% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{vcov.drmTMB}
\alias{vcov.drmTMB}
\alias{model-fit-extractors}
\alias{logLik.drmTMB}
\alias{AIC.drmTMB}
\alias{BIC.drmTMB}
\alias{nobs.drmTMB}
\alias{df.residual.drmTMB}
\alias{deviance.drmTMB}
\title{Extract standard model-fit quantities}
\usage{
\method{vcov}{drmTMB}(object, ...)

\method{logLik}{drmTMB}(object, ...)

\method{AIC}{drmTMB}(object, ..., k = 2)

\method{BIC}{drmTMB}(object, ...)

\method{nobs}{drmTMB}(object, ...)

\method{df.residual}{drmTMB}(object, ...)

\method{deviance}{drmTMB}(object, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future extractor options.}

\item{k}{Numeric penalty per parameter for \code{AIC()}; the default is \code{2}.}
}
\value{
\code{logLik()} returns an object of class \code{"logLik"}. \code{vcov()} returns a
numeric covariance matrix. \code{nobs()}, \code{df.residual()}, and \code{deviance()}
return numeric scalars.
}
\description{
These methods expose \code{drmTMB} fits to standard base-R model summary and
comparison helpers.
}
\details{
\code{logLik()} returns a \code{"logLik"} object with \code{df} and \code{nobs} attributes so
\code{\link[stats:AIC]{stats::AIC()}} and \code{\link[stats:BIC]{stats::BIC()}} use the fitted likelihood, optimized
top-level parameter count, and fitted-row count consistently.
\code{nobs()} returns the number of fitted rows after complete-case filtering.
\code{df.residual()} returns \code{nobs - df}, where \code{df} is the number of optimized
top-level parameters recorded in \code{logLik()}. \code{deviance()} returns
\code{-2 * logLik}; for these likelihood-based distributional models this is an
absolute negative twice log-likelihood value, not a saturated-model GLM
deviance. \code{vcov()} returns the fixed-effect covariance matrix from
\code{TMB::sdreport()} when the fit contains an \code{sdreport} object with
\code{pdHess = TRUE}; the same object is available as \code{fit$sdr} and
\code{fit$sdreport}. If \code{sdreport()} was skipped, failed, or returned
\code{pdHess = FALSE}, Wald standard errors and Wald confidence intervals are
unavailable while point estimates remain usable. \code{vcov()} intentionally does
not include random-effect conditional modes or derived response-scale
quantities.
}
\section{Experimental MSPL fits}{

For a fit made with \code{estimator = "mspl"}, \code{vcov()} behaves differently from
an ordinary maximum-likelihood fit in two ways worth knowing before you use
it.

First, \strong{the shape differs}. It returns the full outer-parameter covariance
after Laplace marginalisation — the fixed effects \emph{and} the covariance
parameters in their frozen Cholesky coordinates (\code{log_sd_mu}, and
\code{eta_cor_mu} for a q = 2 block) — not the fixed-effect-only block an ordinary
fit returns. Code that assumes \code{vcov(fit)} has the same dimension across
estimators will break.

Second, \strong{it is a standard error, not an interval}. The matrix inverts the
Hessian of the \emph{unpenalized} Laplace log-likelihood evaluated at the MSPL
estimate: the penalty obtains a finite estimate but does not describe
sampling variability. Do not form \verb{coef ± 1.96 * se}. Kosmidis and Firth show
that Wald intervals in this setting fail to cover regardless of the nominal
level, and that the failure persists even for profile penalized-likelihood
intervals; the mechanism is the finiteness of the penalized estimator and its
standard error, not separation as such. A second reason applies here: the
MSPL estimate maximises the \emph{penalized} criterion, so the unpenalized score
is not zero at it and the usual "evaluate at the maximum likelihood estimate"
justification does not transfer. The size of that departure is recorded in
\code{fit$mspl$wald$unpenalized_gradient_max_abs}.

Accordingly \code{confint()}, \code{profile()}, \code{logLik()}, \code{AIC}, \code{BIC}, and \code{anova()}
deliberately error for MSPL fits. When the information matrix is not
positive definite — which happens on exactly the strongly separated designs
MSPL exists to handle — the standard errors are \code{NA} and a
\code{drmTMB_mspl_wald_unavailable} warning is signalled, rather than a
fabricated number.
}

\examples{
set.seed(20260524)
n <- 36
x <- seq(-1.5, 1.5, length.out = n)
dat <- data.frame(
  y = 0.3 + 0.6 * x + rnorm(n, sd = 0.7),
  x = x
)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)

logLik(fit)
nobs(fit)
df.residual(fit)
deviance(fit)
AIC(fit)
BIC(fit)
vcov(fit)

}

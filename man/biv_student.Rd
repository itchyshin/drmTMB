% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{biv_student}
\alias{biv_student}
\title{Bivariate Student-t response family}
\usage{
biv_student()
}
\value{
A \code{drm_family} object.
}
\description{
\code{biv_student()} defines an exact two-response elliptical Student-t
distribution with one shared degrees-of-freedom parameter \code{nu}. Its
\code{sigma1} and \code{sigma2} parameters are Student-t scales, and \code{rho12} is the
residual scatter correlation. The response covariance exists because the
fitted transform enforces \code{nu > 2}; its marginal standard deviations are
\code{sigma_j * sqrt(nu / (nu - 2))}.
}
\details{
The first implementation allows fixed effects in \code{mu1} and \code{mu2}, with
intercept-only \code{sigma1}, \code{sigma2}, shared \code{nu}, and \code{rho12}. Random effects,
parameter predictors outside the locations, missing response pairs, and
interval inference are deferred.
}
\examples{
biv_student()
}

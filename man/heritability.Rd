% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heritability.R
\name{heritability}
\alias{heritability}
\alias{heritability.drmTMB}
\alias{icc}
\alias{icc.drmTMB}
\alias{repeatability}
\alias{repeatability.drmTMB}
\title{Heritability, ICC, and repeatability accessors}
\usage{
heritability(object, ...)

\method{heritability}{drmTMB}(
  object,
  component = NULL,
  level = 0.95,
  method = c("delta", "profile"),
  ...
)

icc(object, ...)

\method{icc}{drmTMB}(
  object,
  component = NULL,
  level = 0.95,
  method = c("delta", "profile"),
  ...
)

repeatability(object, ...)

\method{repeatability}{drmTMB}(
  object,
  component = NULL,
  level = 0.95,
  method = c("delta", "profile"),
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future extractor options.}

\item{component}{Optional character string naming the structured mean
component (matching a name in \code{object$sdpars$mu}, e.g. \code{"(1 | id)"} or
\code{"phylo(1 | species)"}; structured markers drop their non-grouping
arguments such as \verb{tree =}/\verb{Ainv =}/\verb{K =} from the label -- see the
\verb{@examples} phylo case below). Required when the fit has more than one
structured component; ignored (and unnecessary) when the fit has exactly
one.}

\item{level}{Confidence level for the Wald interval. Default \code{0.95}.}

\item{method}{Either \code{"delta"} (default) or \code{"profile"}. \code{"profile"} is
accepted for signature parity only and always aborts in this slice.}
}
\value{
A one-row data frame (class \code{drm_heritability}) with columns
\code{quantity}, \code{component}, \code{estimate}, \code{se}, \code{lower}, \code{upper}, \code{level},
and \code{method}.
}
\description{
\code{heritability()}, \code{icc()}, and \code{repeatability()} are derived-quantity
accessors for structured-Gaussian \code{drmTMB} fits, ported term-for-term from
\code{DRM.jl}'s \code{src/heritability.jl} (design doc
\code{docs/design/259-heritability-icc-repeatability.md}). All three report the
share of total variance carried by one structured random-effect component
relative to a denominator on the working (log-SD) scale:
}
\details{
\itemize{
\item \code{heritability()}: \verb{sigma^2_focal / (sum of ALL structured mean-component variances + residual variance)} -- the comparative-biology "phylogenetic
signal" definition.
\item \code{icc()} / \code{repeatability()}: \verb{sigma^2_focal / (sigma^2_focal + residual variance)} -- the classic focal-vs-residual intraclass correlation.
\code{repeatability()} is an alias for \code{icc()}.
}

\code{heritability()} and \code{icc()} return the same value only when the fit has a
single structured mean component (no other components to net out of the
denominator).

\code{summary()}'s derived rows, named \code{total_variance_share} and
\code{phylo_total_variance_share}, use the total-variance (heritability-style)
denominator, so they can differ from \code{icc()}/\code{repeatability()} here whenever
a fit has two or more structured components. Those rows were called
\code{repeatability} and \code{phylogenetic_signal} until 2026-09-03; they were renamed
so that one word no longer names two different quantities (D-213).

Fits must be Gaussian, have a constant residual scale (\code{sigma ~ 1}), and
have at least one structured mean random-effect component (\code{(1 | group)},
\code{phylo(...)}, \code{animal(...)}, \code{relmat(...)}, or \code{spatial(...)}); a
component modelled with \code{sd(group) ~ ...} (location-scale-scale) does not
define a single variance component and is refused. When a fit has more
than one structured component, \code{component} must name one of them (see
\code{object$sdpars$mu} for the available labels).

Standard errors and confidence intervals use a delta method on the
working (log-SD) scale: a numeric gradient of the ratio in the log-SD
parameters, combined with the fixed-parameter covariance
(\code{object$sdr$cov.fixed}) via the quadratic form, then a Wald interval at
\code{level}, clamped to \verb{[0, 1]}. This is a delta-method approximation; not a coverage claim.
See the design doc for the sanity-check evidence this slice carries.

\code{method = "profile"} is accepted for call-site parity with \code{DRM.jl} but is
\strong{not implemented} in this slice; it aborts naming \code{method = "delta"} as
the supported option.
}
\examples{
set.seed(20260525)
n_groups <- 20
n_per <- 8
grp <- factor(rep(seq_len(n_groups), each = n_per))
sd_g <- 1
sd_e <- 0.6
b_g <- rnorm(n_groups, sd = sd_g)
dat <- data.frame(y = 2 + b_g[grp] + rnorm(length(grp), sd = sd_e), grp = grp)
fit <- drmTMB(bf(y ~ 1 + (1 | grp), sigma ~ 1), data = dat)
icc(fit)
repeatability(fit)
heritability(fit)

\donttest{
if (requireNamespace("ape", quietly = TRUE)) {
  # A structured (phylo) component: its sdpars$mu / component label drops
  # the tree argument entirely and is just "phylo(1 | species)".
  set.seed(20260601)
  n_tip <- 20
  phy <- ape::rcoal(n_tip)
  phy$tip.label <- paste0("sp_", seq_len(n_tip))
  A <- ape::vcv(phy, corr = TRUE)
  u <- as.vector(t(chol(A)) \%*\% rnorm(n_tip)) * 0.9
  species <- factor(rep(phy$tip.label, each = 4), levels = phy$tip.label)
  phylo_dat <- data.frame(
    y = 2 + u[rep(seq_len(n_tip), each = 4)] +
      rnorm(length(species), sd = 0.6),
    species = species
  )
  phylo_fit <- drmTMB(
    bf(y ~ 1 + phylo(1 | species, tree = phy), sigma ~ 1),
    data = phylo_dat
  )
  icc(phylo_fit, component = "phylo(1 | species)")
}
}

}

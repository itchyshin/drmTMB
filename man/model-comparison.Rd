% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-comparison.R
\name{model-comparison}
\alias{model-comparison}
\alias{aicc}
\alias{aicc.default}
\alias{aicc.drmTMB}
\title{Corrected Akaike information criterion (AICc)}
\usage{
aicc(object, ...)

\method{aicc}{default}(object, ...)

\method{aicc}{drmTMB}(object, ...)
}
\arguments{
\item{object}{A fitted \code{drmTMB} model (either engine), or any object whose
\code{\link[=logLik]{logLik()}} method reports \code{"df"} and \code{"nobs"} attributes.}

\item{...}{Unused; present for S3 consistency.}
}
\value{
A single number. When \eqn{n - k - 1 \le 0} (too few observations
for the correction to be defined -- a saturated or near-saturated model)
the value is \code{Inf}, as in DRM.jl.
}
\description{
Small-sample (second-order) correction to \code{\link[=AIC]{AIC()}}:
}
\details{
\deqn{\mathrm{AICc} = \mathrm{AIC} + \frac{2k(k + 1)}{n - k - 1}}

with \eqn{k} the number of estimated parameters (the \code{"df"} attribute of
\code{\link[=logLik]{logLik()}}) and \eqn{n} the number of observations (the \code{"nobs"} attribute).
The correction is strictly positive, so \code{aicc(fit) > AIC(fit)}, and it
vanishes as \eqn{n \to \infty}. Prefer AICc over AIC when \eqn{n / k} is
small (a common rule of thumb is \eqn{n / k < 40}). Like AIC, AICc compares
models fit by maximum likelihood on the same data; lower is better.

This is the R port of DRM.jl's \code{aicc(fit)} (\code{src/comparison.jl}); the two
agree to \code{1e-8} on the same fit.

On a REML fit the same caveat as \code{\link[=AIC]{AIC()}} applies and the same warning is
emitted: the restricted likelihood is comparable only across models with
identical fixed effects. Experimental MSPL fits do not expose a likelihood
and error, as they do for \code{\link[=AIC]{AIC()}}.
}
\examples{
\donttest{
set.seed(1)
n <- 60
x <- rnorm(n)
dat <- data.frame(y = 0.5 - 0.8 * x + exp(-0.3 + 0.4 * x) * rnorm(n), x = x)
fit <- drmTMB(bf(y ~ x, sigma ~ x), family = gaussian(), data = dat)
aicc(fit) > AIC(fit)   # the correction is strictly positive
}
}
\seealso{
\code{\link[=AIC]{AIC()}}, \code{\link[=BIC]{BIC()}}, \code{\link[=logLik]{logLik()}}
}

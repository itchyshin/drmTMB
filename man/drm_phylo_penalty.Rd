% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty.R
\name{drm_phylo_penalty}
\alias{drm_phylo_penalty}
\title{Penalty / prior specification for a phylogenetic location-scale fit}
\usage{
drm_phylo_penalty(sd_u = 1, sd_alpha = 0.05, cor_sd = NULL)
}
\arguments{
\item{sd_u, sd_alpha}{Penalised-complexity prior scale and tail probability
for each phylogenetic SD: a priori \code{P(sd > sd_u) = sd_alpha}. \code{sd_u} must
be positive and \code{sd_alpha} must lie in \verb{(0, 1)}.}

\item{cor_sd}{Optional standard deviation of a mean-zero normal penalty on
the phylogenetic cross-parameter correlation parameter. \code{NULL} (the
default) applies no correlation penalty. A non-\code{NULL} \code{cor_sd} requires a
coupled phylogenetic model with at least two phylogenetic SDs (for example
a coupled location-scale or bivariate fit); a location-only phylogenetic
model has a single phylogenetic SD and no correlation parameter to
penalize, so \code{\link[=drmTMB]{drmTMB()}} errors rather than silently ignoring \code{cor_sd}.}
}
\value{
An object of class \code{drm_phylo_penalty}.
}
\description{
Builds an optional penalty (a weakly-informative prior) for the phylogenetic
standard deviations and, optionally, the phylogenetic cross-parameter
correlation of a \code{\link[=drmTMB]{drmTMB()}} fit. Passing the result to the \code{penalty} argument
of \code{\link[=drmTMB]{drmTMB()}} switches the estimator from plain maximum likelihood to a
penalized / maximum-a-posteriori (MAP) estimator.
}
\details{
The standard-deviation penalty is a penalised-complexity (PC) prior (Simpson
et al. 2017): an exponential prior on the SD scale with mass at zero, which
regularises a weakly-identified phylogenetic SD (for example a scale-side
phylogenetic field at about one observation per tip) toward the simpler
"no phylogenetic variance" model in the sense that the \emph{prior} places its
mass there — the reported point estimate itself never reaches it; see
below. The rate is \code{lambda = -log(sd_alpha) / sd_u}
so that, a priori, \code{P(sd > sd_u) = sd_alpha} holds exactly for this prior.
The optional correlation penalty is a mean-zero normal on the unconstrained
phylogenetic correlation parameter.

The optimizer works on \code{log(sd)}, not \code{sd}, so the penalty actually added to
the objective is the negative log-density of the exponential prior expressed
in \code{log(sd)}, including the \verb{|d sd / d log(sd)| = sd} change-of-variables
Jacobian: \code{rate * sd - log(sd) - log(rate)}. Because a maximum-a-posteriori
(MAP) estimate is not invariant to reparameterisation, the fitted penalised
\code{sd_phylo} is the mode of this log-scale expression, not of the SD-scale
prior, and that mode is never zero: the \code{-log(sd)} Jacobian term diverges as
\code{sd -> 0}, so under a flat likelihood the penalty alone pulls the estimate to
\code{sd = 1/rate} (\code{0.334} at the defaults \code{sd_u = 1}, \code{sd_alpha = 0.05}, where
\code{rate = -log(0.05) = 2.996}). The SD-scale prior still has mass at zero as
documented; only the reported point estimate does not sit there. A
penalised \code{sd_phylo} must therefore not be used to test a null hypothesis of
no phylogenetic signal: a likelihood-ratio or Wald test against zero on a
MAP fit is not valid, because the estimator cannot report zero regardless of
the data.

A penalized fit is a MAP point estimate, not a maximum-likelihood fit: its
standard errors are credible-interval-shaped, and likelihood-ratio tests or
AIC across penalized fits are not standard. \code{\link[=logLik]{logLik()}} returns the
unpenalized data log-likelihood; the penalty contribution is stored
separately on the fit as \code{fit$phylo_penalty}.
}
\examples{
# Penalised-complexity prior: a priori P(phylogenetic SD > 1) = 0.05.
pen <- drm_phylo_penalty(sd_u = 1, sd_alpha = 0.05)
pen$rate

# Also penalize the phylogenetic correlation in a coupled location-scale or
# bivariate phylogenetic model.
pen_cor <- drm_phylo_penalty(sd_u = 1, sd_alpha = 0.05, cor_sd = 0.5)
pen_cor$cor_sd
}
\references{
Simpson, D., Rue, H., Riebler, A., Martins, T. G., & Sorbye, S. H. (2017).
Penalising model component complexity: a principled, practical approach to
constructing priors. Statistical Science, 32(1), 1-28.

Chung, Y., Rabe-Hesketh, S., Dorie, V., Gelman, A., & Liu, J. (2013). A
nondegenerate penalized likelihood estimator for variance parameters in
multilevel models. Psychometrika, 78(4), 685-709.
}

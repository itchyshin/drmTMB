% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{truncated_nbinom2}
\alias{truncated_nbinom2}
\title{Zero-truncated negative binomial 2 response family}
\usage{
truncated_nbinom2()
}
\value{
A \code{drm_family} object.
}
\description{
\code{truncated_nbinom2()} defines a one-response positive-count distribution
with formulas for the untruncated NB2 mean \code{mu} and overdispersion scale
\code{sigma}.
}
\details{
Adding \code{hu ~ predictors} to the model formula fits the corresponding hurdle
NB2 model: \code{hu} is the probability of a hurdle zero, and nonzero counts are
drawn from the zero-truncated NB2 component.

The implemented contract is
\code{log(mu) = eta_mu}, \code{log(sigma) = eta_sigma}, and the count response is
distributed as NB2 conditional on being greater than zero. The untruncated
NB2 variance is \code{Var(y) = mu + sigma^2 * mu^2}, with internal
\code{size = 1 / sigma^2}.

Ordinary zero-truncated NB2 models support first-slice random intercepts and
independent numeric slopes in the log-mean predictor, such as
\code{bf(count ~ x + (1 | id) + (0 + x | id), sigma ~ z)}. Hurdle NB2 random
effects, correlated zero-truncated slopes, structured effects, and
overdispersion-side random effects remain planned.
}
\examples{
truncated_nbinom2()
}

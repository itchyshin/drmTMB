% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objective-at.R
\name{objective_at}
\alias{objective_at}
\alias{objective_at.drmTMB}
\title{Evaluate the fitted objective at a supplied point}
\usage{
objective_at(object, ...)

\method{objective_at}{drmTMB}(object, at, ...)
}
\arguments{
\item{object}{A fitted \code{drmTMB} object.}

\item{...}{Passed to methods.}

\item{at}{A named list of values keyed by public start labels (see
\code{drm_control(start = ...)}), e.g.
\code{list("fixef:mu:(Intercept)" = 0.2, "sd:mu:(1 | id)" = 0.3)}. \verb{sd:}
values are given on the natural (positive) scale and \verb{cor:} values on
the natural correlation scale, exactly as for \verb{start=}.}
}
\value{
A single number: the TMB objective (negative log-likelihood) at
\code{at}.
}
\description{
Evaluates a fitted model's TMB objective (negative log-likelihood) at a
point supplied on the public label vocabulary also used by
\code{drm_control(start = ...)}, without refitting. This is a diagnostic: it
selects nothing, reports no uncertainty, and does not change any fitted
quantity or mutate the fitted object.
}

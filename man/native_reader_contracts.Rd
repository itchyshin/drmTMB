% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reader-contracts.R
\name{native_reader_contracts}
\alias{native_reader_contracts}
\title{Stable native reader contracts}
\description{
These contracts define the minimum public output used by drmTMB's native
reader journeys. They do not stabilize every component retained on fitted or
summary objects for backward compatibility.
}
\details{
\code{check_drm()} returns a \code{drm_check} data frame with the character columns
\code{check}, \code{status}, \code{value}, and \code{message}, in that order. Status values are
\code{"ok"}, \code{"note"}, \code{"warning"}, or \code{"error"}. Its \code{"ok"} attribute is \code{TRUE}
exactly when no row has status \code{"warning"} or \code{"error"}.

\code{summary()} guarantees the reader tables \code{coefficients}, \code{parameters},
\code{covariance}, and \code{derived}, plus the \code{confint} key. \code{confint} is \code{NULL} when
intervals were not requested and a table otherwise. Relayed \code{sdpars},
\code{corpars}, \code{ordinal}, \code{uncertainty}, and \code{mspl} components remain available
for compatibility but are outside this stable reader contract.

For \code{ranef()}, \code{terms} is the stable model-scale conditional-deviation
interface on ordinary distributional and named structured-effect blocks.
It is not a random-effect standard-deviation extractor. \code{values}, \code{latent},
\code{covariance_blocks}, and specialised mesh fields are advanced compatibility
components; covariance-block entries do not promise \code{terms}. A fit with no
random effects returns an empty list when \code{dpar = NULL}; requesting a named
block produces an error explaining that the fit contains no random effects.

\code{fitted()} is the response-summary interface, while \code{\link[=predict_parameters]{predict_parameters()}}
is the distributional-component surface. For example, for a lognormal fit,
\code{fitted()} returns the arithmetic response mean \code{exp(mu + sigma^2 / 2)}, not
the log-scale \code{mu} returned by \code{predict_parameters(..., dpar = "mu")}.

In interval tables, \code{conf.status} reports the result or availability state,
\code{interval_source} records provenance, and \code{profile.boundary} is only a flag
that the relevant profile reached a boundary.
}
\seealso{
\code{\link[=check_drm]{check_drm()}}, \code{\link[=summary.drmTMB]{summary.drmTMB()}}, \code{\link[=ranef.drmTMB]{ranef.drmTMB()}},
\code{\link[=fitted.drmTMB]{fitted.drmTMB()}}, and \code{\link[=predict_parameters]{predict_parameters()}}
}

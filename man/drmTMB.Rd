% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmTMB.R
\name{drmTMB}
\alias{drmTMB}
\title{Fit a distributional regression model with TMB}
\usage{
drmTMB(
  formula,
  family = stats::gaussian(),
  data,
  weights = NULL,
  control = list(),
  impute = NULL,
  missing = miss_control(),
  engine = c("tmb", "julia"),
  REML = FALSE,
  penalty = NULL,
  ...
)
}
\arguments{
\item{formula}{A \code{drm_formula} object created by \code{\link[=drm_formula]{drm_formula()}} or \code{\link[=bf]{bf()}}.}

\item{family}{A response family, such as \code{\link[stats:gaussian]{stats::gaussian()}}, \code{\link[=student]{student()}},
\code{\link[=skew_normal]{skew_normal()}}, \code{\link[=lognormal]{lognormal()}}, \code{\link[stats:Gamma]{stats::Gamma()}} with \code{link = "log"},
\code{\link[=tweedie]{tweedie()}}, \code{\link[=beta]{beta()}}, \code{\link[=zero_one_beta]{zero_one_beta()}},
\code{\link[=beta_binomial]{beta_binomial()}}, \code{\link[stats:binomial]{stats::binomial()}} with \code{link = "logit"},
\code{\link[=cumulative_logit]{cumulative_logit()}}, \code{\link[stats:poisson]{stats::poisson()}} with \code{link = "log"}, \code{\link[=nbinom2]{nbinom2()}},
\code{\link[=truncated_nbinom2]{truncated_nbinom2()}}, or \code{\link[=biv_gaussian]{biv_gaussian()}}. Adding
\code{zi ~ predictors} to a Poisson or \code{nbinom2()} model fits the corresponding
zero-inflated count model. Adding \code{hu ~ predictors} to a
\code{truncated_nbinom2()} model fits a hurdle count model whose nonzero counts
use the zero-truncated NB2 component. The current
bivariate Gaussian engine also accepts
\code{family = c(gaussian(), gaussian())} and
\code{family = list(gaussian(), gaussian())}.}

\item{data}{A data frame.}

\item{weights}{Optional non-negative likelihood weights. These are row
log-likelihood multipliers, not known sampling variances. For
meta-analytic sampling variance or covariance, use \code{\link[=meta_V]{meta_V()}} in the model
formula instead.}

\item{control}{Optional list passed to \code{\link[stats:nlminb]{stats::nlminb()}}, or a
\code{\link[=drm_control]{drm_control()}} object when optimizer settings and fitted-object storage
choices should be supplied together.}

\item{impute}{Optional one-element named list of predictor models for the
current missing-predictor routes. Bare formulas such as \code{list(x = x ~ z)}
define Gaussian models for numeric missing predictors. Use
\code{\link[=impute_model]{impute_model()}} for explicit predictor families, such as
\code{list(treatment = impute_model(treatment ~ z, family = binomial()))} for
a binary predictor, \code{list(score = impute_model(score ~ z, family = cumulative_logit()))} for an ordered predictor, or
\code{list(habitat = impute_model(habitat ~ z, family = categorical()))} for
an unordered predictor, or
\code{list(cover = impute_model(cover ~ z, family = beta()))} for a strict
proportion predictor in \verb{(0, 1)}, or
\code{list(cover = impute_model(cover ~ z, family = zero_one_beta()))} for a
boundary proportion predictor in \verb{[0, 1]}, or
\code{list(cover = impute_model(success ~ z, family = beta_binomial(), trials = trials))} for a denominator-aware success/trial proportion predictor, or
\code{list(abundance = impute_model(abundance ~ z, family = poisson()))} or
\code{list(abundance = impute_model(abundance ~ z, family = nbinom2()))} for a
count predictor, including
\code{list(abundance = impute_model(abundance ~ z, family = truncated_nbinom2()))} for a positive zero-truncated count predictor, or
\code{list(biomass = impute_model(biomass ~ z, family = lognormal()))} for a
positive continuous predictor, or
\code{list(biomass = impute_model(biomass ~ z, family = Gamma(link = "log")))}
for a Gamma positive continuous predictor, or
\code{list(biomass = impute_model(biomass ~ z, family = tweedie()))} for a
non-negative semi-continuous predictor with exact zeros. Grouped Gaussian
covariate models use syntax such as
\code{list(x = x ~ z + (1 | group))}; structured Gaussian covariate models use
explicit syntax such as \code{list(x = x ~ z + relmat(1 | line, Q = Q))}.
Most fitted routes use a univariate Gaussian formula containing one \code{mi(x)}
location term and \code{missing = miss_control(predictor = "model")}. The first
non-Gaussian response route also supports \code{family = poisson()} with one
fixed-effect binary \code{mi()} predictor modelled by \code{family = binomial()}.}

\item{missing}{Missing-data policy created by \code{\link[=miss_control]{miss_control()}}. The default
keeps the existing complete-case behaviour. In the current fitted slices,
\code{missing = miss_control(response = "include")} is implemented only for
univariate Gaussian response masks and bivariate Gaussian partial-response
rows without dense known covariance. \code{missing = miss_control(predictor = "model")} is implemented for one \code{mi()}
missing predictor in a univariate Gaussian location model: numeric
Gaussian predictors may use a
fixed-effect, one random-intercept, or one intercept-only structured
Gaussian \code{impute} formula; binary, ordered categorical, unordered
categorical, strict beta/proportion, zero-one beta boundary-proportion,
beta-binomial denominator-aware proportion, Poisson, negative-binomial, or
zero-truncated negative-binomial count, lognormal positive continuous,
Gamma positive continuous, and Tweedie semi-continuous predictors may use
one fixed-effect family-aware \code{impute_model()}. The first Poisson-response
route supports one fixed-effect binary missing predictor with a
Bernoulli/logit \code{impute_model()}, complete count responses, and no
zero-inflation, random, or structured response terms.}

\item{engine}{Computational engine. The default \code{"tmb"} uses the native
\code{drmTMB} TMB backend. The experimental \code{"julia"} route calls DRM.jl
through JuliaCall for the supported bridge slice.}

\item{REML}{Logical; use restricted maximum likelihood where the selected
engine supports it. Native \code{engine = "tmb"} currently supports the first
univariate Gaussian mixed-model slice: ordinary dense \code{mu} fixed effects,
ordinary \code{mu} random intercepts or slopes, diagonal or dense known sampling
covariance through \code{\link[=meta_V]{meta_V()}}, non-unit likelihood \code{weights} (with diagonal
or no known covariance), intercept-only \code{sigma}, complete responses, and no
aggregation, structured effects, or direct \code{sd()} scale formulae.
Experimental \code{engine = "julia"} forwards \code{REML = TRUE} only for supported
Gaussian bridge cells, including fixed-effect location-scale models,
Gaussian \code{sigma}-phylo location-scale models, and the bivariate q = 4
phylogenetic location-scale route. Use the default \code{REML = FALSE} for
likelihood-ratio tests, AIC/BIC comparisons across different fixed-effect
formulas, non-Gaussian models, and currently unsupported extensions.}

\item{penalty}{Optional penalty / prior built by \code{\link[=drm_phylo_penalty]{drm_phylo_penalty()}}, or
\code{NULL} (default) for plain maximum likelihood. A non-\code{NULL} penalty
switches the fit to a penalized / maximum-a-posteriori (MAP) estimator that
regularises a weakly-identified phylogenetic standard deviation; the fit is
labeled \code{MAP} and \code{logLik()} returns the unpenalized data log-likelihood.
Native \code{engine = "tmb"} only.}

\item{...}{Reserved for future model options.}
}
\value{
A \code{drmTMB} fit object.
}
\description{
\code{drmTMB()} is the main model-fitting entry point. The current implementation
supports univariate Gaussian location-scale models,
univariate Student-t and skew-normal location-scale-shape models, lognormal
location-scale models, Gamma mean-CV models for positive responses,
Tweedie mean-scale-power models for non-negative semicontinuous responses,
beta mean-scale models for strict proportions,
zero-one beta mean-scale-boundary models for continuous proportions with
structural exact zeroes or ones,
beta-binomial mean-overdispersion models for success counts,
fixed-effect Bernoulli/binomial event-probability models,
fixed-effect cumulative-logit ordinal location models, fixed-effect Poisson
mean, zero-inflated Poisson, negative-binomial mean-dispersion,
zero-inflated negative-binomial mean-dispersion, zero-truncated
negative-binomial mean-dispersion, and hurdle negative-binomial
mean-dispersion models for counts. Student-t, lognormal, Gamma, beta,
ordinary Poisson, ordinary negative-binomial, beta-binomial, and
zero-truncated negative-binomial \code{mu} formulas support ordinary unlabelled
random intercepts and independent numeric slopes where
documented. Binomial \code{mu} formulas may include standard R \code{offset()} terms
on the logit-event-probability scale. Poisson, ordinary negative-binomial,
and zero-truncated negative-binomial \code{mu} formulas may include standard R
\code{offset(log(exposure))} terms for exposure or effort,
Gaussian random intercepts, independent numeric random slopes,
and labelled or unlabelled correlated numeric random intercept-slope blocks
in the location formula,
known sampling covariance through \code{meta_V(V = V)} with
deprecated \code{meta_known_V(V = V)} retained as a compatibility alias,
residual-scale
random intercepts and independent numeric random slopes in the scale formula,
labelled \code{mu}/\code{sigma}
random-intercept covariance blocks, and one or more group-level
random-effect scale formulae such as \code{sd(id) ~ x_group}, plus
phylogenetic random intercepts, one numeric phylogenetic random slope, and
\code{sd_phylo(species) ~ x_species} direct-SD models in univariate Gaussian
location formulas, Gaussian \code{mu} animal-model and user-supplied relatedness
random intercepts and one numeric random slope, matching
bivariate Gaussian \code{mu1}/\code{mu2} location formulas, and matching labelled
bivariate Gaussian \code{mu1}/\code{mu2}/\code{sigma1}/\code{sigma2} phylogenetic
location-scale blocks, coordinate-based spatial random intercepts and one
numeric coordinate-spatial slope in univariate Gaussian \code{mu},
fixed-effect bivariate Gaussian distributional models, and matched labelled
bivariate Gaussian \code{mu1}/\code{mu2}, \code{sigma1}/\code{sigma2}, and same-response
\code{mu}/\code{sigma} random-intercept covariance blocks, including the first
matching slope-only \code{mu1}/\code{mu2} covariance block, the first all-four q=4
ordinary random-intercept covariance blocks, and
predictor-dependent q=2 ordinary or phylogenetic \code{corpair()} regressions.
Bivariate Gaussian location formulas may be written explicitly as
\code{mu1 = y1 ~ ...}, \code{mu2 = y2 ~ ...}, or with \code{mvbind(y1, y2) ~ ...} shorthand
when both responses share the same location predictors.
}
\examples{
set.seed(20260525)
dat <- data.frame(
  y = 0.2 + 0.6 * seq(-1, 1, length.out = 24) + rnorm(24, sd = 0.5),
  x = seq(-1, 1, length.out = 24)
)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
fit
}

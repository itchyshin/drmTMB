% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adequacy-plots.R
\name{worm_plot}
\alias{worm_plot}
\title{Worm plot of randomized quantile residuals}
\usage{
worm_plot(object, seed = NULL, nsim = 1L, response = NULL, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{seed}{Optional single integer seed, passed to
\code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}.}

\item{nsim}{Number of Dunn-Smyth realizations to overplot as an envelope;
passed to \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}.}

\item{response}{For a bivariate \code{biv_gaussian} fit, \code{1} or \code{2}, selecting
which response's marginal residuals to plot; see
\code{\link[=fitted_distribution]{fitted_distribution()}}. Must be \code{NULL} (the default) for univariate
model types.}

\item{...}{Reserved for future options.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{worm_plot()} draws a detrended QQ plot (van Buuren & Fredriks-style worm
plot) of \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}} against their N(0,1) order-statistic
theoretical quantiles: \verb{deviation = sorted residual - theoretical quantile}. A flat scatter around the dotted zero reference line is no
detectable departure from N(0,1); a systematic bend flags a
mis-specification of the fitted distributional form (see the
GAMLSS-Primer Fig-4c contrast: a location-only fit to heteroscedastic data
bends, the matching location-scale fit is flat).
}
\details{
When \code{nsim > 1}, a pale grey envelope (with a darker outline) overplots the
per-rank range across the \code{nsim} Dunn-Smyth realizations, so a single
randomized draw is not over-read; the first realization's points and
fitted trend are drawn on top.

This is fixed-effect adequacy only -- see \code{\link[=drm_quantile_residuals]{drm_quantile_residuals()}}. A
flat worm plot is "no detectable departure" evidence about the fixed-effect
distributional form, never a general validity or calibration claim.

\strong{What this detects -- and does not.} A worm plot bends when the
underlying quantile residuals depart from N(0,1); see
\code{residuals.drmTMB()}'s Details for the full gated-campaign breakdown (400
seeds x 18 families,
\verb{docs/dev-log/simulation-artifacts/2026-07-12-dg3-power-arm-gated/}). In
short: it flags shape/atom mis-specification a family cannot reabsorb
through its own free parameters (heavy tails, ignored overdispersion or
zero-inflation in a no-free-dispersion family, ignored truncation, a
missing zero/one atom -- gated power >= 0.8), but it stays flat -- a
genuine structural blind spot, not evidence of adequacy -- when a free
nuisance/dispersion/inflation parameter absorbs the mis-specification
(e.g. heteroscedasticity absorbed by Student-t \code{nu}, missing
zero-inflation absorbed by \code{nbinom2} \code{sigma}, or a constant-vs-covariate
zero-inflation/hurdle/zero-one-inflation mechanism mis-set). A
mean-structure diagnostic, not this one, is what catches an absorbed
mis-specification.
}
\examples{
set.seed(20260712)
n <- 60
x <- stats::rnorm(n)
dat <- data.frame(y = 0.5 + 0.8 * x + stats::rnorm(n), x = x)
fit <- drmTMB(bf(y ~ x, sigma ~ 1), family = gaussian(), data = dat)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  worm_plot(fit)
}
}

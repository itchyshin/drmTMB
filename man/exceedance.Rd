% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributional-outputs.R
\name{exceedance}
\alias{exceedance}
\alias{exceedance.drmTMB}
\title{Exceedance probability from a fitted model}
\usage{
exceedance(object, threshold, newdata = NULL, lower.tail = FALSE, ...)

\method{exceedance}{drmTMB}(object, threshold, newdata = NULL, lower.tail = FALSE, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{threshold}{Numeric threshold \code{c}. Either a single value (recycled
across rows) or one value per row of \code{newdata} (or per fitted row when
\code{newdata} is omitted).}

\item{newdata}{Optional data frame for prediction. If omitted, fitted rows
are used; see \code{\link[=fitted_distribution]{fitted_distribution()}}.}

\item{lower.tail}{Logical. If \code{FALSE} (default), returns \code{Pr(Y > threshold)}.
If \code{TRUE}, returns \code{Pr(Y <= threshold)}.}

\item{...}{Reserved for future options.}
}
\value{
A numeric vector, one value per row, with \code{attr(., "calibrated") == FALSE}.
}
\description{
\code{exceedance()} returns \code{Pr(Y > threshold | x)} (or, with \code{lower.tail = TRUE},
\code{Pr(Y <= threshold | x)}) at a fitted model's per-row conditional
distribution. It is a thin wrapper over the shared CDF exposed by
\code{\link[=fitted_distribution]{fitted_distribution()}}: \code{1 - fitted_distribution(object, newdata)$p(threshold)}
(or the \code{p(threshold)} complement for \code{lower.tail = TRUE}).
}
\details{
\code{threshold} is evaluated with the standard CDF convention \code{F(c) = Pr(Y <= c)}, so for atom-bearing families (e.g. Tweedie's point mass at \code{y = 0}) a
threshold exactly at the atom includes that atom's mass in \code{F(c)}:
\code{exceedance(fit, 0)} (the default \code{lower.tail = FALSE}) excludes the atom
at 0, matching \code{Pr(Y > 0)}; \code{exceedance(fit, 0, lower.tail = TRUE)} recovers
the atom mass \code{Pr(Y <= 0) = Pr(Y == 0)} when the family's support has no
continuous mass below the atom.

This is a distributional (plug-in) output at \code{\link[=predict_parameters]{predict_parameters()}}'s
fixed-effect, population-level parameter estimates \code{theta_hat}: the result
carries \code{attr(., "calibrated") <- FALSE} and does not propagate \code{theta_hat}
uncertainty. See \code{\link[=fitted_distribution]{fitted_distribution()}} for the \code{"spike"}/\code{"unimplemented"}
status gate this inherits (a \code{"spike"}-status family emits a one-time
warning; an unregistered \code{model_type}, including bivariate \code{biv_gaussian},
raises a clear error).
}
\examples{
dat <- data.frame(y = c(0.2, 0.5, 1.1, 1.4), x = c(-1, -0.5, 0, 0.5))
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
exceedance(fit, threshold = 1)
}
\seealso{
\code{\link[=fitted_distribution]{fitted_distribution()}}, \code{\link[=predict.drmTMB]{predict.drmTMB()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-parameters.R
\name{predict_parameters}
\alias{predict_parameters}
\alias{predict_parameters.drmTMB}
\title{Predict distributional parameters in long format}
\usage{
predict_parameters(object, ...)

\method{predict_parameters}{drmTMB}(
  object,
  newdata = NULL,
  dpar = NULL,
  type = c("response", "link"),
  include_newdata = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future options.}

\item{newdata}{Optional data frame for prediction. If omitted, fitted rows
are used.}

\item{dpar}{Optional character vector of distributional parameters to
predict, such as \code{"mu"}, \code{"sigma"}, \code{"nu"}, \code{"rho12"}, \code{"sigma1"}, or
\code{"sigma2"}. \code{NULL} predicts all fitted distributional parameters.}

\item{type}{Prediction scale: \code{"response"} or \code{"link"}.}

\item{include_newdata}{Logical; when \code{TRUE} and \code{newdata} is supplied, append
the supplied covariate columns to the returned table.}
}
\value{
A data frame with columns \code{row}, \code{row_label}, \code{dpar}, \code{component},
\code{type}, and \code{estimate}. When \code{include_newdata = TRUE}, supplied \code{newdata}
columns are appended after those core columns.
}
\description{
\code{predict_parameters()} returns predicted distributional parameters from a
\code{drmTMB} fit in one long data frame. It is a compact data surface for
interpretation tables and future plotting or marginalisation helpers: the
same grid can hold location/mean, scale, shape, probability, and coscale
quantities.
}
\details{
The helper calls \code{\link[=predict.drmTMB]{predict.drmTMB()}} for each requested distributional
parameter. With \code{newdata = NULL}, predictions use the fitted rows. With
\code{newdata} supplied, predictions are fixed-effect, population-level
predictions for those rows, matching \code{\link[=predict.drmTMB]{predict.drmTMB()}}.
}
\examples{
dat <- data.frame(
  y = c(0.2, 0.5, 1.1, 1.4, 1.8, 2.2),
  x = c(-1, -0.5, 0, 0.5, 1, 1.5)
)
fit <- drmTMB(bf(y ~ x, sigma ~ x), data = dat)
grid <- data.frame(x = c(0, 1))
predict_parameters(fit, newdata = grid, dpar = c("mu", "sigma"))
}

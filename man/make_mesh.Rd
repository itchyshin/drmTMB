% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mesh.R
\name{make_mesh}
\alias{make_mesh}
\title{Construct a fixed-kappa SPDE mesh}
\usage{
make_mesh(coords, kappa, mesh = NULL, crs = attr(coords, "crs"), ...)
}
\arguments{
\item{coords}{A finite two-column matrix or data frame of metric planar
coordinates, normally returned by \code{\link[=spatial_coords]{spatial_coords()}}. Supply \code{crs} when the
coordinate object does not already carry it.}

\item{kappa}{One finite strictly positive fixed SPDE kappa value, expressed in
inverse units of \code{coords}.}

\item{mesh}{Optional pre-built \code{fmesher} two-dimensional mesh. When supplied,
it is reprojected onto \code{coords} and validated rather than rebuilt.}

\item{crs}{A projected CRS for plain planar coordinate inputs. Geographic CRS
values are rejected.}

\item{...}{Additional arguments passed to \code{\link[fmesher:fm_mesh_2d_inla]{fmesher::fm_mesh_2d_inla()}} when a
mesh is built.}
}
\value{
A \code{drmTMBmesh} object containing \code{loc_xy}, \code{xy_cols}, \code{mesh}, \code{spde},
\code{loc_centers}, \code{A_st}, alignment identifiers, the projected CRS, and the
fixed \code{kappa} configuration.
}
\description{
Builds a two-dimensional \code{fmesher} triangulation, its finite-element matrices,
and the observation-to-vertex projection matrix used by the fixed-kappa mesh
spatial field. This helper only creates the geometry and fixed SPDE inputs; it
neither estimates kappa nor defines a spatial likelihood.
}
\examples{
if (requireNamespace("sf", quietly = TRUE) && requireNamespace("fmesher", quietly = TRUE)) {
  locations <- data.frame(lon = c(-123.2, -123.1, -123.15),
                          lat = c(49.2, 49.3, 49.25))
  xy <- spatial_coords(locations, lon, lat, crs_out = 32610)
  make_mesh(xy, kappa = 1 / 10000)
}
}

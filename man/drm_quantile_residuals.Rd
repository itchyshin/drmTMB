% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adequacy.R
\name{drm_quantile_residuals}
\alias{drm_quantile_residuals}
\title{Randomized quantile residuals (internal)}
\usage{
drm_quantile_residuals(object, seed = NULL, nsim = 1L)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{seed}{Optional single integer. Fixes the Dunn-Smyth randomization
reproducibly (discrete/atom families only) without disturbing the
caller's RNG stream; see \code{\link[=drm_dunn_smyth_u]{drm_dunn_smyth_u()}}. Ignored for continuous,
atom-free families, where the residual has no randomization to fix.}

\item{nsim}{Number of independent randomized realizations to draw (Fisher's
multi-realization seed envelope). \code{nsim = 1} (default) returns a plain
numeric vector. \code{nsim > 1} returns an \code{n}-by-\code{nsim} matrix, one column per
realization, using \code{nsim} distinct derived seeds when \code{seed} is supplied
(\verb{seed, seed + 1, ..., seed + nsim - 1}). For continuous, atom-free
families every column is identical (the residual has no randomization
uncertainty to average over); the envelope is only non-degenerate for
discrete/atom families.}
}
\value{
A numeric vector (\code{nsim = 1}) or an \code{n}-by-\code{nsim} matrix
(\code{nsim > 1}) of approximately N(0,1) residuals under a correctly
specified fixed-effect model. Missing-response rows (see
\code{\link[=drm_mask_missing_response_values]{drm_mask_missing_response_values()}}) are \code{NA}.
}
\description{
\code{drm_quantile_residuals()} computes Dunn-Smyth (1996) randomized quantile
residuals \verb{r_i = qnorm(F(y_i; theta_hat_i))} from the fitted distribution
returned by \code{\link[=fitted_distribution]{fitted_distribution()}}.
}
\details{
For continuous, atom-free families \code{F} has no jumps, so the residual is
exact and deterministic: \code{u_i = F(y_i)}. For discrete families
(\code{fitted_distribution()$discrete}) or families with an isolated atom
(\verb{$has_atom}), \code{F} has jumps, so a plain \code{F(y_i)} residual is not uniform
even under the true model. The Dunn-Smyth fix instead draws
\verb{u_i ~ Uniform(F(y_i-), F(y_i)]} via \code{\link[=drm_dunn_smyth_u]{drm_dunn_smyth_u()}}, where \verb{F(y_i-)}
is the left limit of \code{F} at \code{y_i}: \code{F(y_i - 1)} for a discrete/count family
(support starting at 0), and 0 at the atom (\code{y_i == 0}) for an
atom-bearing, otherwise-continuous family such as Tweedie -- away from the
atom \code{F} is continuous, so the left limit equals \code{F(y_i)} there and the
Dunn-Smyth draw degenerates to the plain continuous case automatically. A
family with atoms at other locations (e.g. a future \code{zero_one_beta} entry,
with atoms at both 0 and 1) needs its own left-limit rule; that is a DO-T3
concern and is not reachable from any family currently registered in
\code{\link[=drm_family_dpq]{drm_family_dpq()}}.

\code{fitted_distribution()$status == "spike"} families (feasibility spikes,
not yet DG2/DG3-verified) still compute a residual, but emit a one-time
\code{\link[cli:cli_warn]{cli::cli_warn()}} per \code{model_type} per session flagging that the residual
is exploratory, not DG-verified. \code{status == "unimplemented"} families
already raise a clear error inside \code{fitted_distribution()}, before this
function's body runs.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data.R
\name{miss_control}
\alias{miss_control}
\title{Configure missing-data handling}
\usage{
miss_control(
  response = c("drop", "include"),
  predictor = c("fail", "model"),
  engine = c("laplace", "em", "profile")
)
}
\arguments{
\item{response}{Response missingness policy. \code{"drop"} keeps existing
complete-case fitting; \code{"include"} keeps rows with supported missing
Gaussian responses and masks or marginalizes their likelihood contribution.}

\item{predictor}{Predictor missingness policy. \code{"fail"} errors on missing
predictors. \code{"model"} enables the current \code{mi()} predictor-model routes when
paired with a matching \code{impute} formula or \code{\link[=impute_model]{impute_model()}} in \code{\link[=drmTMB]{drmTMB()}}.}

\item{engine}{Missing-data engine. Only \code{"laplace"} is implemented in this
slice.}
}
\value{
A \code{drm_missing_control} object.
}
\description{
\code{miss_control()} configures the first \code{drmTMB} missing-data slices. The
default keeps the existing complete-case behaviour. In the current fitted
slices, \code{response = "include"} is implemented for univariate Gaussian
response masks and bivariate Gaussian partial-response rows without dense
known covariance. \code{predictor = "model"} is implemented mainly for one \code{mi()}
missing predictor at a time in a univariate Gaussian location model:
numeric missing predictors can use Gaussian fixed-effect, grouped, or
structured predictor models. Binary,
ordered categorical, unordered categorical, strict beta/proportion,
zero-one beta boundary proportion, denominator-aware beta-binomial
success/trial proportion, Poisson, negative-binomial, or zero-truncated
negative-binomial count, positive continuous lognormal or Gamma, and
exact-zero semi-continuous Tweedie missing predictors can use fixed-effect
predictor models supplied by \code{\link[=impute_model]{impute_model()}}. The first non-Gaussian response
slice supports a Poisson response with one fixed-effect Bernoulli/logit
binary missing predictor. EM/profile engines and simulation-based imputation
summaries are reserved for later slices.
}
\examples{
miss_control()
miss_control(response = "include")
miss_control(predictor = "model")
}

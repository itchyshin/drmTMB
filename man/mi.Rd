% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula-markers.R
\name{mi}
\alias{mi}
\title{Missing-predictor model marker}
\usage{
mi(x)
}
\arguments{
\item{x}{A predictor in a supported missing-predictor route.}
}
\value{
\code{x}, so standard R model-frame construction can evaluate the marker.
}
\description{
\code{mi()} marks a predictor whose missing values should be handled by the
missing-data predictor model. Most fitted routes support one \code{mi(x)} term in
a univariate Gaussian location formula. Numeric missing predictors can use a
matching Gaussian fixed-effect, one random-intercept, or one intercept-only
structured predictor model supplied through \code{impute}, for example
\code{impute = list(x = x ~ z)} or
\code{impute = list(x = x ~ z + relmat(1 | line, Q = Q))}. Family-aware
fixed-effect predictor models supplied with \code{\link[=impute_model]{impute_model()}} cover binary,
ordered categorical, unordered categorical, strict proportion, zero-one
boundary proportion, denominator-aware beta-binomial proportion, count,
positive continuous, and semi-continuous predictors. The non-Gaussian
response routes support one binary \code{mi()} predictor modelled by
\code{family = binomial()} for \code{family = poisson()}, \code{binomial()}, \code{nbinom2()},
and \code{beta()}.
}
\examples{
bf(y ~ z + mi(x), sigma ~ 1)
}

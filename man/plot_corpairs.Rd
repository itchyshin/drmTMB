% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-corpairs.R
\name{plot_corpairs}
\alias{plot_corpairs}
\title{Plot fitted correlation-pair summaries}
\usage{
plot_corpairs(data, colour = "level", facet = NULL, interval = TRUE, ...)
}
\arguments{
\item{data}{A data frame returned by \code{\link[=corpairs]{corpairs()}}, or a compatible table with
columns \code{level}, \code{class}, \code{parameter}, \code{estimate}, and \code{modelled}.}

\item{colour}{Optional character scalar naming a column to map to colour.
Use \code{NULL} to suppress colour mapping.}

\item{facet}{Optional character scalar naming a column to facet by. Use
\code{NULL} to suppress faceting.}

\item{interval}{Logical; draw finite \code{conf.low}/\code{conf.high} intervals when
those columns are present.}

\item{...}{Reserved for future options.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_corpairs()} is a small \code{ggplot2} consumer for tables returned by
\code{\link[=corpairs]{corpairs()}}. It does not compute correlation pairs, fit intervals, or choose
a correlation layer. Build the table first with \code{\link[=corpairs]{corpairs()}}, then pass that
table to this helper.
}
\details{
The helper draws one point per correlation row. If the table contains finite
\code{conf.low} and \code{conf.high} bounds, it draws interval segments for those rows
only. Rows without finite bounds remain visible as point estimates and keep
their display interval status attached to the plotted data.
}
\examples{
pairs <- data.frame(
  level = c("residual", "group"),
  class = c("residual", "mean-slope"),
  parameter = c("rho12", "cor((Intercept),x | p | id)"),
  estimate = c(0.25, 0.45),
  modelled = c(FALSE, FALSE),
  conf.low = c(NA, 0.10),
  conf.high = c(NA, 0.72),
  conf.status = c("not_requested", "profile")
)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot_corpairs(pairs)
}
}

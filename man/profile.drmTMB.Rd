% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{profile.drmTMB}
\alias{profile.drmTMB}
\title{Compute profile-likelihood curves for fitted model targets}
\usage{
\method{profile}{drmTMB}(
  fitted,
  parm,
  level = 0.95,
  trace = FALSE,
  profile_precision = c("default", "fast"),
  profile_maxit = NULL,
  compare = FALSE,
  first_pass_ystep = 0.5,
  first_pass_ytol = 2,
  ...
)
}
\arguments{
\item{fitted}{A \code{drmTMB} fit.}

\item{parm}{Character or integer vector selecting direct profile targets.
Use \code{\link[=profile_targets]{profile_targets()}} to inspect available names. Unlike
\code{\link[=confint.drmTMB]{confint.drmTMB()}}, this helper always uses the full
\code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} curve because the curve itself is the diagnostic.}

\item{level}{Confidence level used for the likelihood-ratio cutoff and
interval endpoint annotations.}

\item{trace}{Logical; passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}}.}

\item{profile_precision}{Profile-control shortcut. \code{"default"} leaves
\code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} controls unchanged. \code{"fast"} supplies
\code{ystep = 0.5} and \code{ytol = 2} unless the caller supplies those controls in
\code{...}.}

\item{profile_maxit}{Optional positive whole number passed to
\code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} as \code{maxit}.}

\item{compare}{Logical; if \code{TRUE}, run a coarse first-pass profile and then
the requested profile controls so the returned object can compare curve
shape and elapsed time.}

\item{first_pass_ystep, first_pass_ytol}{Coarse \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}} controls
used only when \code{compare = TRUE}. The dense pass uses \code{profile_precision},
\code{profile_maxit}, and \code{...}.}

\item{...}{Additional arguments passed to \code{\link[TMB:tmbprofile]{TMB::tmbprofile()}}. \code{drmTMB}
supplies the profiled \code{obj}, \code{name}, \code{lincomb}, and \code{trace} arguments
internally; set the profile target with \code{parm}.}
}
\value{
A data frame with class \code{"profile.drmTMB"}. The main columns are
\code{parm}, \code{profile_value}, \code{profile_value_link}, \code{objective},
\code{delta_objective}, \code{delta_deviance}, \code{estimate}, \code{profile_pass},
\code{elapsed}, \code{profile_source}, \code{conf.low}, \code{conf.high}, \code{conf.status}, and
\code{profile.message}.
}
\description{
\code{profile()} computes and returns the full likelihood-profile curve for one
or more direct \code{\link[=profile_targets]{profile_targets()}}. It is a diagnostic companion to
\code{\link[=confint.drmTMB]{confint.drmTMB()}}. Use \code{\link[=confint.drmTMB]{confint.drmTMB()}} for interval tables, especially
with the fast endpoint engine; use \code{profile()} followed by \code{plot()} when you
need to see whether a target has a peaked, flat, one-sided, or boundary-like
likelihood shape.
}
\details{
The returned x-axis values are transformed to the same scale shown by
\code{\link[=profile_targets]{profile_targets()}}. For example, SD and scale targets are shown on their
public positive scale, and correlation targets are shown on the correlation
scale. The y-axis diagnostic is likelihood-ratio distance,
\code{2 * (profile_nll - min(profile_nll))}, so a flatter curve indicates weaker
likelihood support around the fitted value.
}
\examples{
dat <- data.frame(y = c(0.2, 0.5, 1.1, 1.4), x = c(-1, 0, 1, 2))
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
prof <- profile(fit, parm = "sigma", profile_precision = "fast")
head(prof)
if (requireNamespace("ggplot2", quietly = TRUE)) {
  plot(prof)
}
}

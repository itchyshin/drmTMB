% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control.R
\name{drm_control}
\alias{drm_control}
\title{Control fitting and fitted-object storage}
\usage{
drm_control(
  optimizer = list(),
  se = TRUE,
  keep_data = TRUE,
  keep_model_frame = TRUE,
  keep_tmb_object = TRUE,
  sparse_fixed = FALSE,
  aggregate_gaussian = FALSE,
  logsigma_clamp = c(-12, 12),
  logsigma_clamp_margin = 3,
  optimizer_preset = c("default", "careful", "robust"),
  multi_start = 1L,
  fallback_optimizer = NULL
)
}
\arguments{
\item{optimizer}{Named list passed to the \code{control} argument of
\code{\link[stats:nlminb]{stats::nlminb()}}.}

\item{se}{Logical; compute standard errors and fixed-effect covariance with
\code{\link[TMB:sdreport]{TMB::sdreport()}} after optimization. Set to \code{FALSE} to keep fitted
coefficients, fitted values, residuals, predictions, simulations, and
profile-likelihood paths while skipping Wald standard errors,
\code{\link[stats:vcov]{stats::vcov()}}, and Wald confidence intervals. Profile-likelihood
intervals still require \code{keep_tmb_object = TRUE}.}

\item{keep_data}{Logical; keep the complete-case model data in the fitted
object. Set to \code{FALSE} to drop \code{fit$data} and \code{fit$model$data} after
fitting. Prediction, fitted values, residuals, simulation, and basic
summaries still use the stored model matrices and response vectors.}

\item{keep_model_frame}{Logical; keep model frames in the fitted object. Set
to \code{FALSE} to drop \code{fit$model$model_frame} and random-effect scale model
frames after fitting. Prediction, fitted values, residuals, simulation,
\code{sigma()}, \code{rho12()}, \code{corpairs()}, and \code{check_drm()} use stored model
matrices, terms, response vectors, offsets, and response-name metadata.}

\item{keep_tmb_object}{Logical; keep the TMB automatic-differentiation object
in \code{fit$obj}. Set to \code{FALSE} to reduce fitted-object size after
optimization. \code{check_drm()} will then report the fixed-gradient check as a
note because it cannot re-evaluate the gradient without \code{fit$obj}, and
profile-likelihood confidence intervals will be unavailable.}

\item{sparse_fixed}{Logical; opt-in control for sparse fixed-effect
design matrices. The first fitted path is limited to univariate Gaussian
\code{mu} fixed effects with no random effects and intercept-only \code{sigma}.}

\item{aggregate_gaussian}{Logical; opt-in control for sufficient-
statistic row aggregation in univariate Gaussian fixed-effect models. The
first fitted path rejects random effects, structured effects, known
sampling covariance, bivariate models, non-Gaussian families, non-unit
likelihood weights, and combined sparse fixed-effect matrices.}

\item{logsigma_clamp}{Numeric \code{c(lo, hi)} band, or \code{NULL}. Bounds the
per-observation Gaussian \code{log(sigma)} with an identity-in-band soft-clamp,
a numerical guard against scale overflow on near-degenerate per-group scale
models. The default \code{c(-12, 12)} is identity for any standardized response;
widen it for legitimately huge-variance unstandardized data, or set \code{NULL}
to disable the guard entirely. It is a numerical guard only and does not
change identifiability.}

\item{logsigma_clamp_margin}{Positive number; the soft-clamp saturation
margin beyond \code{logsigma_clamp} (default \code{3}, saturating to \verb{[-15, 15]} for
the default band). Ignored when \code{logsigma_clamp = NULL}.}

\item{optimizer_preset}{Optimizer-budget preset. \code{"default"} adds no
optimizer controls, \code{"careful"} sets \code{iter.max = 1000} and
\code{eval.max = 1000}, and \code{"robust"} sets \code{iter.max = 5000} and
\code{eval.max = 5000}. The optimizer escalates this ladder automatically when a
preset does not converge.}

\item{multi_start}{Whole number \verb{>= 1} (default \code{1}). With \code{multi_start > 1},
each optimizer preset is run from \code{multi_start} starting points -- the
principled start plus reproducibly perturbed starts -- and the lowest-
objective result is kept. Opt-in robustness for weakly identified models;
\code{multi_start = 1} is the single-start fit and is unchanged.}

\item{fallback_optimizer}{\code{NULL} (default) or one \code{\link[stats:optim]{stats::optim()}} method
(\code{"BFGS"}, \code{"L-BFGS-B"}, \code{"Nelder-Mead"}, \code{"CG"}). When set, and no
\code{nlminb()} preset converges, \code{drmTMB()} tries this optimizer as a final
attempt; a different algorithm can succeed on a numerically awkward but
identified problem. Opt-in; \code{NULL} keeps the \code{nlminb()}-only ladder.}
}
\value{
A \code{drm_control} object.
}
\description{
\code{drm_control()} collects optimizer settings and storage choices for
\code{\link[=drmTMB]{drmTMB()}}. Use \code{optimizer} for settings passed to \code{\link[stats:nlminb]{stats::nlminb()}}, or
\code{optimizer_preset} for named \code{nlminb()} budgets that keep ordinary defaults
fast while making complex refits easier to write. Use the storage flags when
a fitted object should keep less R-side state, for
example during large-data experiments where the original data frame and TMB
automatic-differentiation object are expensive to retain.
}
\details{
For optimizer-only settings, \code{control = list(eval.max = 1000)} remains
valid. When using \code{drm_control()}, put optimizer arguments inside
\code{optimizer = list(...)}; do not pass \code{eval.max} directly to \code{drm_control()}.
Presets \code{"careful"} and \code{"robust"} expand to explicit \code{iter.max} and
\code{eval.max} controls for \code{nlminb()}. Values in \code{optimizer} override values from
the selected preset.

When the selected preset uses the standard \code{nlminb()} budget and no explicit
optimizer controls, \code{drmTMB()} escalates the preset ladder
(\code{"default"} -> \code{"careful"} -> \code{"robust"}) when an attempt either raises an
error (such as a non-finite gradient) or does not converge cleanly (a nonzero
convergence code or a non-finite objective). The first cleanly-converged
attempt is returned; if no preset converges, the best (lowest-objective)
attempt is returned and the fit-time convergence warning flags it. Every
attempted preset is recorded in \code{fit$optimizer_attempts} and the selected one
in \code{fit$optimizer_used}.
}
\examples{
dat <- data.frame(y = rnorm(20), x = rnorm(20))
fit <- drmTMB(
  bf(y ~ x, sigma ~ 1),
  data = dat,
  control = drm_control(
    optimizer_preset = "careful",
    se = FALSE,
    keep_data = FALSE,
    keep_model_frame = FALSE,
    keep_tmb_object = FALSE
  )
)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{sigma.drmTMB}
\alias{sigma.drmTMB}
\title{Extract fitted scale or dispersion}
\usage{
\method{sigma}{drmTMB}(object, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future scale-extractor options.}
}
\value{
A numeric vector for univariate models, or a named, roundable list
of numeric vectors for bivariate Gaussian, bivariate lognormal, or
bivariate Student-t models.
}
\description{
\code{sigma()} returns the fitted scale-like parameter from a \code{drmTMB} model. For
univariate Gaussian location-scale models this is the fitted residual
\code{sigma_i} vector on the response scale. For Student-t models this is the
Student-t scale parameter; when \code{nu > 2}, the residual standard deviation is
\code{sigma * sqrt(nu / (nu - 2))}. For skew-normal models this is the response
standard deviation under the public moment parameterization, not the native
Azzalini scale \code{omega}. For lognormal models this is the fitted
standard deviation of \code{log(y)}. For Gamma models this is the fitted
coefficient of variation. For Tweedie models this is the public scale
parameter where internal dispersion is \code{phi = sigma^2}. For beta,
zero-one beta, and beta-binomial models this is the public scale parameter
where internal precision is \code{phi = 1 / sigma^2}.
Cumulative-logit ordinal, binomial, Poisson, and zero-inflated Poisson models have no
fitted residual scale parameter and return a fixed unit dispersion vector
for consistency with base-R \code{sigma()} conventions. For
negative-binomial 2, zero-truncated negative-binomial 2, hurdle
negative-binomial 2, and zero-inflated negative-binomial 2 models this is
the fitted overdispersion scale in the untruncated NB2 component
\code{Var(y | component) = mu + sigma^2 * mu^2}. For bivariate Gaussian,
bivariate lognormal, and bivariate Student-t models it returns a roundable
list with fitted \code{sigma1} and \code{sigma2} vectors. The bivariate-lognormal
values are log-response SDs; the bivariate-Student values are Student-t
scales, not marginal SDs.
}
\details{
In meta-analytic models fitted with \code{meta_V(V = V)}, this is the
modelled residual heterogeneity scale, not the square root of the known
sampling variance plus residual variance. Simulation and Pearson residuals
combine known sampling covariance with residual scale internally.
}
\examples{
dat <- data.frame(y = c(0.2, 0.5, 1.1, 1.4), x = c(-1, 0, 1, 2))
fit <- drmTMB(bf(y ~ x, sigma ~ x), data = dat)
sigma(fit)
}

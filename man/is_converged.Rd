% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{is_converged}
\alias{is_converged}
\alias{is_converged.default}
\alias{is_converged.drmTMB}
\title{Check whether a fit converged}
\usage{
is_converged(object, ...)

\method{is_converged}{default}(object, ...)

\method{is_converged}{drmTMB}(object, include_hessian = FALSE, ...)
}
\arguments{
\item{object}{A \code{drmTMB} fit.}

\item{...}{Reserved for future convergence options.}

\item{include_hessian}{Logical; require a completed \code{TMB::sdreport()} with
positive-definite Hessian in addition to optimizer convergence.}
}
\value{
A single \code{TRUE} or \code{FALSE}.
}
\description{
\code{is_converged()} is a no-rerun logical accessor for downstream tools that
need a compact convergence flag before comparing or displaying fitted
models. It checks the optimizer state already stored on the fit. Use
\code{\link[=check_drm]{check_drm()}} when users need the full diagnostic table with convergence,
Hessian, boundary, standard-error, design, and replication rows.
}
\details{
By default, \code{is_converged(fit)} returns \code{TRUE} when the optimizer convergence
code is zero and the stored objective and log-likelihood are finite. With
\code{include_hessian = TRUE}, it also requires a completed \code{TMB::sdreport()} with
\code{pdHess = TRUE}; this stricter mode is an inference-readiness flag, not just
an optimizer flag.
}
\examples{
dat <- data.frame(y = c(0.1, 0.4, 0.8, 1.1), x = c(-1, 0, 1, 2))
fit <- drmTMB(bf(y ~ x, sigma ~ 1), data = dat)
is_converged(fit)
is_converged(fit, include_hessian = TRUE)
}

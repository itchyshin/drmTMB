% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{drm_provenance}
\alias{drm_provenance}
\title{Build provenance of the installed drmTMB package}
\usage{
drm_provenance()
}
\value{
A list with components:
\describe{
\item{package_version}{Character, \code{packageVersion("drmTMB")}.}
\item{git_sha}{Character SHA-1 baked at build time, or \code{NA_character_}.}
\item{git_dirty}{Logical: was the source tree dirty at build time? \code{NA} if unknown.}
\item{build_time}{Character ISO-8601 UTC timestamp of the bake, or \code{NA_character_}.}
\item{source}{Character: \code{"baked"}, \code{"baked-without-git"}, or \code{"unavailable"}.}
\item{reason}{Character explanation when \code{source != "baked"}, else \code{NA_character_}.}
\item{queried_at}{Character ISO-8601 UTC timestamp of this call.}
}
}
\description{
\code{packageVersion("drmTMB")} identifies a release, not a build: two builds
can report the same version string while differing by commits under
\verb{R/}, \verb{src/}, or \code{NAMESPACE}. \code{drm_provenance()} returns a stable,
machine-readable record that identifies the BUILD instead.
}
\details{
An installed package has no \code{.git} directory to consult, so the git
commit SHA and working-tree dirty flag are never read at call time.
Instead they are captured once, at build time, by
\code{tools/drmtmb_provenance.R} (invoked from \code{configure}/\code{configure.win}
before \verb{inst/} is copied into the installed package) and baked into
\code{inst/build-provenance.dcf}. \code{drm_provenance()} only reads that baked
file. When it is missing -- for example under \code{devtools::load_all()}, or
a build whose \code{configure} step did not run -- the git fields are \code{NA}
with \code{source = "unavailable"} and a stated \code{reason}, rather than a guess.
When \code{configure} ran but git information itself was unavailable at build
time (no git executable, or the tree was not a git checkout, as happens
when installing from a released tarball with \code{.git} excluded via
\code{.Rbuildignore}), \code{source} is \code{"baked-without-git"}.
}
\examples{
drm_provenance()
}

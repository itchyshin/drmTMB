% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing-data.R
\name{drm_imputed_route_conditional_sd}
\alias{drm_imputed_route_conditional_sd}
\title{Posterior standard error of a fitted missing-predictor value}
\usage{
drm_imputed_route_conditional_sd(predictor)
}
\arguments{
\item{predictor}{One element of \code{fit$missing_data$predictors}.}
}
\value{
A numeric vector the same length as \code{predictor$model_row}, in the
same row order, or \code{NULL}.
}
\description{
Every non-Gaussian \code{mi()} route already stores a normalized posterior
probability vector or matrix over a finite-state or quadrature grid on
\code{fit$missing_data$predictors[[variable]]}, and \code{predictor$value} at the
missing rows is already the posterior mean of that exact grid
(\code{predictor$summary} names which one). The posterior standard deviation is
therefore \code{sqrt(sum(p * (x - mean)^2))} over the same grid, with no new TMB
computation. This function dispatches on \code{predictor$summary} and returns
\code{NULL} when no such grid exists for the route.
}
\details{
\code{"conditional_modal_category"} (unordered \code{categorical()} predictors)
always returns \code{NULL}: the reported value is the mode over unordered
nominal codes, and no metrically meaningful variance exists over categories
that carry no order.

\code{"conditional_mode"} (Gaussian predictors, a genuine TMB random effect)
also returns \code{NULL} here; its standard error comes from
\code{TMB::sdreport()}'s \code{diag.cov.random}, computed by the caller,
\code{drm_imputed_missing_predictor_se()}.
}
\keyword{internal}

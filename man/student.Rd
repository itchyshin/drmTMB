% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{student}
\alias{student}
\title{Student-t response family}
\usage{
student()
}
\value{
A \code{drm_family} object.
}
\description{
\code{student()} defines a one-response Student-t distribution with formulas for
location \code{mu}, residual scale \code{sigma}, and degrees of freedom \code{nu}.
}
\details{
Here \code{sigma} is the Student-t \strong{scale}, not the response standard deviation.
The density is the location-scale t evaluated at \code{z = (y - mu) / sigma}, so the
standard deviation of \code{y} is \code{SD[y] = sigma * sqrt(nu / (nu - 2))} for \code{nu > 2}
and is strictly larger than \code{sigma} (about 73\% larger at \code{nu = 3}, shrinking to
\code{sigma} as \code{nu -> Inf}). This is the one implemented family whose public
\code{sigma} is a scale rather than \code{SD[y]}: the location-scale t has no closed-form
standard-deviation parameterization, and both \code{drmTMB} and its \code{DRM.jl} twin
fit \code{sigma} as the scale.

The \code{nu} parameter uses a log link with a lower bound of 2:
\code{nu = 2 + exp(eta_nu)}. This keeps the fitted distribution in the
finite-variance region (\code{nu > 2}) while still allowing heavy tails. The lower
bound is a deliberate design choice, not a standard-deviation requirement: it
guarantees a finite variance and a well-defined \code{SD[y]}. The model therefore
\strong{cannot} represent the very heavy tails of \code{nu <= 2} (for example a
Cauchy-like \code{nu = 1}); data that genuinely need \code{nu <= 2} would require lifting
the floor, which is not implemented. \code{check_drm()} warns when the fitted \code{nu}
approaches the boundary at 2, where the slant of the likelihood in \code{nu} is
weakly identified.
Ordinary \code{mu} random intercepts and independent numeric slopes such as
\code{(1 | id)} and \code{(0 + x | id)} are supported in the first Student-t
mixed-model slice; correlated slopes, \code{sigma} random effects, and \code{nu}
random effects remain separate planned gates.
}
\examples{
student()
}

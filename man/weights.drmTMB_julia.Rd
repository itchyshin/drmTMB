% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-comparison.R
\name{weights.drmTMB_julia}
\alias{weights.drmTMB_julia}
\title{Prior weights of an \code{engine = "julia"} fit}
\usage{
\method{weights}{drmTMB_julia}(object, ...)
}
\arguments{
\item{object}{A \code{drmTMB_julia} fit.}

\item{...}{Unused; present for S3 consistency.}
}
\value{
A numeric vector of ones, one per observation.
}
\description{
\code{engine = "julia"} refuses the \code{weights} argument at fit time
(\code{drmTMB_julia_bridge()} and its structured counterparts in
\code{R/julia-bridge.R} abort on a non-missing \code{weights}), so a bridge fit is unweighted by construction and its
prior weights are all one. Without this method \code{weights()} fell through to
\code{stats:::weights.default()} and returned \code{NULL} silently, while
\code{weights()} on the same model fitted with \code{engine = "tmb"} returns a vector
of ones. This reports the ones, matching both the native engine and
\code{DRM.jl}'s own \code{weights(fit) = ones(nobs(fit))} (\code{src/comparison.jl}).
}
\details{
These are PRIOR (per-observation) weights, not Akaike model weights.
}

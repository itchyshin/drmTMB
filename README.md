# drmTMB
A fast TMB-based distributional regression package with brms-like syntax, focused on univariate and bivariate location–scale–shape models, where not only μ and σ but also shape, zero inflation, random-effect variance, and residual correlation ρ can be modelled by predictors.
